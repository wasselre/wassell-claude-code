Agent **A-GEN** — read `docs/creative-director/briefs/_COMMON.md` first, then ALL of `docs/creative-director-contracts.md` (esp. §0, §5–§8, §10, §12) and `src/lib/creative/contracts.ts` (your JSON schemas MUST produce exactly these shapes); the sibling contract §4–§5; the REUSED modules (never edit): `worker/src/ai/{types,roles,index}.ts` (`CallRequest{system,user,images?,schema,cache?}`, `CallResult`, ledgers), `worker/src/marketing/script/{types,facts,claims,entities}.ts`; the living skill `.claude/skills/writing-post/SKILL.md` fully (recipes, hard rules, Decisions Log = the spec twin) and the Decisions Log in `.claude/skills/writing-video-script/SKILL.md`; `worker/src/marketing/videoScript.ts` (existing prompt style). Peers (code against contracted names; local interfaces if absent; never create their files): A-AI `worker/src/creative/roles.ts` (`callCreativeRole<T>(key, req, ctx)`); A-FACTS `worker/src/creative/facts.ts` (`loadCreativeFacts`), `grounding.ts` (`validateConcepts/validateBase/validateDerivatives`, `buildViolationFeedback`), `placementSpecs.ts` (`PLACEMENT_SPECS`, `masterAspectFor`, `adaptationSkeleton`); A-BRAND `worker/src/creative/brandKit.ts` (`brandKitPromptBlock`, `validatePaletteAgainstKit`).

OWN: `worker/src/creative/director/**` incl. `__tests__/`, `docs/creative-director/reports/A-GEN.md`:
- `schemas.ts` — JSON Schemas for `ConceptsOutput`, `BasePackage`, `DerivativesOutput` mirroring contracts.ts exactly (enums as `enum`, required arrays).
- `prompts.ts` — provider-neutral system prompts (Arabic primary; stable cacheable prefix first): `conceptsSystem(ctx)`, `packageSystem(ctx)`, `derivativesSystem(ctx)`; user builders rendering: facts catalog, brand kit block, writer rules (shared+post+decisions_log), brief (the `mos_script_brief` jsonb: campaign/audience/objective/platforms/language), selected targets + specs, references (with L1/L2 design reads when present), candidate assets (id + metadata + ai_description + dominant colours + headline_space + rights trust), language rule, chosen concept; for regenerate: previous package + reviewer note + violation feedback. Recipes feature_spec, lifestyle, offer, event, occasion, launch with beats. Hard rules embedded: project_name_lead on design (need not repeat in headlines); 1–4 headlines; numbers only from claimable facts with fact_refs (ANY recipe, any field); readiness both ways; only Wassel + developer named, CTA Wassel; hashtags ours; Saudi dialect; Arabic-Indic numerals in design text; no «بدون سعي»; preserve language; organic captions only for selected organic targets; paid copy only for selected paid targets; competitor content is inspiration only, never a fact source; every derivative needs a full VisualAdaptation.
- `policy.ts` — image policy (contracts §7): `checkAiRecommendation(rec, ctx) → {ok, reason}`, AR+EN verb/noun lists; allowed modes; `request_photo` always ok. Keep it pure so A-API can mirror it in `src/lib/creative/policy.ts`.
- `adaptation.ts` — `planAdaptations(base, targets, specs)`: deterministic skeleton per target (`adaptationSkeleton`), slide mapping + asset substitution scaffold; post-process model output so every derivative has complete `VisualAdaptation` strings (explicit "no change" wording when applicable).
- `references.ts` — `selectReferences(rows, intent, {max:4})` with org diversity (≤2/org), ≥1 post-level read for carousels, hallucination guard (drop ids not in candidates), map to `ReferencePick` with preview_url.
- `assets.ts` — `rankCandidateAssets(rows, intent)`; hallucination guard; rights fields copied from rows (the model never decides rights); competitor ids never allowed.
- `runDirector.ts` — `runConcepts(input, deps)`, `runPackage(input, deps)`, `runDerivatives(input, deps)`, `runRegenerate(input, deps)`; `input = { brief, content:{language,title,content_type_key}, facts, brandKit, rules, targets, specs, referenceRows, assetRows, conceptChoice?, previousPackage?, revisionNote? }`, `deps = { callRole, ledger, log }`. Each stage: prompt → role call with schema → validators + policy → on errors retry ONCE with `buildViolationFeedback` → `{ output, validation, rolesJson, cost }`; never throw on validation failure after the retry (return needs_attention with the error list); throw `facts_insufficient:` when `facts.package.viable` is false or the recipe needs a missing price. Pure — no DB access here.
- Tests with a fake `callRole`: single + carousel happy paths; hallucinated asset id dropped; competitor id rejected; number without fact_ref → retry → fixed; policy-blocked recommendation dismissed with a warning; derivatives only for selected targets; language preserved; 4:5→9:16 requires_separate_design; schema key assertions.

Run `cd worker && npm run typecheck && npx vitest run src/creative/director`.
