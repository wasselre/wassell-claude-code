Agent **A-FACTS** — read `docs/creative-director/briefs/_COMMON.md` first, then contracts §0 rules 3–7, §8, §10, §12; the REUSED modules (never edit): `worker/src/marketing/script/{types,facts,entities,claims,brief}.ts`; the existing `src/lib/marketingOS/platformRules.ts` fully (pure module imported by BOTH SPA and `api/marketing-os.ts` — keep it pure, no new imports) and `src/lib/marketingOS/client.ts` lines 1170-1230 (`ASSET_ASPECT_RATIOS`).

OWN: `worker/src/creative/facts.ts`, `worker/src/creative/grounding.ts`, `worker/src/creative/placementSpecs.ts` (a COPY of the placement additions to platformRules — mark as copy, keep identical), `worker/src/creative/__tests__/{facts,grounding,placementSpecs}.test.ts`, `src/lib/marketingOS/platformRules.ts` (ADDITIVE only: append `PLACEMENT_SPECS`, `placementSpec(platform, type)`, `masterAspectFor(targets)`, `adaptationSkeleton(masterAspect, spec)`, `aspectFamily(aspect)`), `src/pages/Marketing/lib/__tests__/placementSpecs.test.ts`, `docs/creative-director/reports/A-FACTS.md`.

DELIVER:
1. `facts.ts`: `loadCreativeFacts(sb, projectId, {developerName?}) → { package: FactsPackage, catalog: string, refs: FactRef[] }` using the sibling `buildFactsPackage` + `loadProjectRecord` + `resolveLookupName` (developer from `record.developer`); catalog lines like `F1 · price_from · تبدأ من 1,050,000 ر.س (available_price_range.min) [claimable]`; `toArabicIndic(n)`, `renderFactAr(fact, {numerals})`. Numbers policy: any claimable fact may be used anywhere with `fact_refs`.
2. `grounding.ts` (pure): `validateConcepts(out, ctx)`, `validateBase(base, ctx)`, `validateDerivatives(out, ctx)` with `ctx = { facts: FactsPackage, refs: FactRef[], language, selectedTargets: DerivativeTarget[], specs: PlacementSpec[], brandKit: BrandKit|null, rules: WriterRules, blocklist: BlockEntry[], allowedTerms: string[], competitorMediaIds: Set<string>, assetMeta: Map<string,{rights, rights_verified, nature}>, policyCheck?: (rec)=>{ok:boolean; reason:string} }` → `{ ok, errors:[{path, rule, detail}], warnings:[…] }`. Rules (contracts §8): every mention (sibling `extractMentions` + `gateByClass`) in headlines/slides/captions/ad copy must be resolved by a claimable fact AND cited in the field's `fact_refs`; readiness wording matches `facts.package.readiness`; entity gate (`detectEntities` with the blocklist; developer allowed via `allowedTerms`); captions only for selected organic targets and ≤ `caption_max`; hashtags ≤ `hashtags_max`, none from blocklist hashtag/org entries; paid copy only for selected paid targets; assets: no competitor ids, no restricted/do_not_use, `needs_rights_confirmation` true whenever `!rights_verified`; palette ⊂ brand kit unless listed in `deviations` (advisory) → error when `mode='constraint'`; language equals record; prohibited phrases from `rules` (e.g. «بدون سعي») anywhere; `design_text.project_name_lead` equals the project name (normAr) or its Latin; headline count 1–4 (single) / cover 1–3 + ≥1 per slide (carousel); AI recommendations reference existing asset ids, allowed modes, and pass `policyCheck`. `buildViolationFeedback(errors) → string` (Arabic + English bullets for the retry prompt).
3. `platformRules.ts` additions: `PLACEMENT_SPECS: PlacementSpec[]` — instagram feed (4:5,1:1 → 1080×1350 / 1080×1080), carousel (same, max_slides 10), story (9:16 1080×1920, safe top/bottom 250); tiktok photo_mode (9:16, 3:4; formats jpg/webp; max 10 — reuse the existing TikTok constants); snapchat story (9:16, one file, caption 160); x post (16:9, 1:1, 4:5; max 4 images; manual_publish); meta ad_feed (1:1, 4:5), ad_story (9:16, safe 250), ad_carousel (1:1), ad_reels (9:16); google ad_display (1.91:1, 1:1; manual_publish); website post (16:9; manual_publish). `caption_max`/`hashtags_max` from the existing `CAPTION_MAX`/hashtag constants. `masterAspectFor(targets)` minimizes re-layouts (same family = crop; different family = separate design). `adaptationSkeleton(master, spec)` → partial `VisualAdaptation` with `aspect, px, safe_zones, requires_separate_design, image_change` deterministic (4:5→9:16 = separate design + extend/replace; 4:5→1:1 = crop; 1:1→4:5 = extend; carousel→story = separate design + slide_mapping placeholder).
4. Tests with Arabic fixtures (Arabic-Indic + Western digits; competitor hashtag; phone; «بدون سعي»; unverified rights; palette deviation advisory vs constraint; caption for unselected target; 4:5→9:16 skeleton).

Run: `cd worker && npm run typecheck && npx vitest run src/creative`; root `npx vitest run src/pages/Marketing/lib/__tests__/placementSpecs.test.ts` and `npx tsc --noEmit -p tsconfig.json`.
