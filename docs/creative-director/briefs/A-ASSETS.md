Agent **A-ASSETS** — read `docs/creative-director/briefs/_COMMON.md` first, then contracts §0 rule 9, §1, §2 `_24`, §3, §9, §12; `src/lib/creative/contracts.ts` (`AssetPick`, rights/nature/source/state enums); `worker/src/runEnrichmentJob.ts` fully (Haiku vision enrichment; tool schema `propose_metadata` ~205-245; image encoding 148-164; completion via `file_enrichment_complete`) + `supabase/migrations/2026-08-31_02_enrichment_primary_category.sql` (how results + provenance are applied); `2026-08-23_10_file_metadata_layers.sql`; `2026-08-31_03_search_primary_category.sql` (the FULL current `business_files_search` body — your `_29` must CREATE OR REPLACE with the full body + additions); `src/lib/files/mediaProbe.ts`, `src/lib/files/library.ts`, `src/types/files.ts` (`LibraryFilters`, `BusinessFileFacets`), `src/pages/Files/library/FilePickerModal.tsx`, `src/pages/Files/library/LibraryFilterBar.tsx`, `worker/src/marketing/content/ffmpegMedia.ts`, `worker/src/marketing/creativeStore.ts` (sharp is available in the worker), `worker/src/ai/{types,roles}.ts` (REUSE `callRole`; never edit). A-DB adds `files.dominant_colors jsonb, has_text boolean, headline_space text, ocr_text text, visual_meta_version text`, `files_rights_v`, `creative_candidate_assets`, `creative_asset_backfill_targets(p_kind 'meta'|'enrich', p_limit)`; A-VIS provides `worker/src/creative/backfill.ts` (`runBackfillBatch`) and A-WORKER `lanes/types.ts` — code against those names.

OWN: `worker/src/runEnrichmentJob.ts` (ADDITIVE, behind `mos_settings.creative_writer.asset_enrich_v2`: extend the tool schema with `has_text`, `headline_space` (none|top|bottom|left|right|center), `ocr_text` (≤500 chars), `scene_subjects[]` (closed list: exterior, interior, plan, aerial, lifestyle, people, amenity, pool, kitchen, bedroom, majlis, facade, garden, street, render, logo, text_card); when the flag is on, route the vision call through the sibling `callRole` with an explicit RoleConfig resolved from `mos_settings.ai_roles.asset_enrich_v2` (fall back to the existing path when absent); keep the existing Haiku path untouched when off; apply the new fields via `file_enrichment_apply_v2`), `supabase/migrations/2026-09-02_29_asset_enrich_v2_apply.sql` (RPC `file_enrichment_apply_v2(p_file_id uuid, p_has_text boolean, p_headline_space text, p_ocr_text text, p_subjects text[], p_model text)` writing the columns + `visual_meta_version='enrich-v2'` + `file_metadata_provenance` rows state 'ai_suggested' for each field + `file_subjects` inserts; PLUS `business_files_search` CREATE OR REPLACE with the full original body and an additional optional filter `aspect_family` (portrait|landscape|square derived from `aspect_ratio` W:H) read from `p_filters` so existing callers are unaffected — the lead applies), `worker/src/creative/assetMeta/{deterministic,rights,index}.ts`, `worker/src/creative/lanes/assetMetaLane.ts`, `worker/src/creative/__tests__/assetMeta.test.ts`, `api/_lib/marketing/creative/rights.ts` (+ identical worker copy in `assetMeta/rights.ts`), `src/pages/Files/library/FilePickerModal.tsx` (additive `filters?` prop: `linked_record_id?, primary_category?, asset_nature?, usage_rights?, aspect_family?` merged into the search filters; `showMeta?: boolean` renders nature/source/rights + verified badges), `src/pages/Files/library/LibraryFilterBar.tsx` + `src/lib/files/library.ts` + `src/types/files.ts` (aspect family facet/filter — additive), `docs/creative-director/reports/A-ASSETS.md`.

DELIVER:
1. `deterministic.ts`: `computeDeterministicMeta(buffer) → {width_px, height_px, aspect_ratio (snap like mediaProbe), dominant_colors:[{hex, share}] (top 5 via sharp resize 64px + quantized histogram), has_text: null}`; `applyDeterministicMeta(sb, fileId, meta)` (UPDATE files, `visual_meta_version='det-v1'`); `assetMetaLane.ts`: tick → flags `creative_writer.asset_enrich_v2` / `creative_backfill.asset_meta.enabled`; deterministic pass over `creative_asset_backfill_targets('meta', batch)` downloading from `wassel-files` with the service client; never fail the lane on one file; then v2 enrichment via `runBackfillBatch('asset_enrich', …)` gated by `approved_cost_usd`.
2. `rights.ts`: `classifyRights(row) → { selectable_for_production, needs_rights_confirmation, reason, badge:'verified'|'unverified'|'blocked'|'reference_only'|'ai_review' }` per contracts §0.9; `recheckRightsForFinal(sb, fileIds) → {ok, blocked:[…], unconfirmed:[…]}` (reads `files_rights_v`).
3. Picker + filter bar additions (bilingual, RTL-safe).
4. Tests: palette on a synthetic PNG (generate with sharp in the test), full rights matrix, lane flag off = no work.

Run: `cd worker && npm run typecheck && npx vitest run src/creative`; root `npx tsc --noEmit -p tsconfig.json`, `npx tsc --noEmit -p tsconfig.api.json`, `npx vitest run src/lib/files` (if tests exist).
