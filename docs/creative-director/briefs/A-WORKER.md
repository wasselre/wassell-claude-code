Agent **A-WORKER** — read `docs/creative-director/briefs/_COMMON.md` first, then contracts §0, §1, §2 (RPC names in `_20`/`_21`/`_25`), §3, §4 (apply is the API's job, not yours), §5–§7, §9, §12; `worker/src/index.ts` (lane structure: `scriptPollLoop`, `cleanTextPollLoop`, `/wake`, the `Promise.all` block near lines 3140-3240, watchdog ticks, shutdown), `worker/src/runScriptJob.ts`, `worker/src/runCleanTextJob.ts` (claim/stage/complete/fail, `notify_emit`, re-host to `marketing-assets`, `media_assets` insert, single-row patch RPC posture), `worker/src/env.ts`, `worker/src/imageGen.ts`, the sibling contract §9/§12, `CLAUDE.md` queue rules. Peers (contracted names; local interfaces if absent): A-GEN `worker/src/creative/director/runDirector.ts`; A-AI `roles.ts` (`callCreativeRole`, ledgers), `imageProvider.ts` (`resolveImageProvider`); A-FACTS `facts.ts` (`loadCreativeFacts`), `placementSpecs.ts`; A-BRAND `brandKit.ts` (`loadBrandKit`); A-VIS `lanes/designReadLane.ts`; A-ASSETS `lanes/assetMetaLane.ts` (you only REGISTER those two loops).

OWN: `worker/src/creative/lanes/types.ts` (write FIRST: `LaneDeps { supabase, env, workerId, sleep(ms), isShuttingDown(), log(msg, extra?) }`, `LaneLoop = (deps) => Promise<void>`), `worker/src/creative/lanes/creativeJobsLane.ts`, `worker/src/creative/lanes/creativeImageLane.ts`, `worker/src/creative/runCreativeJob.ts`, `worker/src/creative/runCreativeImageJob.ts`, `worker/src/creative/io.ts`, `worker/src/creative/__tests__/{io,runCreativeJob,runCreativeImageJob}.test.ts`, `worker/src/index.ts` (ONLY: import the four lane loops — `creativeJobsLoop`, `creativeImageLoop`, `designReadLoop`, `assetMetaLoop` — and register them in the `Promise.all` block in a marked `// ── creative director lanes ──` region; if a peer lane file is missing at typecheck time, register it via a guarded dynamic import that logs `console.error('[creative] lane missing: …')` instead of breaking the build), `worker/src/env.ts` (only new optional keys if truly needed), `docs/creative-director/reports/A-WORKER.md`.

DELIVER:
1. `io.ts` — all DB access: `loadJobContext(sb, job)` (content from `mos_content_v`: language, title, content_type_key, project_id/project_ids, campaign_id, organic_platforms; brief via `rpc('mos_script_brief',{p_content_id})`; targets from `job.params.targets`; existing publications/ads for target_ref; references via `rpc('mkt_creative_references', …)` with `p_include_wassel:true` and intent from concept/recipe; candidate assets via `rpc('creative_candidate_assets', …)`; `loadBrandKit`; `mos_settings.writer_rules`; `mos_settings.creative_writer`; existing design reads for reference candidates; optional intent embedding via sibling `embed('embed_image', …)` only when `process.env.MODAL_CV_URL` is set — never fatal), `nextVersion` (`rpc('mos_creative_package_next_version')`), `insertPackage`, `insertDerivatives`, `insertRefs` (references + selected assets with `rights_snapshot`), `supersedePackage`, `patchPackage` (`rpc('mos_creative_package_patch')`), `notifyRequester(event 'post_creative_ready')` exactly like `runScriptJob.ts` (`notify_emit`).
2. `runCreativeJob.ts` — kinds: `post_concepts` (stages brief→facts→brand→references→assets→targets→concepts→validate→persist; package stage 'concepts'); `post_package` (loads the concepts package + `params.concept_id` or `params.custom`; runs package + derivatives; persists stage 'package' with derivatives + refs); `post_regenerate` (previous package + `params.revision_note` → new version; old → superseded); `post_derivatives` (new `params.targets` on an existing package → new version). `mos_creative_job_stage` at each step; complete with `{package_id, needs_attention, validation}`; failures → `mos_creative_job_fail(p_error, p_error_kind)` mapping prefixes (`provider`→retryable, `facts_insufficient`, `validation_unrepaired`, `rights_blocked`, `policy_blocked`, `transient`, `unknown`). A failed job leaves `mos_content` and existing packages untouched; if derivatives insert fails after the package insert, mark that package `rejected` with `revision_note='persist_failed: …'`. Roles ledger + cost on job and package rows.
3. `creativeJobsLane.ts` — each tick read `mos_settings.creative_writer.post_enabled` (sleep 30 s when off), claim via `mos_creative_job_claim_next`, run, watchdog every 5 min, 4 s poll like `scriptPollLoop`.
4. `runCreativeImageJob.ts` + `creativeImageLane.ts` — `generation_jobs` kind `creative-image` (claim `generation_job_claim_next(worker,'creative-image')`, complete/fail as in `runCleanTextJob.ts`): params `{package_id, index, mode, prompt, source_file_ids[], aspect, must_keep[], must_change[], approved_by}`; gate `creative_writer.ai_image_execution` (fail `policy_blocked: execution disabled` when off); re-check A-GEN `policy.checkAiRecommendation`; resolve sources (service-client signed URL from `wassel-files`, or public url); `resolveImageProvider(roleForMode)` → edit/generate/combine/removeText; re-host output to `marketing-assets/creative/<content_id>/<uuid>.png`; insert `media_assets` (provenance prompt/model/settings {package_id,index,source_file_ids,mode}); create a valid `files` row (bucket marketing-assets, kind image, asset_nature 'ai_edited'|'ai_generated', usage_rights 'needs_review', production_state 'edited', acquisition_source 'internal', primary_category 'ai_content', title `AI · <mode> · <content title>`, uploaded_by_user_id = approved_by — read `2026-05-23_files_system.sql`, `2026-08-23_10_file_metadata_layers.sql`, `worker/src/marketing/assets/runAssetProcess.ts` for NOT NULLs; storage_path unique); link via `mos_assets` find-or-create + `mos_asset_links(role='reference')` (replicate the minimal insert `asset_link_from_file` does in `api/marketing-os.ts`); patch `mos_creative_package_patch(package_id, ['ai_recommendations', index, 'execution'], {...})` with status completed/failed. Never write `role='final'`.
5. Register the four loops in `index.ts`.

Tests: fake Supabase client + fake director/providers: concepts→package→derivatives happy path; failed job leaves packages untouched; requeue on `provider:`; image job refuses when flag off / policy fails; output becomes a `needs_review` candidate; never `final`. `cd worker && npm run typecheck && npx vitest run src/creative`.
