Agent **A-DB** — read `docs/creative-director/briefs/_COMMON.md` first, then contracts §0, §1, §2 (you own it), §6, §9, and `CLAUDE.md` "Database migrations", "Silent Failures", and `supabase/migrations/2026-08-03_restore_whatsapp_reply_kind.sql` (the CHECK re-listing lesson).

OWN: `supabase/migrations/2026-09-02_20_creative_jobs.sql`, `_21_creative_packages.sql`, `_22_visual_design_reads.sql`, `_23_design_examples_wassel_org.sql`, `_24_files_signals_rights.sql`, `_25_performance_flags_jobs.sql`, `docs/creative-director/schema-verification.sql` (a verification script the lead runs after applying: existence checks + sample calls), `docs/creative-director/reports/A-DB.md`. Do NOT write `_26` (A-BRAND), `_28` (A-VIS), `_29` (A-ASSETS).

Write the migrations exactly per contracts §2: tables with RLS enabled and no policies; indexes; SECURITY DEFINER RPCs with `SET search_path TO 'public'`, `REVOKE ALL … FROM PUBLIC`, `GRANT EXECUTE … TO service_role`; idempotent; additive. Study the existing style in `2026-09-01_10_mos_script_jobs.sql` (claim/complete/fail/watchdog) and `2026-09-02_12_mos_script_drafts.sql` (in the sibling contract; the RPC bodies are described there and the objects exist live) and `2026-08-31_08_content_library_project_link.sql` (the library RPC to re-issue as v5 with media ORDER BY carousel_index and internal-org exclusion), `2026-08-31_05_competitor_watch_surfaces.sql`, `2026-08-31_11_attribution_queue_perf_fix.sql` (jsonb-returning RPC style), `2026-08-23_10_file_metadata_layers.sql` + `2026-08-16_01_business_file_metadata.sql` (files columns, provenance, vocabularies), `2026-08-10_file_links_projection.sql` (file_links + role), `2026-07-25_content_understanding.sql` (mkt_content_media / visual_text), `2026-07-22_marketing_intelligence_core.sql` (mkt_content_posts.engagement jsonb, mkt_organizations, mkt_social_accounts), `supabase/branch-bootstrap-02.sql` lines 5275-5616 (mos_publications, mos_metric_snapshots, mos_assets, mos_asset_links), `2026-08-01_04_notifications_platform.sql` (notify_emit; do NOT seed rules — not needed).

Specifics:
- `_20`: `mos_creative_jobs` + `mos_creative_job_enqueue(p_content_id uuid, p_kind text, p_params jsonb, p_requested_by uuid) RETURNS uuid` (raise `active_job_exists` when the partial unique index conflicts — catch unique_violation), `mos_creative_job_claim_next(p_worker_id text)` RETURNS TABLE(job_id uuid, content_id uuid, kind text, params jsonb, requested_by uuid, attempts int) with FOR UPDATE SKIP LOCKED and lease_expires_at = now()+interval '10 minutes', `mos_creative_job_stage(p_job_id, p_stage)`, `mos_creative_job_complete(p_job_id, p_result jsonb, p_roles jsonb, p_cost_usd numeric)`, `mos_creative_job_fail(p_job_id, p_error text, p_error_kind text)` (requeue: status='queued', worker_id NULL, lease NULL when attempts < max_attempts AND p_error_kind IN ('provider','transient'); else 'failed'), `mos_creative_job_cancel(p_job_id)` (queued→cancelled only), `mos_creative_jobs_watchdog()` RETURNS int (running with lease_expires_at < now() → failed, error_kind 'watchdog').
- `_21`: `mos_creative_packages`, `mos_creative_derivatives`, `mos_creative_refs` per contract; `mos_creative_package_next_version(p_content_id) RETURNS int`; `mos_creative_package_patch(p_package_id uuid, p_path text[], p_value jsonb)` = `UPDATE … SET base = jsonb_set(base, p_path, p_value, true), updated_at = now()`; trigger to bump updated_at.
- `_22`: `visual_design_reads` (+ `embedding vector(768)` nullable, HNSW partial index, generated columns `layout_family text GENERATED ALWAYS AS (read->>'layout') STORED`, `density`, `branding_intensity int`, `palette_family`, `format`, `slide_role`), `visual_design_read_upsert(...)` RETURNS uuid (ON CONFLICT on the unique key → update read/confidence/cost/raw/embedding/status), `creative_design_read_targets(p_subject_kind text, p_level text, p_rule_version text, p_model_used text, p_tier int, p_limit int)` RETURNS TABLE(subject_kind text, subject_id uuid, post_id uuid, slide_index int, organization_id uuid, stored_url text, post_type text) implementing tiers: competitor statics (`mkt_content_posts.post_type IN ('image','carousel')`, media `media_kind='image' AND download_status='stored'`, org_type <> 'internal'): tier 1 = enrichment `primary_project_id IS NOT NULL AND (result->>'is_general_branding')::boolean IS DISTINCT FROM true`; tier 2 = not general branding AND published_at > now()-interval '12 months'; tier 3 = post_type='carousel'; tier 4 = all remaining; tier 5 = internal-org (Wassel) posts → returned with subject_kind 'wassel_content' (post level, subject_id = post id) / 'wassel_file' (slide level, subject_id = media id) — the lane maps them; `p_subject_kind` filters accordingly; exclude subjects that already have a row for (level, p_model_used, p_rule_version). `mkt_creative_references(p_project_id uuid, p_district text, p_unit_types text[], p_purpose text[], p_intent jsonb, p_include_wassel boolean, p_qvec vector(768), p_limit int)` RETURNS TABLE(ref_kind text, ref_id uuid, post_id uuid, slide_index int, level text, preview_url text, org_name text, platform text, published_at timestamptz, post_url text, score numeric, why jsonb, read jsonb) — candidates: competitor static posts (post-level, ref_kind 'competitor_post', preview = first media by carousel_index) and their slides ('competitor_media'); scoring in SQL: +3 purpose match (result->>'content_type' = ANY(p_purpose)), +2 district match (result->>'district' ILIKE '%'||p_district||'%'), +1 unit-type overlap, +1 recency (<12 months), −5 when `is_general_branding=true AND NOT ('brand' = ANY(p_purpose))`, +2 when a `visual_design_reads` post-level read exists and matches p_intent keys (format, layout, density, branding_intensity, palette_family — count matches), +3×cosine when p_qvec not null and embedding not null, +2 when in `mos_design_examples` (approved_wassel) and p_include_wassel; internal org excluded unless p_include_wassel (then its posts get ref_kind 'wassel_content'); tie-breaker `(engagement->>'likes')::numeric`; diversity ≤2 per organization via row_number; return `why` jsonb listing the matched criteria.
- `_23`: `mos_design_examples` (+CHECK competitor_post ⇒ study_only) and the Wassel internal org seed: INSERT `mkt_organizations(org_type='internal', name_ar='وصل العقارية', name_en='Wassel Real Estate', website='https://wassel.re', status='active')` only if no row with name_en='Wassel Real Estate' exists; then `mkt_social_accounts` rows for the four handles (provider 'apify', is_active true, collection_enabled FALSE — the operator enables collection explicitly; note this in the report) ON CONFLICT DO NOTHING (check the unique constraints on that table via its migration `2026-07-22_marketing_intelligence_core.sql` and use the matching conflict target, else guard with NOT EXISTS). `mkt_content_library` v5: CREATE OR REPLACE with the full v4 body + `ORDER BY m.carousel_index` for the media array + `AND o.org_type <> 'internal'` in the base filter.
- `_24`: files columns; `files_rights_v`; `creative_candidate_assets(p_project_id uuid, p_limit int)` RETURNS TABLE(file_id uuid, original_name text, primary_category text, document_type text, link_role text, asset_nature text, acquisition_source text, usage_rights text, rights_provenance text, rights_verified boolean, production_state text, aspect_ratio text, width_px int, height_px int, ai_description text, tags text[], subjects text[], dominant_colors jsonb, has_text boolean, headline_space text, storage_bucket text, storage_path text, created_at timestamptz) — files joined via `file_links` (model = the all_projects model: resolve `models.name='all_projects'`) with kind='image', archived_at IS NULL, EXCLUDING usage_rights IN ('restricted','do_not_use'); ORDER BY (rights_verified AND usage_rights IN ('approved','use_after_edit')) DESC, (acquisition_source IN ('developer','internal')) DESC, (production_state='raw') DESC, (asset_nature IN ('real','cgi_render')) DESC, created_at DESC. `creative_asset_backfill_targets(p_kind text, p_limit int)` RETURNS TABLE(file_id uuid, storage_bucket text, storage_path text, mime_type text) — 'meta': project-linked images with width_px IS NULL OR dominant_colors IS NULL; 'enrich': project-linked images with `visual_meta_version` IS DISTINCT FROM 'enrich-v2' (ordered: projects with active campaigns first — join mos_campaigns status='active' on project_ids — then rights_verified, then created_at).
- `_25`: `mos_content_performance_v`; generation_jobs CHECK re-list + 'creative-image'; claude_jobs CHECK re-list + 'mkt_visual_design_slide','mkt_visual_design_post' (read the aqar-lane migration to also extend the lane→kind mapping arrays if they are in a function: `v_ocr_kinds` — ADD both design kinds to the OCR lane array so the existing OCR runner lease serves them; CREATE OR REPLACE that function with its full original body); `creative_backfill_runs` + `creative_backfill_run_start(p_kind text, p_tier int, p_worker_id text) RETURNS uuid` / `creative_backfill_run_finish(p_run_id uuid, p_status text, p_processed int, p_failed int, p_cost_usd numeric, p_note text)`; `mos_settings` seeds (creative_writer all false; role_map; creative_backfill {design_reads:{enabled:false,lane:'runner',batch_size:24,tiers:[1,2,3,4],pilot_ids:[]}, asset_meta:{enabled:false,batch_size:50}, asset_enrich:{enabled:false,lane:'worker',batch_size:20,approved_cost_usd:0,estimated_cost_per_item:0.004}}; writer_rules seeded from the two skills' hard rules + Decisions Logs as `{shared:[…], post:[…], video:[…], decisions_log:[{date,note,source}]}` — read `.claude/skills/writing-post/SKILL.md` and `.claude/skills/writing-video-script/SKILL.md` and transcribe faithfully); `ai_roles` additive merge: `UPDATE mos_settings SET value = value || (SELECT jsonb_object_agg(k, v) FROM jsonb_each('{…nine keys…}'::jsonb) e(k,v) WHERE NOT (value ? k)) WHERE key='ai_roles'` with the values in contracts §5 (provider 'fal' entries are fine as data).
- `schema-verification.sql`: SELECTs the lead runs to prove every object, plus sample calls (`mkt_creative_references(null,null,null,array['project_launch'],'{}'::jsonb,false,null,5)`, a candidate_assets call for a project id found via `select id from unified_records where model_id='220c49b9-de57-492d-9eca-c0d9f54fd40f' limit 1`, `creative_design_read_targets('competitor_media','slide','v1','none',1,5)`, and a check that a known carousel post's media come back in index order from `mkt_content_library`).

Report the exact final RPC signatures and return columns (A-WORKER/A-VIS/A-ASSETS/A-API code against them).
