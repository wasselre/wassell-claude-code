Agent **A-UI** — read `docs/creative-director/briefs/_COMMON.md` first, then contracts §0, §1, §4 (actions + results), §11, §12; `src/lib/creative/contracts.ts` (UI types: CreativePackageRow, BasePackage, Derivative, VisualAdaptation, ReferencePick, AssetPick, AiRecommendation, DesignerHandoff, BrandKit, WriterRules, RoleMap, CreativeFlags, Concept). MOS conventions: `src/pages/Marketing/ContentDetailPage.tsx` (tabs ~57, header actions ~449-503, the video-script button ~450, tab rendering ~691-760, `can()`, `useWorkspace()`, `isAr`, mobile), `src/pages/Marketing/components/{WritingFields,PlacementsTab,MaterialsTab,VideoScriptModal,kit}.tsx`, `src/pages/Marketing/mos.css` (tokens `--copper --choc --gold --sand --line --mute --ink`; classes `.btn .btn-p .btn-d .btn-go .write .doc-lbl .fld .tag .notice .inp .fsel`), `src/pages/Marketing/MarketingWorkspace.tsx` (nav ~158-201), `src/App.tsx` `/m` routes (~515-546), `src/pages/Marketing/components/SettingsAudiences.tsx` (settings page pattern), `src/pages/Marketing/lib/format.ts`, `src/pages/CompetitorWatch/components/ContentLibrary.tsx` + `watch.css`, `src/pages/Files/components/FilePreviewModal.tsx`, `src/pages/Files/library/FilePickerModal.tsx` (A-ASSETS adds `filters`/`showMeta` props). A-API provides `src/lib/marketingOS/creativeClient.ts` and competitorWatch wrappers (names listed in `docs/creative-director/briefs/A-API.md`) — if absent when you start, CREATE `src/lib/marketingOS/creativeClient.ts` with exactly those signatures using `mosCall` from `client.ts` (add the export line `export { call as mosCall, authHeader as mosAuthHeader };` yourself if missing) and say so in your report.

OWN: `src/pages/Marketing/components/creative/**` (CreativeTab, TargetsPicker, ConceptCards, BaseCreativeEditor, SlideNavigator, DerivativesPanel (dimensions + full VisualAdaptation per target: crop/extend, text/logo reposition, layout changes, scaling, slide mapping, asset substitutions, `requires_separate_design` badge), ReferencesPanel (real previews; aspect chip; why/study/adapt/do-not-copy/differ; remove), AssetsPanel (thumbnails, nature/source/rights/verified badges, placement/usage/treatment, Replace → FilePickerModal with project filters, needs-confirmation ribbon), AiRecommendationsPanel (mode, prompt, sources, must keep/change, policy line; Approve execution only when `flags.ai_image_execution`; Dismiss; execution status + output candidate preview), PalettePanel (swatches with roles, kit mode tag, contrast hint), WarningsPanel, VersionsBar, HandoffView (concise designer handoff: message/objective → exact on-design copy → slide plan → palette → exact assets → references → composition/treatment → approved AI production → warnings; copy buttons; print-friendly), `__fixtures__/package.json` (realistic Arabic fixture: single + carousel + organic and paid targets + AI recs)), `src/pages/Marketing/components/{SettingsBrandKit,SettingsWriterRules,SettingsAiRoles,SettingsCreativeFlags}.tsx`, `src/pages/Marketing/ContentDetailPage.tsx` (ADDITIVE: header button «اكتب بوست» / “Write post” for post|carousel + `can('write_content')` + project linked + `flags.post_enabled`; a `creative` tab (post|carousel; gate `view_content_body`) rendering `CreativeTab`; when the open task's `role_key` equals `role_map.design_owner` show `HandoffView` first; job progress from `fetchCreativeJobStatus` polled every 4 s while queued/running), `src/pages/Marketing/MarketingWorkspace.tsx` + `src/App.tsx` (settings sub-routes `/m/settings/brand-kit`, `/m/settings/writer-rules`, `/m/settings/ai-roles`, `/m/settings/creative-flags` gated `manage_settings`; nav entries), `src/pages/CompetitorWatch/components/ContentLibrary.tsx` (design-read chip via `fetchDesignRead` on expand; «مثال للدراسة» study-example action for admins), `docs/prd/marketing-workspace.md` + `docs/prd/competitor-watch.md` (Key behaviors + Key files entries; Last updated 2026-09-02), `docs/creative-director/reports/A-UI.md`.

RULES: bilingual everywhere; RTL-first; mobile stacks via classes (not inline grids); no `any`; every save through the wrappers with loud `.notice.bad` errors; edits create a new human version (server) — render the returned package; Apply disabled while blocking warnings or `needs_rights_confirmation` without the confirm checkbox («أؤكد حقوق الاستخدام لهذه الأصول»); Regenerate requires a note; show `intended_use` + targets prominently; never derive purpose from placements in the UI.

Verify: `npx tsc --noEmit -p tsconfig.json`. If you can run the dev server headlessly, render the tab with the fixture behind a dev-only `window.fetch` mock for `/api/marketing-os` (vite has no `/api`), otherwise describe the manual check in the report.
