Agent **A-API** — read `docs/creative-director/briefs/_COMMON.md` first, then ALL of `docs/creative-director-contracts.md` (esp. §0, §1, §2 RPC names, §4 exact actions/payloads/gates, §12) and `src/lib/creative/contracts.ts`. In `api/marketing-os.ts` read: the `switch (action)` dispatch, helpers (`requireCap`, `jsonOk`, `jsonError`, `dbFail`, `str`, `makeServiceClient`, `emitNotify`), the video-script actions (~2227-2287 and any `script_*` cases), `content_caption_save` (~2473-2520), `content_ad_creative_save` (~2548-2630), `paid_placement_targets`, `asset_link_from_file` (~6237), `content_set_approval_asset` (~6317), `content_update` + `CONTENT_EDITABLE` (~372-381), `publication_publish` success path + `publication_sync` (~2751-3050), `CAPABILITIES` (~624-636). In `api/marketing.ts`: the action pattern + gate (`wassell_mkt_can`), `content_library`, `attribution_queue`, `run_*` admin actions. `src/lib/marketingOS/client.ts` (`authHeader` ~364, `call` ~420 — not exported; wrapper style), `src/lib/competitorWatch/client.ts`, `api/templates/clean-listing-images.ts` (`wakeWorker`). Peers (contracted names; local interfaces if absent): A-BRAND `api/_lib/marketing/creative/brandKit.ts`; A-ASSETS `api/_lib/marketing/creative/rights.ts` (`recheckRightsForFinal`, `classifyRights`); A-VIS `api/_lib/marketing/creative/onPublished.ts`; A-FACTS `src/lib/marketingOS/platformRules.ts` (`masterAspectFor`, `PLACEMENT_SPECS`); A-GEN's `worker/src/creative/director/policy.ts` (mirror it as `src/lib/creative/policy.ts` — identical pure logic; if A-GEN's file is absent, write the policy per contracts §7 yourself and note it).

OWN: `api/_lib/marketing/creative/{targets,packages,apply,handoff,settings,examples,performance,reads,wake}.ts` (one exported handler per action taking `{sb, svc, body, userId}` and returning a Response via the same helpers), `api/marketing-os.ts` (ONLY a marked block `/* ── creative director ── */` with a `case` per action in §4 delegating to the handlers; plus the 3-line best-effort call to `enqueueWasselReadsOnPublish` after a successful publish and on sync→published), `api/marketing.ts` (marked block for `design_read_get`, `design_reads_status`, `creative_backfill_status`, `creative_backfill_control`, `wassel_internal_status`), `src/lib/creative/policy.ts`, `src/lib/marketingOS/creativeClient.ts` (typed wrappers for every action; names: `fetchCreativeFlags, fetchCreativeTargets, writePostCreative, selectCreativeConcept, regenerateCreative, fetchCreativeJobStatus, listCreativePackages, fetchCreativePackage, saveCreativePackage, replaceCreativeAsset, applyCreativePackage, revertCreativePackage, approveCreativeAi, dismissCreativeAi, fetchCreativeHandoff, fetchCreativePerformance, fetchBrandKit, saveBrandKit, reviewBrandKit, fetchWriterRules, saveWriterRules, fetchRoleMap, saveRoleMap, saveCreativeFlags, fetchAiRoles, saveAiRoles, setDesignExample, listDesignExamples`), `src/lib/marketingOS/client.ts` (ONLY add one line `export { call as mosCall, authHeader as mosAuthHeader };` near their definitions), `src/lib/competitorWatch/client.ts` (wrappers `fetchDesignRead, fetchDesignReadsStatus, fetchCreativeBackfillStatus, controlCreativeBackfill, fetchWasselInternalStatus`), tests for pure helpers under `src/lib/creative/__tests__/` (vitest), `docs/creative-director/reports/A-API.md`.

BEHAVIOUR (exact):
- `creative_targets`: organic from `organic_platforms` + `mos_publications` (publication_id); paid via the same query `paid_placement_targets` uses (campaign→executions→ad sets→ads) for the content's campaign and any campaign; default placement types (instagram feed|carousel by content type, tiktok photo_mode, snapchat story, x post, meta ad_feed, google ad_display); `selected` = has a row; `suggested_master_aspect` via `masterAspectFor`.
- `write_post_creative`: require a project; `content_type_key ∈ post|carousel` (server-side); flag `post_enabled` on (else 403 bilingual); `rpc('mos_creative_job_enqueue', …)` with params `{targets, recipe, intended_use, language}`; unique-violation → 409 bilingual; best-effort `/wake` (copy `wakeWorker` into `wake.ts`, env `WASSEL_DECK_WORKER_URL`).
- `creative_concept_select` / `creative_regenerate` / derivatives regeneration → enqueue kinds with params; 409 on active job.
- `creative_package_get` (gate `view_content_body`): package + derivatives + refs + `previews` map (competitor media → `mkt_content_media.stored_url` public; our files → 1 h signed URL via `svc.storage.from(bucket).createSignedUrl(path, 3600)`; wassel_content → its final asset url when any).
- `creative_package_save`: minimal shape validation; NEW version `generated_by='human'`; supersede previous draft; carry derivatives/refs forward with edits.
- `creative_package_apply`: load package (stage 'package', status 'draft'); `recheckRightsForFinal` on `is_production` assets → 422 `rights_blocked` when blocked; unverified allowed only with `confirm_unverified_rights`; capture `applied_snapshot` BEFORE any write (prior `data.headlines/design_brief/hashtags/design_reference_file_ids`, each target publication caption, each target ad creative, existing asset links); then: `mos_content.data` merge (headlines APPEND generated lines — carousel lines prefixed «١/٦ » — unless `overwrite.headlines`; design_brief set only if empty unless overwrite; hashtags set only if empty unless overwrite; design_reference_file_ids union with our-file refs); organic derivatives → same semantics as `content_caption_save` (lazy-upsert draft publication; set caption only if empty unless `overwrite.captions`) — do NOT move existing code; replicate the minimal upsert with a comment pointing at the case; paid derivatives → `mos_execution_ads.creative` keys only when `target_ref.execution_id` (create the waiting ad via the same semantics `content_ad_creative_save` uses) unless `overwrite.ad_copy`; production assets → `mos_assets` find-or-create + `mos_asset_links(role='source')` (replicate `asset_link_from_file`). Set package `status='applied'`, `applied_at/by`, derivatives 'applied'. On mid-way failure restore already-written fields from the snapshot and return 500 listing what was restored.
- `creative_package_revert`: restore every field from `applied_snapshot`; package → 'superseded'.
- `creative_ai_approve`: flag `ai_image_execution`; recommendation `status='recommended'`; policy re-check (`src/lib/creative/policy.ts`); insert `generation_jobs` row kind `creative-image` (`record_id=content_id`, `message_id='<package_id>:<index>'`, `user_id`, `params`); patch recommendation `{status:'queued', execution:{job_id, approved_by, approved_at}}` via `mos_creative_package_patch`; `/wake`.
- `creative_ai_dismiss`: patch status 'dismissed'.
- `creative_handoff`: `DesignerHandoff` from the latest applied package (fallback latest draft with `draft:true`), asset previews + names, target dims, adaptations, approved AI production only, `ai_suggested_not_approved`, `role_map`.
- `creative_performance`: `mos_content_performance_v` + applied package summary.
- settings: `brand_kit_get/save/review` (review requires `approve_creative`: status reviewed, mode constraint, version+1, reviewed_by/at), `writer_rules_*`, `role_map_*`, `creative_flags`/`creative_flags_save`, `ai_roles_get/save` (validate provider/model shape; merge; never drop keys).
- `design_example_set/list` (`approve_creative`) with previews.
- marketing.ts actions per §4; `creative_backfill_control` updates `mos_settings.creative_backfill.<kind>` (admin gate like `run_*`).

Typecheck: `npx tsc --noEmit -p tsconfig.api.json` and root `npx tsc --noEmit -p tsconfig.json`; `npx vitest run src/lib/creative`.
