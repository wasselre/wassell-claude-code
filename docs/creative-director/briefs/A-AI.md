Agent **A-AI** — read `docs/creative-director/briefs/_COMMON.md` first, then contracts §0, §5, §9, §12, §13; the sibling contract §4 and §12; the REUSED adapter (never edit): `worker/src/ai/{types,roles,index,pricing}.ts`, `worker/src/ai/providers/{anthropic,modalEmbed}.ts`, `worker/src/ai/__tests__/*`; `worker/src/imageGen.ts` (`imageGenChat`, `imageGenDesign`, `imageGenTextRemoval`, `pollImageGen`, stub mode when `FAL_KEY==='stub'`); `scripts/eval/_lib/env.mjs`, `scripts/_lib/serviceClient.mjs`, `scripts/eval-project-message-ai.mjs`; `scripts/claude-study-runner.mjs` lines 100-200 and 330-520 (how `claude_jobs` are enqueued/claimed and the manifest→result contract).

OWN: `worker/src/creative/roles.ts`, `worker/src/creative/imageProvider.ts`, `worker/src/creative/runnerProvider.ts`, `worker/src/creative/__tests__/{roles,imageProvider,runnerProvider}.test.ts`, `scripts/eval/creative-build-sets.mjs`, `scripts/eval/creative-run.mjs`, `scripts/eval/creative-compare.mjs`, `docs/eval/README-creative.md`, `docs/creative-director/reports/A-AI.md`. (Generated JSON under `docs/eval/creative-*.json` is produced by running your build script — run it once if `.env.local` allows; if the DB is unreachable, say so.)

DELIVER:
1. `roles.ts`: `CREATIVE_ROLE_KEYS` (nine keys, contracts §5), `CREATIVE_DEFAULTS` (DATA defaults marked non-final), `resolveCreativeRoles(sb, {force?})` reading `mos_settings` key `ai_roles` (same row as the sibling; only my keys; validate like the sibling's `mergeRoles`; 60 s cache; loud console.error on malformed), `callCreativeRole<T>(key, req, ctx)` → delegates to the sibling `callRole(explicitRoleConfig, req, ctx)` using the EXPLICIT `RoleConfig` object path (do not extend the sibling's union); when the resolved provider is `'runner'` delegate to `runnerProvider`; when `'fal'` throw `provider:fal role is an image role — use imageProvider`. Re-export ledger helpers.
2. `imageProvider.ts`: `interface ImageProvider { kind:'fal'|'stub'; generate(req:{prompt, aspect, n}); edit(req:{prompt, sources:string[], aspect?, keepFraming?}); combine(req:{prompt, sources:{url,role}[]}); removeText(req:{source}) }` each resolving to `{ urls:string[], provider, model, cost_usd:number|null, latency_ms }`; `resolveImageProvider(roleKey:'image_edit'|'image_generate'|'image_remove_text', sb)` reads `ai_roles`; the fal adapter wraps `worker/src/imageGen.ts` functions (never edit that file); stub when `FAL_KEY==='stub'`. Cost null when unknown.
3. `runnerProvider.ts`: `kind:'runner'` provider (own type) — `enqueueRunnerRead(sb, kind:'mkt_visual_design_slide'|'mkt_visual_design_post', items:unknown[], params?) → jobId` inserting a `claude_jobs` row (inspect the table via the runner script / `2026-07-22_claude_jobs_queue.sql`: columns kind, params, status 'queued', …), `awaitRunnerJob(sb, jobId, {timeoutMs=30*60_000, pollMs=5000})` → `{status, result}` mapping `ready`→ok, `failed`/`blocked`→throw `provider:runner …`; and a `callRole`-shaped `callViaRunner<T>(kind, req)` that packages `req.images` + `req.user` as manifest items for A-VIS's handlers.
4. Eval harness: `creative-build-sets.mjs` builds `docs/eval/creative-eval-set.json` (20 briefs = project × recipe {feature_spec, lifestyle, offer, event, launch} × format {single, carousel} mixing ready/off-plan/sold-out/conflict; you may reuse the curated project ids from the sibling worktree file `C:\Users\rayan\Claude\wassell-claude-code\.claude\worktrees\elegant-albattani-c38c42\scripts\eval\build-eval-sets.mjs` READ-ONLY) and `docs/eval/creative-design-read-pilot.json` (60 competitor static slides + 25 carousels, deterministic sha1 order, diverse orgs/content_types, each `{content_media_id, content_post_id, stored_url, carousel_index, org, content_type, labels:{slide_role:null, layout:null, palette_family:null, density:null, branding_intensity:null}}`); `creative-run.mjs --set creative-eval-set --stage concepts|package|derivatives --provider … --model …` runs A-GEN's pure director (`worker/src/creative/director/runDirector.ts`, via `npx tsx`) with a role override and NO DB writes, writing `docs/eval/results/<date>-<set>-<model>.jsonl` (validator pass, grounding pass, rule pass, latency, cost, usage per item; if the director module is missing, run facts + validators only and print that); `creative-compare.mjs` prints a comparison table across result files. README documents usage. Nothing may hardcode a model: model from args or `ai_roles`.

Tests: `cd worker && npx vitest run src/creative` with injected fake providers/clients (no network). Typecheck `cd worker && npm run typecheck`.
