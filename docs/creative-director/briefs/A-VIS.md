Agent **A-VIS** — read `docs/creative-director/briefs/_COMMON.md` first, then contracts §0, §1, §2 (`_22`, `_23`, `_25`), §3, §5, §9, §12; `src/lib/creative/contracts.ts` (`SlideRead`, `PostRead`, `VisualDesignReadRow`); the sibling contract (their `mkt_cv_*` is VIDEO; you do STATIC images/carousels; reuse their vocab style); `scripts/claude-study-runner.mjs` fully (HANDLERS ~699, `handleMktVisualOcr` 426-517 — copy its staging/validation/retry, `runClaude`, `withJobTimeout`, `claude_job_*` RPCs, lane/lease mapping); `.claude/skills/visual-ocr/SKILL.md`; `scripts/lib/mkt-enrichment-validate.mjs` + its test; `worker/src/marketing/content/sweepBacklog.ts`; `worker/src/marketing/content/vision.ts`; `worker/src/ai/{types,roles,index}.ts` (REUSE `callRole` with `images`, `embed('embed_image')` when `MODAL_CV_URL` is set; never edit); `worker/src/creative/lanes/types.ts` (A-WORKER writes it — if absent define the identical interface locally). Peers: A-AI `worker/src/creative/roles.ts` (`callCreativeRole('design_read_slide'|'design_read_post', req, ctx)`), `runnerProvider.ts` (`enqueueRunnerRead`, `awaitRunnerJob`); A-DB RPCs `visual_design_read_upsert(...)`, `creative_design_read_targets(p_subject_kind, p_level, p_rule_version, p_model_used, p_tier, p_limit)`, `creative_backfill_run_start/finish`; claude_jobs kinds `mkt_visual_design_slide` / `mkt_visual_design_post` (A-DB adds them to the CHECK and to the OCR lane's kind array).

OWN: `.claude/skills/visual-design-read-slide/SKILL.md`, `.claude/skills/visual-design-read-post/SKILL.md`, `scripts/claude-study-runner.mjs` (ADD two handlers + register them; do not change existing handlers), `scripts/lib/visual-design-validate.mjs` + `scripts/lib/visual-design-validate.test.mjs` (match the existing validator test runner), `worker/src/creative/designRead/{prompts,schemas,readSlide,readPost,persist,wasselOnPublish}.ts`, `worker/src/creative/lanes/designReadLane.ts`, `worker/src/creative/backfill.ts` (shared controller; A-ASSETS reuses), `worker/src/creative/__tests__/{designRead,backfill}.test.ts`, `api/_lib/marketing/creative/onPublished.ts` (`enqueueWasselReadsOnPublish(svc, publicationId)` — no new tables: it verifies the published assets exist and returns the subjects the sweep will pick up; A-API calls it), `docs/creative-director/design-read-vocab.md`, `docs/creative-director/reports/A-VIS.md`.

DELIVER:
1. Skills (manifest→result, headless, provider-neutral, same posture as visual-ocr): SLIDE skill = one image per item → exactly `SlideRead`; POST skill = ALL slides of a post in carousel order (+ slide reads when present) → exactly `PostRead` (narrative arc, slide relationships, recurring layout, continuity score, cover→CTA, information progression, density profile, branding intensity, design system, image strategy, copy/design relationship, learnable). Include the controlled vocabulary (identical to contracts.ts enums) in both skills and in `design-read-vocab.md`.
2. Runner handlers: `handleMktVisualDesignSlide` (items from `job.params.manifest_items` `{media_id, post_id, stored_url, carousel_index, org}`, or self-select ≤24 via `creative_design_read_targets` when params are empty), stage images to a temp dir (download `stored_url` — public), manifest, skill, validate, upsert via `visual_design_read_upsert` with `p_model_used='claude-runner:design-read'`, `p_model_task='design_read_slide'`, `p_rule_version='v1'`, `p_cost_usd=0`; `handleMktVisualDesignPost` (≤6 posts per session, all slides each). Job `result` = `{processed, failed, ids}`. Read `supabase/migrations/2026-07-29_aqar_listing_extract_lane.sql` and the runner's lane/lease logic; if the runner script itself has a kind→lane table, add the two kinds there; the SQL side is A-DB's.
3. Worker path: `readSlide(item, deps)` / `readPost(post, slides, deps)` via `callCreativeRole` with `images` (public URLs) + the same JSON schemas → `visual_design_read_upsert` with `model_used` from the result; optional embedding via `embed('embed_image', {image_urls:[stored_url]})` when `MODAL_CV_URL` is set (never fatal). Cost recorded.
4. `backfill.ts` (contracts §9): config `mos_settings.creative_backfill.<kind>` (`{enabled, lane:'runner'|'worker', batch_size, tiers, paused_at, approved_cost_usd, estimated_cost_per_item, pilot_ids}`); `creative_backfill_runs` via RPCs; tier walk with `creative_design_read_targets`; idempotent; interruptible per batch (re-read flag); versioned by `(model_used, rule_version)`; cost gate for the worker lane (`approved_cost_usd >= items × estimated_cost_per_item`, else refuse loudly); pilot mode = only `pilot_ids` (tier 0). Export `runBackfillBatch(kind, deps, opts)`.
5. `designReadLane.ts` — `LaneLoop`: flag `creative_writer.design_reads_enabled` (sleep 30 s when off); (a) incremental: newly stored competitor static media lacking reads (slide then post); (b) published Wassel assets/content lacking reads; (c) if `creative_backfill.design_reads.enabled` one `runBackfillBatch`. Lane 'runner' → `enqueueRunnerRead` (don't await in the loop); 'worker' → direct reads.
6. Subject mapping: internal-org (Wassel) collected posts → `subject_kind='wassel_content'` (post, subject_id = mkt_content_posts.id) / `'wassel_file'` (slide, subject_id = mkt_content_media.id); competitor → `competitor_post`/`competitor_media`. Document in the vocab file.

Tests: validator (enum rejection, hallucinated ids, PostRead sequence sanity), backfill (tier walk, pause, cost gate, idempotency) with a fake client, designRead with a fake callRole. `cd worker && npm run typecheck && npx vitest run src/creative`; run the validator test the way the existing `scripts/lib/*.test.mjs` run (check package.json).
