TASK: Complete the QA setup-recipe registry so every manifest row can be captured. Files: scripts/mos-qa-setups.mjs (rewrite/extend) and a SMALL ctx extension in scripts/mos-qa.mjs. Nothing else.

READ FIRST: scripts/mos-qa.mjs (harness flow + ctx), docs/marketing-reference/manifest.json (every row's setup id — the registry must cover ALL distinct setup ids that appear there), docs/marketing-reference/coverage-matrix.mjs (per-screen intent + frames notes), scripts/mos-fixtures.mjs (the refs the fixtures create: V-001..V-011, P-011.., C-002.., SR-003.., asset titles).

CTX EXTENSION in mos-qa.mjs: add to makeCtx an `api(action, payload)` helper that POSTs `${APP_URL}/api/marketing-os` with the CURRENT ROW ROLE's bearer token (the harness already has sessions per role — thread the session into makeCtx) and returns parsed json, throwing on error envelopes. Also add `resolveRef(kind, ref)` — resolves a fixture ref to an id at capture time with a per-run cache: kind 'content' → api('content_list') find by ref; 'campaign' → campaign_list; 'shoot' → shoot_list; 'asset' → asset_list find by title or ref. Then make resolveRoute use FIXTURE_IDS first and FALL BACK to resolveRef using these conventions: :v004 → content ref 'V-004', :v001 → 'V-001', :p013 → 'P-013', :c002 → campaign ref 'C-002', :e1 → first execution id of C-002 (campaign_detail), :a012 → the asset the fixtures flag for screen 22 (title match from the fixtures script — read it), :sr003 → shoot ref 'SR-003'. Unresolvable → throw (loud).

RECIPES — implement EVERY setup id present in manifest.json. Guidance per family (READ the matching sNN.html to know the exact end-state each capture needs):
- Simple navigation ids (already exist) — keep.
- Modal states: 'new-content-modal' (goto /m/content then click the «+ محتوى جديد» button via page.click on its selector — find stable selectors from the page components; prefer text-based via page.$$eval), 'new-campaign-modal' (goto /m/campaigns, click «+ حملة جديدة»), 'request-changes-modal' (goto content tasks tab as the approver role, click «طلب تعديلات»).
- Content-detail tabs: 'v004-overview'/'v004-writing'/'v004-materials'/'v004-tasks' → goto resolved route (tab via query param or click the tab button — match how ContentDetailPage switches tabs: LOCAL STATE, so click the tab by its label text). 'p013-writing' likewise. 'v001-publish'/'v001-performance-manual' similarly; 'v001-performance-connected' needs the connected sub-state — if fixtures created api-source snapshots use them, else click the state toggle if the UI has one; if truly impossible, THROW with a clear message (never fake).
- Role-suffixed ids arrive as '<setup>@role=<r>' — the harness already parses; the recipes themselves are role-agnostic.
- 'v004-role-harness' → same as v004-overview (the role comes from the row).
- 'overview-ceo' → goto /m (role ceo from the row).
- 'workflows-editor' → /m/settings/workflows; expand the first workflow's first step editor if s17 shows it expanded (click).
- 'settings-role-matrix' → /m/settings/roles; 'settings-people' → /m/settings/people; 'settings-notifications' → /m/settings/notifications; 'settings-content-types' → /m/settings/content-types with the first type EXPANDED if s27 shows the editor open.
- 'upload-queue-realism' → /m/library/upload. The s23 reference shows a QUEUE OF FILES — synthesize the queue client-side: use page.evaluate to construct File objects (small generated blobs with the mockup's file names — transcribe names from s23.html) and dispatch them to the dropzone via DataTransfer; wait for rows to render. Do NOT actually upload 184 real files — the mockup's visible rows are what matters; add enough entries to fill the visible list.
- 'shoot-request-detail' → resolved /m/shoots/:sr003; 'mobile-site-shoot'/'mobile-site-list' → same + ?mode=site; 'mobile-site-offline' → additionally page.setOfflineMode? (puppeteer: page.emulateNetworkConditions offline or context.setOffline(true)) then tick one item so the queued chip renders.
- 'mobile-*' others: goto the route (viewport already mobile from the row); 'mobile-review-sheet' clicks اعتماد to open the sheet; 'mobile-more-sheet' taps المزيد; 'mobile-publish-assistant' opens the assistant sheet; 'mobile-numbers-task' starts the one-at-a-time mode; 'mobile-account' → /m/account.
- 'zero-records-day-one' → just goto /m (run AFTER fixtures --teardown; the recipe does not tear down).
- 'search-results' → goto the route (already includes ?q=مينا) and wait for results list.
- 'nav-*' rows use 'overview-loaded@role=...' — covered.
- 'calendar-month', 'content-board', 'library-unused', 'c002-*' tabs, 'numbers-weekly-entry', 'team-followup', 'campaigns-list', 'library-grid', 'shoots-bands', 'settings-home', 'content-table', 'content-table-en', 'mywork-writer' → navigation (+ tab clicks where the route has ?tab=).
Waits: every recipe ends with fonts.ready + 400ms settle (ctx.goto does it; after clicks add a small explicit wait for the target element to be visible — page.waitForFunction on a distinctive text from the mockup).

Style: plain ESM, loud throws, no silent skips. After writing, print exactly: QA-SETUPS COMPLETE + count of recipe ids implemented vs distinct setup ids in manifest.json (must be equal — enumerate any gap explicitly).
