TASK: Implement the remaining Marketing OS API actions from docs/marketing-reference/api-contract.md (everything NOT in the "Engine" section). Files you may modify: api/marketing-os.ts, src/lib/marketingOS/client.ts ONLY. Do not touch UI pages.

READ FIRST: docs/marketing-reference/api-contract.md (the contract — implement exactly); api/marketing-os.ts as it stands after the engine rewrite; supabase/migrations/2026-08-01_02_mos_domain_batch.sql, _03_client_attributions.sql, _04_notifications_platform.sql (tables + RPCs you call).

IMPLEMENT (contract sections): Notifications (notifications_list, notifications_read via rpc mark_notifications_read, notification_rules, notification_rule_set, notification_prefs_save, remind), Campaigns (campaign_events, campaign_event_add, campaign_outcomes via rpc mos_campaign_outcomes, campaign_sign, budget_shift, campaign_save CHANGED with brief fields + requires_signature from mos_settings signature_threshold, execution_save CHANGED with platform_campaign_id/purpose + auto campaign_event logging for added/paused/resumed), Attribution (attribution_list over client_attributions_effective + superseded flag from the base table, attribution_stamp INSERT-only), Library (asset_detail, asset_archive, asset_delete CHANGED to return {error:'in_use', used_in} instead of deleting when links/publications exist, assets_unused, assets_bulk incl. create_content pre-linked), Shoots (shoot_detail, shoot_item_add), Numbers (numbers_week, metrics_skip storing extra:{skipped:reason}, metrics_record CHANGED accepting extra), Search CHANGED (surface_access filtering via the caller's levels — reuse the bootstrap surfaces computation as a helper; hidden types absent from results AND chips; add shoots+assets types; match_reason + excerpt with <mark> around the hit), Settings (settings_save upserting mos_settings; settings_data already updated by the engine task — extend it to include notification rules + mos_settings if missing).

EMISSION POINTS (call rpc notify_emit server-side): task-related emissions were left to this task if the engine task didn't add them — check task_complete: after advance, emit 'task_assigned' to the next step's role_key (p_role_keys [role]) with deep link /m/content/<id>?tab=tasks, or 'changes_requested' to the writer role on rejection; shoot_deliver → 'shoot_delivered' to the requesting content's roles; publication_save when scheduled_at set/changed → no immediate emit (the worker tick handles publish_due); campaign_save when requires_signature flips false→true → 'budget_signature' to role ceo; remind → 'manual_reminder' to the open task's role + assignee user. Wrap every notify_emit in a narrow try/catch that console.error's and CONTINUES (a notification failure must not fail the business write) — comment each catch accordingly.

CONVENTIONS: bilingual error envelope + DB_MESSAGES pattern as the file already does; deterministic ordering on every list; no `any`; validate payloads first. Response/type additions go into src/lib/marketingOS/client.ts with explicit interfaces.

VERIFY: npm run typecheck:api passes AND npx tsc --noEmit passes. Print exactly: API-DOMAIN COMPLETE + counts of actions added/changed.
