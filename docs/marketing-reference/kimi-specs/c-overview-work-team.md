TASK: Rebuild the manager/CEO/team surfaces to EXACTLY match design screens 01 (نظرة عامة), 02 (مهامي), 34 (نظرة عامة CEO), 35 (متابعة الفريق). Files: src/pages/Marketing/OverviewPage.tsx, WorkPage.tsx, NEW TeamPage.tsx if /m/team currently lacks its own page (check App.tsx routing + reuse what exists), additive CSS at END of mos.css under `/* === s01/s02/s34/s35 additions === */`.

GROUND TRUTH: READ docs/marketing-reference/source/screens/s01.html, s02.html, s34.html, s35.html. Bilingual verbatim-Arabic; num() digits.

DATA/API: overview, work_list, roles_list, task_transfer, task_update if it exists (else transfer only), remind (contract), campaign_outcomes, notifications actions. Read api-contract.md.

S01 (marketing_manager overview): the four numbers + the two decision lists exactly as mocked; period segmented control هذا الأسبوع/الشهر/الربع WORKING (overview action may need a period param — extend it in api/marketing-os.ts if missing, keeping response shape); stalled rows carry «تذكير» buttons calling the remind action (optimistic toast on ok); density/labels matched to the crop.
S02 (مهامي): role-scoped task list exactly as mocked PLUS the «القادم إليك» band — upcoming steps for MY role from pinned-version paths of in-flight content (visible, explicitly NOT tasks — the mockup's copy explains; derive: for each in-flight content, if a FUTURE step in its pinned steps has role_key = my role, list it with the content ref + the step label + expected order).
S34 (CEO overview): the ROLE STATE of /m for role ceo — month/quarter/year segmented; CRM-derived funnel (campaign_outcomes aggregated over active campaigns); campaigns-by-return list; 6-month production chart (content published per month — derive from publications); «بانتظار توقيعك» card listing campaigns requires_signature && !signed_at with the sign action (campaign_sign — CEO only); NO task lists anywhere on the CEO state. Render the CEO variant from the SAME OverviewPage component branched on active role = 'ceo' (the reference crop for s34 is captured as the ceo role).
S35 (متابعة الفريق): role-load tiles (open tasks per role); sort modes as mocked; stalled column with «تذكير»; imbalance card with تأجيل/تقديم actions (use task_transfer/available actions; if a needed action is missing, wire the button to the contract name and add the action to api/marketing-os.ts minimally); pending-approvals split (creative vs process); «ما لا تستطيع فعله» card verbatim (the ops-supervisor limitation copy).

VERIFY: npx tsc --noEmit clean. Print exactly: S01-S02-S34-S35 REBUILT + files changed.
