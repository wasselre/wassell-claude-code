TASK: Rebuild the settings ENGINE surfaces to EXACTLY match design screens 17 (مسارات العمل), 25 (الإعدادات home), 33 (الأدوار والصلاحيات), 37 (الأدوار ومن يشغلها), 43 (الإشعارات). Files: src/pages/Marketing/SettingsPage.tsx and/or split sections into src/pages/Marketing/components/Settings*.tsx (keep /m/settings/:section routing), a NEW file src/pages/Marketing/styles/settings-engine.css (import from SettingsPage.tsx; .mos-root-scoped; do NOT touch mos.css).

GROUND TRUTH: READ docs/marketing-reference/source/screens/s17.html, s25.html, s33.html, s37.html, s43.html. Bilingual verbatim-Arabic; num() digits.

DATA/API (api-contract.md): workflow_save (canonical role_path rows; DB trigger writes versions), settings_data, settings_save, surface_matrix/surface_set, roles_list/role_grant, task_transfer, notification_rules/notification_rule_set, notifications_list/notifications_read, bootstrap (workflows + settings).

S17 workflows editor over the CANONICAL row: drag-reorder steps (@dnd-kit — repo dep), expanded step editor (role select over the 5 role keys, due_days, is_approval + approval_kind, require_note_on_reject, creates_revision, required_fields/required_files chip editors), «ثم» consequence preview line per step exactly as mocked, add/delete/duplicate step, save via workflow_save (response returns new version_no — show it); the TRUTHFUL freezing warning verbatim from the mockup (in-flight records keep their pinned version; new records get the new one); side cards exactly as mocked (nine actions list, five roles list).
S25 settings home: six state-declaring cards exactly as mocked (each states the CURRENT value from mos_settings/bootstrap — signature threshold, shoot thresholds, attribution window, external effects, platforms count, content types count — match the mockup's card set) + the bottom risk banner verbatim; threshold edits via settings_save.
S33 roles matrix page over surface_access: table with ●/○/— cells EDITABLE (click cycles full→read→hidden via surface_set, optimistic), effective immediately (context refresh); legend verbatim; «لماذا صفّان للاعتماد» card verbatim; workflow-wins card verbatim. The nav consults surfaces already (workspace ctx) — after surface_set, refetch bootstrap.
S37 people page: canonical multi-role assignment — people list with role chips, grant/revoke via role_grant (multi-role allowed), backups section as mocked, single-holder warning rows (roles with exactly one holder), explicit open-task transfer flow (list open tasks of a person, transfer via task_transfer to another holder) exactly as the mockup lays it out.
S43 notifications settings: per-role rules matrix over notification_rules (editable pills toggling enabled via notification_rule_set), the WhatsApp-blocking-only rule copy verbatim, the CEO comparison card («إشعاران فقط») verbatim; PLUS the bell + notification center (this task owns them): bell icon in the workspace header rail with unread badge (bootstrap.unread_notifications + realtime-ish refetch on interval 60s), dropdown/panel listing notifications_list with unread styling, mark-all-read, deep links navigating in-app.

VERIFY: npx tsc --noEmit clean. Print exactly: SETTINGS-ENGINE REBUILT + files changed.
