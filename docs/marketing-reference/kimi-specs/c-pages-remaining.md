TASK: Build the remaining desktop pages to EXACTLY match design screens 22 (تفاصيل المادة), 24 (طلب تصوير), 41 (غير المستخدمة), 42 re-evidence (طلبات التصوير), 44 (نتائج البحث), 45 (اليوم الأول), 50 (إدخال أرقام الأسبوع). Files: NEW src/pages/Marketing/AssetDetailPage.tsx (/m/library/:assetId), NEW ShootRequestPage.tsx (/m/shoots/:requestId), NEW LibraryUnusedPage.tsx (/m/library/unused), ShootsPage.tsx (re-verify vs s42), SearchPage.tsx, NumbersPage.tsx, NEW components/EmptyDayOne.tsx (used by OverviewPage on zero records — wire it in OverviewPage minimally), route registrations in src/App.tsx (inside the /m tree only), additive CSS at END of mos.css under `/* === s22/s24/s41/s44/s45/s50 additions === */`.

GROUND TRUTH: READ the matching sNN.html files in docs/marketing-reference/source/screens/. Bilingual verbatim-Arabic; num() digits.

DATA/API (api-contract.md): asset_detail, asset_archive, asset_delete (blocked-delete contract), assets_unused, assets_bulk, shoot_detail, shoot_item_add, shoot_deliver, search (upgraded), numbers_week, metrics_skip, metrics_record, content_create.

S22 AssetDetailPage: player/preview + spec caption (dims/size/duration); «مستخدم في» list with live-ad flag; «النسخ» parent/child version chain; details rail (shot_by, shot_on, rights with expiry countdown chip as mocked); DELETE BLOCKED with the mockup's explanation when used (asset_delete returns in_use) + أرشفة alternative.
S24 ShootRequestPage: shot table (items with scene/content links, done ticks via shoot_item_toggle) + catch-all row + «إضافة لقطة» (shoot_item_add); الرحلة info card; «يفكّ تعطّل» card listing the content items this request unblocks (items' content refs); «رفع التصوير» deep link to /m/library/upload?shoot=<id> (existing delivery wire).
S41 LibraryUnusedPage: selection checkboxes, filters, bulk actions bar — إنشاء محتوى (assets_bulk create_content → navigates to the new content), أرشفة, وسم (tag prompt); shoot-origin framing text verbatim.
S42: re-verify ShootsPage against s42.html — thresholds now come from mos_settings (bootstrap.settings.shoot_grouping_thresholds) instead of hardcoded constants; fix any drift.
S44 SearchPage: results with match_reason + <mark> excerpts (server provides; render safely — the excerpt is server-built from trusted DB content, still render via a sanitizer that ONLY allows <mark> — implement a tiny splitter, no dangerouslySetInnerHTML of raw strings); per-type count chips; shoots + assets result types with thumbnails; types the caller's surfaces hide are ABSENT (server already filters; the client renders only what arrives — no leaked type chips).
S45 EmptyDayOne: when overview returns zero content AND zero campaigns: the honest empty block verbatim; الإعداد الأولي checklist AUTO-CHECKED from real state (content types exist? workflows exist? roles assigned? platforms configured? — each check from bootstrap data); «ابدأ من مادة موجودة» CTA to /m/library; post-first-create rules copy; no-demo-data panel verbatim.
S50 NumbersPage weekly entry: by-platform cards (platform-specific column sets exactly as mocked; TikTok watch-time goes into snapshot extra); progress header + per-platform counters + time estimate as mocked; تخطٍ per row storing metrics_skip reason; save-and-continue vs finish flows; Snapchat always-manual note verbatim; the three states entered-zero vs missing vs skipped-with-reason VISUALLY distinct per the mockup.

VERIFY: npx tsc --noEmit clean. Print exactly: PAGES-REMAINING REBUILT + files changed.
