TASK: Content detail — part 1 of 2. Rebuild to EXACTLY match design screens 06 (ملف المحتوى — نظرة عامة), 07 (الفيديو), 08 (منشور), 38 (طلب تعديلات modal), and implement the screen-36 role-gating rules. Files: src/pages/Marketing/ContentDetailPage.tsx, src/pages/Marketing/components/WritingFields.tsx, NEW src/pages/Marketing/components/RequestChangesModal.tsx, additive CSS at END of mos.css under `/* === s06-s08/s38 additions === */`. Part 2 (s09-s12 tabs) is a SEPARATE task — do not touch MaterialsTab/PublishTab/PerformanceTab/TaskCard.

GROUND TRUTH: READ docs/marketing-reference/source/screens/s06.html, s07.html, s08.html, s38.html, s36.html. Bilingual verbatim-Arabic; num() digits.

DATA/API (read api-contract.md + the current api/marketing-os.ts): content_detail, content_update, scene_save/scene_delete, task_complete (returns {closed_task_id, opened_task_id, next_step_key, round, done}), content_versions, task_transfer. The page's derived-state doctrine stands: stage/owner ONLY from the open task (content_detail's view fields); version number = max task round; no editable status anywhere.

SCREEN 06: header exactly as mocked (ref + title + type pill + stage chip + round chip); «مقارنة بالنسخة ١» action opening a comparison panel fed by content_versions (side-by-side of version data fields with changed fields highlighted — follow the mockup's layout); header approve/reject shortcut buttons (visible per role rules below, calling task_complete approve / opening RequestChangesModal); persistent ACTIVITY RAIL on the side across ALL tabs (task history + comments merged chronologically — data already loaded by content_detail; match the mockup's rail exactly incl. the pinned last-rejection card).
SCREEN 07 (video writing tab): «قالب» footage status option on scenes (SceneTable already supports have/to_make/missing — extend its status control with 'template' styled per the mockup); everything else per s07 (re-verify the filled state matches — fix drift).
SCREEN 08 (post writing tab): approved headline card gets approver meta line exactly as mocked («اعتمده ريان · ٢٧ يوليو، ٢:١٤م» pattern — from the approval task's closed_by + closed_at via content_detail's task history); the four-card headline layout must match the contract example (docs/marketing-reference/audit.md §12 reference is the mockup itself — s08.html).
SCREEN 38 RequestChangesModal (replaces any inline reject prompt): change-area CHIPS built from the content type's field list + scene list (multi-select → becomes p_targets/revision_targets); mandatory note textarea (submit disabled empty, mockup's helper text); target-role picker if s38 shows one; revision due date input; publish-date impact readout (if target_publish_at set: show the shift the mockup shows); consequence panel text verbatim; 3-rounds alert banner when round >= 3 exactly as mocked. Submits task_complete {result:'changes_requested', note, targets}.
SCREEN 36 role gating (PRODUCTION rules, asserted by QA later): compute canAct = the open task's role_key is held by me (ctx roles) or I'm marketing_manager/admin. Writer when NOT their step: all writing fields LOCKED (read-only rendering) with ONLY the comment composer active (per s36); montage sees «قادم إليك» banner when the pinned path has a later step for their role (s36's exact copy); CEO: read-only AND the script/scenes/comments sections HIDDEN entirely (s36 states CEO sees the overview without script/scenes/comments); ops: may trigger «إسناد تصوير» early (keep existing behavior). Buttons render ONLY when actionable — no disabled-button refusals.

VERIFY: npx tsc --noEmit clean. Print exactly: CD1 REBUILT + files changed.
