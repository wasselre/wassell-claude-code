TASK: Write ONE new file: scripts/mos-fixtures.mjs — the fixture builder for the isolated Supabase branch. It creates the approved design's EXACT dataset (the names/numbers the reference screens show), builds marketing data through the REAL API, and tears down cleanly. Write the file only; do not run it.

CONFIG: reads .mos-branch.local (KEY=VALUE: MOS_BRANCH_URL, MOS_BRANCH_ANON_KEY, MOS_BOOTSTRAP_TOKEN) like scripts/apply-branch-sql.mjs does (copy its loader). Plus env MOS_APP_URL (default http://localhost:3000 — the scripts/mos-dev-server.mjs stack) and MOS_FIXTURE_PASSWORD (default 'MosFixture!2026').

CLI: node scripts/mos-fixtures.mjs [--phase 0|1|all] [--teardown] [--verify]

RAW SQL channel: POST `${MOS_BRANCH_URL}/rest/v1/rpc/mos_bootstrap_exec` with {p_token, p_sql} (copy the exec() helper from scripts/apply-branch-sql.mjs). This runs as definer on the BRANCH only. Use it ONLY for phase 0 platform seeding + CRM record rows + teardown — every marketing-domain object in phase 1 goes through the real /api/marketing-os endpoint as the correct signed-in role.

=== PHASE 0 — platform seed (SQL) ===
1. Profile: INSERT a profile id 'f1de0001-0000-4000-8000-000000000001' label_ar 'فريق التسويق' label_en 'Marketing Staff', is_admin false, model_permissions '{}', page_access '{"marketing_management": true}', is_system false — ON CONFLICT (id) DO NOTHING.
2. Five auth users + five public.users (fixed uuids u1..u5 = 'f1de0002-0000-4000-8000-00000000000N'; auth uid = SAME uuid for simplicity):
   emails mos-ceo@fixtures.wassel.local, mos-marketing_manager@..., mos-ops_supervisor@..., mos-writer@..., mos-montage@...; names AR from the roles (e.g. writer user name_ar 'سارة الكاتبة' — pick names the mockups show if any: READ docs/marketing-reference/source/screens/s35.html + s37.html and use the people names shown there (transcribe exactly — e.g. the team screen shows named people per role); fall back to role-derived names.
   auth.users INSERT (guard ON CONFLICT (id) DO NOTHING): instance_id '00000000-0000-0000-0000-000000000000', aud 'authenticated', role 'authenticated', email, encrypted_password = extensions.crypt(<password>, extensions.gen_salt('bf')), email_confirmed_at now(), raw_app_meta_data '{"provider":"email","providers":["email"]}', raw_user_meta_data '{}', AND CRITICALLY the eight token columns as EMPTY STRINGS not NULL (confirmation_token, recovery_token, email_change, email_change_token_new, email_change_token_current, phone_change, phone_change_token, reauthentication_token = '') — GoTrue 500s "Database error querying schema" on NULLs there (verified live on this branch 2026-08-01; phase-0 users f1de0002-...01..05 already exist and sign in fine — the script must remain idempotent against them). Plus auth.identities (id gen_random_uuid(), user_id, provider_id = user id::text, provider 'email', identity_data jsonb_build_object('sub', id::text, 'email', email), last_sign_in_at now(), created_at/updated_at now()) guarded ON CONFLICT DO NOTHING (identities unique on provider, provider_id).
   public.users INSERT: id = same uuid, auth_uid = same uuid, name_ar/name_en, email, profile_id = the fixture profile, is_active true, role_assignments = jsonb array with the matching canonical role: role ids are 'c0febe01-0000-4000-8000-000000000001'(ceo) ..02(mm) ..03(ops) ..04(writer) ..05(montage). ON CONFLICT (id) DO UPDATE SET role_assignments = EXCLUDED.role_assignments, profile_id = EXCLUDED.profile_id.
   users.phone: set '+9665550100N' per user (notification fixtures).
3. CRM records (INSERT INTO records (id, model_id, data, created_at) ... ON CONFLICT (id) DO NOTHING) — model ids: all_projects = look it up at runtime: SELECT id FROM models WHERE name='all_projects' (same for 'clients'); appointments 'b032a675-6237-4436-9783-a1a253855f74', visits '372ed642-3753-40b4-9dd7-e8390f91b1f8', reservations '5a1e0ffe-0000-4000-8000-000000000002':
   - Projects: 'f1de0010-...-0001' مينا ٥٢ (data: {"project_name":"مينا ٥٢"}), '...-0002' مقام ١٧. (READ s03.html/s14.html for the exact project names used across the mockups and match them.)
   - 12 clients 'f1de0011-...-0001..0012' (data: {"client_name":"عميل تجربة N","phone_number":"+96655501NN"}).
   - CRM funnel records dated within the fixture window (2026-07): 6 appointments (client_id = clients 1..6, appointment_date '2026-07-2XT12:00:00+03', appointment_status 'completed' except one 'cancelled'), 4 visits (scheduled_datetime July dates, client_id 1..4), 2 reservations (client_id 1..2, reservation_date late July, reservation_amount 850000 and 1200000, payment_status 'paid'/'pending').
   NOTE: unfrozen models — direct INSERT into records is the correct storage; keep created_by_user_id NULL.
4. mos_settings: UPDATE external_effects → '{"enabled": false}' (branch NEVER sends WhatsApp/push) and INSERT a marker row ('fixture_stamp', jsonb {created_at, version:1}) so --verify can detect fixture presence.

=== PHASE 1 — marketing dataset THROUGH THE REAL API ===
Sign-in helper: POST `${MOS_BRANCH_URL}/auth/v1/token?grant_type=password` (apikey header = anon key) per role email; keep access_token per role. api() helper: POST `${MOS_APP_URL}/api/marketing-os` json {action, ...payload} with Authorization Bearer <token> — THROW on !ok or {error} body, printing action + payload id.
Idempotency: before building, call api('content_list') as marketing_manager; if refs like 'V-004' already exist, SKIP creation of existing refs (log 'exists, skipping') so re-runs converge.

Ground truth for names/values: READ docs/marketing-reference/source/screens/s02.html, s03.html, s06.html, s07.html, s10.html, s14.html, s16.html, s42.html, s50.html and transcribe the EXACT titles/refs/statuses/numbers they display. Build (as the role that would own each write; marketing_manager where ambiguous):
1. Content items V-001..V-011 (النوع فيديو) and P-011..P-024 (منشور) with the mockup titles (s03's table + s02's task rows are the naming source). Create via content_create with content_type key 'video'/'post' (read the actual keys via api bootstrap/content types), project_id = the matching fixture project, campaign linkage where the mockup shows it. IMPORTANT about refs: mos refs auto-allocate sequentially (V-001, V-002...) — CREATE IN ORDER so numbering matches the mockups; the ref allocator on the branch starts fresh. If an allocator offset makes exact refs impossible, THROW loudly — do not mislabel.
2. V-004 (فيديو مشروع مينا ٥٢ per s06/s07): drive its workflow to the state the mockups show — writer submits (task_complete as writer with result 'submitted'), reviewer rejects round 1 with THE EXACT NOTE from s10.html (transcribe verbatim), writer resubmits, currently sitting at the state s06 shows (read the mockup's current-step chip and stop there). Fill its writing fields (idea/hook/voiceover/scenes) with s07's exact values via content_update + scene_save.
3. Scenes for V-004 per s07 (timings/visual/voiceover/footage_status incl. the missing ones s09 shows).
4. Campaigns C-002..C-007 per s14's table (names, kinds, statuses, budgets, dates — exact); executions + ads + daily entries per s20/s21 where shown; requires_signature case per s34 («بانتظار توقيعك») if a campaign there crosses 50k — create it as marketing_manager, leaving it unsigned.
5. Shoot requests SR-003..SR-005 per s42 (titles/statuses/scheduled dates; items linked to V-004's missing scenes for SR-003 per s24).
6. Assets per s16's sections (titles/kinds/tags/projects; ~12-20 rows is enough to reproduce the visible sections; use asset_save; usage links to content via asset_link for the ones s16 badges as used/معتمدة; leave 4+ unused for s41).
7. Publications + metric snapshots per s11/s12/s50 (scheduled + published rows, snapshots with the numbers s12 shows incl. a «ناقص» missing week).
8. Attribution: attribution_stamp rows (as marketing_manager) linking clients 1..8 to C-002's executions/ads with occurred_at dates that put the phase-0 CRM records IN window — so campaign_outcomes returns the funnel s34/s40 show. If the attribution actions are not yet implemented in the API (action unknown error), fall back to INSERT via SQL channel WITH A LOUD WARNING 'attribution via SQL fallback — switch to API when part2 lands'.
9. Notifications: fire api('remind') once on a stalled item if implemented; else skip with warning.

=== --teardown ===
Via SQL channel, in FK-safe order: DELETE notification_deliveries/notifications/notification_rules only where fixture-created? notification_rules are SEED (keep!). Delete: client_attributions, mos_metric_snapshots, mos_publications, mos_asset_links, mos_assets, mos_shoot_items, mos_shoot_requests, mos_execution_daily, mos_execution_ads, mos_campaign_executions, mos_campaign_events, mos_comments, mos_content_versions, workflow_role_tasks (subject_table='mos_content'), mos_content, mos_campaigns, notifications, notification_prefs; reset mos_ref_counters; DELETE fixture CRM records (ids with the f1de00 prefix) from records; DELETE the fixture_stamp row. KEEP: users/profile/auth users (harmless, needed for the zero-record capture), settings, workflows, roles, surface_access. Print counts deleted per table.

=== --verify ===
Prints a table: content count by type, campaigns, executions, assets, shoots, publications, snapshots, attributions, open tasks by role — and exits nonzero if V-004 is not at the expected step.

Style: plain ESM, no new deps, loud failures, progress log per object. When done print exactly: FIXTURES SCRIPT WRITTEN.
