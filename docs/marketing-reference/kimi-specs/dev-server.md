TASK: Write ONE new file: scripts/mos-dev-server.mjs — a local full-stack dev server that serves the repo's Edge-style api/ functions natively and proxies everything else to Vite. Used to run the app against the isolated Supabase branch for fixtures + visual captures (vercel dev is flaky on Windows; this is the deterministic replacement). Write the file only; do not run it.

HOW THE API FUNCTIONS LOOK: api/marketing-os.ts exports `export const config = { runtime: 'edge' }` and `export default async function handler(req: Request): Promise<Response>` (web-standard Request/Response). Verify by reading api/marketing-os.ts + api/value-translate.ts headers. Some api files are nodejs runtime — out of scope; this server only needs a configurable allowlist of endpoints.

BEHAVIOR:
1. Env/config: loads KEY=VALUE lines from a file given by --env <path> (default .mos-branch.env in repo root) into process.env BEFORE importing any handler (the handlers read process.env at module scope sometimes). Also maps VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY into process.env if only MOS_BRANCH_URL/MOS_BRANCH_ANON_KEY are present (aliases: SUPABASE_URL, VITE_SUPABASE_URL = MOS_BRANCH_URL etc.).
2. CLI: node scripts/mos-dev-server.mjs [--port 3000] [--vite-port 5173] [--env .mos-branch.env]
3. Endpoint registry: const ROUTES = { '/api/marketing-os': 'api/marketing-os.ts', '/api/value-translate': 'api/value-translate.ts', '/api/internal/send-notification-wa': 'api/internal/send-notification-wa.ts' } — lazily bundle each on first hit with esbuild (import esbuild from 'esbuild' — it IS a repo devDependency via vite; verify require resolution works, else spawn npx esbuild) to a temp .mjs (bundle: true, platform 'neutral'... Edge handlers use web APIs + npm deps like @supabase/supabase-js: bundle with platform 'browser', conditions ['worker','import'], external: [] — everything bundled; target es2022; format esm) then dynamic import() it and cache the module. If bundling or import fails: return 500 with the error text AND console.error (loud).
4. HTTP server (node:http): for a ROUTES match — convert IncomingMessage → web Request (method, headers, body as buffer; absolute url http://localhost:<port><path+query>), call handler, write back status/headers/body (handle streams via arrayBuffer()). For anything else — proxy verbatim to http://localhost:<vite-port> (stream both ways, preserve headers, support websockets NOT required — Vite HMR ws may fail through the proxy; that is fine for capture use, but implement basic 'upgrade' passthrough if trivial with http.request + socket piping; otherwise log once that HMR ws is not proxied).
5. On boot: print the port, the env file used, the resolved SUPABASE url (mask keys), and the registered API routes. Do NOT print secrets.
6. SIGINT closes the server cleanly.

Plain ESM JS, no new deps. Loud failures. When done print exactly: DEV-SERVER WRITTEN.
