TASK: Reconcile the fixture dataset's VALUES with the approved screens' exact numbers. Modify ONLY scripts/mos-fixtures.mjs (drive targets, due dates, scheduled publications, task-note timings, counts) — no schema, no API, no UI changes.

WHY: the first filled-data capture of screen 01 shows the structure aligned but the NUMBERS off — the current drive matrix sends ~14 items to 'done' while the mockup shows «تحت الإنتاج ٢٣», «يُنشر هذا الأسبوع ١٢», «بانتظارك أنت ٧» (manager), «متأخر ٤». The dataset must make the app COMPUTE the mockups' numbers from real state — never hardcode a number anywhere.

METHOD:
1. READ docs/marketing-reference/source/screens/s01.html END TO END and extract every number + list row it shows: the four stat values and their sublines, the stalled table's exact rows (refs, titles, stage pills, «منذ» ages, due), the «يُنشر هذا الأسبوع» card rows (refs + weekday + platform), the paid card values (المصروف ٣٨٤٠٠ من ٦٠٠٠٠, العملاء ٢١١, تكلفة العميل ١٨٢, المؤهلون ٦٣ — read the actual values there), the badge counts on the rail if shown.
2. READ s02.html (writer's مهامي rows), s03.html (the table's exact status distribution) and s35.html (role loads: open tasks per role tiles) — the SAME dataset must satisfy all of them simultaneously wherever possible. Where two screens are IRRECONCILABLE (mockups drawn with inconsistent numbers), satisfy s01+s03 (the most-captured screens) and add a `RECONCILIATION` map at the top of the CONTENT section in mos-fixtures.mjs documenting each screen-level discrepancy left behind (screen, mock value, dataset value, why) — that map is read later to write manifest row notes. No silent compromises.
3. Derive the required distribution: how many items at which step (status_key), which have open tasks for which role (بانتظارك أنت ٧ counts the MANAGER's open tasks + approvals? read the subline to know the exact definition the card states), which are overdue (متأخر ٤ → four open tasks with due_at in the past — set explicit past due dates in the drive), which publications are scheduled within the fixture week (١٢ — twelve publication rows with scheduled_at inside 2026-07-26..08-01, matching the week card's rows), paid numbers (executions' spend/leads/qualified sums must equal the card: adjust EXECUTIONS data so the sums land exactly).
4. Adjust the CONTENT array's `drive` targets + add per-item `due_in_days` (negative = overdue) that driveTo applies when opening tasks is not possible directly — due dates are set at task-open time by the engine (due_days from the step), so to make something OVERDUE the fixture must UPDATE the open task's due_at afterwards: use the task_update API action ({task_id, due_at}) as marketing_manager for that — it exists. Add a `-- due dates --` pass after the drive pass doing exactly that for the items the mock shows as متأخر/منذ N أيام.
5. The stalled list's «منذ ٤ أيام» ages derive from opened_at — NOT settable via API. Where the mock's age labels cannot be produced through the API, note it in RECONCILIATION (do NOT reach into SQL from this script).
6. Publications: create/adjust scheduled_at values so the week card shows the mock's exact rows (refs + weekdays). The fixture clock context: captures freeze Date at 2026-07-29T10:00+03 — week = Sun 2026-07-26 .. Sat 2026-08-01. Place them accordingly.
7. Keep the script CONVERGENT: all adjustments must be idempotent re-runs (probe → patch only when different). The V-004 block stays untouched. --verify: extend the table with the s01-derived assertions (production count, scheduled-this-week count, manager-waiting count, overdue count) comparing live API values vs the mock numbers; verify FAILS listing each mismatch.

RUNTIME NOTE (do not run anything): the operator runs `node scripts/mos-fixtures.mjs --teardown` then `--phase 0` then `--phase 1` after your edit if the reshape requires a rebuild; prefer convergent in-place patches (task_update/publication_save/execution_save) over teardown when possible and SAY in your final message whether a teardown rebuild is required.

Print exactly: FIXTURE-VALUES RECONCILED + whether teardown is required + the RECONCILIATION discrepancy count.
