<!-- AUTO-GENERATED by scripts/sync-model-workflow-prds.mjs — DO NOT EDIT BY HAND.
     Regenerate with: npm run sync:prds  (reads the live Supabase DB).
     The git diff of this file is the record of what changed in the app. -->

# Workflow: Search Task Completed / إكمال مهمة البحث

**Status:** Auto-generated (do not hand-edit) — reflects live Supabase
**Last updated (from DB):** 2026-09-20
**Workflow id:** `5a1e7a50-0000-4000-8000-0000000000bb`   ·   **Active:** yes
**Group:** Sales Lifecycle
**Trigger:** When a record is updated
**Trigger model:** Sales Tasks / مهام المبيعات `sales_tasks`

## Logic (branched)

### Branch 1: IF — Still Searching

**Conditions:**
_Match: ALL must pass (AND)_
- Task Kind (`task_kind`) equals "Search Update" (`search_update`)
- Task Status (`task_status`) equals "Completed" (`completed`) · _only when it newly becomes true_
- Result (`task_result`) equals "Still Searching" (`still_searching`)

**Actions (run in order):**

**Action 1 — Create Record**
Create a **Sales Tasks / مهام المبيعات** record with:
- Title (`title`) ← static value تحديث حالة البحث
- Task Kind (`task_kind`) ← static value search_update
- Task Status (`task_status`) ← static value open
- Due Date (`due_date`) ← the current date offset by `+7d`
- Client (`client_id`) ← the trigger record's Client (`client_id`)
- Unanswered Request (`request_id`) ← the trigger record's Unanswered Request (`request_id`)
- Assignee (`assignee`) ← the trigger record's Assignee (`assignee`)
- Creation Source (`creation_source`) ← static value search_recurrence

### Branch 2: ELSE IF — Option Found

**Conditions:**
_Match: ALL must pass (AND)_
- Task Kind (`task_kind`) equals "Search Update" (`search_update`)
- Task Status (`task_status`) equals "Completed" (`completed`) · _only when it newly becomes true_
- Result (`task_result`) equals "Option Found" (`found`)

**Actions (run in order):**

**Action 1 — Update Record**
Update **Unanswered Requests / طلبات غير مجابة** records where `id` (unknown field) = the trigger record's Unanswered Request (`request_id`), setting:
- Request Status (`request_status`) ← static value fulfilled
- Closed At (`closed_at`) ← the current date offset by `+0d`
- Option Found (`found_option`) ← the trigger record's Option Found (`found_option`)

**Action 2 — Update Record**
Update **Clients / العملاء** records where `id` (unknown field) = the trigger record's Client (`client_id`), setting:
- Client Stage (`client_stage`) ← static value الاتصال لحجز موعد
- Client Status (`client_status`) ← static value مهتم

**Action 3 — Create Record**
Create a **Follow-ups / المتابعات** record with:
- Client ID (`client_id`) ← the trigger record's Client (`client_id`)
- Follow-up Type (`followup_type`) ← static value whatsapp_follow_up
- Status (`followup_status`) ← static value open
- Scheduled Follow-up (`scheduled_datetime`) ← the current date offset by `+0d`
- Sales Rep (`sales_rep`) ← the trigger record's Assignee (`assignee`)

### Branch 3: ELSE IF — Client Dropped

**Conditions:**
_Match: ALL must pass (AND)_
- Task Kind (`task_kind`) equals "Search Update" (`search_update`)
- Task Status (`task_status`) equals "Completed" (`completed`) · _only when it newly becomes true_
- Result (`task_result`) equals "Client Dropped" (`client_dropped`)

**Actions (run in order):**

**Action 1 — Update Record**
Update **Unanswered Requests / طلبات غير مجابة** records where `id` (unknown field) = the trigger record's Unanswered Request (`request_id`), setting:
- Request Status (`request_status`) ← static value client_dropped
- Closed At (`closed_at`) ← the current date offset by `+0d`

**Action 2 — Update Record**
Update **Clients / العملاء** records where `id` (unknown field) = the trigger record's Client (`client_id`), setting:
- Client Stage (`client_stage`) ← static value غير مؤهل
- Client Status (`client_status`) ← static value غير مهتم
