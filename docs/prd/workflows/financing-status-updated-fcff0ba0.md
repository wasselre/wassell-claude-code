<!-- AUTO-GENERATED by scripts/sync-model-workflow-prds.mjs — DO NOT EDIT BY HAND.
     Regenerate with: npm run sync:prds  (reads the live Supabase DB).
     The git diff of this file is the record of what changed in the app. -->

# Workflow: Financing Status Updated / تحديث حالة التمويل

**Status:** Auto-generated (do not hand-edit) — reflects live Supabase
**Last updated (from DB):** 2026-06-17
**Workflow id:** `fcff0ba0-2bc7-4ed8-b6db-6b2994a1f507`   ·   **Active:** yes
**Group:** Sales Lifecycle
**Trigger:** When a record is updated
**Trigger model:** Financing / التمويل `financing`

## Logic (branched)

### Branch 1: IF — Submitted to Bank

**Conditions:**
_Match: ALL must pass (AND)_
- Financing Status (`financing_status`) equals "Submitted to Bank" (`bank_submitted`) · _only when it newly becomes true_

**Actions (run in order):**

**Action 1 — Update Record**
Update **Clients / العملاء** records where `id` (unknown field) = the trigger record's Client (`client_id`), setting:
- Client Status (`client_status`) ← static value البنك

### Branch 2: ELSE IF — Valuation

**Conditions:**
_Match: ALL must pass (AND)_
- Financing Status (`financing_status`) equals "Valuation" (`valuation`) · _only when it newly becomes true_

**Actions (run in order):**

**Action 1 — Update Record**
Update **Clients / العملاء** records where `id` (unknown field) = the trigger record's Client (`client_id`), setting:
- Client Status (`client_status`) ← static value التقييم

### Branch 3: ELSE IF — Financing Completed

**Conditions:**
_Match: ALL must pass (AND)_
- Financing Status (`financing_status`) equals "Completed" (`completed`) · _only when it newly becomes true_

**Actions (run in order):**

**Action 1 — Update Record**
Update **Clients / العملاء** records where `id` (unknown field) = the trigger record's Client (`client_id`), setting:
- Client Stage (`client_stage`) ← static value الإفراغ
- Client Status (`client_status`) ← static value تم الحجز

**Action 2 — Create Record**
Create a **Follow-ups / المتابعات** record with:
- Client (`client_id`) ← the trigger record's Client (`client_id`)
- Follow-up Type (`followup_type`) ← static value ownership_transfer_follow_up
- Scheduled Follow-up (`scheduled_datetime`) ← the current date offset by `+1d @10:00`
- `followup_status` (unknown field) ← static value open
- Sales Rep (`sales_rep`) ← the trigger record's Sales Rep (`sales_rep`)
