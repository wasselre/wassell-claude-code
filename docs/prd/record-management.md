# PRD: Record Management

**Status:** Live
**Last updated:** 2026-05-31 (**Related Records rendered as adjustable-column tables (new):** the cross-model "Related Records" panel on a client's **Client Details** tab no longer renders each related record as a bare title/ID row. Each related model now renders as a **full read-only `TableView`** (via the new `RelatedModelTable.tsx`) — type-aware cells (lookups resolve to names, dropdowns as colored badges, dates localized) and sortable column headers, exactly like the main Records list. Columns are user-adjustable through a lightweight "Columns" **popover** (toggle visibility + drag-reorder; deliberately NO sort/filter/sharing controls, unlike the full `ViewEditor`), persisted per `(model, user)` in `localStorage` under `wassell_related_cols_<modelId>_<userId>` with a "Reset" that clears back to the default. Default columns mirror `TableView`'s no-view fallback (`show_in_table` fields + card title prepended). **No filter** — column choice only. `RelatedModelTable` builds a synthetic stable-id `ModelView` to drive `TableView`'s columns. `TableView` gained a `readOnly?` prop: when set, clicking a row calls `onRowClick` (navigate to the record) instead of starting inline edit, and the per-row actions column (open / delete) is hidden — columns stay sortable. Used only by `RelatedModelTable`; the main Records list is unaffected. See clients.md.) | (**Units tab on project-linked records (new):** the record form now shows a **Units** tab (Home icon) alongside Form / Client Details whenever the record either IS an `all_projects` record (`model.name === 'all_projects'` and the record exists) OR has at least one populated `lookup` field whose `lookup_model_id` points at the `all_projects` model. This mirrors the Client Details detection exactly — a new `projectCandidateIds` memo in `RecordFormPage.tsx` parallels `clientCandidateIds`. The tab renders `UnitsTabPane.tsx`, which lists every record in the `units` model linked to that project (matched by scanning the units model's lookup field(s) whose `lookup_model_id` is the `all_projects` model — not a hardcoded `project_id` slug — so a Builder rename doesn't break it; multi-value lookups matched by `.includes`). It reuses the **same** `TableView` + `ViewSelector` + `ViewEditor` machinery as the Units list page, so the shown columns are adjustable exactly like the normal table view (saved views are shared with the Units list page, scoped per user/shared, per-profile hidden-view filtered). View-scope (`useApplyViewScope`) is applied so units a profile can't see don't leak in; pagination (`PageSizeSelector` + `PageNavigator`, default 25/page) handles large projects; row click opens the unit at `/model/units/<id>`; delete goes through a confirm modal. When a record carries multiple populated project lookups (or a multi-value lookup with several projects) the tab adds a `?project=<id>` chip switcher, mirroring the Client Details multi-client switcher. Tab state is `?tab=units&project=<id>` in the URL via `useSearchParams` (deep-linkable, back-button safe; tabs are mutually exclusive — switching clears the other tab's params). Empty state when the project has no linked units yet. New component: `UnitsTabPane.tsx`. No schema/DB change — purely client-side off the in-memory `models` + `records` slices.) | (**Maps — mirror fields as location source + Wassel default map style (new):** Two changes so every model's map matches the All Projects formatting. (1) The Map Builder's **Location URL field** dropdown now also lists `mirror` fields whose ultimate target is a `url`/`text` field — so a project can pin from a location **mirrored off the master All Projects record** without storing the link locally (e.g. targeted_projects' "Project Location" mirror via the `project` lookup → `all_projects.project_location`). New schema-only helper `resolveMirrorTargetField(field, currentModel, allModels)` in `src/lib/mirrorResolver.ts` resolves the mirror's target field definition (no record needed) to type-check it. The location resolvers (`resolveLocation` / `resolveLocationWithCache` / `collectUrlsNeedingResolution` in `locationUtils.ts`) gained an optional `LocationResolveCtx { allRecords, allModels }` 4th arg and a private `readLocationString` that follows a mirror location field through its sibling lookup to the source string — the sync parse, the localStorage cache lookup, AND the short-URL async resolution queue all follow the mirror. `useResolvedLocations` builds the ctx from the store but only when the location field is actually a mirror, so non-mirror models don't recompute pins when unrelated records elsewhere change. (2) **Default map style.** Every model's map now renders the Wassel-branded theme (soft cream geometry, chocolate labels, copper highways, sand arterials) when `maps_config.map_style_json` is null/empty — the exact style All Projects already carried, extracted into a `WASSEL_MAP_STYLE` constant + `resolveMapStyles(json)` fallback in `locationUtils.ts` and applied by both `MapsView` and the Builder preview. A model can still override per-map by pasting its own style JSON, or paste `[]` to opt back into Google's stock theme. The `maps.style_json_hint` i18n string (ar+en) now notes the empty-→-default behavior. No data migration — the fallback is at render time, so all existing models inherit the theme immediately.) | 2026-05-31 (**Auto-growing text fields (new):** the `textarea` and `notes` record-field types — plus `textarea` columns inside `table` fields — now grow to fit their content instead of showing an inner scrollbar. A new shared `src/pages/Records/components/AutoGrowTextarea.tsx` (drop-in for `<textarea>`, forwards every native prop) re-fits height on every input event, whenever the controlled `value` changes (covers record load / form reset), and when its width changes (a `ResizeObserver` guarded on width so our own height writes don't loop) — viewport resize / RTL flip / sidebar toggle. It sets `resize:none` + `overflow:hidden` and uses the `rows` attribute as the minimum height (so it never shrinks below 3 rows for the form `textarea`, 2 for the notes composer, 1 for a table cell). Height math adds the element's top+bottom border back to `scrollHeight` because `.form-input` is `box-sizing:border-box` (Tailwind global) — otherwise the last line clips behind the 1.5px border. Wired into `DynamicField` (textarea case), `NotesField` (composer), and `TableField` (textarea column). Single-line `text` fields are unchanged — they stay single-line by design.) | 2026-05-31 (**Unsaved-changes guard on record exit (new):** clicking the back arrow at the top-left of the record header — or the prev/next chevrons — while the form has pending edits now opens an **Unsaved changes** modal with three options: **Save & Exit** (saves and then completes the navigation the user originally attempted — back to the list, previous record, or next record), **Discard & Exit** (navigates without saving), and **Cancel** (closes the modal, user stays on the current record). A clean form (no edits since last save / mount) exits immediately with no prompt. Replaces the prior prev/next-only `window.confirm` that only offered discard. Implementation: `RecordFormPage.tsx` gains a `pendingExit: { run: () => void } \| null` state and a `tryExit(action)` helper; both the back button (`onClick={() => tryExit(...)}`) and `goToRecord` route through it. `handleSave` was refactored to return `Promise<boolean>` and no longer navigates internally — the toolbar Save button and the modal's Save & Exit each navigate on their own (the toolbar always goes to the list; the modal runs the deferred action). New i18n keys: `records.unsaved_changes_title`, `records.unsaved_changes_message`, `records.save_and_exit`, `records.discard_and_exit`.) | 2026-05-31 (**Multiple-links field rendering (`multi_link`, new):** the record form, the table cell, and the Maps popup now render the new `multi_link` field type (stored as `string[]` of URLs). The form input (`MultiLinkFieldInput` in `DynamicField.tsx`) is an editable list — one URL input per row with a click-to-open icon + remove button, plus an "Add link" button. The table cell (`DynamicCell.tsx`) renders each non-empty URL as a compact numbered button ("Button 1"/"زر 1", "Button 2"/"زر 2", …) opening in a new tab with `stopPropagation`; bare hosts get an `https://` prefix. Table inline-edit shows an "open record to edit" hint (like `notes`) rather than a text input, so the array is never clobbered. The Maps popup renders the same numbered buttons via `MapsView.tsx`'s `renderFieldNode`. Required-field validation treats an all-empty array as missing. Not filterable; export joins the URLs.) | 2026-05-23 (**Our Projects → Units rollups (new):** eleven fields on the `our_projects` model — `total_units`, `units_available`, `sold_units`, `units_reserved`, `price_range`, `area_range`, `bedroom_range`, `bathroom_range`, `min_price_per_meter`, `max_price_per_meter`, `avg_price_per_meter` — are now computed live at READ time from `units` rows linked via the `project → all_projects.id ← units.project_id` chain. New optional `ModelField` props `is_computed: boolean` + `computed_kind: OurProjectsComputedKind` mark the rollup fields; `lib/permissions.ts` `isComputedField` now respects `is_computed` (forces readonly), and `saveRecord` strips every `is_computed` field's slug from data before persisting so they're never stored. New `src/lib/ourProjectsRollup.ts` is the pure compute; `src/hooks/useRolledUpRecords.ts` (`useRolledUpRecord` / `useRolledUpRecordList`) wraps consumers and subscribes to BOTH the host model's records AND the units records so the list view recomputes instantly when a unit changes. Bilingual + substring-tolerant status matching (Available / متاحة / متاح all count). Per-meter rollups skip units with `area_sqm ≤ 0`. The seed (`src/data/seedModels.ts`) adds the `project` lookup to our_projects + `bedrooms`/`bathrooms` to units. Live migration `supabase/migrations/2026-05-23_our_projects_unit_rollups.sql` patches existing deployments — idempotent, adds missing fields, stamps `is_computed`/`computed_kind` on existing fields by slug match.) | 2026-05-23 (**Drive-backed field rendering (new):** the record form and table cells now render five Drive-integrated field types — `image`, `multi_image`, `file`, `multi_file`, `attachment` — by loading file/folder metadata from the Files System via a shared `useFileRowMap` cache. Form-level inputs (`DriveFieldInputs.tsx` + `AttachmentFieldInput.tsx`) open a reusable `DriveBrowserModal` for browse + multi-pick + inline upload + new-folder. Table cells (`DriveCells.tsx`) render a thumb or icon chip with click-to-preview through the existing `FilePreviewModal` (images, PDFs, video, audio, and a download card for other types). `SectionBlock` now passes `modelId` + `recordId` down to `DynamicField` so uploads through these fields are tagged to the owning record via `files.model_id` + `files.record_id`. The previous inline image inputs that wrote to the legacy `marketing-assets` bucket were removed. See model-builder.md "Drive-backed fields" and files.md.) | 2026-05-18 (**Wassel-branded date/datetime picker (new):** `date` and `datetime` fields in the record form no longer render the browser-native `<input type="date">` / `<input type="datetime-local">`. They now use a new `DateTimePicker` component that opens a Wassel-styled calendar popover — month header + chevron nav in copper-500, weekday row in copper-500 starting on Sunday (Saudi week start), today rendered as copper text on transparent, selected day rendered as a white digit on a filled copper `#B8734F` circle, faded charcoal/25 days for previous/next month spill-over. For `datetime` mode the popover adds a `صباحاً`/`مساءً` (AM/PM) pill toggle in copper, plus stacked up/down spinners for hour and minute. The trigger button shows a copper calendar icon and the value formatted as `HH:MM:SS YYYY-MM-DD` in RTL (date-first in LTR). Storage shape is unchanged (`YYYY-MM-DD` for date mode, `YYYY-MM-DDTHH:MM` for datetime mode) so downstream filters, the formula engine, `formatDateHumanAr`, and workflow `{slug|human_ar}` tokens keep working without modification. Inline date editing inside `TableField` (the `table` field type) keeps the native input — the table cell context is too narrow to host the popover.) | 2026-05-11 (**Appointments: phone-first entry with auto-create client (new):** the appointments model gains a `phone_number` field at order 0 and a `client_name` field at order 2. Typing a phone debounce-searches clients by normalized phone; on a unique match auto-links `client_id`, on zero matches **creates a new minimal client with just the phone and links to it**. The auto-create primitive is a new opt-in extension of the existing `useAutoLink` hook — two new `ModelField` props (`auto_link_create_if_missing` boolean + `auto_link_create_min_length` number to guard against mid-typing creates). The seed (`src/data/seedModels.ts`) is updated; existing installs migrate via `supabase/migrations/2026-05-11_appointments_phone_first_autolink.sql` (idempotent, defensive against custom user-added fields that don't follow the canonical name → order map — falls through to order 99+ for unknowns instead of colliding via blanket "+2 all" bumps).) | 2026-05-10 (**Client 360 view — tab strip on every record + clients model gains WhatsApp / Calls sections (new):** the record edit page now shows a tab strip whenever the record either IS a client (`model.name === 'clients'` and the record exists) OR has at least one populated `lookup` field whose `lookup_model_id` points at the clients model. Two tabs: **Form** (the existing record form, default) and **Client Details** (with a Users icon — embedded read-only view of the linked client, including its full record + a cross-model "Related Records" panel listing every record across every model that points at this client via a lookup). Tab state is `?tab=client` in the URL via `useSearchParams` so it's deep-linkable and survives back-button. When a non-clients record has multiple populated client lookups (e.g. a deal with buyer + seller), the Client Details tab adds a chip switcher above the embedded view that writes `?client=<recordId>` and re-renders for the chosen client. Cross-model "Related Records" query is purely client-side off the in-memory `records` map (memoized lookup-fields-by-target-model index, so iteration is cheap; renders one group per source model with title/subtitle from the model's `card_config` and click-through to `/model/<modelName>/<recordId>`). The clients system model now seeds two new sections — "WhatsApp History" / "سجل واتساب" with a single `whatsapp_history` field, and "Calls" / "سجل المكالمات" with a single `call_history` field. Both render inline panels (chats UI's `MessageBubble` for WhatsApp; the existing `CallHistoryPanel` with `chrome="naked"` for calls). The bottom-of-form `CallHistoryPanel` is **suppressed for the clients model** to avoid duplication; other models still get the bottom-of-form fallback for backward compat (they can opt into the in-section render later by adding a `call_history` field via the Builder). New components: `RecordTabBar.tsx`, `ClientDetailsTabPane.tsx`, `RelatedRecordsPanel.tsx`, `WhatsAppHistoryPanel.tsx`. Refactored: `CallHistoryPanel.tsx` (gained optional `chrome?: 'card' \| 'naked'` prop), `SectionBlock.tsx` (gained `recordId?: string`, special-cases the two new field types BEFORE the standard `DynamicField` dispatch — full-width, no chrome, ignores readonly/edit toggles since the panels are inherently read-only). Migration path: `healClientsSchema` in `src/lib/schemaMigrations.ts` runs on every `initialize()` and APPENDS the two new sections to existing installs whose stored clients model lacks them — idempotent (checks for the field types' presence, not section ids). See `docs/prd/clients.md` for the dedicated PRD on the Client 360 surface.) | 2026-05-10 (**Follow-Ups action buttons + rebuilt Visits model (new):** the Follow-Ups record form gained three custom buttons in the action header — **Book a visit** (opens a Visits modal prefilled with the current client), **Schedule follow-up** (opens a new Follow-Up modal prefilled with the current client), and **Register a visit** (looks up the latest visit for this client; opens it for editing if found, otherwise opens a blank Visits modal prefilled with the client). The buttons reuse the existing `custom_buttons` infrastructure with two new action types: `create_record` (open RecordFormModal new) and `find_or_create_record` (search target by criteria; on no match, open new with prefill). A new reusable `RecordFormModal` component renders the same `SectionBlock` + `DynamicField` stack as the full page, but with a stripped-down save flow (no URL nav, no prev/next) and goes through `saveRecord` with `expectedVersion` for optimistic concurrency. The user-built visits model at `/model/item_mo5t69tn` was deleted (records and all) and replaced with a clean `visits` model whose schema supports a bidirectional client auto-fill UX: `client_id` lookup displays the client's `client_code` (CLT-0001…) instead of the name; typing into `phone` runs `useAutoLink` (debounced 400 ms, normalizes via `normalizePhone()` so 0xx / 5xx / 966 / +966 all collapse to one E.164) and on a unique match by `phone_number` writes the matched client's id into `client_id`; picking a client_id runs `useAutoFill` to populate `name` from the client's `client_name` and `phone` from the client's `phone_number`. Auto-fill respects user edits — if the user has already typed something, it doesn't overwrite until the lookup id changes. Auto-link respects user-picked lookups — if the lookup currently holds a value the user manually selected (not one we auto-set), we don't overwrite. Both primitives are surfaced as four new optional `ModelField` props (`auto_link_lookup_field_id`, `auto_link_target_field_name`, `auto_link_normalize`, `auto_fill_from_lookup_field_id`, `auto_fill_source_field_name`) and the same hooks are mounted in the full RecordFormPage too, so the same UX works whether the visits form is opened in the modal or directly via URL. The visits model is deliberately kept as a Builder JSONB model (NOT frozen) per explicit request — schema can keep evolving before being promoted to a typed table.) | 2026-05-07 (**Maps view — clustered pill markers + custom popup card (rewrite):** the map no longer draws each record as a generic teardrop pin with a Google InfoWindow on click. Markers are now copper-bordered **pill labels** showing the value of the configured `pin_label_field_id` (project name by default) — width auto-sizes to the text, long values truncate with `…`, the pill background uses the configured `pin_color_field_id` mapping (falls back to Wassel charcoal `#4A4E54`), pill anchor is bottom-center so it sits above its lat/lng. When pills crowd together they auto-group into **copper count circles** via `@googlemaps/markerclusterer`; circle diameter steps with cluster size (36 / 42 / 48 / 54 px for 1–9 / 10–49 / 50–99 / 100+); clicking a cluster zooms in until it splits. Pin clicks open a **custom HTML popup card** rendered as a `FLOAT_PANE` `<OverlayView>` instead of Google's default InfoWindow — Wassel-styled rounded card with badge / title / subtitle / shown-fields list and a copper "فتح السجل" / "Open record" CTA bar at the bottom; close X in the top corner; uses the same `formatFieldValue` resolver as the popup body so dropdowns, lookups, ranges, currency, dates, etc. all render correctly (lookups → target's display field, dropdowns → `label_ar`/`label_en`, ranges → `min – max unit`, currency → `… ر.س`). Marker management is imperative — a `useEffect` builds `google.maps.Marker` instances + a `MarkerClusterer` whenever `resolved` / `pinLabels` / `click_action` change, and tears down on unmount. The Builder preview map mirrors the same pill style so what users configure is what they see live. Replaces the previous `<Marker icon=...>` + `<InfoWindow>` JSX. Adds dependency `@googlemaps/markerclusterer ^2.6.2`.) | (**Optimistic concurrency on save — Phase F.2, deployed 2026-05-07.** Saving an unfrozen record now goes through the 5-arg `record_save(p_model_id, p_id, p_data, p_created_by, p_expected_version)` RPC that compares the version we LOADED with against the database's current version. If another user edited the same record in the meantime, the RPC raises `serialization_failure` (SQLSTATE 40001) with `version_mismatch: …` and the form shows a toast: **"Another user just edited this record. Reload to see their changes before re-saving."** The write is **not** enqueued for retry (replay would just hit the same conflict). New records pass `null` (skip the check). The `records.version` column starts at 1 and a `BEFORE UPDATE` trigger auto-bumps it on every UPDATE, so the next save sees the bumped value. This replaces the previous last-write-wins behavior. Frozen models don't have version columns yet (Phase F.2.1 — future). **Atomic auto-IDs — Phase F.1, deployed 2026-05-07.** Auto-incrementing identifiers (the `auto_id` field type) used to assign via a client-side read-modify-write of a JSONB counter on the field definition with a documented race condition. The new `record_assign_auto_id` SECURITY DEFINER RPC backed by an `auto_id_counters` table uses Postgres's atomic `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` so concurrent saves get strictly increasing distinct values. `saveRecord` is now async and awaits `assignAutoIdsAsync`; the legacy sync path remains as the offline fallback. **Faster boot + live multi-user updates.** `RecordListPage` now renders before records finish loading (chrome-first via `criticalDataReady`), and changes from other browsers propagate live via the `RealtimeOrchestrator` (records is now on the `supabase_realtime` publication at PK-only replica identity; merge handlers refetch by id on event). See data-storage.md for the architectural details — this PRD just notes the user-visible effects.) | 2026-04-28 (**Range field comma formatting (new):** the range field's min/max inputs (and the Builder's range bound config — Min/Max/Step) now display thousands separators on blur — `100,000` in English, `١٠٠٬٠٠٠` in Arabic locale. While typing, the raw text is preserved so the cursor never jumps. Parser accepts ASCII commas, Arabic comma `،` (U+060C), Arabic thousands separator `٬` (U+066C), and normalizes Arabic-Indic digits back to ASCII before parsing — paste from any source works. Helpers `formatNumberWithCommas` / `parseFormattedNumber` are exported from `src/pages/Records/components/RangeField.tsx` and reused by `FieldEditor.tsx`.) | (**Research Project button — full Browserbase flow (rewrite):** the button now drives the entire 2 km market study end-to-end without involving the previous Claude Code routine. Click → POST `/api/research-project` (Node serverless, `maxDuration: 240s`) → spin a Browserbase Chromium session → log in to paseet.ai with `PASEET_EMAIL`/`PASEET_PASSWORD` → navigate to `/ar/chat` → send an Arabic prompt that requests 9 transaction-stat columns for a 2 km radius around the project's Maps URL (50–300 m² floor sizes) → wait for the streamed response to settle → parse the rendered `<table>` (rows mapped by Arabic header label so column reordering is tolerated) → resolve the matching `targeted_projects` record by walking the lookup field that points at `all_projects` (creates a new Targeted Projects record if none exists) → append rows into the `جدول مقارنة 2 كيلو حول المشروع` table field (`item_moi6wwdw`, 9 columns col_1..col_9) → return `{ ok, project_name, rows_added, sources }`. Toast on success: "Researched <project> — added N rows to the 2 km study". The previous design (button → routine on claude.com/code → Paseet/CRM) is removed because the routine had no browser tool and could not log in to Paseet or click the CRM UI. Env vars: `BROWSERBASE_API_KEY`, `BROWSERBASE_PROJECT_ID`, `PASEET_EMAIL`, `PASEET_PASSWORD` (all Vercel-side, never bundled). Probe scripts at `scripts/probe-paseet.mjs` and `scripts/probe-paseet-chat.mjs` were used to map Paseet's auth surface and chat-input selectors before writing the route.) | 2026-04-28 (**Research Project button on All Projects (new):** every All Projects record now shows a copper "Research Project" / "بحث عن المشروع" button in the record-edit header toolbar (between the prev/next chevrons and the Delete/Save cluster, matching the slot the `projects_research` PDF button occupies on its own model). Clicking it fires the Claude Code "Research Project" routine — a pre-configured remote agent on claude.com/code that builds a 2 km market-study record in the CRM for one Saudi real-estate project — by POSTing to `/api/research-project` with the live form values for `project_name` and `location`. The endpoint forwards a plain-text trigger message (`PROJECT: <name>\nLOCATION: <url>`) to the routine's `/fire` URL with a long-lived OAuth token in the Authorization header. URL + token live in `CLAUDE_ROUTINE_URL` + `CLAUDE_ROUTINE_TOKEN` env vars; neither is `VITE_`-prefixed so the OAuth token never reaches the browser bundle. Validation happens client-side first (red toast if either field is empty) and again server-side (400 on missing values, 500 on missing env vars, 502 on routine fetch failure, pass-through status + body on routine error responses); every failure mode surfaces as a red toast — no silent skips. Loading state on the button (label flips to "Researching..." / "جاري البحث..." and the button disables) while the request is in flight. The routine itself is async — the button returns as soon as the trigger is accepted; the user sees the new study record appear in `projects_research` whenever the routine finishes its work. Button only renders for saved records (uses `existingRecord` guard); not on the `/new` form. Uses the live `formData` (not `existingRecord.data`) so a user can correct the project name or paste a fresh Maps URL and click without saving first. Added: `api/research-project.ts` (edge route), `Sparkles` icon import + `handleResearchProject` handler + button in `RecordFormPage.tsx`, `records.research_project` / `records.research_project_running` keys in i18n.ts, `CLAUDE_ROUTINE_URL` + `CLAUDE_ROUTINE_TOKEN` blocks in `.env.example`.) | 2026-04-26 (**Advanced Filter "Is not" mode (new):** the values picker in the faceted filter popover (`dropdown` / `multiselect` / `section_selector` / `lookup` / `assignee`) gained a tiny segmented Is / Is not toggle at the top. "Is not" passes records whose field value is none of the picked values; records with no value for the field also pass under "Is not". Persisted as `mode: 'is' | 'is_not'` on the `values` filter kind (optional, defaults to `'is'` so previously-saved filters keep working). Active "Is not" chips render their summary prefixed with `≠`.) | 2026-04-23 (**Searchable dropdown / multi-select (new):** `DropdownSelect` / `MultiSelect` now render a search input at the top of their option menu when the field has more than 6 options (matching the `> 6` threshold used by the Advanced Filter panel). The input auto-focuses when the menu opens, filters options case-insensitively against both `label_ar` and `label_en` so the user can type in either language, and force-expands every surviving group while a query is non-empty so matches inside collapsed groups stay visible. The query resets when the menu closes (single-select menus close on pick; multi-select menus stay open and preserve the query so the user can keep picking). When every option is filtered out, the menu shows a bilingual "No results / لا توجد نتائج" line; the empty-options-entirely state is unchanged. This applies to every rendering surface these components drive — record form, table inline-edit (which uses `compact`), and bulk-edit — because both components gained the behavior at the source. | **Prev/next record navigation (new):** the record edit page now shows two arrow buttons at the start of the top-right action cluster (before PDF / Delete / Save) that step to the previous or next record in the list the user was just browsing — same filter, same search, same saved-view sort. Powered by a new in-memory `recordNavContext: { modelId, orderedIds } | null` slot on `appStore`; `RecordListPage` publishes `orderedFilteredRecords.map(r => r.id)` to it whenever the filtered+sorted list changes, and `RecordFormPage` reads it to compute `prevId` / `nextId`. Arrows are disabled at the edges (no wrap), hidden on `/new`, and guard unsaved edits via a `window.confirm(t('records.discard_changes_confirm'))` prompt — confirm discards, cancel keeps the user on the current record. Dirty state (`isDirty`) flips `true` in `handleFieldChange` / `handleMirrorFieldChange` and clears in `handleSave` + the existing recordId-reset effect. `App.tsx` now wraps the `:recordId` route in a small `RecordFormPageRoute` component that passes `key={recordId}` so the form fully remounts on navigation — all local state (formData, mirrorEdits, activeResearchViewId, isDirty) resets without scattering effects. Chevron icons use `rtl:rotate-180` so they point "back" / "forward" correctly in both directions. Known v1 limitation: if the user clicks a column header in `TableView` to ad-hoc re-sort, that sort lives locally in the view component and isn't reflected in the nav context — prev/next still follows the saved view's default sort. Deep-linking to a record without visiting the list first falls back to model insertion order. | **Maps view (new):** every model now exposes a third view toggle next to Table and Cards — a Google Maps canvas that plots every record from the current filter/search as a pin. Powered by `@react-google-maps/api` + `VITE_GOOGLE_MAPS_API_KEY`; when the key is unset the view shows a bilingual setup message instead of crashing. Pins are resolved via `resolveLocation(record, maps_config, fields)` in `src/lib/locationUtils.ts`: (1) parse lat/lng from a configured URL/text field (handles `@LAT,LNG`, `?q=`, `?ll=`, `!3dLAT!4dLNG`, bare `lat,lng`) → (2) fall back to manual lat/lng number fields → (3) record lands in the "Records without a pinnable location" side panel. Pin color comes from the first selected option's color on a configured dropdown/multiselect field (falls back to `model.color`). Click action is configurable per model (`popup` shows an InfoWindow with title/subtitle/badge/shown fields; `navigate` routes to the record detail). Full Google Maps style JSON drives the theme (roads/water/terrain/POI colors); invalid JSON silently falls back to the default. Consumes `filteredRecords` (not `pagedRecords`) — pagination has no spatial meaning and is hidden while Maps is active. Selection UI is hidden in MVP so bulk-action math (which operates on `pagedRecords`) stays unchanged. | 2026-04-22 **Table view inline-edit polish:** (a) a second horizontal scrollbar now floats above the header row on overflowing tables — `TableView.tsx` wraps the data-table in two stacked scroll containers (`topScrollRef` / `bottomScrollRef`) and syncs `scrollLeft` in both directions via a `syncingRef` guard that prevents feedback loops; a `ResizeObserver` watches the table to track `scrollWidth` vs `clientWidth` and hides the top bar entirely when the table fits. (b) Inline dropdown / multi-select / section_selector editors now open the full `DropdownSelect` / `MultiSelect` popovers (new `compact` prop on both) — the same picker used in the record form, including option-group collapsing — instead of the old native `<select>` and flat checkbox chip strip. Other field types still fall back to native inputs. (c) Inline text / email / textarea inputs auto-size to their content — width is `clamp(8ch, len + 2ch, 48ch)` via an inline style that overrides `form-input`'s `width: 100%`, so the cell grows as the user types rather than truncating. Numeric / date / datetime / phone / range / checkbox / lookup editors keep their existing compact widths. (d) URL fields render as an "Edit link" / "تعديل الرابط" copper chip in inline-edit mode; clicking opens a small popover (`UrlInlineEditor` sub-component) with a `dir="ltr"` URL input + Save / Cancel, committing via Enter and dismissing on Escape or click-outside. A secondary external-link icon next to the chip jumps to the current value in a new tab when one is set. Previously: **research-link prompt is now a queue with in-modal drafts:** `pendingResearchPromptTargetedId: string | null` → `pendingResearchPromptTargetedIds: string[]`; the singular setter is replaced by `queueResearchPromptTargetedId(id)` + `dismissResearchPrompts(ids?)`. When multiple Targeted Projects records are created in a burst (bulk edit / bulk import / workflow fan-out) the modal now shows **one row per project** with a three-way per-row chooser — (1) Create new study + optional draft-name input, (2) Add to a study with a target-picker `<select>` that lists **both** new drafts from sibling rows above **and** saved research records, or (3) Skip. Rows pointing at the same draft merge into one research record on Apply (deduped). This replaces the old single-slot behavior that clobbered each record and only prompted for the last id — and makes it possible to group several projects into the same brand-new study without leaving the popup. **Bulk edit (new):** the copper action bar now carries a **"Edit selected"** button (permission-gated on `edit`) that opens a full-model bulk-edit modal. The modal groups editable fields by section; each field has a checkbox to opt in + a `DynamicField` input; touching the input auto-enables the checkbox. On Apply the user sees a confirmation modal ("N field(s) will be updated across M record(s) — cannot be undone"), then a patch is built from only the enabled fields and applied to every selected record via `saveRecord` — so auto-id assignment, formula recompute, and workflow `update` triggers all fire per record, just like a normal edit. Derived / auto-assigned field types are hidden from the bulk form: `mirror`, `section_mirror`, `auto_id`, `formula`. Mirrored sections are hidden entirely because they edit linked records. Lives in `src/pages/Records/components/BulkEditModal.tsx`; wired into `RecordListPage.tsx`; **formula rendering overhaul:** the record form's live preview now re-evaluates the full model formula graph via `evaluateFormulaInModel` — so nested formula-on-formula references resolve correctly as the user edits upstream inputs instead of showing `#REF`; both the form and the table cell now route computed values through `formatFormulaValue(value, field, locale)` so `formula_output_type` / `formula_decimals` / `formula_thousands_separator` / `formula_currency` are respected uniformly — numbers get rounded + localized, currency suffixes with `ر.س` / `SAR` / currency code, percentages append `%`, text passes through, error sentinels render in red; record form sections are now **collapsible** — each section header is a button that toggles the body; initial open/closed state seeds from the model's `default_collapsed` flag and users can override at runtime; dropdown / multi-select in the record form now render optional **option groups** as collapsible headers inside the menu; bulk-select bar gains "Select all N records in this view" button to extend selection beyond the current page; range-field saved-view filter support via min/max sub-path picker; Research comparison flipped + editable + view-scoped: projects as rows, fields as columns; cells sync back to All Projects via section_mirror edit/sync modes; saved views pick both columns and rows; PDF respects active view)
**Related PRDs:** model-builder.md, access-control.md, import-export.md, data-storage.md, dashboards.md, clients.md, calling.md, chats.md

## Clients model (rebuilt 2026-04-18, Client 360 sections added 2026-05-10)
The seeded `clients` model has four sections:
- **Basic** (base) — `client_name` (text), `phone_number` (phone, default +966), `client_status` (dropdown, 23 options covering the sales pipeline from "No Answer" through "Title Transferred"), `client_stage` (dropdown, 10 options: New → Title Transfer), `client_sources` (multiselect, 9 options), and `notes` (notes — append-only history, see below).
- **Client Preferences** (base) — `preferred_projects` (**multi-select lookup** → `all_projects`), `preferred_units` (**multi-select lookup** → new `units` model), `preferred_direction` (multiselect, 8 compass options), `preferred_neighborhoods` (multiselect, Riyadh districts), `preferred_area` (range, 100–2000 m², step 50), `preferred_city` (multiselect, Saudi cities), `preferred_language` (dropdown: Arabic / English), `preferred_country` (multiselect, 120+ countries), `preferred_unit_type` (multiselect: Villa / Apartment / Floor / Duplex / Townhouse / Studio / Annex), `preferred_amenities` (multiselect: Majlis / Maid Room / Driver Room / Pool / Yard / Rooftop / Elevator), and `budget` (range, 50,000–5,000,000 SAR, step 50,000).
- **WhatsApp History** / **سجل واتساب** (non-base, added 2026-05-10) — single field of type `whatsapp_history`. Renders every chat message exchanged with this client across every Haberchat device (i.e. every one of OUR phone numbers), grouped by day, each bubble tagged with a "via &lt;our device name&gt;" label.
- **Calls** / **سجل المكالمات** (non-base, added 2026-05-10) — single field of type `call_history`. Renders the same `CallHistoryPanel` other models get at the bottom of the form, but inline as a section. Bottom-of-form `CallHistoryPanel` is suppressed for the clients model to avoid duplication.

Both new sections are NOT wired through `section_selector_field_id` (`section_selector_field_id` is still `null` on clients) — they're plain non-base sections that render by default. The new field types are non-storable / virtual; freeze SKIPS them (see [data-storage.md](data-storage.md) and [clients.md](clients.md)).

Returning users' existing clients records are migrated in place: `client_name` kept, `phone` → `phone_number`, a numeric `budget` wrapped as `{ min, max }`, a textarea `notes` string converted to a single Notes entry. Data under removed slugs (`status`, `source`, `email`, `city`, `address`) is dropped. The latest migration (v7, 2026-04-18) wraps scalar `preferred_projects` IDs into arrays for the new multi-select shape; `preferred_units` values are cleared because the field now points at the new `units` model (old IDs would be dangling). The 2026-05-10 Client 360 rollout reaches existing installs via an idempotent heal in `healClientsSchema` that APPENDS the WhatsApp History + Calls sections when the stored clients model lacks either field type — Builder customizations on existing sections are preserved.

## Follow-Ups model (rebuilt 2026-04-18)
The seeded `followups` model was rebuilt so one model tracks every follow-up type (call, appointment confirmation, post-visit, WhatsApp) via one `section_selector`:
- **Basic Info** (base) — `client_id` (single-select lookup → Clients, required), `client_name` (mirror via client_id → `client_name`), `client_phone` (mirror via client_id → `phone_number`), `scheduled_datetime` (required; when the follow-up is planned), `actual_datetime` (filled in after the follow-up happens — one source of truth across all types), `sales_rep` (assignee), `followup_type` (section_selector with four type options).
- **Client Preferences** (base, **mirrored** from the Clients model's "Client Preferences" section via `client_id`) — renders every preference field of the linked client; edits save back to the client record on form save.
- **Call** (conditional) — `call_result` (dropdown: Interested / Not Interested / No Answer / Postponed / Rescheduled / Needs Follow-Up).
- **Appointment Confirmation** (conditional) — `appointment_id` (lookup → new Appointments model), `appt_call_result` (same dropdown), `appointment_date` (mirror via appointment_id → `appointment_date`), `appointment_project` (mirror via appointment_id → `project_id`).
- **Post-Visit Follow-Up** (conditional) — `post_visit_project` (lookup → `all_projects`), `post_visit_unit` (lookup → new Units model), `pv_call_result` (same dropdown).
- **WhatsApp Follow-Up** (conditional) — `whatsapp_result` (dropdown: Replied / No Reply / Needs Info / Not Interested / Will Reply Later), `next_followup_after_days` (number of days until the next follow-up — user types after finishing).

Migration wipes old follow-up records on first load after v7 (their slugs like `call_duration`, `meeting_summary`, `contract_value` don't exist in the new schema).

**Action buttons (new 2026-05-10).** Three custom buttons sit in the form's action header next to the existing Save / Delete cluster, each opening a `RecordFormModal` overlay so the user never leaves the follow-up:
- **Book a visit** (`create_record` action → visits model). Opens a blank Visits modal with `client_id` prefilled from the current follow-up. User fills the visit details and saves; modal closes; user is back on the follow-up.
- **Schedule follow-up** (`create_record` action → followups model). Opens a blank Follow-Up modal with `client_id` prefilled. Used to chain the next follow-up without leaving this one.
- **Register a visit** (`find_or_create_record` action → visits model). Searches the visits model for the **most recent** record (by `created_at desc`) whose `client_id` matches the current follow-up's `client_id`. If found, opens that record in the modal for editing; if not, opens a blank visits modal with the client prefilled. The "found" path lets a sales rep mark a previously-booked visit as completed (entering scheduled_datetime / phone / etc.) without hunting through the visits list.

## Appointments model (new 2026-04-18, phone-first entry 2026-05-11)
System model, ungrouped. One base section "Basic" with fields, in display order: `phone_number` (phone, default +966), `client_id` (lookup → Clients, required), `client_name` (text), `appointment_date` (datetime, required), `project_id` (lookup → `all_projects`), `sales_rep` (assignee), `appointment_status` (dropdown: Scheduled / Confirmed / Rescheduled / Completed / No-show / Cancelled), `notes` (textarea). Used by the Appointment Confirmation section in Follow-Ups to mirror appointment info.

**Phone-first entry (new 2026-05-11).** The form opens with `phone_number` as the first focus. Typing a phone runs `useAutoLink` (same hook used by the visits model) — debounced 400ms, normalized via `normalizePhone` so `0xx` / `5xx` / `966` / `+966` all collapse to one E.164 form. On a unique match by `phone_number` in the clients model, the matched client's id is written into `client_id`. **New behavior on zero matches:** if the normalized phone has at least 12 chars (configurable via `auto_link_create_min_length`), the form auto-creates a new minimal client with `{ phone_number: <normalized> }` and links to it. A per-source-field `lastCreatedFor` ref prevents the debounce re-firing on unrelated formData changes from double-creating. Once `client_id` is set (either matched or freshly created), `client_name` auto-fills from the linked client's `client_name` (empty for newly auto-created clients — the user can type a name into the appointment's `client_name` and the auto-fill won't clobber their edit until the lookup id changes again). The user can still pick a client from the `client_id` dropdown directly; in that case the phone backfills via `auto_fill_from_lookup_field_id → client_id` and `auto_fill_source_field_name = phone_number`.

Two new `ModelField` props back the auto-create: `auto_link_create_if_missing` (boolean, opt-in) and `auto_link_create_min_length` (number, minimum normalized length before create fires — guards against mid-typing creates). See `src/types/index.ts` and the matching branch in `src/pages/Records/hooks/useAutoLink.ts`. Migration `supabase/migrations/2026-05-11_appointments_phone_first_autolink.sql` applies the schema change idempotently to existing installs.

## Visits model (new 2026-05-10)
System model, ungrouped. One base section "Visit Details" with five fields:
- `client_id` (lookup → Clients, **required**) — dropdown displays the client's `client_code` (e.g. `CLT-0001`), not the client's name. Identifying clients by code is the convention sales reps use during a visit.
- `phone` (phone, default +966) — editable, NOT required. Wired with `auto_link_lookup_field_id → client_id` + `auto_link_target_field_name = phone_number` + `auto_link_normalize = phone`, so typing a phone (in any of the 0xx / 5xx / 966 / +966 forms) debounce-searches Clients by `phone_number` and on a unique match auto-selects `client_id`. Also wired with `auto_fill_from_lookup_field_id → client_id` + source `phone_number`, so picking a client backfills the canonical phone.
- `name` (text) — editable, NOT required. Auto-fills from the linked client's `client_name` when `client_id` changes; user override survives until the lookup id changes again.
- `scheduled_datetime` (datetime) — when the visit is planned.
- `project_id` (lookup → `our_projects`) — which project the client is visiting.

Kept as a Builder-managed JSONB model (NOT frozen) per explicit request — the schema is expected to evolve before being promoted to a typed table. Reached primarily from the Follow-Ups buttons (see below); also browsable directly at `/model/visits`.

A 2026-05-10 migration (`supabase/migrations/2026-05-10_visits_rebuild_and_followup_buttons.sql`) tears down a prior user-built visits model (slug `item_mo5t69tn`) and seeds the new one. Destructive on records under the old model — approved in plan mode.

## Units model (new 2026-04-18; bedrooms + bathrooms added 2026-05-23)
System model, in the Projects group. One base section "Basic" with fields: `unit_name` (text, required), `project_id` (lookup → `all_projects`, required), `unit_type` (dropdown — same option set as Clients' `preferred_unit_type`), `unit_status` (dropdown: Available / Reserved / Sold), `area_sqm` (number, m²), `price` (currency), `bedrooms` (number), `bathrooms` (number), `notes` (textarea). Used by Follow-Ups' Post-Visit section, by Clients' `preferred_units`, and as the source data for the eleven cross-record rollup fields on `our_projects` (see next section).

## Our Projects model — computed unit rollups (new 2026-05-23)
The `our_projects` model has a `project` lookup field pointing to `all_projects` and eleven read-only fields whose values are aggregated live from `units` rows that share an `all_projects` record id. Path: `our_projects.data.project → all_projects.id ← units.data.project_id`.

The eleven computed fields, each marked `is_computed: true` on the model schema with a matching `computed_kind`:

| Field slug              | Kind                      | Type     | What it shows                                                                 |
|-------------------------|---------------------------|----------|-------------------------------------------------------------------------------|
| `total_units`           | `units_count`             | number   | COUNT of all units linked to the same all_projects record                     |
| `units_available`       | `units_available_count`   | number   | COUNT where `unit_status` is "available" (English value or متاحة / متاح)      |
| `sold_units`            | `units_sold_count`        | number   | COUNT where `unit_status` is "sold" (English value or مباعة / مباع / مبيع)    |
| `units_reserved`        | `units_reserved_count`    | number   | COUNT where `unit_status` is "reserved" (English value or محجوزة / محجوز)     |
| `price_range`           | `price_range`             | range    | `{ min, max }` of unit `price`                                                |
| `area_range`            | `area_range`              | range    | `{ min, max }` of unit `area_sqm`                                             |
| `bedroom_range`         | `bedroom_range`           | range    | `{ min, max }` of unit `bedrooms`                                             |
| `bathroom_range`        | `bathroom_range`          | range    | `{ min, max }` of unit `bathrooms`                                            |
| `min_price_per_meter`   | `min_price_per_meter`     | currency | MIN of `price / area_sqm`, skipping units with `area_sqm ≤ 0`                 |
| `max_price_per_meter`   | `max_price_per_meter`     | currency | MAX of `price / area_sqm`, skipping units with `area_sqm ≤ 0`                 |
| `avg_price_per_meter`   | `avg_price_per_meter`     | currency | AVG of `price / area_sqm`, skipping units with `area_sqm ≤ 0`                 |

**Behaviors:**
- **Live, never stored.** The values compute at READ time. `saveRecord` strips every `is_computed` field's slug from the data before persisting, so what lands in Supabase has only the user-edited base fields. Round-trip after save: stored data + recomputed rollups on next read.
- **Live updates without reload.** The list view consumes records through `useRolledUpRecordList`, which subscribes to BOTH `records[our_projects.id]` AND `records[units.id]` — flipping a unit's `unit_status` instantly updates the matching project row's counts. The form consumes through `useRolledUpRecord` — opens fresh with live values; mid-edit unit changes refresh on next form mount (acceptable v1 tradeoff for state isolation between user form edits and external updates).
- **Read-only in the form + table.** `lib/permissions.ts` `isComputedField` now returns true for any `is_computed: true` field, alongside the type-level computed kinds (formula / mirror / auto_id / section_mirror). The form input is disabled, the table cell renders without inline-edit.
- **Status matching is bilingual + substring-tolerant.** A unit with `unit_status` of "available" / "Available" / "متاحة" / "متاح" all count toward `units_available`. Lets admins rename the dropdown option labels without breaking the rollups.
- **Per-meter rollups skip zero-area units.** A unit with `area_sqm = 0` is still counted in `units_count` and contributes to `area_range` (the 0 shows as the min), but it's excluded from `min/max/avg_price_per_meter` to prevent Infinity poisoning.
- **Cross-project isolation.** A unit whose `project_id` doesn't match the our_projects record's `project` field is excluded — verified.

Implementation lives in `src/lib/ourProjectsRollup.ts` (pure compute) + `src/hooks/useRolledUpRecords.ts` (React subscription wrapper). Wired into `RecordListPage` and `RecordFormPage`. Save-time strip is in `appStore.saveRecord`. The live migration that adds the schema flags to existing deployments is `supabase/migrations/2026-05-23_our_projects_unit_rollups.sql` — also adds `bedrooms` + `bathrooms` to units and the `project` lookup to our_projects if either is missing. Idempotent.

**Limitations:**
- Field slugs are hardcoded conventions (`project`, `project_id`, `bedrooms`, `bathrooms`, `area_sqm`, `price`, `unit_status`). Renaming them via the Builder would silently break the rollups. No protection yet.
- No general-purpose rollup field type — the eleven kinds are hardcoded by `OurProjectsComputedKind`. Adding a new kind requires a code change in `ourProjectsRollup.ts`. A generic rollup field type is future work.
- The form's `formData` is initialized from the rolled-up record at mount and only re-syncs on navigation. If a unit changes while the form is open, the rolled-up cells in the open form stay stale until the user navigates away and back. The list view IS live.
- Frozen-model interaction is untested. our_projects is not currently frozen; if it were, the save-strip path would need to verify the dispatcher RPC also drops computed fields.

## What it is (in plain English)
This is where users actually enter and browse their data. Each model (Clients, Projects, Follow-Ups, etc.) has a list page where its records are shown as a table or as cards, and a form page for creating or editing one record. The list has filtering, sorting, searching, and bulk import/export. The form dynamically renders the fields defined in the Model Builder, including special types like lookups, multi-select, and the section selector.

## Why it exists
Once models exist, the real day-to-day work of the business happens here — salespeople log calls, property researchers add listings, managers review clients. This is the primary UI of the CRM.

## Key behaviors
- URL structure: `/model/:modelName` (list), `/model/:modelName/new` (create), `/model/:modelName/:recordId` (edit).
- **List view modes:** Table, Card, and Maps. User toggles between them; preference is per-model (persisted as `view_mode_<modelName>` in localStorage).
  - **Table view** shows columns driven by the active **saved view** (if any), or falls back to every field marked `show_in_table` when no view is active.
  - **Card view** uses the model's `card_config` (title, subtitle, badge, shown fields).
  - **Maps view (new 2026-04-23)** uses the model's `maps_config` (Google Maps JS API). Each record resolves to a pin from the configured location field — a `url`/`text` field, **or a `mirror` field that surfaces one** (e.g. a project's location mirrored from the master All Projects record, so the link needn't be stored locally) — via: (1) client-side URL parse for long Google Maps URLs, (2) Vercel edge function at `/api/resolve-maps-url` that follows short `maps.app.goo.gl` redirects server-side and extracts coords from the resolved URL — CORS-blocked in the browser but fine server-side, cached in localStorage under `wassell_maps_url_cache` forever so each unique URL only hits the edge function once, (3) optional manual lat/lng number fields as a backup. Records that exhaust all three paths land in a "records without a pinnable location" side panel. Pins are **labeled pills** showing the configured `pin_label_field_id` value (project name etc.) auto-sized to the text, on a charcoal-or-`pin_color_field_id`-resolved background; crowded pills auto-cluster into copper count circles via `@googlemaps/markerclusterer` and split on zoom-in. Click action is configurable per model: `popup` opens a custom Wassel-styled popup card (rendered as a `FLOAT_PANE` `<OverlayView>`, not Google's default InfoWindow) showing badge / title / subtitle / shown-fields with a copper "Open record" CTA, with all values formatted via the shared `formatFieldValue` resolver (lookups → target's display field, dropdowns → option labels, ranges → `min – max unit`, currency → `… ر.س`); `navigate` routes straight to the record detail. Full Google Maps style JSON drives the theme; a model that leaves `map_style_json` empty inherits the shared **Wassel-branded** map theme (cream geometry, chocolate labels, copper roads) via `resolveMapStyles()`, so every model's map matches the All Projects formatting by default — paste `[]` to opt back into Google's stock theme. Saved views, text search, and the advanced filter panel all apply upstream — Maps consumes `filteredRecords` so it shows exactly the same set Table would. Page size / page navigator are hidden while Maps is active. Shared `useResolvedLocations(model, records)` hook in `src/hooks/` is used by both MapsView and MapsBuilder so async resolutions fill in live without duplicate work. Requires `VITE_GOOGLE_MAPS_API_KEY`; missing key → bilingual setup state, no crash.
- **Saved views (table only):** Every model gets an implicit "Default view" (the `show_in_table` fallback), plus any number of saved views per user.
  - A view captures: selected columns + their order, sort field + direction, and filter conditions.
  - Views can be **private** (author only) or **shared** (visible to all users under "Shared Views"). Only the author can edit or delete a shared view.
  - Each user can mark at most one view per model as their **default** — it's selected automatically on first visit. The constraint is enforced in-store and by a partial unique index on `model_views`.
  - **Last-used view per (user, model)** is persisted in localStorage (`wassell_view_last_<modelId>_<userId>`), so refreshing the page keeps the same view active.
  - The ViewSelector dropdown above the table lists "My Views" and "Shared Views", plus a "Save as new view" action and per-row edit / delete / set-default controls.
  - When the active view changes, the filter conditions are applied to the record list (ANDed together, in addition to the text search), and the column order + sort are reset to what the view defines.
  - **Range-field conditions:** when a saved-view filter targets a range field (stored as `{ min?: number; max?: number }`), the editor shows a min/max bound picker next to the field dropdown; the chosen bound is stored in `condition.field_path` and used to resolve the scalar that the operator compares. `is_empty`/`is_not_empty` evaluate against the whole range value and don't require a bound. Conditions saved before this support (range field + no `field_path` + numeric operator) are silently skipped at evaluation time rather than filtering every record out.
- **Advanced Filter panel (faceted, table only):** A collapsible panel above the toolbar shows one chip per filterable field. Clicking a chip opens a type-aware popover:
  - `dropdown` / `multiselect` / `section_selector` / `lookup` / `assignee` → checkbox list of all possible values (with a search box when > 6 options). A small **Is / Is not** toggle at the top of the popover flips the match: **Is** (default) passes records where ANY picked value matches (OR); **Is not** passes records where NONE of the picked values match — records with no value for the field also pass under "Is not", since they have none of the picked values. Active "Is not" chips show their summary prefixed with `≠`. Mode is persisted alongside the picked values (`{ kind: 'values', values, mode: 'is' | 'is_not' }`).
  - `checkbox` → yes / no tri-state.
  - `date` / `datetime` → from/to range (inclusive; date-only inputs use end-of-day for "to").
  - `number` / `currency` → min/max range.
  - `text` / `textarea` / `email` / `phone` / `url` → "contains" text input.
  - `notes` and `range` → not exposed in the faceted panel (their structured value isn't meaningful with the existing filter kinds).
  - Across different fields, records must pass all active filters (AND).
  - Active chips show a short summary (e.g. "Status: Active, Pending" or "≥ 100") and an X to clear; the panel header shows a count badge and a "Clear all" action.
  - State is persisted **per (user, model, view)** in localStorage (`wassell_adhoc_<modelId>_<userId>_<viewId|default>`). Switching views swaps the ad-hoc filter set; returning to a view restores its ad-hoc filters.
  - Collapse/expand state is persisted per model (`wassell_adhoc_collapsed_<modelId>`).
  - Ad-hoc filters **layer on top of** the active view's saved conditions — view conditions apply first, then ad-hoc, then text search.
- **Record form** renders sections in order; each section renders its fields honoring `width` (half/full) for responsive layout.
- **Collapsible sections (new 2026-04-20):** each section header in the form is a button that toggles the section body open/closed. The initial state seeds from the model's `ModelSection.default_collapsed` flag (configured in the Builder via a chevron toggle in the section header). A per-section `useState` holds the current state, so expand/collapse is local and does not trigger form saves. Collapsed sections still participate in required-field validation — hiding fields does not skip them. Mirrored sections, base sections, and regular sections all behave identically.
- **In-section field groups (new 2026-04-20):** if a section defines `field_groups` (see model-builder.md), the record form splits its body into an ungrouped grid at the top followed by a collapsible mini-card per group. `SectionBlock` extracts its field-rendering logic into a local `renderFieldNodes` helper and runs it twice — once for ungrouped fields, then once per group inside a `FieldGroupBlock` (the collapsible wrapper with the group's label as a clickable header). Each group's collapse state is its own `useState` seeded from `default_collapsed`; collapsing or expanding a group does not affect the parent section's collapse. When the parent section is collapsed, all groups are hidden along with the grid. Groups are display-only — required-field validation still runs across every field, regardless of which group hides it. A field whose `field_group_id` points at a deleted/missing group falls back to ungrouped at render time.
- **Section selector** on the form controls which non-base sections are visible. Base sections always show. Custom (non-section) values on a `section_selector` field (see model-builder.md) render as normal multi-select options but are ignored by the visibility filter — they never hide or reveal a section.
- **Prev/next record navigation (new 2026-04-23):** on the record edit page, two arrow icon-buttons sit at the start of the top-right action cluster (before the optional PDF button, before Delete, before Save). Left chevron → previous record; right chevron → next record, with `rtl:rotate-180` so the direction reads correctly in both LTR and RTL. Arrows cycle through **the same filtered + sorted list the user was browsing in the list view**, not the entire model — `RecordListPage` publishes the ordered id list to `recordNavContext` in the store, and the form reads from it. Arrows disable at the first / last record (no wrap) and are hidden entirely on `/new`. If the form has unsaved edits, clicking prev or next triggers a `window.confirm` asking whether to discard — Cancel keeps the user on the record; OK navigates. When a user deep-links into a record without visiting the list first, the arrows fall back to the full model's insertion order. Column-header sorts applied ad-hoc inside `TableView` are a v1 limitation — they live in local component state and don't update the nav context, so prev/next follows the saved view's default sort in that case.
- **Required fields** block save with inline error messages.
- **Lookup fields** open a searchable combobox of records from the lookup target model, showing the chosen display field. When the field has `is_multi: true`, the combobox becomes a multi-select chip list — picking a record adds it, the X on each chip removes it, and the search input stays open for adding more. Stored value becomes `string[]` (array of record IDs); table/card cells render it as a badge list. **Inline-create (new 2026-04-23):** when the user types a non-empty query into the combobox that doesn't exactly match an existing record's label (case-insensitive), a copper "Create: '<query>'" row appears at the bottom of the dropdown. Clicking it calls `saveRecord` on a freshly-minted record in the target model with the typed text written to the configured `lookup_display_field`, then auto-selects the new record — single-mode closes the dropdown with the new record as the value; multi-mode adds it as a chip and clears the search so the user can keep typing. Because the new record is a regular record write, it fires `auto_id` assignment, formula recompute, workflow `create` triggers, and Supabase sync just like a manually-created record, and it shows up immediately in its own list view. The empty "No results" placeholder is suppressed while the Create row is visible; when matches exist, the Create row renders below them with a top divider.
- **Mirror fields (new 2026-04-18):** render as a read-only, muted input whose value comes from hopping through a sibling lookup. Updates live as soon as the sibling lookup's selection changes. When the sibling lookup is multi-select, the mirror resolves to an array of values and renders as a chip list of resolved cells. In the table view, mirror columns cannot be sorted or inline-edited. In Excel export, mirrors are resolved; in import, they are excluded from the column mapping. Uses `src/lib/mirrorResolver.ts` for all surfaces.
- **Mirrored sections (new 2026-04-18):** a section flagged `is_mirrored` renders its fields from a linked record's source section (resolved at render time). Empty-states: if the sibling lookup on this record isn't set yet, the section shows "Select the linked record first"; if the linked record was deleted, it shows "The linked record has been deleted." When the section resolves OK, each field renders with the linked record's value — fully editable. Edits don't save into the current record; they're collected into `mirrorEdits[targetRecordId]` and, on form save, merged into the linked record's `data` and persisted via `saveRecord`. Required-field validation runs on the effective value (linked record data overlaid with pending mirror edits). Mirrored sections always render regardless of `section_selector` state. Uses `src/lib/sectionMirrorResolver.ts`.
- **Notes fields (new 2026-04-18):** render as an append-only journal of entries — a composer at top (textarea + "Save note" button; ⌘/Ctrl+Enter submits) and a reverse-chronological list below. Each entry captures text, the `currentUserId` as author, and an ISO `created_at`. The form's usual Save must still be pressed to persist new entries; entries are never edited or deleted in this UI (no undo — edits would be adds). In the table, a notes column shows a compact "N notes · <latest preview>" summary and isn't inline-editable (click through to the record). In Excel export, entries serialize to a multi-line `[YYYY-MM-DD] text` block separated by `---`; import is skipped (no way to re-author history). Stored shape is `NoteEntry[]` (see `src/types/index.ts`).
- **Range fields (new 2026-04-18):** two number inputs side-by-side (min — max), configurable in the Builder with `range_min`, `range_max`, `range_step`, and optional `range_unit_ar` / `range_unit_en` (unit suffix shown inside each input and in the read-only cell). Stored shape is `{ min?: number; max?: number }` — either bound is optional so the user can express "≥ X" or "≤ Y". The table cell renders `min – max <unit>` (or the one-sided form). Import is skipped because the structured shape can't be parsed from a free-form cell.
- **Auto ID fields (new 2026-04-18):** render as a read-only copper-tinted badge in the record form (Fingerprint icon + the formatted string, e.g. `CLT-0001`). On create, the form submits no value for the field; `appStore.saveRecord` detects the `auto_id` type, calls `assignAutoIds`, writes the generated value into `record.data[slug]`, and bumps the counter on the model's schema JSONB — both state updates happen in one atomic `set(...)` transition. On edit, auto_ids are immutable: `assignAutoIds` skips fields that already have a value. Scoped counters (per-value-of-another-field) use a slugified scope key; IDs are NOT re-assigned when the scope field's value changes on an existing record. Table cells render as a mono-font copper string. Not importable from Excel, not exposed in the advanced filter panel.
- **Formula fields (new 2026-04-18):** render as a read-only strip (Calculator icon + live-computed value) in the record form. The form recomputes as the user edits other fields using `evaluateFormula(field.formula_expression, formData)` on every render — no save required to see the preview. On save, `appStore.saveRecord` calls `computeAllFormulas(model, data)` which topo-sorts formula fields by their `{slug}` dependencies, evaluates in order, and writes the result into `record.data[slug]` as a snapshot. Cached values are what filters, exports, dashboards, and table cells read (the engine never re-runs at render time in those surfaces). Error sentinels (`#DIV0`, `#REF`, `#ERR`, `#CYCLE`) render red in both form and table. **Known staleness tradeoff:** if a formula references a lookup-or-mirror-derived value and the source record changes later in another model, the stored formula value doesn't auto-refresh — it refreshes on the next save of this record. This matches Excel/Airtable behavior and keeps reads cheap. Not importable from Excel, not exposed in the advanced filter panel.
- **Dropdown / multi-select** show colored option chips. When the field defines `option_groups` (see model-builder.md), the menu organizes options into **collapsible group headers**: ungrouped options render first at the top, then each group header shows its label and option count (or `selected/total` for multi-select), collapsed by default. Clicking a header expands that group inline with indented option rows. On reopen, the group containing the currently selected value (or any selected values, for multi-select) auto-expands so the user sees their picks immediately. Groups are display-only — stored record values remain the option's raw `value` slug, so adding, removing, or rearranging groups never touches existing data. **Searchable (new 2026-04-23):** when the field has more than 6 options, a search input sits at the top of the menu and auto-focuses on open; typing filters options case-insensitively against both `label_ar` and `label_en`, so the user can search in either language regardless of UI language; while a query is active every surviving group is force-expanded so matches inside a collapsed group stay visible, and when every option is filtered out the menu shows "No results / لا توجد نتائج". The query resets when the menu closes — single-select menus close on pick (query naturally resets for the next open), multi-select menus stay open after each pick and preserve the query so the user can keep selecting matches. In **table inline-edit mode (new 2026-04-22)**, these field types render the same picker popovers used in the record form via a new `compact` prop on `DropdownSelect` / `MultiSelect`, so inline editing has full option-group support, search, and color chips — not the trimmed native `<select>` and flat checkbox strip that v1 used. **Inline-create (new 2026-04-23):** regular `dropdown` and `multiselect` fields in the record form now show a copper "Create: '<query>'" row at the bottom of the menu when the user types a search that doesn't exactly match any existing option label (case-insensitive, either language). Clicking it calls a new optional `onCreateOption(label) → string` prop on `DropdownSelect` / `MultiSelect` — `DynamicField` implements it by appending a fresh `FieldOption` (uuid id + uuid value + both labels = the typed text + rotating palette color) to the field inside its owning model's schema and calling `saveModel`, then returning the new value so the picker selects it immediately (single-select closes; multi-select adds the chip, clears the query, and keeps focus for more creates). When `onCreateOption` is set the search input is always shown regardless of the 6-option threshold, so fields with few options aren't locked out. `section_selector` fields are intentionally excluded because their options encode section ids. Inline-created options are fully editable in the Builder afterward (rename, recolor, regroup) just like any other option.
- **Inline text / email / textarea (new 2026-04-22):** in table inline-edit mode, the input auto-sizes to its content — width is `clamp(8ch, len + 2ch, 48ch)` via an inline `style={{ width }}` override of `form-input`'s `width: 100%`, so the cell widens as the user types. Cells containing longer values open wider so the full text is visible while editing.
- **Auto-growing text areas (new 2026-05-31):** `textarea` record fields, the `notes` composer, and `textarea` columns inside `table` fields render through `AutoGrowTextarea`, which grows the box's height to fit its content (no inner scrollbar) and shrinks it back down to the `rows` minimum when text is removed. It re-fits on every keystroke, whenever the stored value changes (record load / form reset / programmatic update), and when the field's width changes (RTL flip, sidebar toggle, viewport resize — via a width-guarded `ResizeObserver`). Single-line `text` fields are deliberately left unchanged — they stay one line so the `text` vs `textarea` distinction the Builder offers stays meaningful.
- **URL fields inline (new 2026-04-22):** in table inline-edit mode, URL fields render as an "Edit link" / "تعديل الرابط" copper chip with a small external-link icon next to it when a value is set. Clicking the chip opens a compact popover (`UrlInlineEditor` sub-component in `TableView.tsx`) with a `dir="ltr"` URL input, a cancel button, and a save button — Enter commits, Escape or click-outside dismisses without saving. This replaces the v1 inline `<input type="url">` which stretched to fill the cell and forced the table to scroll sideways just to edit one link. The `DynamicCell` read-only rendering of URLs is unchanged.
- **Top horizontal scrollbar (new 2026-04-22):** when the table's content is wider than the viewport, a ghost scrollbar appears **above** the header row in addition to the native scrollbar under the last data row. The two are kept in sync via bidirectional `scrollLeft` handlers (guarded by a `syncingRef` to prevent feedback loops) so dragging either moves the table. A `ResizeObserver` watches the table and hides the top bar entirely when `scrollWidth <= clientWidth`. This matters for wide tables on small screens where the bottom bar requires scrolling the page down first.
- **Phone fields** render three inline icon buttons next to the number in both table and card views: a WhatsApp button (opens `wa.me/<digits>` in a new tab), a Call button (fires a `tel:` link), and a Copy button (copies the raw phone value to the clipboard via `navigator.clipboard.writeText` and shows a success/error toast). Icons are copper-tinted to match the design system. Clicking the icons does NOT trigger row inline-edit or card open-record (propagation is stopped). The WhatsApp and Call icons are hidden when the value can't be parsed as a phone number; the Copy button is always available whenever the raw text is shown. In the record form and inline table edit, phone fields use a two-part input (country-code dropdown + number); the default country code is configurable per-field (falls back to +966). Stored values are canonicalized to E.164 (e.g. `+966501234567`, no spaces).
- **File upload** stores the file in Supabase storage (or base64 in localStorage offline) and saves a URL in the record.
- **Bulk import** from Excel — user maps columns to fields, preview shown before import.
- **Bulk export** to Excel of the current filtered list.
- **Bulk edit (new 2026-04-20):** When ≥1 record is selected, the copper action bar exposes a permission-gated ("edit") **Edit selected** button between Export and Delete. Clicking it opens `BulkEditModal` — a single form that lists every editable field in the model, grouped by section (in their display order). Each field row has a checkbox + a `DynamicField` input; editing the input auto-enables the checkbox (so the user doesn't fight a double toggle), and unchecking the box discards the pending value. The footer's Apply button is disabled until at least one field is enabled and shows the selected-record count. Applying opens a nested confirmation modal ("N field(s) × M record(s), cannot be undone") — on confirm, a patch of `{ enabledField: value, ... }` is merged into each selected record's `data` and each record is saved via `saveRecord`, so the normal save pipeline runs per record: auto_id / formula recomputation, Supabase upsert, and `update`-trigger workflows all fire. After the write, selection is cleared and a success toast ("N records updated") appears. Hidden field types (can't be bulk-edited): `mirror`, `section_mirror`, `auto_id`, `formula`. Hidden sections: any section with `is_mirrored: true` (those edit a *linked* record, which isn't the mental model for bulk edit). Base fields, required fields, dropdowns, multi-selects, lookups (single + multi), assignees, phones, ranges, notes, and every other editable field type render exactly as they do in the single-record form, because the modal composes `DynamicField` directly.
- **Research-link prompt — batched queue with in-modal drafts (new 2026-04-20, previously singular):** Whenever any `targeted_projects` record is created — manual form, bulk edit, bulk import, or a workflow fan-out from All Projects — its id is **appended** to a store-level queue (`pendingResearchPromptTargetedIds: string[]`). The global `ResearchPromptModal` opens whenever the queue is non-empty and renders **one row per queued project**, so a burst of N new Targeted Projects produces a single modal with N rows instead of one-popup-per-record. Each row shows: the resolved project name, an "auto-matched" badge when a matching All Projects record is found by lookup id or exact-name text match (manual `<select>` picker shown when both strategies fail), and a three-way radio — **Create a new study** (with an inline "study name, optional" text input that labels the in-modal draft) / **Add to a study** (with a target-picker `<select>` that groups options into "**New drafts in this batch**" and "**Saved studies**") / **Skip this project**. The draft label defaults to the draft owner's project name (or `First +N` when more rows merge into it) and updates live as siblings opt in. Defaults to "Create new" per row; the user can mix actions across rows and group multiple rows into the same study by pointing them at the same draft. The Apply footer button validates every non-skipped row has a resolved project id + (for "existing") a selected target; if a row points at a draft whose owner has since flipped away from "new", that row fails validation too. On Apply: each draft becomes one research record containing every project that flowed into it (deduped), appends to saved studies are grouped so all projects going into the same saved study go through a single `saveRecord` merge, skips are no-ops. The typed draft name is a label for the modal only — today the research model has no dedicated "study name" field, so the grouping is what gets persisted (the research record's auto-derived title is still "first project +N"). Success toast reports counts per outcome ("N new studies, M projects appended, K skipped"). The modal dismisses on Apply or "Later"; "Later" discards the entire queue. Navigation happens only when exactly one new study resulted (preserves the prior single-record UX); larger batches stay on the current page. Previously the singular `pendingResearchPromptTargetedId` slot meant each new Targeted Projects record overwrote the previous one in a bulk-save loop, and the user only saw a prompt for the last record created; and there was no way to group several new Targeted Projects into a shared study without first saving one, closing the modal, and coming back.
- **Bulk select (new 2026-04-18, expanded 2026-04-19):** Both table and card views expose per-record selection. Table adds a leading checkbox column with a tri-state select-all checkbox in the header; card view adds a "Select all / Deselect all" text button above the grid and an overlay checkbox in the top-`end` corner of each card. The header checkbox / card "Select all" toggles the **currently visible (paginated)** records — matching Gmail / Sheets — and selection persists across pages as the user paginates. When the current page is fully selected and more records exist beyond it, the copper action bar also shows an inline **"Select all N records in this view"** button (`selectAllInView`) that expands the selection to every record matching the current filters / search / view. While in that state the bar's label flips to "All N records in this view are selected". Selection survives filter / search / view changes but resets when the model itself changes. The bar also carries a "Clear selection" link and (permission-gated) **Delete selected** and **Export selected** buttons. Bulk delete opens a confirmation modal and then calls `deleteRecord` per ID. Bulk export calls `exportToExcel` with the selected record set.
- **Research Project button (new 2026-04-28, All Projects only):** every saved `all_projects` record shows a copper "Research Project" / "بحث عن المشروع" button at the start of the action cluster in the record-edit header (left of Delete/Save). Clicking it fires the Claude Code "Research Project" routine — a remote agent configured at claude.com/code — by POSTing the record's `project_name` and `location` (live form values, so unsaved edits ride along) to `/api/research-project`. The server route formats them as a plain-text trigger message (`PROJECT: <name>\nLOCATION: <url>`) and forwards to the routine's `/fire` URL with the OAuth bearer token from `CLAUDE_ROUTINE_TOKEN`. The token is server-only — env vars without a `VITE_` prefix never bundle into the browser. Validation runs both sides: empty `project_name` or `location` produces a red toast immediately; server returns 400 on the same; missing env vars 500; routine network failure 502; routine error responses pass through with their status + body — every path surfaces a toast loudly per the silent-failures rules. While the request is in flight the button shows "Researching..." / "جاري البحث..." and disables. The routine runs asynchronously on the Anthropic side, so the button completes when the trigger is accepted; the resulting market-study record appears later in `projects_research` when the routine finishes. Button is gated on `existingRecord`, so it doesn't render on `/model/all_projects/new`.
- **Research PDF** — certain models (e.g. `projects_research`) have a "Generate PDF" button that produces an Arabic-RTL PDF using the research template. The `projects_research` schema was rewritten 2026-04-18 to pair a Developer lookup (→ the new `developers` model) and a Project Name lookup (→ `all_projects`) with four mirror fields (project_location, brochure_link, city, district — all pulled from the selected project) and a Market Data section (unit_types, min/max area, min/max price, min/max bedrooms, unit counts, accommodations). `all_projects` gained `brochure_link` (URL), `city` (dropdown, Saudi cities), and `district` (dropdown, Riyadh districts) at the same time. Returning users' old research records are cleared by the schema migration — dangling saved-view / dashboard / workflow references to removed field IDs surface as console warnings and become no-ops; user must reconfigure.
- **Multi-project comparison (new 2026-04-18, redesigned 2026-04-19):** `projects_research.project_name` is a multi-select lookup, and the model now seeds a `project_comparison` `section_mirror` container pointing at All Projects' base section. When ≥2 projects are linked, the container renders a **flipped comparison table** (projects as rows, fields as columns) via `SectionMirrorComparison.tsx`. Cells are **editable in place** — the container's `section_mirror_edit_mode` and `section_mirror_sync_mode` (both default `'all'`) control per-field editability and whether edits sync back to the source All Projects record on save. Synced edits fan out through the same `mirrorEdits` → `saveRecord` pipeline used by single-target mirrored sections (`RecordFormPage.tsx`), so the same required-field validation and workflow triggers fire. Non-syncing edits are stored as local overrides keyed by `formData[container.name][targetId][fieldName]`. A per-cell badge strip shows "Will sync" / "Local" when a cell has an uncommitted change.
- **Research comparison views (new 2026-04-19):** A research record's comparison table can be scoped by a saved **ModelView** that picks both **columns** (fields from the source section, via `field_ids` — drag-reorderable) and **rows** (linked projects, via the new `project_ids` column on `model_views`). Views are reusable across research records; at render time `project_ids` is intersected with the record's currently-linked ids and the set of still-existing All Projects records, so stale ids silently drop without mutating the stored view. The `research_container_field_id` column pins a view to a specific `section_mirror` container (future-proofing when more containers exist). The view selector appears above the comparison table, reusing `ViewSelector`; a dedicated `ResearchViewEditor.tsx` modal handles create/edit with a field picker (drag-reorder) + project checklist. The active view is persisted per (research record, user) in `localStorage` under `wassell_research_view_last_<recordId>_<userId>`. `generateResearchPDF` accepts an optional active view and filters both axes accordingly.
- **Research PDF (updated 2026-04-19):** The PDF template is now projects-as-rows / fields-as-columns (matching the in-form comparison). Landscape A4 when the active view has ≥3 field columns (tunable), portrait otherwise. When no view is active, defaults to all included fields × all linked projects. File naming preserves `<first-project-name>-and-<N>-others.pdf` convention; PDF column list is dynamic (walks the research model at runtime, so Builder edits are respected).
- A schema migration (v8→9) and an always-run idempotent heal (`healResearchComparisonContainer`) insert the `project_comparison` container for returning users whose stored research schema predates the 2026-04-19 redesign.
- **Save** updates localStorage immediately, then syncs to Supabase via the `record_save` RPC (unfrozen) or the dispatcher RPC (frozen). For unfrozen records the RPC carries the loaded `version` as `p_expected_version` (Phase F.2). On a `version_mismatch` conflict, the user sees "Another user just edited this record. Reload to see their changes before re-saving." and the write is not retried. Auto-IDs are assigned atomically via a server-side counter (Phase F.1).
- **Delete** is permission-gated (see access-control.md).
- **Permissions** gate view/create/edit/delete per model via `usePermission` hook.
- **Client Details tab strip (new 2026-05-10):** every record edit page renders a tab strip ABOVE the section blocks when the record is associated with a client — i.e. when the record either IS a client (`model.name === 'clients'` AND the record exists, not `/new`) OR has at least one populated `lookup` field whose `lookup_model_id` points at the clients model (single OR multi-select; multi counts every populated id). Two tabs: **Form** (the existing record form, default) and **Client Details** (a Users icon — embedded read-only view of the linked client's full record + a "Related Records" cross-model panel below). Tab state is persisted in `?tab=client` via `useSearchParams` so the user can deep-link, share a link, or use the back button without losing context. When a non-clients record has multiple populated client lookups, the Client Details pane adds a chip switcher above the embedded view that writes `?client=<recordId>` and re-renders for the chosen client. Hidden tab strip (`tabs.length <= 1`) when no client is in scope. The full Client 360 surface is documented in [clients.md](clients.md); this PRD just notes that the strip exists on every record that qualifies, not just on clients.

## User flows
1. **Browse records:** Click model in sidebar → `/model/:modelName` → see list. Switch between table/card view with toggle.
2. **Create record:** Click "+ New" → form opens with all sections → fill required fields → Save → redirected to the edit page for the new record.
3. **Edit record:** Click a row/card → form loads with existing values → change any field → Save.
3a. **Step through records:** From any record edit page, click the left or right chevron at the top of the header → jumps to the previous or next record in the same filter/sort order the user was browsing. If there are unsaved edits, the unsaved-changes guard appears first (see 3b).
3b. **Exit a dirty record:** Clicking the back arrow at the top-left of the record header — or the prev/next chevrons — while the form has unsaved edits opens an **Unsaved changes** modal with three options: **Save & Exit** (runs the same save flow as the toolbar Save button, then completes the navigation the user originally attempted — back to the list, previous record, or next record), **Discard & Exit** (navigates without saving — pending edits are dropped), and **Cancel** (closes the modal, the user stays on the current record with edits intact). A clean form (no edits since last save / mount) exits immediately with no prompt. If Save & Exit hits a validation or version-conflict failure, the underlying save toast surfaces and the modal stays open so the user can fix the problem.
3c. **View a project's units:** Open any record that is an All Projects record, or any record that references a project through a lookup (an appointment, our-project, project-details record, etc.) → a **Units** tab appears next to Form (and Client Details, if present). Click it → a table lists every unit linked to that project, with the same column / saved-view controls as the Units list page (click the view pill to switch or "Save as new view" to adjust columns). If the record links to more than one project, a chip switcher above the table flips between them. Click a unit row to open it; the tab is deep-linkable via `?tab=units&project=<id>`.
4. **Filter/search:** Top of list has a search box, plus (in table mode) an Advanced Filter panel with a chip per field and a ViewSelector for saved views.
5. **Faceted filter a field:** In the Advanced Filter panel, click any field chip → popover opens → select values / set a range / type a "contains" query → popover commits on change → table updates live. Click the X on an active chip to clear that one field, or "Clear all" in the panel header to clear every chip.
6. **Save a custom table view:** In table mode, click the view pill → "Save as new view" → pick columns (drag to reorder), set sort, add filter conditions, toggle "Share with team" and "Set as my default" → Save. The new view becomes active immediately.
7. **Switch views:** Click the view pill → pick any row under "My Views" or "Shared Views" → table re-renders with that view's columns/sort/filters. Pick "Default view" to fall back to `show_in_table`.
8. **Edit / delete a view:** Hover the row in the ViewSelector to reveal pencil / trash / star icons. Star toggles the per-user default.
9. **Section selector in action:** On a Follow-Ups record, user picks "Phone Call" in the section selector → only the "Phone Call Details" section becomes visible along with base sections.
10. **Import:** Click "Import" → modal accepts `.xlsx` → user maps each column to a model field → preview → confirm → records created.
11. **Export:** Click "Export" → downloads the current filtered list as `.xlsx`.
12. **Research PDF (special):** On a research record, click "Generate PDF" → downloads an Arabic-RTL PDF of the comparison table, flipped (projects as rows, fields as columns). The active comparison view scopes both the column set and the row set; default view = all linked projects × all included fields. Landscape A4 for 3+ field columns, portrait for fewer.
13. **Compare projects side-by-side:** Pick multiple projects in the `اسم المشروع` lookup → the `project_comparison` section renders a flipped comparison table (rows = projects, columns = fields). Cells are editable inline; changes sync back to the source All Projects record on save (subject to the container's `edit_mode` / `sync_mode`). A per-cell badge shows "Will sync" (writes to All Projects) or "Local" (stored as an override on this research record) while a change is pending. Use the **view selector** above the table to pick a saved comparison view, or click "Save as new view" to pick which columns + rows appear. The active view is remembered per research record.

## Data touched
- Reads/writes: `records.data` (JSONB per record, keyed by field slug).
- Reads: `models.schema` to know what fields to render.
- Reads: other `records` (for lookup fields).
- Reads/writes: `model_views` rows (per model, per user saved table configurations). Research-comparison views additionally populate the optional `project_ids` (row picker) and `research_container_field_id` (container scope) columns.
- Writes: Supabase storage (for file uploads).
- **Research comparison writes:** editing a cell in the flipped comparison table updates the linked `all_projects` record's `data` JSONB via `saveRecord`. Workflow triggers on `all_projects` update fire as expected. Required-field validation on synced edits runs before fan-out.

## Key files
| File | What it does |
|---|---|
| `src/pages/Records/RecordListPage.tsx` | List page (table or cards) |
| `src/pages/Records/RecordFormPage.tsx` | Create/edit form; owns `mirrorEdits` state and fans out saves to linked records for mirrored sections; hosts the Client Details + Units tab strip + chip switchers (`clientCandidateIds` / `projectCandidateIds` detection); gates the bottom-of-form `CallHistoryPanel` on `model.name !== 'clients'` |
| `src/pages/Records/components/RecordTabBar.tsx` | Pure tab strip component (Form / Client Details / Units — each tab shows only when its candidate set is non-empty). Controlled by parent — no internal state |
| `src/pages/Records/components/ClientDetailsTabPane.tsx` | Embedded read-only client view + Related Records panel below. Reuses `SectionBlock` with `formReadOnly={true}` |
| `src/pages/Records/components/RelatedRecordsPanel.tsx` | Cross-model "every record that references this client" list. Memoized lookup-fields-by-target-model index over `models`; grouped by source model; click-through routing |
| `src/pages/Records/components/UnitsTabPane.tsx` | Embedded Units table for the Units tab. Lists every `units` record whose lookup field (scanned by `lookup_model_id === all_projects`, not a hardcoded slug) points at the active project; reuses `TableView` + `ViewSelector` + `ViewEditor` (saved views shared with the Units list page → adjustable columns) + `useApplyViewScope` + pagination; row click opens the unit, delete via confirm modal |
| `src/pages/Records/components/WhatsAppHistoryPanel.tsx` | Unified WhatsApp history across every Haberchat device for one client. Mounts `subscribeToAllChats` on first render; loads each linked chat's last 50 messages via `loadMessagesForChat`; per-bubble device label |
| `src/pages/Records/components/CallHistoryPanel.tsx` | UI for the call list. Now accepts `chrome?: 'card' \| 'naked'` so the same panel renders either as a freestanding card (legacy bottom-of-form) or naked inside a `call_history` section field |
| `src/pages/Records/components/SectionBlock.tsx` | Section renderer; branches on `is_mirrored` for mirrored sections; **special-cases `whatsapp_history` + `call_history` BEFORE the standard `DynamicField` dispatch** — full-width, no chrome, ignores readonly/edit toggles. Empty-state when `recordId` is missing (`/new` form) |
| `src/lib/haberchat/clientHistory.ts` | `selectClientChatMessages(input)` — pure selector that flattens every chat record's messages for one client across every device |
| `src/pages/Records/components/TableView.tsx` | Table rendering with dual top/bottom synced horizontal scrollbars (ResizeObserver-tracked); inline edit uses `LookupCombobox` for multi-select lookup, `DropdownSelect` / `MultiSelect` (compact) for enum fields, auto-sizing text inputs, and `UrlInlineEditor` for URL fields |
| `src/pages/Records/components/CardView.tsx` | Card rendering (resolveFieldDisplay handles array lookup values) |
| `src/pages/Records/components/MapsView.tsx` | Maps view — `<GoogleMap>` canvas with imperative `google.maps.Marker` instances managed by `@googlemaps/markerclusterer`, custom-styled popup card via `<OverlayView>` (FLOAT_PANE), shared `formatFieldValue` resolver for type-aware popup field rendering; side panel of unresolvable records; "resolving N links" indicator while the async resolver runs |
| `src/lib/locationUtils.ts` | Pure helpers: `parseGoogleMapsUrl`, `resolveLocation`, `resolveLocationWithCache`, `parseMapStyleJson`, `resolveMapStyles` (+ `WASSEL_MAP_STYLE` constant — the branded default theme), `resolvePinColor`, `buildColoredPinIcon`, `buildPillIcon`, `buildClusterIcon`, `DEFAULT_MAP_CENTER` (Riyadh), `DEFAULT_MAP_ZOOM`; plus `resolveMapsUrlAsync` + `collectUrlsNeedingResolution` + `getCachedResolution` for the server-side short-URL fallback. The three location resolvers take an optional `LocationResolveCtx { allRecords, allModels }` so a `mirror` location field resolves through its sibling lookup to the source URL (private `readLocationString` does the hop) |
| `src/lib/mapsLoader.ts` | Shared `useJsApiLoader` options singleton (one `<script>` tag across MapsView + MapsBuilder) + `isMapsKeyConfigured` |
| `src/hooks/useResolvedLocations.ts` | Hook shared by MapsView + MapsBuilder — runs sync resolution first, then kicks off `resolveMapsUrlAsync` in parallel for URLs that need server-side redirect-following; re-renders as resolutions land; tracks `resolving` + `resolvingCount` for the UI indicator. Builds the `LocationResolveCtx` from the store (allRecords/allModels) **only when the location field is a `mirror`**, so non-mirror models don't recompute pins on unrelated record edits |
| `api/resolve-maps-url.ts` | Vercel edge function that follows the shortened URL's redirect server-side and parses coords out of the final URL or HTML body; one-year immutable cache headers so Vercel's edge cache serves repeat hits without re-running |
| `api/research-project.ts` | Vercel edge function for the All Projects "Research Project" button — auth-gated by `withAuth`, takes `{ project_name, location }`, formats a plain-text trigger message (`PROJECT: …\nLOCATION: …`), and forwards to the Claude Code routine's `/fire` URL with `CLAUDE_ROUTINE_TOKEN` as bearer auth. OAuth token never reaches the browser. |
| `src/pages/Records/components/DynamicField.tsx` | Renders a field in the form based on type (passes `isMulti` to lookup; handles array mirror values; auto_id badge; formula live-preview via `evaluateFormulaInModel` — full-graph re-eval + `formatFormulaValue`). `date` / `datetime` cases delegate to `DateTimePicker` (replaces native browser inputs). Drive-backed types (`image` / `multi_image` / `file` / `multi_file` / `attachment`) delegate to inputs in `DriveFieldInputs.tsx` and `AttachmentFieldInput.tsx`. Accepts optional `modelId` + `recordId` props that `SectionBlock` populates so file uploads through Drive-backed inputs are tagged with the owning record. The `textarea` case renders `AutoGrowTextarea` so the field grows to fit long content instead of scrolling internally |
| `src/pages/Records/components/DriveFieldInputs.tsx` | Form inputs for `image` / `multi_image` / `file` / `multi_file` — open `DriveBrowserModal` for browse + pick + upload; render chips/thumbs with click-to-preview through `FilePreviewModal` |
| `src/pages/Records/components/AttachmentFieldInput.tsx` | Form input for `attachment` — multi-value reference to files AND folders; folder chip click re-opens the picker scoped to that folder |
| `src/pages/Records/components/DriveCells.tsx` | Compact table-cell renderers for the same five Drive-backed field types (thumb or icon chip + click-to-preview). Shares the `useFileRowMap` cache with the form inputs |
| `src/pages/Records/components/useFileRowMap.ts` | Process-lifetime in-memory cache + React hook for resolving `files.id` UUIDs to `FileRow` metadata. Backs every Drive-backed input/cell so multiple fields on the same form don't re-query the same id |
| `src/pages/Files/components/DriveBrowserModal.tsx` | Reusable Drive picker invoked from every Drive-backed input — folder-only mode, files-only multi-select mode, files+folders multi-select mode. Inline Upload + New Folder buttons |
| `src/pages/Records/components/DateTimePicker.tsx` | Wassel-branded calendar + time popover used for every `date` / `datetime` record field. RTL-aware (Sunday-first weekday row), copper-circle selected day, copper-text today, `صباحاً`/`مساءً` toggle for datetime mode. Storage format unchanged (`YYYY-MM-DD` / `YYYY-MM-DDTHH:MM`) so downstream filters / formula engine / `formatDateHumanAr` keep working |
| `src/pages/Records/components/DynamicCell.tsx` | Renders a field value in a table cell (array lookup + array mirror support; auto_id mono-font badge; formula rendered via `formatFormulaValue` — decimals / currency / percentage / thousands — with `#REF` / `#DIV0` / `#ERR` in red). Drive-backed cells (`image` / `multi_image` / `file` / `multi_file` / `attachment`) delegate to renderers in `DriveCells.tsx` |
| `src/lib/autoIdAssigner.ts` | Pure auto_id helpers used by the store's save pipeline and by the Builder's Restart modal |
| `src/lib/fieldFallbackResolver.ts` | Save-pipeline step that fills empty target fields from their configured source field (see model-builder.md). Runs after auto_id, before formula snapshots |
| `src/lib/formulaEngine.ts` | Recursive-descent formula parser + evaluator used at save time (snapshot via `computeAllFormulas`) and in the form's live preview (`evaluateFormulaInModel`); also owns `formatFormulaValue` for shared display formatting |
| `src/lib/sectionMirrorResolver.ts` | Resolves a mirrored section to (source section, target record) for rendering |
| `src/lib/sectionMirrorExpand.ts` | Container-expansion helpers for section_mirror fields; includes `resolveSectionMirrorFieldMulti` for multi-project comparison and the shared `computeEditAndSync` helper used by both single- and multi-target resolvers |
| `src/pages/Records/components/SectionMirrorComparison.tsx` | Flipped comparison table (projects = rows, fields = columns) with editable cells, sync-back to source records, per-cell badges, view selector, and empty-states. Renders when a section_mirror's sibling lookup is multi with ≥2 linked records |
| `src/pages/Records/components/ResearchViewEditor.tsx` | Modal to create/edit a research comparison view: drag-reorderable field picker (columns) + project checklist (rows) + labels/shared/default toggles |
| `src/components/ui/FieldPicker.tsx` | Shared checklist component (lifted out of `FieldEditor.tsx`) used by both the Builder's section_mirror config and the research view editor |
| `src/pages/Records/components/NotesField.tsx` | Append-only notes composer + entry list. Composer uses `AutoGrowTextarea` so it grows as the user types a long note |
| `src/pages/Records/components/AutoGrowTextarea.tsx` | Drop-in `<textarea>` that auto-grows to fit its content (no inner scrollbar). Re-fits on input, on controlled-value change, and on width change (ResizeObserver). `resize:none` + `overflow:hidden`; `rows` attr = minimum height; adds border width to `scrollHeight` for `box-sizing:border-box`. Used by `DynamicField` (textarea), `NotesField` (composer), `TableField` (textarea column) |
| `src/pages/Records/components/RangeField.tsx` | Dual min/max inputs + `formatRangeValue` helper |
| `src/pages/Records/components/TableField.tsx` | Editable sub-table for the `table` field type; per-column cell inputs (textarea columns use `AutoGrowTextarea` so rows grow with content) |
| `src/pages/Records/components/LookupCombobox.tsx` | Lookup picker |
| `src/pages/Records/components/MultiSelect.tsx` | Multi-select chips (accepts `compact` prop for table inline-edit use) |
| `src/pages/Records/components/DropdownSelect.tsx` | Single-select dropdown (accepts `compact` prop for table inline-edit use) |
| `src/pages/Records/components/ImportModal.tsx` | Excel import wizard |
| `src/pages/Records/components/BulkEditModal.tsx` | Bulk-edit modal: per-field opt-in + `DynamicField` inputs, confirmation, per-record `saveRecord` fan-out |
| `src/components/ResearchPromptModal.tsx` | Global research-link prompt; consumes `pendingResearchPromptTargetedIds` queue and renders one chooser row per queued Targeted Projects record |
| `src/pages/Records/components/ViewSelector.tsx` | Saved-view dropdown above the table |
| `src/pages/Records/components/ViewEditor.tsx` | Modal for creating/editing a saved view (columns, sort, filters, sharing) |
| `src/pages/Records/components/AdvancedFilterPanel.tsx` | Collapsible faceted filter panel above the toolbar |
| `src/pages/Records/components/FieldFilterPopover.tsx` | Type-aware popover rendered by a filter chip |
| `src/lib/adhocFilterUtils.ts` | Ad-hoc filter shape, evaluator, localStorage helpers |
| `src/pages/Records/components/ResearchPDFTemplate.tsx` | Research PDF comparison-table layout (landscape when ≥2 projects) |
| `src/lib/pdfGenerator.ts` | Walks the research model's fields dynamically, resolves per-project values via mirror + section_mirror, renders to A4 via html2canvas |
| `src/lib/schemaMigrations.ts` | `SCHEMA_VERSION`, per-version migrations, and always-run heals — including `healResearchMultiProject` (flips `project_name.is_multi` + wraps scalar values) |
| `src/hooks/usePermission.ts` | Permission checks for CRUD actions |
| `src/lib/dashboardUtils.ts` | `applyConditions` — reused to evaluate view filter conditions against records |
| `src/stores/appStore.ts` | `records`, `views` state, `saveRecord` / `deleteRecord` / `saveView` / `deleteView` / `setDefaultView` |
| `supabase/schema.sql` | `model_views` table + RLS + unique-default-per-user index |

## Open questions / known limitations
- No record history / audit log. (The unified activity log captures CRUD events with diffs but not field-level versioning. The `records.version` column added in Phase F.2 is for optimistic concurrency, not history — it tracks the current generation, not the past.)
- Bulk edit is live as of 2026-04-20 — mass-assigning field values across selected records. Bulk delete / bulk export have been live since 2026-04-18. Known caveat: bulk edit runs `saveRecord` per record, so for very large selections (hundreds+ of records) the apply step is a long burst of writes (but each is cheap — localStorage + async Supabase upsert + workflow kick). No progress UI yet — the confirmation dismisses and the toast appears once all writes complete. Each per-record save now also pays the `record_save` RPC's version-check round-trip (Phase F.2); if another user edited any selected record mid-bulk, that single record fails with the version-mismatch toast and the rest still apply.
- Non-image file previews go through `FilePreviewModal` (PDFs, video, audio, and a download card for other types) for the new `file` / `multi_file` / `attachment` field types. Other inline file rendering surfaces (e.g. raw URL fields holding a PDF link) are still no-op — click opens in a new tab.
- Saved views apply to the **table** view only; card view still uses `card_config`. The Advanced Filter panel is likewise table-only.
- There's no "duplicate view" action yet — users have to recreate from scratch if they want a near-copy.
- Ad-hoc faceted filters are localStorage-only and don't sync across browsers/devices. There's no one-click "promote these chips into the view's saved conditions" because the view's condition model is AND-only and can't express the multi-value OR semantics of a chip pick.
- Filter chips are currently committed live on every popover change; there's no Apply/Cancel — accidental selections require a manual Clear.
