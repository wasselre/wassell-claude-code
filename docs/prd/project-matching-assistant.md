# PRD: Project Finder (deterministic project matching, boundary-verified)

**Status:** Live — the deterministic **Project Finder** is the active product surface. The broad conversational assistants are **unwired** (see "Direction narrowed" below).
**Last updated:** 2026-07-19 (**Softer budget/bedroom filters + must-have amenities.** Three matching-semantics fixes from a live miss (a 6BR client's غرفة سائق+ملحق villas never surfaced): (1) **Soft budget floor** — a candidate priced up to 10% BELOW `budget_min` now surfaces (`BUDGET_FLOOR_TOLERANCE` 0.9): the market DB pre-filter receives the softened floor and the budget subscore gives 0.9 credit in that band (under-budget is a good problem; far-below stays 0.2 "wrong segment"). (2) **Bedrooms are a MINIMUM, not exact** — the market RPC filters `bedrooms >= p_bedrooms` (was `=`; migration `2026-07-19_finder_bedrooms_at_least.sql`, applied to prod) and the bedrooms subscore gives full credit when any unit offers ≥ the requested count (a 7BR villa satisfies a 6BR request; one-under = 0.6 near-miss), matching the UI's `bedroomsMin` refine which always treated it as a minimum. (3) **Must-have amenities** — `MatchRequirements.required_amenities`: a HARD gate (`passesRequiredAmenities`) dropping any candidate (all three sources) that doesn't fuzzy-match ALL entries against its amenity evidence (Aqar feature tags for listings, `preferred_amenities` for projects); fails CLOSED on no-amenity-data, and a core `notes` entry reports how many candidates it excluded. Both finder UIs grew an «إلزامية / Must have» checkbox under the amenities picker that promotes the selected amenities to `required_amenities` (UI-only state — never saved to the client record); required amenities also count toward the amenities subscore. The live clients model's `preferred_amenities` options gained ملحق (Annex) + قبو (Basement).) | 2026-07-18 (**Element rules are now BOUNDED, VISIBLE, RESIZABLE areas (user decision — behavior change).** Direction rules («جنوب طريق الملك سلمان») no longer mean an unbounded half-plane: they now mean "on the chosen side of the road AND within `distance_m` of it" — a band. `DirectionalCondition` gained `distance_m` (default 5 km, `DIRECTION_DEFAULT_M`); rules saved before the change compile with the default, so every existing direction rule is bounded too. SQL: `wassell_compile_geo_items` compiles direction rules to geom = the road's distance buffer (ref line simplified + normalized to its longest single linestring) with ref_geom+direction kept for the side test; `wassell_geo_match`'s polygon arm was restructured to LATERAL per-table GiST probes (a UNION ALL candidate subquery was seq-scanned — gate for the جنوب-طريق-الملك-سلمان client: 34,394 ids/11.8s → 3,504 ids/~1.5s). The TS twin (`api/_lib/geoMatch.ts`) applies the same bound. EDITOR: direction rules take a «العمق من الطريق (كم)» input (default 5), and adding ANY element rule auto-opens the map picker so the rep immediately SEES the area. PICKER: element rules are now editable in place — each rule's chip shows its covered-listings count (new `listing_count`/`project_count` per row from `wassell_preview_geo_items`, which also clips direction buffers to the matched side for display) plus a −/+ 0.5km stepper that recompiles and redraws the shape live, and a delete ×; Apply re-emits the resized rules. TOO-BIG GUIDANCE: any rule covering more than the market cap (4,000 listings, mirrors MARKET_SCAN_LIMIT) turns red with a banner naming THAT rule, its exact count, and the instruction to shrink it — replacing the generic "narrow your preferences" dead end. Migrations: `2026-07-18_bounded_direction_rules.sql`, `2026-07-18_preview_direction_display_clip.sql` (+ live-applied `direction_ref_single_line`, `geo_match_lateral_index_scan`) — all applied to prod.) | 2026-07-18 (**Direction-rule finder timeout fixed at the root (perf, live incident).** A client whose only location preference was a direction rule («جنوب طريق الملك سلمان») gated ~34k records and the finder request died at the Edge 25s ceiling — the timeout-recovery panel showed with zero results. Measured: geo match 11.8s + market scan 24.7-37s. Three DB-side fixes (no app code): (1) `wassell_compile_geo_items` stores a SIMPLIFIED direction reference line (ST_SimplifyPreserveTopology 0.001° ≈ 111m; 362-vertex King Salman Rd → 60) — the per-point side-of-road test is O(ref vertices) × ~43k candidate points. (2) `wassell_geo_match` gained an EXACT-semantics bbox shortcut: a candidate beyond the ref line's bbox on the requested side is definitely a match (the closest point on the line lies within its bbox), so only candidates inside the road's lat/lng band run ST_ClosestPoint — geo match 11.8s → ~4.5s. (3) `wassell_market_candidates_json`: the p_record_ids branch evaluated `id = any(34k array)` LINEARLY per row AND detoasted each fat listing jsonb just to count — `listing_points` (the trigger-maintained geo side table) now carries typed `price`/`bedrooms`/`type_text`/`unit_age` columns (backfilled; `_sync_geo_point` keeps them fresh), and for unrestricted-scope users the geo-gate branch counts entirely over those typed columns, touching `records` only for the ≤ p_limit returned rows; filtered-scope users keep the legacy per-row-RLS path (now with hashed id membership + ids-only counting). Market scan 24.7s → 1.3s, identical totals verified (6,016). End-to-end: the client-scoped finder that 504'd now returns its 11 matches in ~seconds with the honest too-many-market-ads notice. Migrations: `2026-07-18_fast_direction_geo_gate.sql`, `2026-07-18_geo_match_direction_bbox_shortcut.sql`, `2026-07-18_listing_points_typed_filters.sql` — all applied live.) | 2026-07-18 (**Map picker: element-rule areas on the map, editable drawn shapes, district-coverage labels, undo-point (new).** (1) ELEMENT RULES NOW RENDER on the picker map as their COMPILED geometry — the new `wassell_preview_geo_items(p_items)` RPC (migration `2026-07-18_preview_geo_items_rpc.sql`) runs the REAL matcher compiler (`wassell_compile_geo_items`) against a throwaway client id inside one transaction, returns per-item simplified GeoJSON, and deletes the temp rows — so "ضمن 3 كم من X" shows the exact buffer circle, `inside_area` the exact zone, and north/south/east/west-of rules the reference road polyline plus a ~13km shaded band on the included side (the match itself stays unbounded; the band only shows direction). Terracotta = include, red = exclude, labeled with the same `describeLocationItem` chip text; display-only (rules edit from the chips list); a preview failure only costs the overlays. (2) DRAWN SHAPES ARE EDITABLE: outside draw mode every drawn area renders `editable` — drag a vertex or midpoint handle to stretch/shrink; right-click a vertex deletes it (below 3 points deletes the shape); edits debounce (500ms) back into the working `location_items` with recomputed labels. (3) DRAWN-AREA LABELS NAME THE DISTRICTS THEY COVER — "منطقة مرسومة: النرجس، العارض +2" (client-side ray-cast against the loaded district rings: district centroid inside the shape OR a shape vertex inside the district), falling back to the old numbered label when the shape covers no district; labels recompute as borders are edited. (4) RIGHT-CLICK A DISTRICT copies its official boundary into a new editable drawn shape (decimated to ≤80 vertices for usable handles) and clears that district's selection — "adjust this district's borders". (5) DRAW-MODE UNDO: a تراجع button + right-click removes the last clicked point so one bad segment doesn't force a full redraw. Footer now also counts element rules; hint texts updated.) | 2026-07-18 (**Select-all-filtered bulk selection.** Both client-scoped finder surfaces (the follow-up Suggested-Projects view + the standalone Project Finder page) gained a header «تحديد الكل / Select all» toggle beside «حفظ الخيارات»: one click selects EVERY match passing the CURRENT client-side refinement (score slider + refine hard filters, across all tabs); when all filtered matches are already selected it flips to «إلغاء التحديد / Clear selection» (removes only the filtered ones — filtered-out selections are never touched). Bulk save persists only the filtered∩selected set, so select-all → tighten the slider to 90% → save writes only the ≥90 matches. Badge on the button shows the filtered total; live-verified offline: 7 selected → threshold 90 → exactly the 3 ≥90 options saved.) | 2026-07-17 (**Unit-age (عمر العقار) preference wired end-to-end.** New clients dropdown preferred_max_unit_age (جديد فقط/حتى سنتين/حتى 5/حتى 10 — STRING values 0/2/5/10; live field added by migration 2026-07-17_unit_age_preference.sql, seedModels mirrored). Mapper: draftToMatchRequirements → requirements.max_unit_age (0 is meaningful). ENGINE: new unit_age scoring dimension (weight 6, renormalized like the rest): age ≤ max → 1, ≤ max+2 → 0.5, else 0; unknown age = requested-but-missing honesty penalty; CATALOG projects (our/all) are stamped unit_age 0 (new-development stock by definition — never penalized as unknown). Aqar free-text age parses via wassell_parse_unit_age (SQL, IMMUTABLE) ⇄ parseUnitAgeText (TS twin in matchAgent, tests): جديد→0, سنتين/سنتان→2, N سنوات/bare digits→N, أكثر من N→N+1, digit-less سنة forms→1; 99.93% live coverage (28/38k unparsed). MARKET DB pre-filter: wassell_market_candidates_json gained p_max_age (old signature DROPPED first — new param would otherwise create an ambiguous overload) — drops only listings whose PARSED age is known and over the max, null ages survive to scoring; age added to MARKET_SLIM_FIELDS (without it the slim projection would hide the field from the adapter). UI: field editable in both finder pref panels (EDIT_SLUGS/FILTER_SLUGS), chips in the finder prefs row (assistantContext) + chat-header prefs (prefChips), and market cards show an العمر chip (facts.unit_age; catalog cards skip the redundant implicit-new chip). Options snapshots inherit it via facts.) | 2026-07-13 (**Map picker: trackpad draw fix + on-map district names + landmark pins.** (1) DRAW CLICK FIX: with panning on, a trackpad's few-pixel wobble between mousedown/mouseup read as a DRAG, so the map panned and the vertex click never fired ('I click but it just moves around' — live report). Draw mode now pins the map (draggable:false, restored on exit; zoom controls still work) and the hint says so. (2) CLEAR BORDERS + NAMES: unselected district strokes bumped (0.35/1px → 0.65/1.5px, selected 2.5px), and every district shows its NAME on the map — transparent-icon markers at each polygon's largest-ring centroid, zoom-gated (≥11) so the city view stays clean. (3) LANDMARKS: curated geo_elements (landmarks/malls/universities/airports_transport, ~182 verified Riyadh anchors, direct RLS-scoped select) render as terracotta pins from zoom ≥11 with hover tooltips; names label in at zoom ≥13; pins turn unclickable during draw so vertex clicks pass through. All in DistrictMapPicker.tsx; landmark load failure costs only the pins (console.error, never the picker).) | 2026-07-13 (**Exclusion zones in the map picker.** Draw mode gained a polarity toggle («أريدها» gold / «استثناء» red; the preview polyline recolors mid-draft): an exclude-polarity shape becomes a drawn_area item with polarity exclude — no results inside it — labelled «منطقة مستثناة N» and rendered red on the map. No engine change needed (compile + the exclude-subtraction path already handled polygon geoms); verified live on prod: an exclusion box covering all 4 of a client's districts dropped the geo gate 476→0, and a box covering none of them left it at 476.) | 2026-07-13 (**Free drawing on the map picker (drawn_area location items).** The map picker gained a **«رسم منطقة» / Draw an area** toggle — MANUAL drawing (map click = add a vertex to a gold preview polyline, double-click or «إنهاء الشكل» closes the shape; Google REMOVED DrawingManager in Maps JS v3.65 — instantiating it throws and blanked the app on the first deploy of this feature, caught in the live smoke-test; do NOT re-add the drawing library). The rep can draw SEVERAL separate shapes — each completed shape becomes ONE independent location_items entry of the new kind **drawn_area** ({id, kind:'drawn_area', polarity, coordinates: closed [lng,lat] ring, label}), OR-unioned exactly like districts. Drawn shapes render gold on the picker (red if a saved exclude), are deletable via footer chips, and existing ones load for review; Apply re-emits the drawn set alongside the district selection (element rules untouched). Compile support in wassell_compile_geo_items (migration 2026-07-13_drawn_area_location_items.sql, applied to prod): the ring compiles via ST_GeomFromGeoJSON→ST_MakeValid to a plain polygon in client_pref_geometry — matched on the SAME GiST-indexed ST_Contains path as districts (fast; verified live: a probe box near النرجس compiled ok and grew هيثم's gate 476→1,549); malformed rings compile to needs_review, never dropped. TS twin api/_lib/geoMatch.ts gained the same branch + 3 tests; frontend shape + chip labels in src/lib/geo/locationItems.ts (newDrawnAreaItem, describeLocationItem → «منطقة مرسومة N») and the chat pref-chips reader. Known v1 edge: a client whose ONLY criterion is a drawn area (no city/type/budget) still hits the needs_preferences short-circuit — in practice a city is always set.) | 2026-07-13 (**Map-based district picker (tap-to-select) in the location editor.** The `location_items` editor (`LocationItemsEditor`, embedded in `ClientLocationField` — client form, finder edit-preferences panel, standalone finder) gained a third add-control: **«اختيار من الخريطة» / Pick on map** (enabled once a city is selected). It opens `src/components/DistrictMapPicker.tsx` — a full-screen modal Google Map (branded `WASSEL_MAP_STYLE`) rendering EVERY district of the selected city as its REAL boundary polygon via the new `wassell_city_district_shapes(p_city_id)` RPC (`supabase/migrations/2026-07-13_city_district_shapes_rpc.sql`, applied to prod): SECURITY DEFINER, returns `[{district_id, name, geojson}]` from `district_boundaries` simplified server-side (ST_SimplifyPreserveTopology 0.0002, 5-decimal coords — Riyadh's 189 districts ≈ 145 kB in one call). These are the SAME polygons the finder's point-in-polygon verification matches against, so what the rep taps is exactly what matches. Tap toggles the district as an INCLUDE rule (copper fill = selected, charcoal outline = unselected, red = saved EXCLUDE rules — display-only on the map, managed from the chips); hover shows the district name; footer shows the live selected count + names; **تم / Apply** rebuilds ONLY the include-district items in `location_items` (element rules + excludes untouched, existing rules keep their ids). Free-shape drawing (multiple independent polygons) is the planned next phase.) | 2026-07-13 (**Market-scan fast path + slim payload + concurrent fetch (~17s → ~8s three-source search).** Follow-up to the same-day index fix below — the per-row RLS call was the next bottleneck and is now gone for unrestricted users. (1) New `wassell_view_scope_class(uid, model)` → `'none'|'all'|'filtered'`, computed ONCE per statement: when a user's market_listings view scope is unrestricted (admin / no scope rule / mode `all` / zero conditions — every live profile with market access today), `wassell_market_candidates_json` provably doesn't need the per-row `wassell_can_view_record` call and SKIPS it (CASE-guarded); `'filtered'` keeps the exact per-row semantics; `'none'` returns zero rows. Verified equivalent on prod: same 6,345-row set + identical id fingerprint vs per-row RLS for admin, full-view non-admin, no-permission, and anon. (2) The RPC gained `p_fields text[]` (default NULL = full data, back-compat): the engine passes `MARKET_SLIM_FIELDS` (the ~21 keys the adapter/scorer actually reads) so each row's data is projected via a single `jsonb_each` pass — payload 15MB → 5.4MB (do NOT "simplify" to per-key `->` extraction: it re-detoasts the blob per key and measured SLOWER than full data, 9.9s vs 2.6s). RPC in-DB time for the 3-district admin scan: ~6.7s → **~2.6s**. (3) `matchProjectsCore` now STARTS the market fetch right after district resolution (never-rejecting promise, consumed by PASS 3), so its DB time overlaps the polygon-verification + name-map queries instead of running after them. (4) too_many short-circuit: when `total > p_limit` the RPC returns `rows:[]` instead of building + shipping a 4000-row agg the engine discards anyway. (5) The same fast path for whole-model reads: new `wassell_model_records_json(model_id, fields?)` replaces the finder's paged per-row-RLS `unified_records` reads for all_projects/our_projects (`loadModelRecords` in matchAgent, paged fallback kept) — 980 all_projects rows dropped ~6.4s → ~0.2-1.3s, identical row set verified by id fingerprint. Live end-to-end on app.wassel.re for سراج الجهني (3 districts × 3 sources): no-criteria/too_many ~16.6s → ~6s; the criteria-rich 519-result search ~17s → ~9.3-11.9s (avg ~10.4s; commit 27b17f5 also overlapped the ~2s geo-gate compile/match with the engine loads — the endpoint passes the gate as a PROMISE through FinderOptions/MatchCoreOptions.geoMatchIds and the core awaits it at first use. The remaining floor is the in-district JSONB detoast of 6,345 rows ~3.7s in-DB — VALUES-lateral and LATERAL-OFFSET-0 pre-extraction both measured no better; a further cut needs flat/generated columns, deliberately not done). Migration `2026-07-13_market_candidates_fast_path.sql` (applied live); `wassell_view_scope_class` also in `schema.sql` with a MUST-mirror warning next to the RLS helpers it classifies. Perf-testing gotcha: do NOT project jsonb keys via unnest(fields) + per-key `->` — it re-detoasts the blob per key and measured SLOWER (9.9s) than returning full data; jsonb_each in ONE pass is the right shape.) | 2026-07-13 (**Save-options-first exit guard.** The bulk **حفظ الخيارات / Save options** button moved from the results footer to the TOP header of every client-scoped finder surface (`SuggestedProjectsView` header — primary copper button with a live selected-count badge, next to a now-secondary **تم / Done** — and the standalone `ProjectFinderPage` header when a client is selected). The old footer bulk-save button is gone; the footer keeps the selected count plus an amber "لم يُحفظ أي خيار لهذا العميل بعد" hint until something is saved. **Leaving a client-scoped finder without having saved a single option this visit now asks for confirmation** — "هل أنت متأكد أنك لن تحفظ أي خيار لهذا العميل؟" via the shared `src/components/matching/LeaveWithoutSavingModal.tsx` (Stay is the primary action; "الخروج بدون حفظ" proceeds). The guard intercepts the Done button and Esc in `SuggestedProjectsView`, clearing the selected client on `ProjectFinderPage`, and arms a `beforeunload` browser prompt for tab close/reload. It arms only AFTER results were actually shown (an empty/failed search never nags) and disarms on ANY successful option write this visit — single save, bulk save, inline status set, eliminate, or reactivate.) | 2026-07-13 (**Finder failure honesty + market-scan index (live incident: 504 rendered as "no matching projects").** A search for a client with district `location_items` (سراج الجهني: النرجس/العارض/القيروان) consistently 504'd — `wassell_market_candidates_json` full-scanned ~51k market rows on `(data->'location'->>'district')` (no index, ~4.5s JSONB detoast) plus the per-row `wassell_can_view_record` RLS check (~4–8s), measuring ~30s against `/api/project-finder`'s 25s function ceiling. The SPA then LIED about the failure: on HTTP/2 `res.statusText` is the empty string, so `fetchProjectFinder`'s non-JSON-body fallback threw an `Error('')`, `SuggestedProjectsView`'s `error` state stayed falsy, and the UI fell through to the **"لا توجد مشاريع مطابقة" empty state** — while the catalog actually held 14 exact-district matches (5 scoring 100). Fixes: (1) `fetchProjectFinder` uses `||` (not `??`) so a blank error message never masquerades as success — a failed request now shows the red error box; (2) new DB index `idx_records_model_district` on `records (model_id, (data->'location'->>'district'))` (applied live with CONCURRENTLY; migration `2026-07-13_records_model_district_index.sql`) — the market RPC drops ~30s → ~2s and the full three-source search responds ~17s with 516 exact-district results. Known follow-up: ~17s is still near the 25s ceiling; the per-row RLS call in the market RPC is the next bottleneck.) | 2026-07-04 (**"إرسال للعميل / Send to client" on every finder + option card (QA-002), collapsible preferences area (QA-001).** (1) Every Project Finder result card (`FinderCard`, client-scoped surfaces only — hidden with `hideClientActions`) and every Client Options card (projects + market listings) now carries a copper **Send to client** action that WhatsApps that project/listing to the CONNECTED client without opening the record: a prepared `chat_templates` message is reused (project → `project_id` link via the re-wired `ProjectWhatsAppFlow`; listing → `listing_id` + `status:'ready'` via the new `src/components/matching/ListingWhatsAppFlow.tsx`), and when none exists the EXISTING creation flow runs right from the card (project: deterministic `resolveProjectFacts`→`composeProjectMessage` preview→save; listing: `ListingMessageModal` AI text + text-removed photos → on approve the flow continues straight into the chat). The chat opens as `StartChatModal` with the client preselected + body prefilled; the template's gallery/cleaned photos ride along as separate messages. **Video support:** `sendProjectImageMessages` now sends each ride-along by MIME (`video` for video bytes, extension fallback for typeless servers), and direct video-FILE URLs on the source (`all_projects.project_videos`, listing `video_urls`) are appended via the new `directVideoUrls` helper — HLS streams (Aqar/Cloudflare `.m3u8`) and page links are excluded (not uploadable to WhatsApp). Shared pure helpers + tests: `src/lib/matching/sendToClient.ts` (`findProjectTemplate` / `findReadyListingTemplate` / `templateListingImageUrls` / `templateBody` / `directVideoUrls`), `__tests__/sendToClient.test.ts`. `ProjectWhatsAppFlow` is UN-orphaned. (2) **QA-001:** the finder's always-visible preferences-chips + missing-warnings block is now COLLAPSIBLE (chevron toggle; collapsed = one thin row with count + truncated value summary + missing-count badge); `SuggestedProjectsView` gained `defaultPrefsCollapsed`, set by the Client-Options popup host (`ClientOptionsModal`) so popup results get the vertical space — full-page hosts stay expanded. The edit-preferences panel already toggled + auto-collapsed on Search (2026-07-02). See [clients.md](clients.md), [chats.md](chats.md).) | 2026-07-04 (**Saved market-listing option cards show the quality grade even when saved before the feature (fix).** The A–D `QualityBadge` on a Client Options market-listing card read ONLY the snapshot (`facts.quality_grade`), which was captured at save time — but the finder only started writing quality into `facts` on 2026-07-02, so every option saved before then (all 252 live market-listing options at the time, saved ~2026-07-01) showed a BLANK chip. `ClientOptionsTab` now falls back to the LIVE listing record in the slim `market_listings` store (which the DB trigger keeps `quality_grade`/`quality_score` on for every row, both riding the slim store) via a new `qualityBySourceId` map — the exact same "snapshot-first, live-store fallback" pattern already used for the ad-id chip. New saves keep using their snapshot; pre-feature options now render the current grade; a listing dropped from the catalogue simply shows no chip. Verified live: 252/252 pre-feature options had a null snapshot grade but a real live grade in the 47k-row store. `src/pages/Clients/components/tabs/ClientOptionsTab.tsx`. See [clients.md](clients.md).) | 2026-07-02 (**Listing quality in the finder.** Every market_listings result now carries the DB-computed `quality_score`/`quality_grade` in its `facts` (adapter → scoreProject facts → card → saved-option snapshot), rendered as the shared colored A–D chip (`src/components/market/QualityBadge.tsx`) on finder cards (both surfaces) and on saved market-listing option cards (`ClientOptionsTab`, incl. the manual `AddOptionModal` snapshot). Ranking: quality is an EQUAL-match-score TIEBREAK ONLY — server `byBandThenScore` and the client default score sort (`finderRefine.sortItems`) append `quality_score` as the final key; relevance always dominates, no minimum-grade filter, no change to scoreProject or the too_many guard. Items without a quality score (projects/units) tie-break as 0.) | 2026-07-02 (**Location matching + grouping fixes (3 bugs from field screenshots).** (1) *Wrong / missed "same district" matches:* the SPA now sends the client's selected districts as AUTHORITATIVE record ids (`requirements.district_ids`, built in `draftToMatchRequirements` from the location cascade + `preferred_districts` + `location_items` include-rules); the engine matches on those ids directly and runs the fuzzy per-name ILIKE resolution ONLY when no id resolved — so a same-named district in another city can no longer be admitted as "في نفس الحي", nor can a selected district silently mis-resolve and mark its own projects "outside". Multiple selected districts stay OR — inside ANY one = exact. (2) *Grouping is now purely location-centric:* `finderGroupFor` groups by the verified `location_tier` alone; the old "band `partial` → broader_fallback" override is GONE, so a wrong-unit-type or no-price project INSIDE a requested district stays under "في نفس الحي" (its weak fit still shows via the band badge + in-group ordering) instead of being falsely labelled "خارج المنطقة المطلوبة". (3) *Distance everywhere, measured to the nearest selected reference:* `scoreProject` computes `distance_km` for EVERY tier (same-city + fallback included), to the NEAREST of ALL selected references — every requested district centroid PLUS the client's selected location ELEMENTS (`location_items` element anchors — within_radius / within_distance / inside_area — resolved endpoint-side from `geo_elements` and passed as `FinderOptions.refPoints`) — and names WHICH one via the new `nearest_ref_name` field (on `ScoredProject` → `MatchResultItem` → `FinderMatch`). The card distance pill now reads "~4.2 كم من النرجس", and the deterministic nearby/same-city/fallback explanations name the closest selected area (fallback: "خارج الأحياء المحددة (يبعد حوالي X كم عن «Y»)"). Tests in `projectFinder.test.ts` + `requirements.test.ts`.) | 2026-07-02 (**Multi-select preferences now match as OR (any acceptable option counts).** When a client selects MULTIPLE acceptable options for one preference, they are alternatives — a result matching ANY of them now gets full credit for that dimension. (1) **Unit types:** `preferred_unit_type` is a multiselect but only the FIRST value was sent — a client wanting "شقة أو دور" saw every دور project scored `type: 0` and band-capped into the fallback tab. `draftToMatchRequirements` now sends ALL selected types as `requirements.property_types[]` (`property_type` stays = the first for single-type paths); the engine (`MatchRequirements.property_types`, `scoreProject`) unions the synonym expansion across all requested types and a project offering ANY of them earns full type credit — the wrong-type band cap fires only when NONE match. The market-listings DB pre-filter unions the ILIKE terms across all types too (previously only the first type's listings ever became candidates), and the `match_projects` AI-agent tool + the finder's LLM text parser can now express type alternatives (`property_types`). (2) **Cities:** the `location` cascade allows multiple cities but only the first was sent; `cities[]` now flows through and `matchProjectsCore` resolves EACH into the city-match set (`reqCityIds`), so a project in ANY acceptable city city-matches. (3) **Districts** already had OR semantics (`districts[]`, 2026-06-29) — unchanged. Deliberately NOT changed: `preferred_amenities` keeps proportional credit (amenities are cumulative wants, not alternatives — a client wanting "غرفة خادمة ومسبح" wants both). No scoring weights, bands, or tiers changed. Tests: `requirements.test.ts`, `projectMatchScoring.test.ts`.) | 2026-07-02 (**Finder embedded in the Client Options popup (new surface).** The shared `SuggestedProjectsView` gained a FOURTH host: the new `ClientOptionsModal` (`src/components/clients/ClientOptionsModal.tsx`) — the client-options popup opened from the WhatsApp chat header — embeds the finder inside the popup itself ("البحث عن خيارات أكثر / Find more options" swaps the popup body to the finder, `followupId=null`, seeded + geo-gated from the client; "تم / Done" returns to the options list). **Same-day fix (live report):** embedded in the popup on a laptop-height screen, the finder's open edit-preferences panel — part of the fixed chrome — consumed the whole height, leaving the results area 0px with nothing scrollable; the panel is now height-capped (max-h-[42vh], own scrollbar) and auto-collapses when Search is pressed so results are visible immediately (benefits the full-page finder on short viewports too), and the popup uses near-full-screen height. `ClientOptionsTab` also gained an `onFindMore` prop, so the Client 360 Options tab now links to the full-page client finder (`/model/clients/:id/projects`) too. See [chats.md](chats.md), [clients.md](clients.md).) | 2026-07-02 (**Market-listing finder cards: "التواصل مع المعلن / Contact advertiser" button.** Every `market_listings` result card (both finder surfaces — standalone `/project-finder` AND the Follow-up/client `SuggestedProjectsView`, including the standalone no-client mode) now carries a compact copper **Contact advertiser** action next to Details: if the listing already has a scraped `advertiser_phone` it opens the in-app WhatsApp `StartChatModal` immediately (pre-filled with the Aqar listing link); otherwise it enqueues the REGA advertiser lookup (`/api/rega-lookup` → Browserbase worker), shows a "جاري جلب الرقم…" spinner, and auto-opens the chat when the worker's write lands via Realtime — after which every later click opens the chat directly (the phone + `advertiser` link persist on the listing). Failures surface as an error toast. Shared component `src/components/market/ContactAdvertiserButton.tsx` + shared hook `src/lib/regaLookup/useContactAdvertiser.ts` (extracted from `ContactAdvertiserPanel`); reads the listing from the market_listings slim store by `project_id`, so no extra fetch per card. Same button also renders on the client Options tab's market-listing cards — see [clients.md](clients.md), [record-management.md](record-management.md).) | 2026-07-01 (**Project Finder is now reachable from the CLIENT profile.** Per the user, the finder can be run directly on a client. The Client 360 header (`DetailHeader`) gained a prominent copper **"الباحث عن المشاريع / Find projects"** button that navigates to a new full-page route `/model/clients/:recordId/projects` (`ClientProjectsPage`) — a thin wrapper (mirroring the follow-up `SuggestedProjectsPage`) that loads the client, seeds `prefDraft` from the saved client data, and renders the SAME shared `SuggestedProjectsView` with `followupId=null`. So the client-scoped finder has the full feature set — map/list view, refinement toolbar, edit-preferences + re-run, and saving into the client's unified `client_property_options` — geo-gated by the client (client_id + their saved `location_items`); **"تم / Done"** returns to the client profile. `SuggestedProjectsView` already tolerates a null `followupId` (the finder-handoff write is guarded; audit/save attach via `clientRec.id`). Route precedes the generic `:recordId` detail route (like the follow-up one). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Standalone Project Finder: pick a client → seeded, geo-gated search + full client-option actions; unified location editor.** The sidebar `/project-finder` tool reached parity with the Follow-up finder. (1) A **client picker** (optional, top of the filters panel) searches the `clients` records by name/phone/code; selecting one SEEDS the pickers from that client's saved preferences (`location` + `location_items`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`, `preferred_bedrooms`→bedrooms), passes `client_id` + the current `location_items` to `/api/project-finder` (so the PostGIS geo gate compiles + narrows for that client), and turns EVERY result card into a full client-option control — inline status selector (`onSetStatus`), bulk-select + "Save selected to client options" footer, and the eliminate-with-reason modal — writing into that client's `client_property_options` (`added_from:'project_finder'`). A "clear client" X returns to pure discovery (cards show Details only). (2) The `location` field now renders the **unified `ClientLocationField`** (district include/exclude chips + geo-element rules with "+ add district / + add element") instead of the bare cascade — threaded via `modelId`/`onPatch` into `DynamicField`, writing both `location` and `location_items` into the draft. Without a selected client, district include-rules still tier results (via the `requirements.districts` mirror) but geo-element rules don't gate (the server geo gate needs a `client_id`). All in `ProjectFinderPage.tsx`; handlers mirror `SuggestedProjectsView`. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Finder results now have a MAP view (list ⇄ map toggle).** Per the user — "when presenting listings give me an option for a map view." Both finder surfaces (standalone `ProjectFinderPage` + Follow-up `SuggestedProjectsView`) gained a **List / Map** segmented toggle in the shared `FinderRefinementBar`. In map mode the active tab's results — the pinned OUR-projects **plus** that tab's other matches — render on the branded `WASSEL_MAP_STYLE` Google basemap as pins colored by SOURCE (green = our projects, chocolate = market, charcoal = listings DB), clustered by density (`MarkerClusterer` + `buildClusterIcon`), auto-fit to the pins. Clicking a pin opens an InfoWindow summary (source · score · name · district/city · distance · price) with a **"التفاصيل / Details"** button that runs the same `onOpenDetails` as a card. New component `src/pages/Followups/components/FinderMapView.tsx` (shared, read-only — client-option actions stay on the list). Coordinates now flow from the engine: `scoreProject` puts `facts.latitude`/`facts.longitude` (the resolved pin — verified polygon centroid when geo-verification ran, else the stored lat/lng) in `matchAgent.ts`; the finder endpoint passes `facts` through unchanged. A legend + coverage note ("N on the map · M without coordinates") sits under the map; the toggle only appears once results exist. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder cards: ad-ID chip + inline client-option status selector; district include-rules now land results in the exact-district tier.** (1) Market-listing cards now show a small ad-id chip (the external Aqar ad number, for looking the ad up) — reformatted to "@<external_id>" on 2026-07-04, when the id also reached the finder MAP pin hover titles, the Client Options tab cards, and the manual add-option picker (see clients.md) — `scoreProject` puts `facts.external_id`, `adaptListingToScorable` carries it through. (2) On the Follow-up finder EVERY card gained an inline STATUS `<select>` (main focus / reserved / interested / presented / suitable / not interested / closed / eliminated) that ENSURES the client option exists (creating it if new) THEN applies the chosen status in one step — `main_focus` routes through `setMainOption` (single active main), `eliminated` opens the reason-notes modal, and any non-eliminated pick reactivates a previously-eliminated option. This REPLACES the old per-card Save-as-option / Eliminate / Reactivate buttons (`StatusSelect` in `FinderCard.tsx`, colored to the status badge, RTL-safe native select; `onSetStatus` in `SuggestedProjectsView.tsx`). Bulk-select checkbox + footer bulk-save are unchanged; the standalone Project Finder (no client) still shows Details only. (3) A district picked in the unified location box is stored as a `location_items` district INCLUDE-rule (it drives the PostGIS geo gate), NOT the `location` cascade — so `draftToMatchRequirements` now mirrors those include-rule district ids (with the label stashed on the item as a name fallback) into `requirements.districts`; without it every gated result fell to the "same city / different district" tier even though it sat inside the requested district. Test in `requirements.test.ts`. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder result tabs: 3 tabs (Same district / Nearby / Alternatives) + our-projects pinned to the top of EVERY tab.** Per the user: the four engine tiers now collapse to THREE display tabs — "Same city" is dropped (the whole set is already the same city) and its items fold into **Alternatives** (= same_city + broader_fallback) — and OUR portfolio (source `our_projects`, ≥70) is lifted out of the tiers into a pinned "مشاريعنا / Our Projects" list shown at the TOP of every tab, best-first (score desc → closest), followed by that tab's other matches under a "خيارات أخرى / Other options" divider. Pure client-side display: shared `buildFinderTabs` + `DISPLAY_TAB_KEYS`/`DISPLAY_TAB_LABELS` in `finderRefine.ts`, wired into BOTH `ProjectFinderPage` and `SuggestedProjectsView`; tab badge = pinned-ours + that tab's tier count; the tier list stays windowed/infinite-scrolled while our-projects always render in full. The engine's four-group response is unchanged. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Geo gate ~5× faster + un-truncated (was 7s/capped-at-1000).** Follow-up to the timeout band-aid below. (1) PERF: `wassell_geo_match` was a 7.05s seq-scan of all ~47.6k project+listing points with a per-row geometry fn. Rewrote it to use the GiST point indexes — polygon/buffer items (within_radius/inside_area/district, `geom NOT NULL`) via an `ST_Contains` spatial JOIN (**~38ms**, was ~7s), direction items (halfplane, `geom NULL`) via a GiST bbox pre-filter that drops the far side (27,019/47,580 for the north-of rule) then the exact `wassell_geo_dir_match` (**~1.5s**, was 7s). Verified identical record-id sets on prod before the swap (direction client 8046=8046; synthetic 3km polygon 349=349). Migration `2026-07-01_geo_match_index_optimized.sql`. (2) CORRECTNESS: the endpoint's row-returning `wassell_geo_match` call was capped at PostgREST's ~1000-row response (the area can be 8046), silently truncating the gate to 1000 AND non-deterministically (market count flickered 329↔489). Added `ORDER BY record_id` + a new `wassell_geo_match_ids(uuid)→uuid[]` (one row, no cap, scan runs once); `api/project-finder.ts` calls it instead. Live element-only finder: **8.7s → 3.4s** with the FULL 8046-record gate. Follow-up: clip direction halfplanes to the region polygon at compile time to put them on the index path too. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Geo gate was silently timing out for authenticated users — the element never narrowed anything.** Sibling fix to the market-AREA change below: even after it shipped, the element-only client STILL saw `geo_filter:null` + `needs_district`. Root cause: `wassell_geo_match` scans all ~46.6k project+listing points with a per-row geometry predicate (7.05s EXPLAIN ANALYZE for a Riyadh-wide rule), tripping the `authenticated` role's 8s `statement_timeout`; the endpoint's catch left `geoMatchIds` null. As SECURITY DEFINER fns, a function-level `statement_timeout` overrides the caller's value, so `alter function wassell_geo_match / wassell_compile_client_geo set statement_timeout='20s'` (migration `2026-06-30_geo_match_statement_timeout.sql`, applied to prod) lets the scan finish. Verified live: `geo_filter` now applied, the element-only client sees **489 in-area market listings** (was 0), no district picked (~8.7s; perf follow-up = make polygon items index-usable / clip direction halfplanes to the region). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Market listings now respect a `location_items` AREA — no district pick required.** The user set a geo-element rule ("داخل النخيل مول") but the finder still said "حدّد الحي لعرض إعلانات السوق" (needs_district) and hid market ads — defeating the point of the element. The market scan now scopes by the client's geo-matched record-id set when no district is set: `wassell_geo_match` already returns the in-area LISTING ids (`listing_points` populated, 46,608 rows; النخيل area = 7,904 listings). The market-candidates RPCs gained an optional `p_record_ids uuid[]` (migration `2026-06-30_market_candidates_geo_ids.sql`, applied to prod) that scopes by `r.id = any(p_record_ids)` instead of the district filter — same DB-side budget/bed/type filters, RLS re-check, `ORDER BY r.id`, count-then-paginate `too_many` guard. `matchProjectsCore` market block: scope = district when set, ELSE the `geoGate` area, ELSE `needs_district`. An element-only client now sees market ads (النخيل: 7,904 → 2,595 villas → 911 in 3–4M); a too-broad area still returns `too_many` asking for budget/bedrooms. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder edit-preferences panel now shows + edits the geo-element / district rules (`location_items`).** Bug: the panel rendered the `location` field via a bare `DynamicField`, so the unified `ClientLocationField` (district + "within radius of a landmark" chips with "+ إضافة حي / + إضافة عنصر" controls) never appeared — selected geo-elements weren't shown and there was no way to add one. Fix: thread `modelId` + `recordId` + `onPatch` into that `DynamicField` (the exact trigger that makes it render `ClientLocationField`, same as `PreferenceSummary`), so the box now shows the saved chips + add controls and writes both `location` and `location_items` into `editDraft` (location field spans full width). Because the server geo-gate compiles from the SAVED client (`wassell_compile_client_geo(p_client_id)` reads `clients.data.location_items` at request time), the "بحث / Search" button now **persists the edited prefs (incl. `location_items`) to the client first** (version-safe, only when changed) THEN re-runs the match — so a district/element rule actually narrows the results. The separate "Save to client" button was merged into Search (caption: "تُحفظ التفضيلات للعميل عند البحث"). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md), [client_geo_matching](client_geo_matching).) | 2026-06-30 (**Edit client preferences on the finder page → re-run the search.** The full-page finder (`SuggestedProjectsView`) gained an "تعديل التفضيلات / Edit preferences" header toggle that opens an inline editor of the client's preference fields (those present on the live model: `location` cascade, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_bedrooms`, `preferred_amenities`) via `DynamicField` into a local `editDraft`. "بحث بالتفضيلات الجديدة / Search" rebuilds the SERVER-SIDE requirements (`buildReqs` — also maps `preferred_bedrooms.min` → the matcher's single `bedrooms`) and RE-RUNS `/api/project-finder` (a genuinely more-detailed search, not just the client-side slider/refine). The view's fetch was refactored from a once-on-mount snapshot into a reusable `runSearch(draft)` (abortable; resets slider/refine; re-seeds the 100%-auto-select); preference chips + `ctx` now reflect the last *searched* draft. Each search also updates the workspace hand-off so the edits carry back, and an explicit "حفظ التفضيلات للعميل / Save to client" persists `editDraft` to the client record version-safe (`saveRecord` + `expectedVersion`, same posture as `PreferenceSummary`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Follow-up Suggested Projects is now a FULL PAGE, not a modal.** Per the user — "most of the time we will use it here" and "it still doesn't look good" — the cramped 860px modal was replaced by a full-page route `/model/followups/:recordId/projects` (`SuggestedProjectsPage` → shared `SuggestedProjectsView`). It opens in the SAME tab via client-side nav from the workspace's "المشاريع المقترحة" launcher, stays CONNECTED to the follow-up (its id drives the recommendation audit log + saves into THAT client's `client_property_options`), lays cards out in a responsive 1/2/3-column grid inside a centered max-w-6xl, and carries the same refinement toolbar + images + bulk-save. A prominent **"تم / Done"** button (header + footer, plus Esc) returns the rep to the follow-up record (`navigate('/model/followups/:id')`). Preferences ride a module-singleton hand-off (`src/lib/matching/finderHandoff.ts`) the panel sets at click time (so unsaved edits are honored); on a cold URL load the page falls back to the saved client data, and on return the workspace restores the hand-off draft then clears it. `SuggestedProjectsModal.tsx` was deleted; `SalesAssistantSidePanel` now navigates instead of opening a modal. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Refinement toolbar now in the Follow-up Suggested Projects modal too (shared with the standalone page).** The score slider + sort + hard-filter "Refine" panel were extracted to a shared module (`src/lib/matching/finderRefine.ts` — `SortKey`/`Refine`/`passesRefine`/`sortItems`/`refineGroups`/`FETCH_FLOOR`) + a shared component (`FinderRefinementBar.tsx`), and wired into BOTH `ProjectFinderPage` and `SuggestedProjectsModal` so they behave identically. The modal already fetched everything ≥70; now it filters/sorts that set client-side, its group-tab counts + footer follow the refinement, bulk-save only saves cards still VISIBLE under the active refinement (never slider/refine-hidden ones), and it auto-hops to the first non-empty tab when refining empties the active one. This was the user's primary surface ("most of the time we will use it here"). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder cards now show the listing/project main image.** Every `FinderCard` (standalone Project Finder + Follow-up Suggested Projects modal) renders an `h-40` hero thumbnail at the top. The engine adds a single `facts.image` in `scoreProject`: market listings carry a raw photo URL (`adaptListingToScorable` reads `main_image_url`, else first `image_urls[]`); projects carry a files.id (`main_image`, else first `project_images[]`). The client resolves either through the existing `useSignedImage` hook — http(s) passthrough for the raw market URL, short-lived signed view URL for the project files.id (process-cached). The image block self-hides when there's no image or the load fails (`onError`). No new server signing per result — projects sign lazily client-side only for the ~24 windowed cards. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder — results refinement toolbar (score slider + sort + hard refine filters).** The standalone page now always fetches EVERYTHING scoring ≥ 70 (`FETCH_FLOOR`, no longer a left-panel select) and adds a client-side refinement toolbar above the results that operates on that set with NO re-fetch: (1) a **score slider** 70→100 (step 5) — drag right to raise the minimum and shrink the list, with a live "showing X of Y" count and per-group tab counts that follow; (2) a **sort** dropdown — best match (default), price ↑/↓, area large/small, nearest, most available units, name; (3) a **"Refine" panel** of HARD post-filters to focus a large result set — max price, area min/max, min bedrooms, available-only, coordinate-verified-only (these are real gates, unlike the soft scoring dimensions, so they directly answer "the finder returns far more than the table because area/price are only soft 10–25% nudges"). A fresh search resets the slider to 70 and clears refine. All logic is pure client-side in `ProjectFinderPage.tsx` (`refinedGroups` memo: filter by score≥threshold + `passesRefine`, then `sortItems`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder page — a sidebar tool usable WITHOUT a client.** The deterministic Finder is now reachable on its own at `/project-finder` (sidebar entry "الباحث عن المشاريع / Project Finder", `Compass` icon), registered as a `project_finder` custom page in `src/lib/customPages.ts` with `default_access:'all'` (gateable per-profile in Settings → Profiles, admins always see it). The new page `src/pages/ProjectFinder/ProjectFinderPage.tsx` renders STRUCTURED pickers only — no free text, no AI: it pulls the live `clients` model's own field definitions (`location` cascade, `preferred_districts`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`) by slug and renders each via `DynamicField` into a draft buffer, plus a native Bedrooms select, Sources checkboxes (our_projects + all_projects default, market_listings opt-in), and a Minimum-match selector (40/60/70/80). It feeds the SAME `draftToMatchRequirements` → `fetchProjectFinder` path the Follow-up modal uses (`parse:false`, `explain:false`, `perGroup:0`, no `client_id` → no geo-preference gate), and renders the four location-centric groups with `FinderCard`. Because there is no client to attach options to, `FinderCard` gained a `hideClientActions` prop that shows **Details only** (no select checkbox / save / eliminate / reactivate); the Follow-up modal still passes the full client-action set. Route guarded by `RequirePageAccess pageId="project_finder"` in `App.tsx`. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder results now save into the unified Client Property Options store — replacing the old "Add to client" → preferences flow.** Finder cards (`FinderCard` in the Follow-up Suggested Projects modal) gained **selection checkboxes** — only matches scoring exactly **100** are auto-checked; everything below 100 is visible but unchecked unless the rep selects it — plus a footer **"Save selected to client options"** bulk-save, a per-card **"Save as option"**, and a per-card **"Eliminate"** (required reason notes). Saves write to the new `client_property_options` model (default status `suitable`, `added_from` `project_finder`) via `src/lib/matching/clientOptions.ts`, the deterministic dedup/main/eliminate engine — **not** to `clients.preferred_projects` / `preferred_market_listings` anymore. Cards cross-reference existing options (status chip; an eliminated source shows Reactivate and is never silently revived). The `preferred_*` fields remain preference INPUTS, no longer the finder's save target. Deterministic, no AI. See [clients.md](clients.md), [followups-workspace.md](followups-workspace.md).) | 2026-06-29 (**Multi-district search wired end-to-end.** A client with several preferred districts is now searched against ALL of them (previously only the first was sent). The requirements mapper (`src/lib/matching/requirements.ts`) now collects every district id from the `location` cascade array — union'd with `preferred_districts` if that field exists — resolves each to a name, and sends `districts[]`; `MatchRequirementsInput` gained `districts`. The endpoint + engine already supported it (resolve each, a project in ANY requested district is exact, across our_projects + all_projects + the market RPC). 12 live clients already had multiple districts. Tests: `src/lib/matching/requirements.test.ts`.) | 2026-06-29 (**Score honesty: a requested dimension with no project data no longer scores 100%.** Bug: a listing missing the price scored a perfect 100/strong when the client had a budget — because `scoreProject` RENORMALIZED the weighted average over only the dimensions it could evaluate, so a `null` budget (no price) was dropped from the denominator and the remaining dims (location/type/area/availability, all perfect) yielded 100%. Fix: a dimension the customer ASKED about but the project has NO data for is now kept at FULL weight earning ZERO (tracked via `requestedMissing` in `matchAgent.ts`), so it drags the score down instead of being ignored — e.g. the screenshot's no-price الازدهار villa now scores ~72/good with `budget: 0%` in the breakdown + the existing "no price data" gap chip, never 100/strong. A dimension the customer did NOT ask about is still dropped (no penalty), so unspecified prefs never inflate or deflate. This also means a data-complete listing always outranks a data-poor one. Tests added in `projectFinder.test.ts`.) | 2026-06-29 (**Market-listing cards now support Add-to-client — source-routed into a new `preferred_market_listings` field.** A new multi-value lookup `preferred_market_listings` (→ market_listings, display `title`) was added to the clients model's "Client Preferences" section, mirroring `preferred_projects` (→ all_projects). With it, market-listing cards in the Suggested Projects modal now show **Add to client** (previously omitted to avoid a broken cross-model lookup): `src/lib/matching/addToClient.ts` gained `addMarketListingToClient` (project → `preferred_projects`, listing → `preferred_market_listings`), and `SuggestedProjectsModal.tsx`'s `onAddToClient` / `FinderCard.tsx`'s actions route by `item.source`. Migration `supabase/migrations/2026-06-29_clients_preferred_market_listings.sql` (clients is unfrozen JSONB → updates `models.schema`; mirrored in `src/data/seedModels.ts`). Commit 810623d. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-06-29 (**Suggested Projects modal: all three sources + Arabic explanations.** The Follow-up Suggested Projects modal now opts into ALL three sources (`our_projects` + `all_projects` + `market_listings`) instead of the catalog-only default, so reps see the full picture; each card stays source-labelled, and non-portfolio sources carry a source-accurate "verify before offering" banner (market listings now say "external market listings", not "All Projects database"). The deterministic per-card **explanation is now localized** — `/api/project-finder` takes a `locale` param (`buildExplanation` is bilingual; the modal sends the current UI language, default Arabic server-side), fixing the English description on an otherwise-Arabic card. Market-listing cards open Details on `/model/market_listings/<id>` (their id is a market_listings record, not all_projects) and **omit Add-to-client** — `clients.preferred_projects` is an all_projects lookup, so pushing a market id would create a broken lookup. Changed: `api/_lib/projectFinder.ts` (`FinderOptions.locale`, bilingual `buildExplanation`), `api/project-finder.ts` (reads `locale`), `src/lib/matching/projectFinder.ts` (sends `locale`+`sources`), `SuggestedProjectsModal.tsx` (requests all 3 + locale + source-aware Details), `FinderCard.tsx` (source-accurate banner, hide Add-to-client for market). **Market scan: narrow-don't-cap (never drop a good listing):** live-measured 2026-06-29, including `market_listings` via the old 20km-neighbour expansion (النرجس ≈ 16,871 listings) took ~25s and 504'd FUNCTION_INVOCATION_TIMEOUT (our+all alone = ~1.7s). A first attempt capped to the newest N — **rejected** because it silently drops potentially-good listings. Replaced with: (1) market is scoped to the **requested district(s) only** (the neighbour expansion was the blow-up; exact-district is the salesperson's actual ask — distribution: 167/170 districts ≤ 2,500 listings, max 5,866); (2) a new RLS-respecting RPC **`wassell_market_candidates`** applies **budget / bedrooms / unit-type filters DB-side** (price-/bed-/type-missing listings are NEVER dropped — only known-out-of-range ones), so adding criteria genuinely shrinks the set (النرجس 2,985 → 1,205 under 2M); (3) if the filtered exact-district set still exceeds `MARKET_SCAN_LIMIT = 4000`, the engine returns a **`too_many`** signal (with the missing criteria) and the modal asks the salesperson to add budget/type/bedrooms — it **never truncates**. `MatchCoreSuccess.marketInfo` (`ok | needs_district | too_many`) flows to `metadata.market`; the modal renders an amber notice. Migration `supabase/migrations/2026-06-29_market_candidates_rpc.sql`.) | 2026-06-29 (**The Follow-up Suggested Projects modal now consumes `/api/project-finder` — `/api/suggest-projects` retired from the UI.** The modal (`SuggestedProjectsModal`) was cut over from the legacy grouped `/api/suggest-projects` endpoint to the deterministic, boundary-verified `/api/project-finder` (via the new `src/lib/matching/projectFinder.ts`), sending `parse:false` + `explain:false` so NO LLM runs on the path — selection, scoring, ranking, AND each card's explanation are all deterministic. It now shows the **four** finder groups (exact district / nearby / same city / broader) via a new `FinderCard`, with actions limited to Details + Add to client. `/api/suggest-projects` still exists but is no longer reached by the Follow-up flow; its old card/chat components (`SuggestionCard`, `AssistantChatPane`, `ProjectWhatsAppFlow`) are now orphaned. **Production-verified live:** modal calls `/api/project-finder` (200); النرجس → 2 exact (coordinate-verified, ~1.75s); العارض → 1 exact (~1.75s); الدرعية → 0 false-exacts; no-preference → "ask the client" prompt; no 504; `used_ai` parse/explain = false. See [followups-workspace.md](followups-workspace.md).) | 2026-06-28 (**Direction narrowed to a single deterministic Project Finder; broad AI assistants unwired.** A new deterministic Project Finder engine became the one and only assistant surface: given a client's requirements it FINDS and RANKS the right projects — selection, scoring, ranking, and grouping are 100% code, geography **boundary-verified** by PostGIS point-in-polygon (`districts_for_points` against the 3,732 `district_boundaries` polygons), with results split into **four location-centric groups**. The LLM is bounded to TWO touchpoints only — *parse* messy text into requirements BEFORE ranking, and *explain* each already-ranked match AFTER — and a hard `assertRankingUnchanged()` guard guarantees it can never change score/band/source/match_type/order. New: `api/project-finder.ts`, `api/_lib/projectFinder.ts`, `api/_lib/projectFinderAI.ts`, `api/_lib/geoVerify.ts`, `supabase/migrations/2026-06-28_project_finder_geo.sql`; `matchProjectsCore` gained an opt-in `verifyGeo` option (+ multi-district `req.districts[]` and a bathrooms dimension); `api/suggest-projects.ts` now passes `verifyGeo:true`. **Concurrently the broad AI assistants were UNWIRED** behind the static build flag `PROJECT_FINDER_ONLY` in `src/lib/featureFlags.ts` (code + DB models/data kept for rollback): the AI sales agent (`ai_chats`), the reel copywriter (`copywriter_chats`), and the conversational match co-pilot (`matching_chats` — with its next-best-action / lead-temperature / WhatsApp-draft / task-creation tools) are hidden from the sidebar and their routes render a `RetiredAssistantNotice`. The Follow-up workspace assistant panel is now a focused launcher for the deterministic Suggested Projects finder (no free-text chat); the modal's right-pane chat and the chat-handoff card actions (Pitch / WhatsApp / Ask) are hidden under the flag. See [ai-agent.md](ai-agent.md), [copywriter-intelligence.md](copywriter-intelligence.md), [followups-workspace.md](followups-workspace.md), [data-storage.md](data-storage.md).) | 2026-06-28 (**Geography unified onto the `location` cascade field.** Every project / listing / client now stores district/city/region in ONE `location` cascade compound ({region,city,district} ids; multi-arrays for clients) instead of separate `district_lookup`/`city_lookup`/`region_lookup`/`preferred_districts`/`preferred_cities` fields. The engine is unchanged in behavior: the caller extracts each record's `location.district`/`location.city` ids and passes them (plus display names resolved from the geography records) to `scoreProject` via the GeoContext (`projDistrictId`/`projCityId`/`projDistrictName`/`projCityName`) — district id drives the exact tier, city id the same-city tier, exactly as before. The market-listings AREA pre-filter now filters `data->'location'->>'district'`/`'city'` (with swapped partial indexes `idx_records_market_loc_district`/`_city`). Client requirements read `location.district`/`location.city` arrays. New `matchAgent.ts` helpers: `recordLocationIds`, `geoNameMap`. Migration: `supabase/migrations/2026-06-28_location_cascade_cutover.sql`. See model-builder.md for the field type.) | 2026-06-27 (**Market Listings now AREA-pre-filtered server-side, with full ~46k coverage.** `market_listings` is now populated (~46,315 external Aqar for-sale ads), so the third source is live with real data — and `matchProjectsCore({ includeMarket })` no longer pulls an arbitrary order-less slice of it. Before scoring, the engine narrows the ~46k listings to the client's relevant AREA in Postgres: (1) when the requested district resolves with a centroid, every listing in that district OR any district whose centroid is within **20 km** of it (`nearbyDistrictIds` → `.in('data->>district_lookup', ids)`); (2) else, when only a city resolves, every listing in that city (`.in('data->>city_lookup', [cityId])`); (3) else (no area signal) the **newest 4,000** listings in a stable `created_at`-desc order (a capped, logged scan — never an arbitrary slice). The same deterministic `scoreProject` then tiers/scores them in memory alongside our_projects + all_projects (budget/type/area/etc. are applied there, not in the pre-filter). A market read failure is non-fatal and logged — the other two sources still return. Backed by two **partial expression indexes** on `records((data->>'district_lookup'))` / `((data->>'city_lookup'))` scoped to the market_listings model id (measured: ~624ms/page nested-loop scan → ~70ms/page index scan, flat across pages, on the النرجس case of 16,871 in-area listings). New helpers in `matchAgent.ts`: `nearbyDistrictIds`, `loadMarketByFilter`, `loadMarketNewestCapped`. Migration: `supabase/migrations/2026-06-27_market_listings_lookup_indexes.sql`. The grouped `/api/suggest-projects` endpoint surfaces all three sources with a source badge + verify banner exactly as before. See [data-storage.md](data-storage.md) for the lazy-load posture of the market_listings model.) | 2026-06-27 (**Third source — Market Listings (`market_listings`).** The Suggested Projects modal now matches across THREE sources, each labelled on the card: `our_projects` (مشاريعنا), `market_listings` (إعلانات السوق — external scraped public ads), and `all_projects` (كل المشاريع). A market listing is a single unit (scalar `price`/`area`/`bedrooms` + real per-listing lat/lng + `district_lookup`); `adaptListingToScorable` maps it into the project shape so the ONE deterministic `scoreProject` grades all three identically. `matchProjectsCore(req, {alwaysScoreAll, includeMarket})` — the grouped endpoint passes both so all three sources show side by side; the LLM `match_projects` tool calls it with NO opts so its two-tier fallback output is unchanged. **Source is now orthogonal to quality:** an all_projects/market match is bucketed by how well it matches (exact/nearby/budget/location) and carries a source badge + a verify banner (both all_projects and market are `requires_verification`, low confidence), instead of being auto-dumped to fallback. A **source filter** (الكل / مشاريعنا / إعلانات السوق / كل المشاريع, with counts from `metadata.source_counts`) sits above the cards. Market Listings has 0 rows today, so it shows nothing until listings are scraped in; the pipeline degrades gracefully (empty source). New: `adaptListingToScorable` + `MatchSource` type + `MatchCoreOptions` in `matchAgent.ts`; `SourcePill` in `SuggestionCard.tsx`; source filter in `SuggestedProjectsModal.tsx`. Tests: `marketListingAdapter.test.ts` + market/source cases in `suggestGroups.test.ts`.) | 2026-06-26 (**Assistant Product Completion — grouped "Suggested Projects" modal + Add-to-client + audit log + feature flag.** The Follow-up side panel's "Suggested Projects" button now opens a LARGE modal: top = current preferences + missing-preference warnings; left = grouped, ranked project cards in five honest buckets (`exact_matches` / `nearby_alternatives` / `budget_matches` / `location_matches` / `fallback_matches`); right = the same assistant chat for explain/compare/pitch/WhatsApp. Grouping is **deterministic, no LLM** — a new `POST /api/suggest-projects` runs the SAME `matchProjectsCore` scoring brain (extracted from the `match_projects` tool, which is unchanged) and a pure `groupMatchResults` that buckets candidates and stamps each with a `data_confidence` (high/medium/low) + normalized `data_gaps` (e.g. `missing_price_range`, `missing_project_coordinates`, `legacy_text_match_only`, `requires_verification`). Honest about the sparse data: a project with no price NEVER enters `budget_matches`; distance is district-CENTROID based (no project pins exist). Each card has actions: Why / Compare / Pitch / WhatsApp / Open details / **Add to client** (appends to `clients.preferred_projects` via the version-aware store write — user-triggered only). Every run writes one row to the new `assistant_recommendation_runs` audit table. The whole feature is gated by the `feature_flags.sales_assistant_enabled` kill switch (fail-open). The modal reads the UNSAVED follow-up preference draft, so recommendations reflect what the rep is editing before Save. See [followups-workspace.md](followups-workspace.md).) | 2026-06-25 (**Lookup-first matching with dual-read (Saudi geography layer):** the engine now resolves the requested district to the authoritative `districts` record (id + real centroid) via a new `resolveRequestedDistrict` helper, and a project whose `district_lookup` equals the resolved id is an EXACT relational match. The old text fuzzy-match on `preferred_neighborhoods` + per-project averaged centroids is RETAINED as the dual-read fallback for projects not yet backfilled. New geo helpers in `src/lib/locationUtils.ts`: `pointInPolygon`, `pointInDistrict`, `distanceBetweenCentroids`, `haversineKm`, `normalizeArabicDistrictName`, `normalizeEnglishDistrictName`. See [record-management.md](record-management.md) and [data-storage.md](data-storage.md).) | 2026-06-21 | 2026-07-13 (**Timeout recovery panel in the finder UIs.** `/api/project-finder` runs on the Vercel Edge runtime (~25s hard budget); a wide geo scope — measured live: a "south of King Salman Rd" direction rule matches ~36.7k points and adds ~9s to the request (~17s total, 504 on a cold/loaded DB) — can push it past the budget and Vercel kills it with `FUNCTION_INVOCATION_TIMEOUT` (504), which previously rendered as a raw red error string (or read as "no options"). `fetchProjectFinder` now throws a typed `FinderRequestError` (`status`, `timeout` = 504/408/524); on timeout BOTH finder surfaces (`SuggestedProjectsView` + `ProjectFinderPage`) show a dedicated bilingual recovery panel — "البحث استغرق وقتاً أطول من المسموح" + guidance to narrow the location scope (specific districts instead of a city-wide direction rule) — with **تضييق التفضيلات / Narrow preferences** (opens the edit-preferences panel) and **إعادة المحاولة / Try again** (re-sends the last attempted draft via `lastDraftRef`). Non-timeout errors keep the red box, now with a Try-again button on the follow-up/client surface. Root-cause follow-ups deliberately NOT done yet (user chose UI-first): move the endpoint to the Node runtime with a higher `maxDuration`, and/or compile directional rules into indexed polygons.) | 2026-07-13 (**Save-options-first exit guard.** The bulk **حفظ الخيارات / Save options** button moved from the results footer to the TOP header of every client-scoped finder surface (`SuggestedProjectsView` header — primary copper button with a live selected-count badge, next to a now-secondary **تم / Done** — and the standalone `ProjectFinderPage` header when a client is selected). The old footer bulk-save button is gone; the footer keeps the selected count plus an amber "لم يُحفظ أي خيار لهذا العميل بعد" hint until something is saved. **Leaving a client-scoped finder without having saved a single option this visit now asks for confirmation** — "هل أنت متأكد أنك لن تحفظ أي خيار لهذا العميل؟" via the shared `src/components/matching/LeaveWithoutSavingModal.tsx` (Stay is the primary action; "الخروج بدون حفظ" proceeds). The guard intercepts the Done button and Esc in `SuggestedProjectsView`, clearing the selected client on `ProjectFinderPage`, and arms a `beforeunload` browser prompt for tab close/reload. It arms only AFTER results were actually shown (an empty/failed search never nags) and disarms on ANY successful option write this visit — single save, bulk save, inline status set, eliminate, or reactivate.) | 2026-07-13 (**Finder failure honesty + market-scan index (live incident: 504 rendered as "no matching projects").** A search for a client with district `location_items` (سراج الجهني: النرجس/العارض/القيروان) consistently 504'd — `wassell_market_candidates_json` full-scanned ~51k market rows on `(data->'location'->>'district')` (no index, ~4.5s JSONB detoast) plus the per-row `wassell_can_view_record` RLS check (~4–8s), measuring ~30s against `/api/project-finder`'s 25s function ceiling. The SPA then LIED about the failure: on HTTP/2 `res.statusText` is the empty string, so `fetchProjectFinder`'s non-JSON-body fallback threw an `Error('')`, `SuggestedProjectsView`'s `error` state stayed falsy, and the UI fell through to the **"لا توجد مشاريع مطابقة" empty state** — while the catalog actually held 14 exact-district matches (5 scoring 100). Fixes: (1) `fetchProjectFinder` uses `||` (not `??`) so a blank error message never masquerades as success — a failed request now shows the red error box; (2) new DB index `idx_records_model_district` on `records (model_id, (data->'location'->>'district'))` (applied live with CONCURRENTLY; migration `2026-07-13_records_model_district_index.sql`) — the market RPC drops ~30s → ~2s and the full three-source search responds ~17s with 516 exact-district results. Known follow-up: ~17s is still near the 25s ceiling; the per-row RLS call in the market RPC is the next bottleneck.) | 2026-07-04 (**"إرسال للعميل / Send to client" on every finder + option card (QA-002), collapsible preferences area (QA-001).** (1) Every Project Finder result card (`FinderCard`, client-scoped surfaces only — hidden with `hideClientActions`) and every Client Options card (projects + market listings) now carries a copper **Send to client** action that WhatsApps that project/listing to the CONNECTED client without opening the record: a prepared `chat_templates` message is reused (project → `project_id` link via the re-wired `ProjectWhatsAppFlow`; listing → `listing_id` + `status:'ready'` via the new `src/components/matching/ListingWhatsAppFlow.tsx`), and when none exists the EXISTING creation flow runs right from the card (project: deterministic `resolveProjectFacts`→`composeProjectMessage` preview→save; listing: `ListingMessageModal` AI text + text-removed photos → on approve the flow continues straight into the chat). The chat opens as `StartChatModal` with the client preselected + body prefilled; the template's gallery/cleaned photos ride along as separate messages. **Video support:** `sendProjectImageMessages` now sends each ride-along by MIME (`video` for video bytes, extension fallback for typeless servers), and direct video-FILE URLs on the source (`all_projects.project_videos`, listing `video_urls`) are appended via the new `directVideoUrls` helper — HLS streams (Aqar/Cloudflare `.m3u8`) and page links are excluded (not uploadable to WhatsApp). Shared pure helpers + tests: `src/lib/matching/sendToClient.ts` (`findProjectTemplate` / `findReadyListingTemplate` / `templateListingImageUrls` / `templateBody` / `directVideoUrls`), `__tests__/sendToClient.test.ts`. `ProjectWhatsAppFlow` is UN-orphaned. (2) **QA-001:** the finder's always-visible preferences-chips + missing-warnings block is now COLLAPSIBLE (chevron toggle; collapsed = one thin row with count + truncated value summary + missing-count badge); `SuggestedProjectsView` gained `defaultPrefsCollapsed`, set by the Client-Options popup host (`ClientOptionsModal`) so popup results get the vertical space — full-page hosts stay expanded. The edit-preferences panel already toggled + auto-collapsed on Search (2026-07-02). See [clients.md](clients.md), [chats.md](chats.md).) | 2026-07-04 (**Saved market-listing option cards show the quality grade even when saved before the feature (fix).** The A–D `QualityBadge` on a Client Options market-listing card read ONLY the snapshot (`facts.quality_grade`), which was captured at save time — but the finder only started writing quality into `facts` on 2026-07-02, so every option saved before then (all 252 live market-listing options at the time, saved ~2026-07-01) showed a BLANK chip. `ClientOptionsTab` now falls back to the LIVE listing record in the slim `market_listings` store (which the DB trigger keeps `quality_grade`/`quality_score` on for every row, both riding the slim store) via a new `qualityBySourceId` map — the exact same "snapshot-first, live-store fallback" pattern already used for the ad-id chip. New saves keep using their snapshot; pre-feature options now render the current grade; a listing dropped from the catalogue simply shows no chip. Verified live: 252/252 pre-feature options had a null snapshot grade but a real live grade in the 47k-row store. `src/pages/Clients/components/tabs/ClientOptionsTab.tsx`. See [clients.md](clients.md).) | 2026-07-02 (**Listing quality in the finder.** Every market_listings result now carries the DB-computed `quality_score`/`quality_grade` in its `facts` (adapter → scoreProject facts → card → saved-option snapshot), rendered as the shared colored A–D chip (`src/components/market/QualityBadge.tsx`) on finder cards (both surfaces) and on saved market-listing option cards (`ClientOptionsTab`, incl. the manual `AddOptionModal` snapshot). Ranking: quality is an EQUAL-match-score TIEBREAK ONLY — server `byBandThenScore` and the client default score sort (`finderRefine.sortItems`) append `quality_score` as the final key; relevance always dominates, no minimum-grade filter, no change to scoreProject or the too_many guard. Items without a quality score (projects/units) tie-break as 0.) | 2026-07-02 (**Location matching + grouping fixes (3 bugs from field screenshots).** (1) *Wrong / missed "same district" matches:* the SPA now sends the client's selected districts as AUTHORITATIVE record ids (`requirements.district_ids`, built in `draftToMatchRequirements` from the location cascade + `preferred_districts` + `location_items` include-rules); the engine matches on those ids directly and runs the fuzzy per-name ILIKE resolution ONLY when no id resolved — so a same-named district in another city can no longer be admitted as "في نفس الحي", nor can a selected district silently mis-resolve and mark its own projects "outside". Multiple selected districts stay OR — inside ANY one = exact. (2) *Grouping is now purely location-centric:* `finderGroupFor` groups by the verified `location_tier` alone; the old "band `partial` → broader_fallback" override is GONE, so a wrong-unit-type or no-price project INSIDE a requested district stays under "في نفس الحي" (its weak fit still shows via the band badge + in-group ordering) instead of being falsely labelled "خارج المنطقة المطلوبة". (3) *Distance everywhere, measured to the nearest selected reference:* `scoreProject` computes `distance_km` for EVERY tier (same-city + fallback included), to the NEAREST of ALL selected references — every requested district centroid PLUS the client's selected location ELEMENTS (`location_items` element anchors — within_radius / within_distance / inside_area — resolved endpoint-side from `geo_elements` and passed as `FinderOptions.refPoints`) — and names WHICH one via the new `nearest_ref_name` field (on `ScoredProject` → `MatchResultItem` → `FinderMatch`). The card distance pill now reads "~4.2 كم من النرجس", and the deterministic nearby/same-city/fallback explanations name the closest selected area (fallback: "خارج الأحياء المحددة (يبعد حوالي X كم عن «Y»)"). Tests in `projectFinder.test.ts` + `requirements.test.ts`.) | 2026-07-02 (**Multi-select preferences now match as OR (any acceptable option counts).** When a client selects MULTIPLE acceptable options for one preference, they are alternatives — a result matching ANY of them now gets full credit for that dimension. (1) **Unit types:** `preferred_unit_type` is a multiselect but only the FIRST value was sent — a client wanting "شقة أو دور" saw every دور project scored `type: 0` and band-capped into the fallback tab. `draftToMatchRequirements` now sends ALL selected types as `requirements.property_types[]` (`property_type` stays = the first for single-type paths); the engine (`MatchRequirements.property_types`, `scoreProject`) unions the synonym expansion across all requested types and a project offering ANY of them earns full type credit — the wrong-type band cap fires only when NONE match. The market-listings DB pre-filter unions the ILIKE terms across all types too (previously only the first type's listings ever became candidates), and the `match_projects` AI-agent tool + the finder's LLM text parser can now express type alternatives (`property_types`). (2) **Cities:** the `location` cascade allows multiple cities but only the first was sent; `cities[]` now flows through and `matchProjectsCore` resolves EACH into the city-match set (`reqCityIds`), so a project in ANY acceptable city city-matches. (3) **Districts** already had OR semantics (`districts[]`, 2026-06-29) — unchanged. Deliberately NOT changed: `preferred_amenities` keeps proportional credit (amenities are cumulative wants, not alternatives — a client wanting "غرفة خادمة ومسبح" wants both). No scoring weights, bands, or tiers changed. Tests: `requirements.test.ts`, `projectMatchScoring.test.ts`.) | 2026-07-02 (**Finder embedded in the Client Options popup (new surface).** The shared `SuggestedProjectsView` gained a FOURTH host: the new `ClientOptionsModal` (`src/components/clients/ClientOptionsModal.tsx`) — the client-options popup opened from the WhatsApp chat header — embeds the finder inside the popup itself ("البحث عن خيارات أكثر / Find more options" swaps the popup body to the finder, `followupId=null`, seeded + geo-gated from the client; "تم / Done" returns to the options list). **Same-day fix (live report):** embedded in the popup on a laptop-height screen, the finder's open edit-preferences panel — part of the fixed chrome — consumed the whole height, leaving the results area 0px with nothing scrollable; the panel is now height-capped (max-h-[42vh], own scrollbar) and auto-collapses when Search is pressed so results are visible immediately (benefits the full-page finder on short viewports too), and the popup uses near-full-screen height. `ClientOptionsTab` also gained an `onFindMore` prop, so the Client 360 Options tab now links to the full-page client finder (`/model/clients/:id/projects`) too. See [chats.md](chats.md), [clients.md](clients.md).) | 2026-07-02 (**Market-listing finder cards: "التواصل مع المعلن / Contact advertiser" button.** Every `market_listings` result card (both finder surfaces — standalone `/project-finder` AND the Follow-up/client `SuggestedProjectsView`, including the standalone no-client mode) now carries a compact copper **Contact advertiser** action next to Details: if the listing already has a scraped `advertiser_phone` it opens the in-app WhatsApp `StartChatModal` immediately (pre-filled with the Aqar listing link); otherwise it enqueues the REGA advertiser lookup (`/api/rega-lookup` → Browserbase worker), shows a "جاري جلب الرقم…" spinner, and auto-opens the chat when the worker's write lands via Realtime — after which every later click opens the chat directly (the phone + `advertiser` link persist on the listing). Failures surface as an error toast. Shared component `src/components/market/ContactAdvertiserButton.tsx` + shared hook `src/lib/regaLookup/useContactAdvertiser.ts` (extracted from `ContactAdvertiserPanel`); reads the listing from the market_listings slim store by `project_id`, so no extra fetch per card. Same button also renders on the client Options tab's market-listing cards — see [clients.md](clients.md), [record-management.md](record-management.md).) | 2026-07-01 (**Project Finder is now reachable from the CLIENT profile.** Per the user, the finder can be run directly on a client. The Client 360 header (`DetailHeader`) gained a prominent copper **"الباحث عن المشاريع / Find projects"** button that navigates to a new full-page route `/model/clients/:recordId/projects` (`ClientProjectsPage`) — a thin wrapper (mirroring the follow-up `SuggestedProjectsPage`) that loads the client, seeds `prefDraft` from the saved client data, and renders the SAME shared `SuggestedProjectsView` with `followupId=null`. So the client-scoped finder has the full feature set — map/list view, refinement toolbar, edit-preferences + re-run, and saving into the client's unified `client_property_options` — geo-gated by the client (client_id + their saved `location_items`); **"تم / Done"** returns to the client profile. `SuggestedProjectsView` already tolerates a null `followupId` (the finder-handoff write is guarded; audit/save attach via `clientRec.id`). Route precedes the generic `:recordId` detail route (like the follow-up one). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Standalone Project Finder: pick a client → seeded, geo-gated search + full client-option actions; unified location editor.** The sidebar `/project-finder` tool reached parity with the Follow-up finder. (1) A **client picker** (optional, top of the filters panel) searches the `clients` records by name/phone/code; selecting one SEEDS the pickers from that client's saved preferences (`location` + `location_items`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`, `preferred_bedrooms`→bedrooms), passes `client_id` + the current `location_items` to `/api/project-finder` (so the PostGIS geo gate compiles + narrows for that client), and turns EVERY result card into a full client-option control — inline status selector (`onSetStatus`), bulk-select + "Save selected to client options" footer, and the eliminate-with-reason modal — writing into that client's `client_property_options` (`added_from:'project_finder'`). A "clear client" X returns to pure discovery (cards show Details only). (2) The `location` field now renders the **unified `ClientLocationField`** (district include/exclude chips + geo-element rules with "+ add district / + add element") instead of the bare cascade — threaded via `modelId`/`onPatch` into `DynamicField`, writing both `location` and `location_items` into the draft. Without a selected client, district include-rules still tier results (via the `requirements.districts` mirror) but geo-element rules don't gate (the server geo gate needs a `client_id`). All in `ProjectFinderPage.tsx`; handlers mirror `SuggestedProjectsView`. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Finder results now have a MAP view (list ⇄ map toggle).** Per the user — "when presenting listings give me an option for a map view." Both finder surfaces (standalone `ProjectFinderPage` + Follow-up `SuggestedProjectsView`) gained a **List / Map** segmented toggle in the shared `FinderRefinementBar`. In map mode the active tab's results — the pinned OUR-projects **plus** that tab's other matches — render on the branded `WASSEL_MAP_STYLE` Google basemap as pins colored by SOURCE (green = our projects, chocolate = market, charcoal = listings DB), clustered by density (`MarkerClusterer` + `buildClusterIcon`), auto-fit to the pins. Clicking a pin opens an InfoWindow summary (source · score · name · district/city · distance · price) with a **"التفاصيل / Details"** button that runs the same `onOpenDetails` as a card. New component `src/pages/Followups/components/FinderMapView.tsx` (shared, read-only — client-option actions stay on the list). Coordinates now flow from the engine: `scoreProject` puts `facts.latitude`/`facts.longitude` (the resolved pin — verified polygon centroid when geo-verification ran, else the stored lat/lng) in `matchAgent.ts`; the finder endpoint passes `facts` through unchanged. A legend + coverage note ("N on the map · M without coordinates") sits under the map; the toggle only appears once results exist. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder cards: ad-ID chip + inline client-option status selector; district include-rules now land results in the exact-district tier.** (1) Market-listing cards now show a small ad-id chip (the external Aqar ad number, for looking the ad up) — reformatted to "@<external_id>" on 2026-07-04, when the id also reached the finder MAP pin hover titles, the Client Options tab cards, and the manual add-option picker (see clients.md) — `scoreProject` puts `facts.external_id`, `adaptListingToScorable` carries it through. (2) On the Follow-up finder EVERY card gained an inline STATUS `<select>` (main focus / reserved / interested / presented / suitable / not interested / closed / eliminated) that ENSURES the client option exists (creating it if new) THEN applies the chosen status in one step — `main_focus` routes through `setMainOption` (single active main), `eliminated` opens the reason-notes modal, and any non-eliminated pick reactivates a previously-eliminated option. This REPLACES the old per-card Save-as-option / Eliminate / Reactivate buttons (`StatusSelect` in `FinderCard.tsx`, colored to the status badge, RTL-safe native select; `onSetStatus` in `SuggestedProjectsView.tsx`). Bulk-select checkbox + footer bulk-save are unchanged; the standalone Project Finder (no client) still shows Details only. (3) A district picked in the unified location box is stored as a `location_items` district INCLUDE-rule (it drives the PostGIS geo gate), NOT the `location` cascade — so `draftToMatchRequirements` now mirrors those include-rule district ids (with the label stashed on the item as a name fallback) into `requirements.districts`; without it every gated result fell to the "same city / different district" tier even though it sat inside the requested district. Test in `requirements.test.ts`. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder result tabs: 3 tabs (Same district / Nearby / Alternatives) + our-projects pinned to the top of EVERY tab.** Per the user: the four engine tiers now collapse to THREE display tabs — "Same city" is dropped (the whole set is already the same city) and its items fold into **Alternatives** (= same_city + broader_fallback) — and OUR portfolio (source `our_projects`, ≥70) is lifted out of the tiers into a pinned "مشاريعنا / Our Projects" list shown at the TOP of every tab, best-first (score desc → closest), followed by that tab's other matches under a "خيارات أخرى / Other options" divider. Pure client-side display: shared `buildFinderTabs` + `DISPLAY_TAB_KEYS`/`DISPLAY_TAB_LABELS` in `finderRefine.ts`, wired into BOTH `ProjectFinderPage` and `SuggestedProjectsView`; tab badge = pinned-ours + that tab's tier count; the tier list stays windowed/infinite-scrolled while our-projects always render in full. The engine's four-group response is unchanged. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Geo gate ~5× faster + un-truncated (was 7s/capped-at-1000).** Follow-up to the timeout band-aid below. (1) PERF: `wassell_geo_match` was a 7.05s seq-scan of all ~47.6k project+listing points with a per-row geometry fn. Rewrote it to use the GiST point indexes — polygon/buffer items (within_radius/inside_area/district, `geom NOT NULL`) via an `ST_Contains` spatial JOIN (**~38ms**, was ~7s), direction items (halfplane, `geom NULL`) via a GiST bbox pre-filter that drops the far side (27,019/47,580 for the north-of rule) then the exact `wassell_geo_dir_match` (**~1.5s**, was 7s). Verified identical record-id sets on prod before the swap (direction client 8046=8046; synthetic 3km polygon 349=349). Migration `2026-07-01_geo_match_index_optimized.sql`. (2) CORRECTNESS: the endpoint's row-returning `wassell_geo_match` call was capped at PostgREST's ~1000-row response (the area can be 8046), silently truncating the gate to 1000 AND non-deterministically (market count flickered 329↔489). Added `ORDER BY record_id` + a new `wassell_geo_match_ids(uuid)→uuid[]` (one row, no cap, scan runs once); `api/project-finder.ts` calls it instead. Live element-only finder: **8.7s → 3.4s** with the FULL 8046-record gate. Follow-up: clip direction halfplanes to the region polygon at compile time to put them on the index path too. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Geo gate was silently timing out for authenticated users — the element never narrowed anything.** Sibling fix to the market-AREA change below: even after it shipped, the element-only client STILL saw `geo_filter:null` + `needs_district`. Root cause: `wassell_geo_match` scans all ~46.6k project+listing points with a per-row geometry predicate (7.05s EXPLAIN ANALYZE for a Riyadh-wide rule), tripping the `authenticated` role's 8s `statement_timeout`; the endpoint's catch left `geoMatchIds` null. As SECURITY DEFINER fns, a function-level `statement_timeout` overrides the caller's value, so `alter function wassell_geo_match / wassell_compile_client_geo set statement_timeout='20s'` (migration `2026-06-30_geo_match_statement_timeout.sql`, applied to prod) lets the scan finish. Verified live: `geo_filter` now applied, the element-only client sees **489 in-area market listings** (was 0), no district picked (~8.7s; perf follow-up = make polygon items index-usable / clip direction halfplanes to the region). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Market listings now respect a `location_items` AREA — no district pick required.** The user set a geo-element rule ("داخل النخيل مول") but the finder still said "حدّد الحي لعرض إعلانات السوق" (needs_district) and hid market ads — defeating the point of the element. The market scan now scopes by the client's geo-matched record-id set when no district is set: `wassell_geo_match` already returns the in-area LISTING ids (`listing_points` populated, 46,608 rows; النخيل area = 7,904 listings). The market-candidates RPCs gained an optional `p_record_ids uuid[]` (migration `2026-06-30_market_candidates_geo_ids.sql`, applied to prod) that scopes by `r.id = any(p_record_ids)` instead of the district filter — same DB-side budget/bed/type filters, RLS re-check, `ORDER BY r.id`, count-then-paginate `too_many` guard. `matchProjectsCore` market block: scope = district when set, ELSE the `geoGate` area, ELSE `needs_district`. An element-only client now sees market ads (النخيل: 7,904 → 2,595 villas → 911 in 3–4M); a too-broad area still returns `too_many` asking for budget/bedrooms. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder edit-preferences panel now shows + edits the geo-element / district rules (`location_items`).** Bug: the panel rendered the `location` field via a bare `DynamicField`, so the unified `ClientLocationField` (district + "within radius of a landmark" chips with "+ إضافة حي / + إضافة عنصر" controls) never appeared — selected geo-elements weren't shown and there was no way to add one. Fix: thread `modelId` + `recordId` + `onPatch` into that `DynamicField` (the exact trigger that makes it render `ClientLocationField`, same as `PreferenceSummary`), so the box now shows the saved chips + add controls and writes both `location` and `location_items` into `editDraft` (location field spans full width). Because the server geo-gate compiles from the SAVED client (`wassell_compile_client_geo(p_client_id)` reads `clients.data.location_items` at request time), the "بحث / Search" button now **persists the edited prefs (incl. `location_items`) to the client first** (version-safe, only when changed) THEN re-runs the match — so a district/element rule actually narrows the results. The separate "Save to client" button was merged into Search (caption: "تُحفظ التفضيلات للعميل عند البحث"). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md), [client_geo_matching](client_geo_matching).) | 2026-06-30 (**Edit client preferences on the finder page → re-run the search.** The full-page finder (`SuggestedProjectsView`) gained an "تعديل التفضيلات / Edit preferences" header toggle that opens an inline editor of the client's preference fields (those present on the live model: `location` cascade, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_bedrooms`, `preferred_amenities`) via `DynamicField` into a local `editDraft`. "بحث بالتفضيلات الجديدة / Search" rebuilds the SERVER-SIDE requirements (`buildReqs` — also maps `preferred_bedrooms.min` → the matcher's single `bedrooms`) and RE-RUNS `/api/project-finder` (a genuinely more-detailed search, not just the client-side slider/refine). The view's fetch was refactored from a once-on-mount snapshot into a reusable `runSearch(draft)` (abortable; resets slider/refine; re-seeds the 100%-auto-select); preference chips + `ctx` now reflect the last *searched* draft. Each search also updates the workspace hand-off so the edits carry back, and an explicit "حفظ التفضيلات للعميل / Save to client" persists `editDraft` to the client record version-safe (`saveRecord` + `expectedVersion`, same posture as `PreferenceSummary`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Follow-up Suggested Projects is now a FULL PAGE, not a modal.** Per the user — "most of the time we will use it here" and "it still doesn't look good" — the cramped 860px modal was replaced by a full-page route `/model/followups/:recordId/projects` (`SuggestedProjectsPage` → shared `SuggestedProjectsView`). It opens in the SAME tab via client-side nav from the workspace's "المشاريع المقترحة" launcher, stays CONNECTED to the follow-up (its id drives the recommendation audit log + saves into THAT client's `client_property_options`), lays cards out in a responsive 1/2/3-column grid inside a centered max-w-6xl, and carries the same refinement toolbar + images + bulk-save. A prominent **"تم / Done"** button (header + footer, plus Esc) returns the rep to the follow-up record (`navigate('/model/followups/:id')`). Preferences ride a module-singleton hand-off (`src/lib/matching/finderHandoff.ts`) the panel sets at click time (so unsaved edits are honored); on a cold URL load the page falls back to the saved client data, and on return the workspace restores the hand-off draft then clears it. `SuggestedProjectsModal.tsx` was deleted; `SalesAssistantSidePanel` now navigates instead of opening a modal. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Refinement toolbar now in the Follow-up Suggested Projects modal too (shared with the standalone page).** The score slider + sort + hard-filter "Refine" panel were extracted to a shared module (`src/lib/matching/finderRefine.ts` — `SortKey`/`Refine`/`passesRefine`/`sortItems`/`refineGroups`/`FETCH_FLOOR`) + a shared component (`FinderRefinementBar.tsx`), and wired into BOTH `ProjectFinderPage` and `SuggestedProjectsModal` so they behave identically. The modal already fetched everything ≥70; now it filters/sorts that set client-side, its group-tab counts + footer follow the refinement, bulk-save only saves cards still VISIBLE under the active refinement (never slider/refine-hidden ones), and it auto-hops to the first non-empty tab when refining empties the active one. This was the user's primary surface ("most of the time we will use it here"). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder cards now show the listing/project main image.** Every `FinderCard` (standalone Project Finder + Follow-up Suggested Projects modal) renders an `h-40` hero thumbnail at the top. The engine adds a single `facts.image` in `scoreProject`: market listings carry a raw photo URL (`adaptListingToScorable` reads `main_image_url`, else first `image_urls[]`); projects carry a files.id (`main_image`, else first `project_images[]`). The client resolves either through the existing `useSignedImage` hook — http(s) passthrough for the raw market URL, short-lived signed view URL for the project files.id (process-cached). The image block self-hides when there's no image or the load fails (`onError`). No new server signing per result — projects sign lazily client-side only for the ~24 windowed cards. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder — results refinement toolbar (score slider + sort + hard refine filters).** The standalone page now always fetches EVERYTHING scoring ≥ 70 (`FETCH_FLOOR`, no longer a left-panel select) and adds a client-side refinement toolbar above the results that operates on that set with NO re-fetch: (1) a **score slider** 70→100 (step 5) — drag right to raise the minimum and shrink the list, with a live "showing X of Y" count and per-group tab counts that follow; (2) a **sort** dropdown — best match (default), price ↑/↓, area large/small, nearest, most available units, name; (3) a **"Refine" panel** of HARD post-filters to focus a large result set — max price, area min/max, min bedrooms, available-only, coordinate-verified-only (these are real gates, unlike the soft scoring dimensions, so they directly answer "the finder returns far more than the table because area/price are only soft 10–25% nudges"). A fresh search resets the slider to 70 and clears refine. All logic is pure client-side in `ProjectFinderPage.tsx` (`refinedGroups` memo: filter by score≥threshold + `passesRefine`, then `sortItems`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder page — a sidebar tool usable WITHOUT a client.** The deterministic Finder is now reachable on its own at `/project-finder` (sidebar entry "الباحث عن المشاريع / Project Finder", `Compass` icon), registered as a `project_finder` custom page in `src/lib/customPages.ts` with `default_access:'all'` (gateable per-profile in Settings → Profiles, admins always see it). The new page `src/pages/ProjectFinder/ProjectFinderPage.tsx` renders STRUCTURED pickers only — no free text, no AI: it pulls the live `clients` model's own field definitions (`location` cascade, `preferred_districts`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`) by slug and renders each via `DynamicField` into a draft buffer, plus a native Bedrooms select, Sources checkboxes (our_projects + all_projects default, market_listings opt-in), and a Minimum-match selector (40/60/70/80). It feeds the SAME `draftToMatchRequirements` → `fetchProjectFinder` path the Follow-up modal uses (`parse:false`, `explain:false`, `perGroup:0`, no `client_id` → no geo-preference gate), and renders the four location-centric groups with `FinderCard`. Because there is no client to attach options to, `FinderCard` gained a `hideClientActions` prop that shows **Details only** (no select checkbox / save / eliminate / reactivate); the Follow-up modal still passes the full client-action set. Route guarded by `RequirePageAccess pageId="project_finder"` in `App.tsx`. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder results now save into the unified Client Property Options store — replacing the old "Add to client" → preferences flow.** Finder cards (`FinderCard` in the Follow-up Suggested Projects modal) gained **selection checkboxes** — only matches scoring exactly **100** are auto-checked; everything below 100 is visible but unchecked unless the rep selects it — plus a footer **"Save selected to client options"** bulk-save, a per-card **"Save as option"**, and a per-card **"Eliminate"** (required reason notes). Saves write to the new `client_property_options` model (default status `suitable`, `added_from` `project_finder`) via `src/lib/matching/clientOptions.ts`, the deterministic dedup/main/eliminate engine — **not** to `clients.preferred_projects` / `preferred_market_listings` anymore. Cards cross-reference existing options (status chip; an eliminated source shows Reactivate and is never silently revived). The `preferred_*` fields remain preference INPUTS, no longer the finder's save target. Deterministic, no AI. See [clients.md](clients.md), [followups-workspace.md](followups-workspace.md).) | 2026-06-29 (**Multi-district search wired end-to-end.** A client with several preferred districts is now searched against ALL of them (previously only the first was sent). The requirements mapper (`src/lib/matching/requirements.ts`) now collects every district id from the `location` cascade array — union'd with `preferred_districts` if that field exists — resolves each to a name, and sends `districts[]`; `MatchRequirementsInput` gained `districts`. The endpoint + engine already supported it (resolve each, a project in ANY requested district is exact, across our_projects + all_projects + the market RPC). 12 live clients already had multiple districts. Tests: `src/lib/matching/requirements.test.ts`.) | 2026-06-29 (**Score honesty: a requested dimension with no project data no longer scores 100%.** Bug: a listing missing the price scored a perfect 100/strong when the client had a budget — because `scoreProject` RENORMALIZED the weighted average over only the dimensions it could evaluate, so a `null` budget (no price) was dropped from the denominator and the remaining dims (location/type/area/availability, all perfect) yielded 100%. Fix: a dimension the customer ASKED about but the project has NO data for is now kept at FULL weight earning ZERO (tracked via `requestedMissing` in `matchAgent.ts`), so it drags the score down instead of being ignored — e.g. the screenshot's no-price الازدهار villa now scores ~72/good with `budget: 0%` in the breakdown + the existing "no price data" gap chip, never 100/strong. A dimension the customer did NOT ask about is still dropped (no penalty), so unspecified prefs never inflate or deflate. This also means a data-complete listing always outranks a data-poor one. Tests added in `projectFinder.test.ts`.) | 2026-06-29 (**Market-listing cards now support Add-to-client — source-routed into a new `preferred_market_listings` field.** A new multi-value lookup `preferred_market_listings` (→ market_listings, display `title`) was added to the clients model's "Client Preferences" section, mirroring `preferred_projects` (→ all_projects). With it, market-listing cards in the Suggested Projects modal now show **Add to client** (previously omitted to avoid a broken cross-model lookup): `src/lib/matching/addToClient.ts` gained `addMarketListingToClient` (project → `preferred_projects`, listing → `preferred_market_listings`), and `SuggestedProjectsModal.tsx`'s `onAddToClient` / `FinderCard.tsx`'s actions route by `item.source`. Migration `supabase/migrations/2026-06-29_clients_preferred_market_listings.sql` (clients is unfrozen JSONB → updates `models.schema`; mirrored in `src/data/seedModels.ts`). Commit 810623d. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-06-29 (**Suggested Projects modal: all three sources + Arabic explanations.** The Follow-up Suggested Projects modal now opts into ALL three sources (`our_projects` + `all_projects` + `market_listings`) instead of the catalog-only default, so reps see the full picture; each card stays source-labelled, and non-portfolio sources carry a source-accurate "verify before offering" banner (market listings now say "external market listings", not "All Projects database"). The deterministic per-card **explanation is now localized** — `/api/project-finder` takes a `locale` param (`buildExplanation` is bilingual; the modal sends the current UI language, default Arabic server-side), fixing the English description on an otherwise-Arabic card. Market-listing cards open Details on `/model/market_listings/<id>` (their id is a market_listings record, not all_projects) and **omit Add-to-client** — `clients.preferred_projects` is an all_projects lookup, so pushing a market id would create a broken lookup. Changed: `api/_lib/projectFinder.ts` (`FinderOptions.locale`, bilingual `buildExplanation`), `api/project-finder.ts` (reads `locale`), `src/lib/matching/projectFinder.ts` (sends `locale`+`sources`), `SuggestedProjectsModal.tsx` (requests all 3 + locale + source-aware Details), `FinderCard.tsx` (source-accurate banner, hide Add-to-client for market). **Market scan: narrow-don't-cap (never drop a good listing):** live-measured 2026-06-29, including `market_listings` via the old 20km-neighbour expansion (النرجس ≈ 16,871 listings) took ~25s and 504'd FUNCTION_INVOCATION_TIMEOUT (our+all alone = ~1.7s). A first attempt capped to the newest N — **rejected** because it silently drops potentially-good listings. Replaced with: (1) market is scoped to the **requested district(s) only** (the neighbour expansion was the blow-up; exact-district is the salesperson's actual ask — distribution: 167/170 districts ≤ 2,500 listings, max 5,866); (2) a new RLS-respecting RPC **`wassell_market_candidates`** applies **budget / bedrooms / unit-type filters DB-side** (price-/bed-/type-missing listings are NEVER dropped — only known-out-of-range ones), so adding criteria genuinely shrinks the set (النرجس 2,985 → 1,205 under 2M); (3) if the filtered exact-district set still exceeds `MARKET_SCAN_LIMIT = 4000`, the engine returns a **`too_many`** signal (with the missing criteria) and the modal asks the salesperson to add budget/type/bedrooms — it **never truncates**. `MatchCoreSuccess.marketInfo` (`ok | needs_district | too_many`) flows to `metadata.market`; the modal renders an amber notice. Migration `supabase/migrations/2026-06-29_market_candidates_rpc.sql`.) | 2026-06-29 (**The Follow-up Suggested Projects modal now consumes `/api/project-finder` — `/api/suggest-projects` retired from the UI.** The modal (`SuggestedProjectsModal`) was cut over from the legacy grouped `/api/suggest-projects` endpoint to the deterministic, boundary-verified `/api/project-finder` (via the new `src/lib/matching/projectFinder.ts`), sending `parse:false` + `explain:false` so NO LLM runs on the path — selection, scoring, ranking, AND each card's explanation are all deterministic. It now shows the **four** finder groups (exact district / nearby / same city / broader) via a new `FinderCard`, with actions limited to Details + Add to client. `/api/suggest-projects` still exists but is no longer reached by the Follow-up flow; its old card/chat components (`SuggestionCard`, `AssistantChatPane`, `ProjectWhatsAppFlow`) are now orphaned. **Production-verified live:** modal calls `/api/project-finder` (200); النرجس → 2 exact (coordinate-verified, ~1.75s); العارض → 1 exact (~1.75s); الدرعية → 0 false-exacts; no-preference → "ask the client" prompt; no 504; `used_ai` parse/explain = false. See [followups-workspace.md](followups-workspace.md).) | 2026-06-28 (**Direction narrowed to a single deterministic Project Finder; broad AI assistants unwired.** A new deterministic Project Finder engine became the one and only assistant surface: given a client's requirements it FINDS and RANKS the right projects — selection, scoring, ranking, and grouping are 100% code, geography **boundary-verified** by PostGIS point-in-polygon (`districts_for_points` against the 3,732 `district_boundaries` polygons), with results split into **four location-centric groups**. The LLM is bounded to TWO touchpoints only — *parse* messy text into requirements BEFORE ranking, and *explain* each already-ranked match AFTER — and a hard `assertRankingUnchanged()` guard guarantees it can never change score/band/source/match_type/order. New: `api/project-finder.ts`, `api/_lib/projectFinder.ts`, `api/_lib/projectFinderAI.ts`, `api/_lib/geoVerify.ts`, `supabase/migrations/2026-06-28_project_finder_geo.sql`; `matchProjectsCore` gained an opt-in `verifyGeo` option (+ multi-district `req.districts[]` and a bathrooms dimension); `api/suggest-projects.ts` now passes `verifyGeo:true`. **Concurrently the broad AI assistants were UNWIRED** behind the static build flag `PROJECT_FINDER_ONLY` in `src/lib/featureFlags.ts` (code + DB models/data kept for rollback): the AI sales agent (`ai_chats`), the reel copywriter (`copywriter_chats`), and the conversational match co-pilot (`matching_chats` — with its next-best-action / lead-temperature / WhatsApp-draft / task-creation tools) are hidden from the sidebar and their routes render a `RetiredAssistantNotice`. The Follow-up workspace assistant panel is now a focused launcher for the deterministic Suggested Projects finder (no free-text chat); the modal's right-pane chat and the chat-handoff card actions (Pitch / WhatsApp / Ask) are hidden under the flag. See [ai-agent.md](ai-agent.md), [copywriter-intelligence.md](copywriter-intelligence.md), [followups-workspace.md](followups-workspace.md), [data-storage.md](data-storage.md).) | 2026-06-28 (**Geography unified onto the `location` cascade field.** Every project / listing / client now stores district/city/region in ONE `location` cascade compound ({region,city,district} ids; multi-arrays for clients) instead of separate `district_lookup`/`city_lookup`/`region_lookup`/`preferred_districts`/`preferred_cities` fields. The engine is unchanged in behavior: the caller extracts each record's `location.district`/`location.city` ids and passes them (plus display names resolved from the geography records) to `scoreProject` via the GeoContext (`projDistrictId`/`projCityId`/`projDistrictName`/`projCityName`) — district id drives the exact tier, city id the same-city tier, exactly as before. The market-listings AREA pre-filter now filters `data->'location'->>'district'`/`'city'` (with swapped partial indexes `idx_records_market_loc_district`/`_city`). Client requirements read `location.district`/`location.city` arrays. New `matchAgent.ts` helpers: `recordLocationIds`, `geoNameMap`. Migration: `supabase/migrations/2026-06-28_location_cascade_cutover.sql`. See model-builder.md for the field type.) | 2026-06-27 (**Market Listings now AREA-pre-filtered server-side, with full ~46k coverage.** `market_listings` is now populated (~46,315 external Aqar for-sale ads), so the third source is live with real data — and `matchProjectsCore({ includeMarket })` no longer pulls an arbitrary order-less slice of it. Before scoring, the engine narrows the ~46k listings to the client's relevant AREA in Postgres: (1) when the requested district resolves with a centroid, every listing in that district OR any district whose centroid is within **20 km** of it (`nearbyDistrictIds` → `.in('data->>district_lookup', ids)`); (2) else, when only a city resolves, every listing in that city (`.in('data->>city_lookup', [cityId])`); (3) else (no area signal) the **newest 4,000** listings in a stable `created_at`-desc order (a capped, logged scan — never an arbitrary slice). The same deterministic `scoreProject` then tiers/scores them in memory alongside our_projects + all_projects (budget/type/area/etc. are applied there, not in the pre-filter). A market read failure is non-fatal and logged — the other two sources still return. Backed by two **partial expression indexes** on `records((data->>'district_lookup'))` / `((data->>'city_lookup'))` scoped to the market_listings model id (measured: ~624ms/page nested-loop scan → ~70ms/page index scan, flat across pages, on the النرجس case of 16,871 in-area listings). New helpers in `matchAgent.ts`: `nearbyDistrictIds`, `loadMarketByFilter`, `loadMarketNewestCapped`. Migration: `supabase/migrations/2026-06-27_market_listings_lookup_indexes.sql`. The grouped `/api/suggest-projects` endpoint surfaces all three sources with a source badge + verify banner exactly as before. See [data-storage.md](data-storage.md) for the lazy-load posture of the market_listings model.) | 2026-06-27 (**Third source — Market Listings (`market_listings`).** The Suggested Projects modal now matches across THREE sources, each labelled on the card: `our_projects` (مشاريعنا), `market_listings` (إعلانات السوق — external scraped public ads), and `all_projects` (كل المشاريع). A market listing is a single unit (scalar `price`/`area`/`bedrooms` + real per-listing lat/lng + `district_lookup`); `adaptListingToScorable` maps it into the project shape so the ONE deterministic `scoreProject` grades all three identically. `matchProjectsCore(req, {alwaysScoreAll, includeMarket})` — the grouped endpoint passes both so all three sources show side by side; the LLM `match_projects` tool calls it with NO opts so its two-tier fallback output is unchanged. **Source is now orthogonal to quality:** an all_projects/market match is bucketed by how well it matches (exact/nearby/budget/location) and carries a source badge + a verify banner (both all_projects and market are `requires_verification`, low confidence), instead of being auto-dumped to fallback. A **source filter** (الكل / مشاريعنا / إعلانات السوق / كل المشاريع, with counts from `metadata.source_counts`) sits above the cards. Market Listings has 0 rows today, so it shows nothing until listings are scraped in; the pipeline degrades gracefully (empty source). New: `adaptListingToScorable` + `MatchSource` type + `MatchCoreOptions` in `matchAgent.ts`; `SourcePill` in `SuggestionCard.tsx`; source filter in `SuggestedProjectsModal.tsx`. Tests: `marketListingAdapter.test.ts` + market/source cases in `suggestGroups.test.ts`.) | 2026-06-26 (**Assistant Product Completion — grouped "Suggested Projects" modal + Add-to-client + audit log + feature flag.** The Follow-up side panel's "Suggested Projects" button now opens a LARGE modal: top = current preferences + missing-preference warnings; left = grouped, ranked project cards in five honest buckets (`exact_matches` / `nearby_alternatives` / `budget_matches` / `location_matches` / `fallback_matches`); right = the same assistant chat for explain/compare/pitch/WhatsApp. Grouping is **deterministic, no LLM** — a new `POST /api/suggest-projects` runs the SAME `matchProjectsCore` scoring brain (extracted from the `match_projects` tool, which is unchanged) and a pure `groupMatchResults` that buckets candidates and stamps each with a `data_confidence` (high/medium/low) + normalized `data_gaps` (e.g. `missing_price_range`, `missing_project_coordinates`, `legacy_text_match_only`, `requires_verification`). Honest about the sparse data: a project with no price NEVER enters `budget_matches`; distance is district-CENTROID based (no project pins exist). Each card has actions: Why / Compare / Pitch / WhatsApp / Open details / **Add to client** (appends to `clients.preferred_projects` via the version-aware store write — user-triggered only). Every run writes one row to the new `assistant_recommendation_runs` audit table. The whole feature is gated by the `feature_flags.sales_assistant_enabled` kill switch (fail-open). The modal reads the UNSAVED follow-up preference draft, so recommendations reflect what the rep is editing before Save. See [followups-workspace.md](followups-workspace.md).) | 2026-06-25 (**Lookup-first matching with dual-read (Saudi geography layer):** the engine now resolves the requested district to the authoritative `districts` record (id + real centroid) via a new `resolveRequestedDistrict` helper, and a project whose `district_lookup` equals the resolved id is an EXACT relational match. The old text fuzzy-match on `preferred_neighborhoods` + per-project averaged centroids is RETAINED as the dual-read fallback for projects not yet backfilled. New geo helpers in `src/lib/locationUtils.ts`: `pointInPolygon`, `pointInDistrict`, `distanceBetweenCentroids`, `haversineKm`, `normalizeArabicDistrictName`, `normalizeEnglishDistrictName`. See [record-management.md](record-management.md) and [data-storage.md](data-storage.md).) | 2026-06-21 | 2026-07-19 (**Map picker: element-rule areas are directly editable too (new).** Same drag-to-own pattern as selected districts: each element rule's compiled area (radius circle / direction band / road buffer / zone) renders its LARGEST ring with drag handles (decimated to ≤80). Dragging a handle — or right-click-deleting a vertex — converts the edited ring into a `drawn_area` of the SAME polarity (exclude rules become excluded drawn areas), labeled by district coverage with a fallback to «منطقة مرسومة: <اسم العنصر>», replacing the element rule. Untouched rules keep following the element + km (the chip −/+ steppers still resize WITHOUT converting — the two paths coexist: stepper = stay element-bound, drag = become a custom shape). Clipped direction bands that arrive as MultiPolygon draw their non-largest pieces display-only. Handles pause during draw mode; stale preview rows for just-converted rules are skipped so a shape never renders twice.) | 2026-07-18 (**Map picker: SELECTED districts are directly editable (new).** Tapping a district to include it (or opening the picker with saved/option-list district rules) now shows the highlighted area WITH vertex/midpoint handles (its ring decimated to ≤80 handles): dragging any handle — or right-click-deleting a vertex — converts the edited ring into a `drawn_area` item labeled by district coverage («منطقة مرسومة: النرجس»), replacing the district rule with the custom shape; untouched selections stay ordinary district rules matching the official boundary (keeping the district-id fallback for coordinate-less records). Right-clicking an UNselected district still copies its boundary as a new drawn shape without selecting it. District edit handles pause during draw mode. The editor's add-district list now auto-opens the picker after adding an include district (same always-see-the-area flow as element rules; excludes don't jump to the map). NOTE: an edited district area matches by polygon only — records without coordinates that carry the district id no longer match once the rule is converted.) | 2026-07-18 (**Element rules are now BOUNDED, VISIBLE, RESIZABLE areas (user decision — behavior change).** Direction rules («جنوب طريق الملك سلمان») no longer mean an unbounded half-plane: they now mean "on the chosen side of the road AND within `distance_m` of it" — a band. `DirectionalCondition` gained `distance_m` (default 5 km, `DIRECTION_DEFAULT_M`); rules saved before the change compile with the default, so every existing direction rule is bounded too. SQL: `wassell_compile_geo_items` compiles direction rules to geom = the road's distance buffer (ref line simplified + normalized to its longest single linestring) with ref_geom+direction kept for the side test; `wassell_geo_match`'s polygon arm was restructured to LATERAL per-table GiST probes (a UNION ALL candidate subquery was seq-scanned — gate for the جنوب-طريق-الملك-سلمان client: 34,394 ids/11.8s → 3,504 ids/~1.5s). The TS twin (`api/_lib/geoMatch.ts`) applies the same bound. EDITOR: direction rules take a «العمق من الطريق (كم)» input (default 5), and adding ANY element rule auto-opens the map picker so the rep immediately SEES the area. PICKER: element rules are now editable in place — each rule's chip shows its covered-listings count (new `listing_count`/`project_count` per row from `wassell_preview_geo_items`, which also clips direction buffers to the matched side for display) plus a −/+ 0.5km stepper that recompiles and redraws the shape live, and a delete ×; Apply re-emits the resized rules. TOO-BIG GUIDANCE: any rule covering more than the market cap (4,000 listings, mirrors MARKET_SCAN_LIMIT) turns red with a banner naming THAT rule, its exact count, and the instruction to shrink it — replacing the generic "narrow your preferences" dead end. Migrations: `2026-07-18_bounded_direction_rules.sql`, `2026-07-18_preview_direction_display_clip.sql` (+ live-applied `direction_ref_single_line`, `geo_match_lateral_index_scan`) — all applied to prod.) | 2026-07-18 (**Direction-rule finder timeout fixed at the root (perf, live incident).** A client whose only location preference was a direction rule («جنوب طريق الملك سلمان») gated ~34k records and the finder request died at the Edge 25s ceiling — the timeout-recovery panel showed with zero results. Measured: geo match 11.8s + market scan 24.7-37s. Three DB-side fixes (no app code): (1) `wassell_compile_geo_items` stores a SIMPLIFIED direction reference line (ST_SimplifyPreserveTopology 0.001° ≈ 111m; 362-vertex King Salman Rd → 60) — the per-point side-of-road test is O(ref vertices) × ~43k candidate points. (2) `wassell_geo_match` gained an EXACT-semantics bbox shortcut: a candidate beyond the ref line's bbox on the requested side is definitely a match (the closest point on the line lies within its bbox), so only candidates inside the road's lat/lng band run ST_ClosestPoint — geo match 11.8s → ~4.5s. (3) `wassell_market_candidates_json`: the p_record_ids branch evaluated `id = any(34k array)` LINEARLY per row AND detoasted each fat listing jsonb just to count — `listing_points` (the trigger-maintained geo side table) now carries typed `price`/`bedrooms`/`type_text`/`unit_age` columns (backfilled; `_sync_geo_point` keeps them fresh), and for unrestricted-scope users the geo-gate branch counts entirely over those typed columns, touching `records` only for the ≤ p_limit returned rows; filtered-scope users keep the legacy per-row-RLS path (now with hashed id membership + ids-only counting). Market scan 24.7s → 1.3s, identical totals verified (6,016). End-to-end: the client-scoped finder that 504'd now returns its 11 matches in ~seconds with the honest too-many-market-ads notice. Migrations: `2026-07-18_fast_direction_geo_gate.sql`, `2026-07-18_geo_match_direction_bbox_shortcut.sql`, `2026-07-18_listing_points_typed_filters.sql` — all applied live.) | 2026-07-18 (**Map picker: element-rule areas on the map, editable drawn shapes, district-coverage labels, undo-point (new).** (1) ELEMENT RULES NOW RENDER on the picker map as their COMPILED geometry — the new `wassell_preview_geo_items(p_items)` RPC (migration `2026-07-18_preview_geo_items_rpc.sql`) runs the REAL matcher compiler (`wassell_compile_geo_items`) against a throwaway client id inside one transaction, returns per-item simplified GeoJSON, and deletes the temp rows — so "ضمن 3 كم من X" shows the exact buffer circle, `inside_area` the exact zone, and north/south/east/west-of rules the reference road polyline plus a ~13km shaded band on the included side (the match itself stays unbounded; the band only shows direction). Terracotta = include, red = exclude, labeled with the same `describeLocationItem` chip text; display-only (rules edit from the chips list); a preview failure only costs the overlays. (2) DRAWN SHAPES ARE EDITABLE: outside draw mode every drawn area renders `editable` — drag a vertex or midpoint handle to stretch/shrink; right-click a vertex deletes it (below 3 points deletes the shape); edits debounce (500ms) back into the working `location_items` with recomputed labels. (3) DRAWN-AREA LABELS NAME THE DISTRICTS THEY COVER — "منطقة مرسومة: النرجس، العارض +2" (client-side ray-cast against the loaded district rings: district centroid inside the shape OR a shape vertex inside the district), falling back to the old numbered label when the shape covers no district; labels recompute as borders are edited. (4) RIGHT-CLICK A DISTRICT copies its official boundary into a new editable drawn shape (decimated to ≤80 vertices for usable handles) and clears that district's selection — "adjust this district's borders". (5) DRAW-MODE UNDO: a تراجع button + right-click removes the last clicked point so one bad segment doesn't force a full redraw. Footer now also counts element rules; hint texts updated.) | 2026-07-18 (**Select-all-filtered bulk selection.** Both client-scoped finder surfaces (the follow-up Suggested-Projects view + the standalone Project Finder page) gained a header «تحديد الكل / Select all» toggle beside «حفظ الخيارات»: one click selects EVERY match passing the CURRENT client-side refinement (score slider + refine hard filters, across all tabs); when all filtered matches are already selected it flips to «إلغاء التحديد / Clear selection» (removes only the filtered ones — filtered-out selections are never touched). Bulk save persists only the filtered∩selected set, so select-all → tighten the slider to 90% → save writes only the ≥90 matches. Badge on the button shows the filtered total; live-verified offline: 7 selected → threshold 90 → exactly the 3 ≥90 options saved.) | 2026-07-17 (**Unit-age (عمر العقار) preference wired end-to-end.** New clients dropdown preferred_max_unit_age (جديد فقط/حتى سنتين/حتى 5/حتى 10 — STRING values 0/2/5/10; live field added by migration 2026-07-17_unit_age_preference.sql, seedModels mirrored). Mapper: draftToMatchRequirements → requirements.max_unit_age (0 is meaningful). ENGINE: new unit_age scoring dimension (weight 6, renormalized like the rest): age ≤ max → 1, ≤ max+2 → 0.5, else 0; unknown age = requested-but-missing honesty penalty; CATALOG projects (our/all) are stamped unit_age 0 (new-development stock by definition — never penalized as unknown). Aqar free-text age parses via wassell_parse_unit_age (SQL, IMMUTABLE) ⇄ parseUnitAgeText (TS twin in matchAgent, tests): جديد→0, سنتين/سنتان→2, N سنوات/bare digits→N, أكثر من N→N+1, digit-less سنة forms→1; 99.93% live coverage (28/38k unparsed). MARKET DB pre-filter: wassell_market_candidates_json gained p_max_age (old signature DROPPED first — new param would otherwise create an ambiguous overload) — drops only listings whose PARSED age is known and over the max, null ages survive to scoring; age added to MARKET_SLIM_FIELDS (without it the slim projection would hide the field from the adapter). UI: field editable in both finder pref panels (EDIT_SLUGS/FILTER_SLUGS), chips in the finder prefs row (assistantContext) + chat-header prefs (prefChips), and market cards show an العمر chip (facts.unit_age; catalog cards skip the redundant implicit-new chip). Options snapshots inherit it via facts.) | 2026-07-13 (**Map picker: trackpad draw fix + on-map district names + landmark pins.** (1) DRAW CLICK FIX: with panning on, a trackpad's few-pixel wobble between mousedown/mouseup read as a DRAG, so the map panned and the vertex click never fired ('I click but it just moves around' — live report). Draw mode now pins the map (draggable:false, restored on exit; zoom controls still work) and the hint says so. (2) CLEAR BORDERS + NAMES: unselected district strokes bumped (0.35/1px → 0.65/1.5px, selected 2.5px), and every district shows its NAME on the map — transparent-icon markers at each polygon's largest-ring centroid, zoom-gated (≥11) so the city view stays clean. (3) LANDMARKS: curated geo_elements (landmarks/malls/universities/airports_transport, ~182 verified Riyadh anchors, direct RLS-scoped select) render as terracotta pins from zoom ≥11 with hover tooltips; names label in at zoom ≥13; pins turn unclickable during draw so vertex clicks pass through. All in DistrictMapPicker.tsx; landmark load failure costs only the pins (console.error, never the picker).) | 2026-07-13 (**Exclusion zones in the map picker.** Draw mode gained a polarity toggle («أريدها» gold / «استثناء» red; the preview polyline recolors mid-draft): an exclude-polarity shape becomes a drawn_area item with polarity exclude — no results inside it — labelled «منطقة مستثناة N» and rendered red on the map. No engine change needed (compile + the exclude-subtraction path already handled polygon geoms); verified live on prod: an exclusion box covering all 4 of a client's districts dropped the geo gate 476→0, and a box covering none of them left it at 476.) | 2026-07-13 (**Free drawing on the map picker (drawn_area location items).** The map picker gained a **«رسم منطقة» / Draw an area** toggle — MANUAL drawing (map click = add a vertex to a gold preview polyline, double-click or «إنهاء الشكل» closes the shape; Google REMOVED DrawingManager in Maps JS v3.65 — instantiating it throws and blanked the app on the first deploy of this feature, caught in the live smoke-test; do NOT re-add the drawing library). The rep can draw SEVERAL separate shapes — each completed shape becomes ONE independent location_items entry of the new kind **drawn_area** ({id, kind:'drawn_area', polarity, coordinates: closed [lng,lat] ring, label}), OR-unioned exactly like districts. Drawn shapes render gold on the picker (red if a saved exclude), are deletable via footer chips, and existing ones load for review; Apply re-emits the drawn set alongside the district selection (element rules untouched). Compile support in wassell_compile_geo_items (migration 2026-07-13_drawn_area_location_items.sql, applied to prod): the ring compiles via ST_GeomFromGeoJSON→ST_MakeValid to a plain polygon in client_pref_geometry — matched on the SAME GiST-indexed ST_Contains path as districts (fast; verified live: a probe box near النرجس compiled ok and grew هيثم's gate 476→1,549); malformed rings compile to needs_review, never dropped. TS twin api/_lib/geoMatch.ts gained the same branch + 3 tests; frontend shape + chip labels in src/lib/geo/locationItems.ts (newDrawnAreaItem, describeLocationItem → «منطقة مرسومة N») and the chat pref-chips reader. Known v1 edge: a client whose ONLY criterion is a drawn area (no city/type/budget) still hits the needs_preferences short-circuit — in practice a city is always set.) | 2026-07-13 (**Map-based district picker (tap-to-select) in the location editor.** The `location_items` editor (`LocationItemsEditor`, embedded in `ClientLocationField` — client form, finder edit-preferences panel, standalone finder) gained a third add-control: **«اختيار من الخريطة» / Pick on map** (enabled once a city is selected). It opens `src/components/DistrictMapPicker.tsx` — a full-screen modal Google Map (branded `WASSEL_MAP_STYLE`) rendering EVERY district of the selected city as its REAL boundary polygon via the new `wassell_city_district_shapes(p_city_id)` RPC (`supabase/migrations/2026-07-13_city_district_shapes_rpc.sql`, applied to prod): SECURITY DEFINER, returns `[{district_id, name, geojson}]` from `district_boundaries` simplified server-side (ST_SimplifyPreserveTopology 0.0002, 5-decimal coords — Riyadh's 189 districts ≈ 145 kB in one call). These are the SAME polygons the finder's point-in-polygon verification matches against, so what the rep taps is exactly what matches. Tap toggles the district as an INCLUDE rule (copper fill = selected, charcoal outline = unselected, red = saved EXCLUDE rules — display-only on the map, managed from the chips); hover shows the district name; footer shows the live selected count + names; **تم / Apply** rebuilds ONLY the include-district items in `location_items` (element rules + excludes untouched, existing rules keep their ids). Free-shape drawing (multiple independent polygons) is the planned next phase.) | 2026-07-13 (**Market-scan fast path + slim payload + concurrent fetch (~17s → ~8s three-source search).** Follow-up to the same-day index fix below — the per-row RLS call was the next bottleneck and is now gone for unrestricted users. (1) New `wassell_view_scope_class(uid, model)` → `'none'|'all'|'filtered'`, computed ONCE per statement: when a user's market_listings view scope is unrestricted (admin / no scope rule / mode `all` / zero conditions — every live profile with market access today), `wassell_market_candidates_json` provably doesn't need the per-row `wassell_can_view_record` call and SKIPS it (CASE-guarded); `'filtered'` keeps the exact per-row semantics; `'none'` returns zero rows. Verified equivalent on prod: same 6,345-row set + identical id fingerprint vs per-row RLS for admin, full-view non-admin, no-permission, and anon. (2) The RPC gained `p_fields text[]` (default NULL = full data, back-compat): the engine passes `MARKET_SLIM_FIELDS` (the ~21 keys the adapter/scorer actually reads) so each row's data is projected via a single `jsonb_each` pass — payload 15MB → 5.4MB (do NOT "simplify" to per-key `->` extraction: it re-detoasts the blob per key and measured SLOWER than full data, 9.9s vs 2.6s). RPC in-DB time for the 3-district admin scan: ~6.7s → **~2.6s**. (3) `matchProjectsCore` now STARTS the market fetch right after district resolution (never-rejecting promise, consumed by PASS 3), so its DB time overlaps the polygon-verification + name-map queries instead of running after them. (4) too_many short-circuit: when `total > p_limit` the RPC returns `rows:[]` instead of building + shipping a 4000-row agg the engine discards anyway. (5) The same fast path for whole-model reads: new `wassell_model_records_json(model_id, fields?)` replaces the finder's paged per-row-RLS `unified_records` reads for all_projects/our_projects (`loadModelRecords` in matchAgent, paged fallback kept) — 980 all_projects rows dropped ~6.4s → ~0.2-1.3s, identical row set verified by id fingerprint. Live end-to-end on app.wassel.re for سراج الجهني (3 districts × 3 sources): no-criteria/too_many ~16.6s → ~6s; the criteria-rich 519-result search ~17s → ~9.3-11.9s (avg ~10.4s; commit 27b17f5 also overlapped the ~2s geo-gate compile/match with the engine loads — the endpoint passes the gate as a PROMISE through FinderOptions/MatchCoreOptions.geoMatchIds and the core awaits it at first use. The remaining floor is the in-district JSONB detoast of 6,345 rows ~3.7s in-DB — VALUES-lateral and LATERAL-OFFSET-0 pre-extraction both measured no better; a further cut needs flat/generated columns, deliberately not done). Migration `2026-07-13_market_candidates_fast_path.sql` (applied live); `wassell_view_scope_class` also in `schema.sql` with a MUST-mirror warning next to the RLS helpers it classifies. Perf-testing gotcha: do NOT project jsonb keys via unnest(fields) + per-key `->` — it re-detoasts the blob per key and measured SLOWER (9.9s) than returning full data; jsonb_each in ONE pass is the right shape.) | 2026-07-13 (**Save-options-first exit guard.** The bulk **حفظ الخيارات / Save options** button moved from the results footer to the TOP header of every client-scoped finder surface (`SuggestedProjectsView` header — primary copper button with a live selected-count badge, next to a now-secondary **تم / Done** — and the standalone `ProjectFinderPage` header when a client is selected). The old footer bulk-save button is gone; the footer keeps the selected count plus an amber "لم يُحفظ أي خيار لهذا العميل بعد" hint until something is saved. **Leaving a client-scoped finder without having saved a single option this visit now asks for confirmation** — "هل أنت متأكد أنك لن تحفظ أي خيار لهذا العميل؟" via the shared `src/components/matching/LeaveWithoutSavingModal.tsx` (Stay is the primary action; "الخروج بدون حفظ" proceeds). The guard intercepts the Done button and Esc in `SuggestedProjectsView`, clearing the selected client on `ProjectFinderPage`, and arms a `beforeunload` browser prompt for tab close/reload. It arms only AFTER results were actually shown (an empty/failed search never nags) and disarms on ANY successful option write this visit — single save, bulk save, inline status set, eliminate, or reactivate.) | 2026-07-13 (**Finder failure honesty + market-scan index (live incident: 504 rendered as "no matching projects").** A search for a client with district `location_items` (سراج الجهني: النرجس/العارض/القيروان) consistently 504'd — `wassell_market_candidates_json` full-scanned ~51k market rows on `(data->'location'->>'district')` (no index, ~4.5s JSONB detoast) plus the per-row `wassell_can_view_record` RLS check (~4–8s), measuring ~30s against `/api/project-finder`'s 25s function ceiling. The SPA then LIED about the failure: on HTTP/2 `res.statusText` is the empty string, so `fetchProjectFinder`'s non-JSON-body fallback threw an `Error('')`, `SuggestedProjectsView`'s `error` state stayed falsy, and the UI fell through to the **"لا توجد مشاريع مطابقة" empty state** — while the catalog actually held 14 exact-district matches (5 scoring 100). Fixes: (1) `fetchProjectFinder` uses `||` (not `??`) so a blank error message never masquerades as success — a failed request now shows the red error box; (2) new DB index `idx_records_model_district` on `records (model_id, (data->'location'->>'district'))` (applied live with CONCURRENTLY; migration `2026-07-13_records_model_district_index.sql`) — the market RPC drops ~30s → ~2s and the full three-source search responds ~17s with 516 exact-district results. Known follow-up: ~17s is still near the 25s ceiling; the per-row RLS call in the market RPC is the next bottleneck.) | 2026-07-04 (**"إرسال للعميل / Send to client" on every finder + option card (QA-002), collapsible preferences area (QA-001).** (1) Every Project Finder result card (`FinderCard`, client-scoped surfaces only — hidden with `hideClientActions`) and every Client Options card (projects + market listings) now carries a copper **Send to client** action that WhatsApps that project/listing to the CONNECTED client without opening the record: a prepared `chat_templates` message is reused (project → `project_id` link via the re-wired `ProjectWhatsAppFlow`; listing → `listing_id` + `status:'ready'` via the new `src/components/matching/ListingWhatsAppFlow.tsx`), and when none exists the EXISTING creation flow runs right from the card (project: deterministic `resolveProjectFacts`→`composeProjectMessage` preview→save; listing: `ListingMessageModal` AI text + text-removed photos → on approve the flow continues straight into the chat). The chat opens as `StartChatModal` with the client preselected + body prefilled; the template's gallery/cleaned photos ride along as separate messages. **Video support:** `sendProjectImageMessages` now sends each ride-along by MIME (`video` for video bytes, extension fallback for typeless servers), and direct video-FILE URLs on the source (`all_projects.project_videos`, listing `video_urls`) are appended via the new `directVideoUrls` helper — HLS streams (Aqar/Cloudflare `.m3u8`) and page links are excluded (not uploadable to WhatsApp). Shared pure helpers + tests: `src/lib/matching/sendToClient.ts` (`findProjectTemplate` / `findReadyListingTemplate` / `templateListingImageUrls` / `templateBody` / `directVideoUrls`), `__tests__/sendToClient.test.ts`. `ProjectWhatsAppFlow` is UN-orphaned. (2) **QA-001:** the finder's always-visible preferences-chips + missing-warnings block is now COLLAPSIBLE (chevron toggle; collapsed = one thin row with count + truncated value summary + missing-count badge); `SuggestedProjectsView` gained `defaultPrefsCollapsed`, set by the Client-Options popup host (`ClientOptionsModal`) so popup results get the vertical space — full-page hosts stay expanded. The edit-preferences panel already toggled + auto-collapsed on Search (2026-07-02). See [clients.md](clients.md), [chats.md](chats.md).) | 2026-07-04 (**Saved market-listing option cards show the quality grade even when saved before the feature (fix).** The A–D `QualityBadge` on a Client Options market-listing card read ONLY the snapshot (`facts.quality_grade`), which was captured at save time — but the finder only started writing quality into `facts` on 2026-07-02, so every option saved before then (all 252 live market-listing options at the time, saved ~2026-07-01) showed a BLANK chip. `ClientOptionsTab` now falls back to the LIVE listing record in the slim `market_listings` store (which the DB trigger keeps `quality_grade`/`quality_score` on for every row, both riding the slim store) via a new `qualityBySourceId` map — the exact same "snapshot-first, live-store fallback" pattern already used for the ad-id chip. New saves keep using their snapshot; pre-feature options now render the current grade; a listing dropped from the catalogue simply shows no chip. Verified live: 252/252 pre-feature options had a null snapshot grade but a real live grade in the 47k-row store. `src/pages/Clients/components/tabs/ClientOptionsTab.tsx`. See [clients.md](clients.md).) | 2026-07-02 (**Listing quality in the finder.** Every market_listings result now carries the DB-computed `quality_score`/`quality_grade` in its `facts` (adapter → scoreProject facts → card → saved-option snapshot), rendered as the shared colored A–D chip (`src/components/market/QualityBadge.tsx`) on finder cards (both surfaces) and on saved market-listing option cards (`ClientOptionsTab`, incl. the manual `AddOptionModal` snapshot). Ranking: quality is an EQUAL-match-score TIEBREAK ONLY — server `byBandThenScore` and the client default score sort (`finderRefine.sortItems`) append `quality_score` as the final key; relevance always dominates, no minimum-grade filter, no change to scoreProject or the too_many guard. Items without a quality score (projects/units) tie-break as 0.) | 2026-07-02 (**Location matching + grouping fixes (3 bugs from field screenshots).** (1) *Wrong / missed "same district" matches:* the SPA now sends the client's selected districts as AUTHORITATIVE record ids (`requirements.district_ids`, built in `draftToMatchRequirements` from the location cascade + `preferred_districts` + `location_items` include-rules); the engine matches on those ids directly and runs the fuzzy per-name ILIKE resolution ONLY when no id resolved — so a same-named district in another city can no longer be admitted as "في نفس الحي", nor can a selected district silently mis-resolve and mark its own projects "outside". Multiple selected districts stay OR — inside ANY one = exact. (2) *Grouping is now purely location-centric:* `finderGroupFor` groups by the verified `location_tier` alone; the old "band `partial` → broader_fallback" override is GONE, so a wrong-unit-type or no-price project INSIDE a requested district stays under "في نفس الحي" (its weak fit still shows via the band badge + in-group ordering) instead of being falsely labelled "خارج المنطقة المطلوبة". (3) *Distance everywhere, measured to the nearest selected reference:* `scoreProject` computes `distance_km` for EVERY tier (same-city + fallback included), to the NEAREST of ALL selected references — every requested district centroid PLUS the client's selected location ELEMENTS (`location_items` element anchors — within_radius / within_distance / inside_area — resolved endpoint-side from `geo_elements` and passed as `FinderOptions.refPoints`) — and names WHICH one via the new `nearest_ref_name` field (on `ScoredProject` → `MatchResultItem` → `FinderMatch`). The card distance pill now reads "~4.2 كم من النرجس", and the deterministic nearby/same-city/fallback explanations name the closest selected area (fallback: "خارج الأحياء المحددة (يبعد حوالي X كم عن «Y»)"). Tests in `projectFinder.test.ts` + `requirements.test.ts`.) | 2026-07-02 (**Multi-select preferences now match as OR (any acceptable option counts).** When a client selects MULTIPLE acceptable options for one preference, they are alternatives — a result matching ANY of them now gets full credit for that dimension. (1) **Unit types:** `preferred_unit_type` is a multiselect but only the FIRST value was sent — a client wanting "شقة أو دور" saw every دور project scored `type: 0` and band-capped into the fallback tab. `draftToMatchRequirements` now sends ALL selected types as `requirements.property_types[]` (`property_type` stays = the first for single-type paths); the engine (`MatchRequirements.property_types`, `scoreProject`) unions the synonym expansion across all requested types and a project offering ANY of them earns full type credit — the wrong-type band cap fires only when NONE match. The market-listings DB pre-filter unions the ILIKE terms across all types too (previously only the first type's listings ever became candidates), and the `match_projects` AI-agent tool + the finder's LLM text parser can now express type alternatives (`property_types`). (2) **Cities:** the `location` cascade allows multiple cities but only the first was sent; `cities[]` now flows through and `matchProjectsCore` resolves EACH into the city-match set (`reqCityIds`), so a project in ANY acceptable city city-matches. (3) **Districts** already had OR semantics (`districts[]`, 2026-06-29) — unchanged. Deliberately NOT changed: `preferred_amenities` keeps proportional credit (amenities are cumulative wants, not alternatives — a client wanting "غرفة خادمة ومسبح" wants both). No scoring weights, bands, or tiers changed. Tests: `requirements.test.ts`, `projectMatchScoring.test.ts`.) | 2026-07-02 (**Finder embedded in the Client Options popup (new surface).** The shared `SuggestedProjectsView` gained a FOURTH host: the new `ClientOptionsModal` (`src/components/clients/ClientOptionsModal.tsx`) — the client-options popup opened from the WhatsApp chat header — embeds the finder inside the popup itself ("البحث عن خيارات أكثر / Find more options" swaps the popup body to the finder, `followupId=null`, seeded + geo-gated from the client; "تم / Done" returns to the options list). **Same-day fix (live report):** embedded in the popup on a laptop-height screen, the finder's open edit-preferences panel — part of the fixed chrome — consumed the whole height, leaving the results area 0px with nothing scrollable; the panel is now height-capped (max-h-[42vh], own scrollbar) and auto-collapses when Search is pressed so results are visible immediately (benefits the full-page finder on short viewports too), and the popup uses near-full-screen height. `ClientOptionsTab` also gained an `onFindMore` prop, so the Client 360 Options tab now links to the full-page client finder (`/model/clients/:id/projects`) too. See [chats.md](chats.md), [clients.md](clients.md).) | 2026-07-02 (**Market-listing finder cards: "التواصل مع المعلن / Contact advertiser" button.** Every `market_listings` result card (both finder surfaces — standalone `/project-finder` AND the Follow-up/client `SuggestedProjectsView`, including the standalone no-client mode) now carries a compact copper **Contact advertiser** action next to Details: if the listing already has a scraped `advertiser_phone` it opens the in-app WhatsApp `StartChatModal` immediately (pre-filled with the Aqar listing link); otherwise it enqueues the REGA advertiser lookup (`/api/rega-lookup` → Browserbase worker), shows a "جاري جلب الرقم…" spinner, and auto-opens the chat when the worker's write lands via Realtime — after which every later click opens the chat directly (the phone + `advertiser` link persist on the listing). Failures surface as an error toast. Shared component `src/components/market/ContactAdvertiserButton.tsx` + shared hook `src/lib/regaLookup/useContactAdvertiser.ts` (extracted from `ContactAdvertiserPanel`); reads the listing from the market_listings slim store by `project_id`, so no extra fetch per card. Same button also renders on the client Options tab's market-listing cards — see [clients.md](clients.md), [record-management.md](record-management.md).) | 2026-07-01 (**Project Finder is now reachable from the CLIENT profile.** Per the user, the finder can be run directly on a client. The Client 360 header (`DetailHeader`) gained a prominent copper **"الباحث عن المشاريع / Find projects"** button that navigates to a new full-page route `/model/clients/:recordId/projects` (`ClientProjectsPage`) — a thin wrapper (mirroring the follow-up `SuggestedProjectsPage`) that loads the client, seeds `prefDraft` from the saved client data, and renders the SAME shared `SuggestedProjectsView` with `followupId=null`. So the client-scoped finder has the full feature set — map/list view, refinement toolbar, edit-preferences + re-run, and saving into the client's unified `client_property_options` — geo-gated by the client (client_id + their saved `location_items`); **"تم / Done"** returns to the client profile. `SuggestedProjectsView` already tolerates a null `followupId` (the finder-handoff write is guarded; audit/save attach via `clientRec.id`). Route precedes the generic `:recordId` detail route (like the follow-up one). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Standalone Project Finder: pick a client → seeded, geo-gated search + full client-option actions; unified location editor.** The sidebar `/project-finder` tool reached parity with the Follow-up finder. (1) A **client picker** (optional, top of the filters panel) searches the `clients` records by name/phone/code; selecting one SEEDS the pickers from that client's saved preferences (`location` + `location_items`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`, `preferred_bedrooms`→bedrooms), passes `client_id` + the current `location_items` to `/api/project-finder` (so the PostGIS geo gate compiles + narrows for that client), and turns EVERY result card into a full client-option control — inline status selector (`onSetStatus`), bulk-select + "Save selected to client options" footer, and the eliminate-with-reason modal — writing into that client's `client_property_options` (`added_from:'project_finder'`). A "clear client" X returns to pure discovery (cards show Details only). (2) The `location` field now renders the **unified `ClientLocationField`** (district include/exclude chips + geo-element rules with "+ add district / + add element") instead of the bare cascade — threaded via `modelId`/`onPatch` into `DynamicField`, writing both `location` and `location_items` into the draft. Without a selected client, district include-rules still tier results (via the `requirements.districts` mirror) but geo-element rules don't gate (the server geo gate needs a `client_id`). All in `ProjectFinderPage.tsx`; handlers mirror `SuggestedProjectsView`. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Finder results now have a MAP view (list ⇄ map toggle).** Per the user — "when presenting listings give me an option for a map view." Both finder surfaces (standalone `ProjectFinderPage` + Follow-up `SuggestedProjectsView`) gained a **List / Map** segmented toggle in the shared `FinderRefinementBar`. In map mode the active tab's results — the pinned OUR-projects **plus** that tab's other matches — render on the branded `WASSEL_MAP_STYLE` Google basemap as pins colored by SOURCE (green = our projects, chocolate = market, charcoal = listings DB), clustered by density (`MarkerClusterer` + `buildClusterIcon`), auto-fit to the pins. Clicking a pin opens an InfoWindow summary (source · score · name · district/city · distance · price) with a **"التفاصيل / Details"** button that runs the same `onOpenDetails` as a card. New component `src/pages/Followups/components/FinderMapView.tsx` (shared, read-only — client-option actions stay on the list). Coordinates now flow from the engine: `scoreProject` puts `facts.latitude`/`facts.longitude` (the resolved pin — verified polygon centroid when geo-verification ran, else the stored lat/lng) in `matchAgent.ts`; the finder endpoint passes `facts` through unchanged. A legend + coverage note ("N on the map · M without coordinates") sits under the map; the toggle only appears once results exist. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder cards: ad-ID chip + inline client-option status selector; district include-rules now land results in the exact-district tier.** (1) Market-listing cards now show a small ad-id chip (the external Aqar ad number, for looking the ad up) — reformatted to "@<external_id>" on 2026-07-04, when the id also reached the finder MAP pin hover titles, the Client Options tab cards, and the manual add-option picker (see clients.md) — `scoreProject` puts `facts.external_id`, `adaptListingToScorable` carries it through. (2) On the Follow-up finder EVERY card gained an inline STATUS `<select>` (main focus / reserved / interested / presented / suitable / not interested / closed / eliminated) that ENSURES the client option exists (creating it if new) THEN applies the chosen status in one step — `main_focus` routes through `setMainOption` (single active main), `eliminated` opens the reason-notes modal, and any non-eliminated pick reactivates a previously-eliminated option. This REPLACES the old per-card Save-as-option / Eliminate / Reactivate buttons (`StatusSelect` in `FinderCard.tsx`, colored to the status badge, RTL-safe native select; `onSetStatus` in `SuggestedProjectsView.tsx`). Bulk-select checkbox + footer bulk-save are unchanged; the standalone Project Finder (no client) still shows Details only. (3) A district picked in the unified location box is stored as a `location_items` district INCLUDE-rule (it drives the PostGIS geo gate), NOT the `location` cascade — so `draftToMatchRequirements` now mirrors those include-rule district ids (with the label stashed on the item as a name fallback) into `requirements.districts`; without it every gated result fell to the "same city / different district" tier even though it sat inside the requested district. Test in `requirements.test.ts`. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder result tabs: 3 tabs (Same district / Nearby / Alternatives) + our-projects pinned to the top of EVERY tab.** Per the user: the four engine tiers now collapse to THREE display tabs — "Same city" is dropped (the whole set is already the same city) and its items fold into **Alternatives** (= same_city + broader_fallback) — and OUR portfolio (source `our_projects`, ≥70) is lifted out of the tiers into a pinned "مشاريعنا / Our Projects" list shown at the TOP of every tab, best-first (score desc → closest), followed by that tab's other matches under a "خيارات أخرى / Other options" divider. Pure client-side display: shared `buildFinderTabs` + `DISPLAY_TAB_KEYS`/`DISPLAY_TAB_LABELS` in `finderRefine.ts`, wired into BOTH `ProjectFinderPage` and `SuggestedProjectsView`; tab badge = pinned-ours + that tab's tier count; the tier list stays windowed/infinite-scrolled while our-projects always render in full. The engine's four-group response is unchanged. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Geo gate ~5× faster + un-truncated (was 7s/capped-at-1000).** Follow-up to the timeout band-aid below. (1) PERF: `wassell_geo_match` was a 7.05s seq-scan of all ~47.6k project+listing points with a per-row geometry fn. Rewrote it to use the GiST point indexes — polygon/buffer items (within_radius/inside_area/district, `geom NOT NULL`) via an `ST_Contains` spatial JOIN (**~38ms**, was ~7s), direction items (halfplane, `geom NULL`) via a GiST bbox pre-filter that drops the far side (27,019/47,580 for the north-of rule) then the exact `wassell_geo_dir_match` (**~1.5s**, was 7s). Verified identical record-id sets on prod before the swap (direction client 8046=8046; synthetic 3km polygon 349=349). Migration `2026-07-01_geo_match_index_optimized.sql`. (2) CORRECTNESS: the endpoint's row-returning `wassell_geo_match` call was capped at PostgREST's ~1000-row response (the area can be 8046), silently truncating the gate to 1000 AND non-deterministically (market count flickered 329↔489). Added `ORDER BY record_id` + a new `wassell_geo_match_ids(uuid)→uuid[]` (one row, no cap, scan runs once); `api/project-finder.ts` calls it instead. Live element-only finder: **8.7s → 3.4s** with the FULL 8046-record gate. Follow-up: clip direction halfplanes to the region polygon at compile time to put them on the index path too. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Geo gate was silently timing out for authenticated users — the element never narrowed anything.** Sibling fix to the market-AREA change below: even after it shipped, the element-only client STILL saw `geo_filter:null` + `needs_district`. Root cause: `wassell_geo_match` scans all ~46.6k project+listing points with a per-row geometry predicate (7.05s EXPLAIN ANALYZE for a Riyadh-wide rule), tripping the `authenticated` role's 8s `statement_timeout`; the endpoint's catch left `geoMatchIds` null. As SECURITY DEFINER fns, a function-level `statement_timeout` overrides the caller's value, so `alter function wassell_geo_match / wassell_compile_client_geo set statement_timeout='20s'` (migration `2026-06-30_geo_match_statement_timeout.sql`, applied to prod) lets the scan finish. Verified live: `geo_filter` now applied, the element-only client sees **489 in-area market listings** (was 0), no district picked (~8.7s; perf follow-up = make polygon items index-usable / clip direction halfplanes to the region). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Market listings now respect a `location_items` AREA — no district pick required.** The user set a geo-element rule ("داخل النخيل مول") but the finder still said "حدّد الحي لعرض إعلانات السوق" (needs_district) and hid market ads — defeating the point of the element. The market scan now scopes by the client's geo-matched record-id set when no district is set: `wassell_geo_match` already returns the in-area LISTING ids (`listing_points` populated, 46,608 rows; النخيل area = 7,904 listings). The market-candidates RPCs gained an optional `p_record_ids uuid[]` (migration `2026-06-30_market_candidates_geo_ids.sql`, applied to prod) that scopes by `r.id = any(p_record_ids)` instead of the district filter — same DB-side budget/bed/type filters, RLS re-check, `ORDER BY r.id`, count-then-paginate `too_many` guard. `matchProjectsCore` market block: scope = district when set, ELSE the `geoGate` area, ELSE `needs_district`. An element-only client now sees market ads (النخيل: 7,904 → 2,595 villas → 911 in 3–4M); a too-broad area still returns `too_many` asking for budget/bedrooms. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder edit-preferences panel now shows + edits the geo-element / district rules (`location_items`).** Bug: the panel rendered the `location` field via a bare `DynamicField`, so the unified `ClientLocationField` (district + "within radius of a landmark" chips with "+ إضافة حي / + إضافة عنصر" controls) never appeared — selected geo-elements weren't shown and there was no way to add one. Fix: thread `modelId` + `recordId` + `onPatch` into that `DynamicField` (the exact trigger that makes it render `ClientLocationField`, same as `PreferenceSummary`), so the box now shows the saved chips + add controls and writes both `location` and `location_items` into `editDraft` (location field spans full width). Because the server geo-gate compiles from the SAVED client (`wassell_compile_client_geo(p_client_id)` reads `clients.data.location_items` at request time), the "بحث / Search" button now **persists the edited prefs (incl. `location_items`) to the client first** (version-safe, only when changed) THEN re-runs the match — so a district/element rule actually narrows the results. The separate "Save to client" button was merged into Search (caption: "تُحفظ التفضيلات للعميل عند البحث"). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md), [client_geo_matching](client_geo_matching).) | 2026-06-30 (**Edit client preferences on the finder page → re-run the search.** The full-page finder (`SuggestedProjectsView`) gained an "تعديل التفضيلات / Edit preferences" header toggle that opens an inline editor of the client's preference fields (those present on the live model: `location` cascade, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_bedrooms`, `preferred_amenities`) via `DynamicField` into a local `editDraft`. "بحث بالتفضيلات الجديدة / Search" rebuilds the SERVER-SIDE requirements (`buildReqs` — also maps `preferred_bedrooms.min` → the matcher's single `bedrooms`) and RE-RUNS `/api/project-finder` (a genuinely more-detailed search, not just the client-side slider/refine). The view's fetch was refactored from a once-on-mount snapshot into a reusable `runSearch(draft)` (abortable; resets slider/refine; re-seeds the 100%-auto-select); preference chips + `ctx` now reflect the last *searched* draft. Each search also updates the workspace hand-off so the edits carry back, and an explicit "حفظ التفضيلات للعميل / Save to client" persists `editDraft` to the client record version-safe (`saveRecord` + `expectedVersion`, same posture as `PreferenceSummary`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Follow-up Suggested Projects is now a FULL PAGE, not a modal.** Per the user — "most of the time we will use it here" and "it still doesn't look good" — the cramped 860px modal was replaced by a full-page route `/model/followups/:recordId/projects` (`SuggestedProjectsPage` → shared `SuggestedProjectsView`). It opens in the SAME tab via client-side nav from the workspace's "المشاريع المقترحة" launcher, stays CONNECTED to the follow-up (its id drives the recommendation audit log + saves into THAT client's `client_property_options`), lays cards out in a responsive 1/2/3-column grid inside a centered max-w-6xl, and carries the same refinement toolbar + images + bulk-save. A prominent **"تم / Done"** button (header + footer, plus Esc) returns the rep to the follow-up record (`navigate('/model/followups/:id')`). Preferences ride a module-singleton hand-off (`src/lib/matching/finderHandoff.ts`) the panel sets at click time (so unsaved edits are honored); on a cold URL load the page falls back to the saved client data, and on return the workspace restores the hand-off draft then clears it. `SuggestedProjectsModal.tsx` was deleted; `SalesAssistantSidePanel` now navigates instead of opening a modal. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Refinement toolbar now in the Follow-up Suggested Projects modal too (shared with the standalone page).** The score slider + sort + hard-filter "Refine" panel were extracted to a shared module (`src/lib/matching/finderRefine.ts` — `SortKey`/`Refine`/`passesRefine`/`sortItems`/`refineGroups`/`FETCH_FLOOR`) + a shared component (`FinderRefinementBar.tsx`), and wired into BOTH `ProjectFinderPage` and `SuggestedProjectsModal` so they behave identically. The modal already fetched everything ≥70; now it filters/sorts that set client-side, its group-tab counts + footer follow the refinement, bulk-save only saves cards still VISIBLE under the active refinement (never slider/refine-hidden ones), and it auto-hops to the first non-empty tab when refining empties the active one. This was the user's primary surface ("most of the time we will use it here"). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder cards now show the listing/project main image.** Every `FinderCard` (standalone Project Finder + Follow-up Suggested Projects modal) renders an `h-40` hero thumbnail at the top. The engine adds a single `facts.image` in `scoreProject`: market listings carry a raw photo URL (`adaptListingToScorable` reads `main_image_url`, else first `image_urls[]`); projects carry a files.id (`main_image`, else first `project_images[]`). The client resolves either through the existing `useSignedImage` hook — http(s) passthrough for the raw market URL, short-lived signed view URL for the project files.id (process-cached). The image block self-hides when there's no image or the load fails (`onError`). No new server signing per result — projects sign lazily client-side only for the ~24 windowed cards. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder — results refinement toolbar (score slider + sort + hard refine filters).** The standalone page now always fetches EVERYTHING scoring ≥ 70 (`FETCH_FLOOR`, no longer a left-panel select) and adds a client-side refinement toolbar above the results that operates on that set with NO re-fetch: (1) a **score slider** 70→100 (step 5) — drag right to raise the minimum and shrink the list, with a live "showing X of Y" count and per-group tab counts that follow; (2) a **sort** dropdown — best match (default), price ↑/↓, area large/small, nearest, most available units, name; (3) a **"Refine" panel** of HARD post-filters to focus a large result set — max price, area min/max, min bedrooms, available-only, coordinate-verified-only (these are real gates, unlike the soft scoring dimensions, so they directly answer "the finder returns far more than the table because area/price are only soft 10–25% nudges"). A fresh search resets the slider to 70 and clears refine. All logic is pure client-side in `ProjectFinderPage.tsx` (`refinedGroups` memo: filter by score≥threshold + `passesRefine`, then `sortItems`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder page — a sidebar tool usable WITHOUT a client.** The deterministic Finder is now reachable on its own at `/project-finder` (sidebar entry "الباحث عن المشاريع / Project Finder", `Compass` icon), registered as a `project_finder` custom page in `src/lib/customPages.ts` with `default_access:'all'` (gateable per-profile in Settings → Profiles, admins always see it). The new page `src/pages/ProjectFinder/ProjectFinderPage.tsx` renders STRUCTURED pickers only — no free text, no AI: it pulls the live `clients` model's own field definitions (`location` cascade, `preferred_districts`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`) by slug and renders each via `DynamicField` into a draft buffer, plus a native Bedrooms select, Sources checkboxes (our_projects + all_projects default, market_listings opt-in), and a Minimum-match selector (40/60/70/80). It feeds the SAME `draftToMatchRequirements` → `fetchProjectFinder` path the Follow-up modal uses (`parse:false`, `explain:false`, `perGroup:0`, no `client_id` → no geo-preference gate), and renders the four location-centric groups with `FinderCard`. Because there is no client to attach options to, `FinderCard` gained a `hideClientActions` prop that shows **Details only** (no select checkbox / save / eliminate / reactivate); the Follow-up modal still passes the full client-action set. Route guarded by `RequirePageAccess pageId="project_finder"` in `App.tsx`. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder results now save into the unified Client Property Options store — replacing the old "Add to client" → preferences flow.** Finder cards (`FinderCard` in the Follow-up Suggested Projects modal) gained **selection checkboxes** — only matches scoring exactly **100** are auto-checked; everything below 100 is visible but unchecked unless the rep selects it — plus a footer **"Save selected to client options"** bulk-save, a per-card **"Save as option"**, and a per-card **"Eliminate"** (required reason notes). Saves write to the new `client_property_options` model (default status `suitable`, `added_from` `project_finder`) via `src/lib/matching/clientOptions.ts`, the deterministic dedup/main/eliminate engine — **not** to `clients.preferred_projects` / `preferred_market_listings` anymore. Cards cross-reference existing options (status chip; an eliminated source shows Reactivate and is never silently revived). The `preferred_*` fields remain preference INPUTS, no longer the finder's save target. Deterministic, no AI. See [clients.md](clients.md), [followups-workspace.md](followups-workspace.md).) | 2026-06-29 (**Multi-district search wired end-to-end.** A client with several preferred districts is now searched against ALL of them (previously only the first was sent). The requirements mapper (`src/lib/matching/requirements.ts`) now collects every district id from the `location` cascade array — union'd with `preferred_districts` if that field exists — resolves each to a name, and sends `districts[]`; `MatchRequirementsInput` gained `districts`. The endpoint + engine already supported it (resolve each, a project in ANY requested district is exact, across our_projects + all_projects + the market RPC). 12 live clients already had multiple districts. Tests: `src/lib/matching/requirements.test.ts`.) | 2026-06-29 (**Score honesty: a requested dimension with no project data no longer scores 100%.** Bug: a listing missing the price scored a perfect 100/strong when the client had a budget — because `scoreProject` RENORMALIZED the weighted average over only the dimensions it could evaluate, so a `null` budget (no price) was dropped from the denominator and the remaining dims (location/type/area/availability, all perfect) yielded 100%. Fix: a dimension the customer ASKED about but the project has NO data for is now kept at FULL weight earning ZERO (tracked via `requestedMissing` in `matchAgent.ts`), so it drags the score down instead of being ignored — e.g. the screenshot's no-price الازدهار villa now scores ~72/good with `budget: 0%` in the breakdown + the existing "no price data" gap chip, never 100/strong. A dimension the customer did NOT ask about is still dropped (no penalty), so unspecified prefs never inflate or deflate. This also means a data-complete listing always outranks a data-poor one. Tests added in `projectFinder.test.ts`.) | 2026-06-29 (**Market-listing cards now support Add-to-client — source-routed into a new `preferred_market_listings` field.** A new multi-value lookup `preferred_market_listings` (→ market_listings, display `title`) was added to the clients model's "Client Preferences" section, mirroring `preferred_projects` (→ all_projects). With it, market-listing cards in the Suggested Projects modal now show **Add to client** (previously omitted to avoid a broken cross-model lookup): `src/lib/matching/addToClient.ts` gained `addMarketListingToClient` (project → `preferred_projects`, listing → `preferred_market_listings`), and `SuggestedProjectsModal.tsx`'s `onAddToClient` / `FinderCard.tsx`'s actions route by `item.source`. Migration `supabase/migrations/2026-06-29_clients_preferred_market_listings.sql` (clients is unfrozen JSONB → updates `models.schema`; mirrored in `src/data/seedModels.ts`). Commit 810623d. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-06-29 (**Suggested Projects modal: all three sources + Arabic explanations.** The Follow-up Suggested Projects modal now opts into ALL three sources (`our_projects` + `all_projects` + `market_listings`) instead of the catalog-only default, so reps see the full picture; each card stays source-labelled, and non-portfolio sources carry a source-accurate "verify before offering" banner (market listings now say "external market listings", not "All Projects database"). The deterministic per-card **explanation is now localized** — `/api/project-finder` takes a `locale` param (`buildExplanation` is bilingual; the modal sends the current UI language, default Arabic server-side), fixing the English description on an otherwise-Arabic card. Market-listing cards open Details on `/model/market_listings/<id>` (their id is a market_listings record, not all_projects) and **omit Add-to-client** — `clients.preferred_projects` is an all_projects lookup, so pushing a market id would create a broken lookup. Changed: `api/_lib/projectFinder.ts` (`FinderOptions.locale`, bilingual `buildExplanation`), `api/project-finder.ts` (reads `locale`), `src/lib/matching/projectFinder.ts` (sends `locale`+`sources`), `SuggestedProjectsModal.tsx` (requests all 3 + locale + source-aware Details), `FinderCard.tsx` (source-accurate banner, hide Add-to-client for market). **Market scan: narrow-don't-cap (never drop a good listing):** live-measured 2026-06-29, including `market_listings` via the old 20km-neighbour expansion (النرجس ≈ 16,871 listings) took ~25s and 504'd FUNCTION_INVOCATION_TIMEOUT (our+all alone = ~1.7s). A first attempt capped to the newest N — **rejected** because it silently drops potentially-good listings. Replaced with: (1) market is scoped to the **requested district(s) only** (the neighbour expansion was the blow-up; exact-district is the salesperson's actual ask — distribution: 167/170 districts ≤ 2,500 listings, max 5,866); (2) a new RLS-respecting RPC **`wassell_market_candidates`** applies **budget / bedrooms / unit-type filters DB-side** (price-/bed-/type-missing listings are NEVER dropped — only known-out-of-range ones), so adding criteria genuinely shrinks the set (النرجس 2,985 → 1,205 under 2M); (3) if the filtered exact-district set still exceeds `MARKET_SCAN_LIMIT = 4000`, the engine returns a **`too_many`** signal (with the missing criteria) and the modal asks the salesperson to add budget/type/bedrooms — it **never truncates**. `MatchCoreSuccess.marketInfo` (`ok | needs_district | too_many`) flows to `metadata.market`; the modal renders an amber notice. Migration `supabase/migrations/2026-06-29_market_candidates_rpc.sql`.) | 2026-06-29 (**The Follow-up Suggested Projects modal now consumes `/api/project-finder` — `/api/suggest-projects` retired from the UI.** The modal (`SuggestedProjectsModal`) was cut over from the legacy grouped `/api/suggest-projects` endpoint to the deterministic, boundary-verified `/api/project-finder` (via the new `src/lib/matching/projectFinder.ts`), sending `parse:false` + `explain:false` so NO LLM runs on the path — selection, scoring, ranking, AND each card's explanation are all deterministic. It now shows the **four** finder groups (exact district / nearby / same city / broader) via a new `FinderCard`, with actions limited to Details + Add to client. `/api/suggest-projects` still exists but is no longer reached by the Follow-up flow; its old card/chat components (`SuggestionCard`, `AssistantChatPane`, `ProjectWhatsAppFlow`) are now orphaned. **Production-verified live:** modal calls `/api/project-finder` (200); النرجس → 2 exact (coordinate-verified, ~1.75s); العارض → 1 exact (~1.75s); الدرعية → 0 false-exacts; no-preference → "ask the client" prompt; no 504; `used_ai` parse/explain = false. See [followups-workspace.md](followups-workspace.md).) | 2026-06-28 (**Direction narrowed to a single deterministic Project Finder; broad AI assistants unwired.** A new deterministic Project Finder engine became the one and only assistant surface: given a client's requirements it FINDS and RANKS the right projects — selection, scoring, ranking, and grouping are 100% code, geography **boundary-verified** by PostGIS point-in-polygon (`districts_for_points` against the 3,732 `district_boundaries` polygons), with results split into **four location-centric groups**. The LLM is bounded to TWO touchpoints only — *parse* messy text into requirements BEFORE ranking, and *explain* each already-ranked match AFTER — and a hard `assertRankingUnchanged()` guard guarantees it can never change score/band/source/match_type/order. New: `api/project-finder.ts`, `api/_lib/projectFinder.ts`, `api/_lib/projectFinderAI.ts`, `api/_lib/geoVerify.ts`, `supabase/migrations/2026-06-28_project_finder_geo.sql`; `matchProjectsCore` gained an opt-in `verifyGeo` option (+ multi-district `req.districts[]` and a bathrooms dimension); `api/suggest-projects.ts` now passes `verifyGeo:true`. **Concurrently the broad AI assistants were UNWIRED** behind the static build flag `PROJECT_FINDER_ONLY` in `src/lib/featureFlags.ts` (code + DB models/data kept for rollback): the AI sales agent (`ai_chats`), the reel copywriter (`copywriter_chats`), and the conversational match co-pilot (`matching_chats` — with its next-best-action / lead-temperature / WhatsApp-draft / task-creation tools) are hidden from the sidebar and their routes render a `RetiredAssistantNotice`. The Follow-up workspace assistant panel is now a focused launcher for the deterministic Suggested Projects finder (no free-text chat); the modal's right-pane chat and the chat-handoff card actions (Pitch / WhatsApp / Ask) are hidden under the flag. See [ai-agent.md](ai-agent.md), [copywriter-intelligence.md](copywriter-intelligence.md), [followups-workspace.md](followups-workspace.md), [data-storage.md](data-storage.md).) | 2026-06-28 (**Geography unified onto the `location` cascade field.** Every project / listing / client now stores district/city/region in ONE `location` cascade compound ({region,city,district} ids; multi-arrays for clients) instead of separate `district_lookup`/`city_lookup`/`region_lookup`/`preferred_districts`/`preferred_cities` fields. The engine is unchanged in behavior: the caller extracts each record's `location.district`/`location.city` ids and passes them (plus display names resolved from the geography records) to `scoreProject` via the GeoContext (`projDistrictId`/`projCityId`/`projDistrictName`/`projCityName`) — district id drives the exact tier, city id the same-city tier, exactly as before. The market-listings AREA pre-filter now filters `data->'location'->>'district'`/`'city'` (with swapped partial indexes `idx_records_market_loc_district`/`_city`). Client requirements read `location.district`/`location.city` arrays. New `matchAgent.ts` helpers: `recordLocationIds`, `geoNameMap`. Migration: `supabase/migrations/2026-06-28_location_cascade_cutover.sql`. See model-builder.md for the field type.) | 2026-06-27 (**Market Listings now AREA-pre-filtered server-side, with full ~46k coverage.** `market_listings` is now populated (~46,315 external Aqar for-sale ads), so the third source is live with real data — and `matchProjectsCore({ includeMarket })` no longer pulls an arbitrary order-less slice of it. Before scoring, the engine narrows the ~46k listings to the client's relevant AREA in Postgres: (1) when the requested district resolves with a centroid, every listing in that district OR any district whose centroid is within **20 km** of it (`nearbyDistrictIds` → `.in('data->>district_lookup', ids)`); (2) else, when only a city resolves, every listing in that city (`.in('data->>city_lookup', [cityId])`); (3) else (no area signal) the **newest 4,000** listings in a stable `created_at`-desc order (a capped, logged scan — never an arbitrary slice). The same deterministic `scoreProject` then tiers/scores them in memory alongside our_projects + all_projects (budget/type/area/etc. are applied there, not in the pre-filter). A market read failure is non-fatal and logged — the other two sources still return. Backed by two **partial expression indexes** on `records((data->>'district_lookup'))` / `((data->>'city_lookup'))` scoped to the market_listings model id (measured: ~624ms/page nested-loop scan → ~70ms/page index scan, flat across pages, on the النرجس case of 16,871 in-area listings). New helpers in `matchAgent.ts`: `nearbyDistrictIds`, `loadMarketByFilter`, `loadMarketNewestCapped`. Migration: `supabase/migrations/2026-06-27_market_listings_lookup_indexes.sql`. The grouped `/api/suggest-projects` endpoint surfaces all three sources with a source badge + verify banner exactly as before. See [data-storage.md](data-storage.md) for the lazy-load posture of the market_listings model.) | 2026-06-27 (**Third source — Market Listings (`market_listings`).** The Suggested Projects modal now matches across THREE sources, each labelled on the card: `our_projects` (مشاريعنا), `market_listings` (إعلانات السوق — external scraped public ads), and `all_projects` (كل المشاريع). A market listing is a single unit (scalar `price`/`area`/`bedrooms` + real per-listing lat/lng + `district_lookup`); `adaptListingToScorable` maps it into the project shape so the ONE deterministic `scoreProject` grades all three identically. `matchProjectsCore(req, {alwaysScoreAll, includeMarket})` — the grouped endpoint passes both so all three sources show side by side; the LLM `match_projects` tool calls it with NO opts so its two-tier fallback output is unchanged. **Source is now orthogonal to quality:** an all_projects/market match is bucketed by how well it matches (exact/nearby/budget/location) and carries a source badge + a verify banner (both all_projects and market are `requires_verification`, low confidence), instead of being auto-dumped to fallback. A **source filter** (الكل / مشاريعنا / إعلانات السوق / كل المشاريع, with counts from `metadata.source_counts`) sits above the cards. Market Listings has 0 rows today, so it shows nothing until listings are scraped in; the pipeline degrades gracefully (empty source). New: `adaptListingToScorable` + `MatchSource` type + `MatchCoreOptions` in `matchAgent.ts`; `SourcePill` in `SuggestionCard.tsx`; source filter in `SuggestedProjectsModal.tsx`. Tests: `marketListingAdapter.test.ts` + market/source cases in `suggestGroups.test.ts`.) | 2026-06-26 (**Assistant Product Completion — grouped "Suggested Projects" modal + Add-to-client + audit log + feature flag.** The Follow-up side panel's "Suggested Projects" button now opens a LARGE modal: top = current preferences + missing-preference warnings; left = grouped, ranked project cards in five honest buckets (`exact_matches` / `nearby_alternatives` / `budget_matches` / `location_matches` / `fallback_matches`); right = the same assistant chat for explain/compare/pitch/WhatsApp. Grouping is **deterministic, no LLM** — a new `POST /api/suggest-projects` runs the SAME `matchProjectsCore` scoring brain (extracted from the `match_projects` tool, which is unchanged) and a pure `groupMatchResults` that buckets candidates and stamps each with a `data_confidence` (high/medium/low) + normalized `data_gaps` (e.g. `missing_price_range`, `missing_project_coordinates`, `legacy_text_match_only`, `requires_verification`). Honest about the sparse data: a project with no price NEVER enters `budget_matches`; distance is district-CENTROID based (no project pins exist). Each card has actions: Why / Compare / Pitch / WhatsApp / Open details / **Add to client** (appends to `clients.preferred_projects` via the version-aware store write — user-triggered only). Every run writes one row to the new `assistant_recommendation_runs` audit table. The whole feature is gated by the `feature_flags.sales_assistant_enabled` kill switch (fail-open). The modal reads the UNSAVED follow-up preference draft, so recommendations reflect what the rep is editing before Save. See [followups-workspace.md](followups-workspace.md).) | 2026-06-25 (**Lookup-first matching with dual-read (Saudi geography layer):** the engine now resolves the requested district to the authoritative `districts` record (id + real centroid) via a new `resolveRequestedDistrict` helper, and a project whose `district_lookup` equals the resolved id is an EXACT relational match. The old text fuzzy-match on `preferred_neighborhoods` + per-project averaged centroids is RETAINED as the dual-read fallback for projects not yet backfilled. New geo helpers in `src/lib/locationUtils.ts`: `pointInPolygon`, `pointInDistrict`, `distanceBetweenCentroids`, `haversineKm`, `normalizeArabicDistrictName`, `normalizeEnglishDistrictName`. See [record-management.md](record-management.md) and [data-storage.md](data-storage.md).) | 2026-06-21 | 2026-07-13 (**Timeout recovery panel in the finder UIs.** `/api/project-finder` runs on the Vercel Edge runtime (~25s hard budget); a wide geo scope — measured live: a "south of King Salman Rd" direction rule matches ~36.7k points and adds ~9s to the request (~17s total, 504 on a cold/loaded DB) — can push it past the budget and Vercel kills it with `FUNCTION_INVOCATION_TIMEOUT` (504), which previously rendered as a raw red error string (or read as "no options"). `fetchProjectFinder` now throws a typed `FinderRequestError` (`status`, `timeout` = 504/408/524); on timeout BOTH finder surfaces (`SuggestedProjectsView` + `ProjectFinderPage`) show a dedicated bilingual recovery panel — "البحث استغرق وقتاً أطول من المسموح" + guidance to narrow the location scope (specific districts instead of a city-wide direction rule) — with **تضييق التفضيلات / Narrow preferences** (opens the edit-preferences panel) and **إعادة المحاولة / Try again** (re-sends the last attempted draft via `lastDraftRef`). Non-timeout errors keep the red box, now with a Try-again button on the follow-up/client surface. Root-cause follow-ups deliberately NOT done yet (user chose UI-first): move the endpoint to the Node runtime with a higher `maxDuration`, and/or compile directional rules into indexed polygons.) | 2026-07-13 (**Save-options-first exit guard.** The bulk **حفظ الخيارات / Save options** button moved from the results footer to the TOP header of every client-scoped finder surface (`SuggestedProjectsView` header — primary copper button with a live selected-count badge, next to a now-secondary **تم / Done** — and the standalone `ProjectFinderPage` header when a client is selected). The old footer bulk-save button is gone; the footer keeps the selected count plus an amber "لم يُحفظ أي خيار لهذا العميل بعد" hint until something is saved. **Leaving a client-scoped finder without having saved a single option this visit now asks for confirmation** — "هل أنت متأكد أنك لن تحفظ أي خيار لهذا العميل؟" via the shared `src/components/matching/LeaveWithoutSavingModal.tsx` (Stay is the primary action; "الخروج بدون حفظ" proceeds). The guard intercepts the Done button and Esc in `SuggestedProjectsView`, clearing the selected client on `ProjectFinderPage`, and arms a `beforeunload` browser prompt for tab close/reload. It arms only AFTER results were actually shown (an empty/failed search never nags) and disarms on ANY successful option write this visit — single save, bulk save, inline status set, eliminate, or reactivate.) | 2026-07-13 (**Finder failure honesty + market-scan index (live incident: 504 rendered as "no matching projects").** A search for a client with district `location_items` (سراج الجهني: النرجس/العارض/القيروان) consistently 504'd — `wassell_market_candidates_json` full-scanned ~51k market rows on `(data->'location'->>'district')` (no index, ~4.5s JSONB detoast) plus the per-row `wassell_can_view_record` RLS check (~4–8s), measuring ~30s against `/api/project-finder`'s 25s function ceiling. The SPA then LIED about the failure: on HTTP/2 `res.statusText` is the empty string, so `fetchProjectFinder`'s non-JSON-body fallback threw an `Error('')`, `SuggestedProjectsView`'s `error` state stayed falsy, and the UI fell through to the **"لا توجد مشاريع مطابقة" empty state** — while the catalog actually held 14 exact-district matches (5 scoring 100). Fixes: (1) `fetchProjectFinder` uses `||` (not `??`) so a blank error message never masquerades as success — a failed request now shows the red error box; (2) new DB index `idx_records_model_district` on `records (model_id, (data->'location'->>'district'))` (applied live with CONCURRENTLY; migration `2026-07-13_records_model_district_index.sql`) — the market RPC drops ~30s → ~2s and the full three-source search responds ~17s with 516 exact-district results. Known follow-up: ~17s is still near the 25s ceiling; the per-row RLS call in the market RPC is the next bottleneck.) | 2026-07-04 (**"إرسال للعميل / Send to client" on every finder + option card (QA-002), collapsible preferences area (QA-001).** (1) Every Project Finder result card (`FinderCard`, client-scoped surfaces only — hidden with `hideClientActions`) and every Client Options card (projects + market listings) now carries a copper **Send to client** action that WhatsApps that project/listing to the CONNECTED client without opening the record: a prepared `chat_templates` message is reused (project → `project_id` link via the re-wired `ProjectWhatsAppFlow`; listing → `listing_id` + `status:'ready'` via the new `src/components/matching/ListingWhatsAppFlow.tsx`), and when none exists the EXISTING creation flow runs right from the card (project: deterministic `resolveProjectFacts`→`composeProjectMessage` preview→save; listing: `ListingMessageModal` AI text + text-removed photos → on approve the flow continues straight into the chat). The chat opens as `StartChatModal` with the client preselected + body prefilled; the template's gallery/cleaned photos ride along as separate messages. **Video support:** `sendProjectImageMessages` now sends each ride-along by MIME (`video` for video bytes, extension fallback for typeless servers), and direct video-FILE URLs on the source (`all_projects.project_videos`, listing `video_urls`) are appended via the new `directVideoUrls` helper — HLS streams (Aqar/Cloudflare `.m3u8`) and page links are excluded (not uploadable to WhatsApp). Shared pure helpers + tests: `src/lib/matching/sendToClient.ts` (`findProjectTemplate` / `findReadyListingTemplate` / `templateListingImageUrls` / `templateBody` / `directVideoUrls`), `__tests__/sendToClient.test.ts`. `ProjectWhatsAppFlow` is UN-orphaned. (2) **QA-001:** the finder's always-visible preferences-chips + missing-warnings block is now COLLAPSIBLE (chevron toggle; collapsed = one thin row with count + truncated value summary + missing-count badge); `SuggestedProjectsView` gained `defaultPrefsCollapsed`, set by the Client-Options popup host (`ClientOptionsModal`) so popup results get the vertical space — full-page hosts stay expanded. The edit-preferences panel already toggled + auto-collapsed on Search (2026-07-02). See [clients.md](clients.md), [chats.md](chats.md).) | 2026-07-04 (**Saved market-listing option cards show the quality grade even when saved before the feature (fix).** The A–D `QualityBadge` on a Client Options market-listing card read ONLY the snapshot (`facts.quality_grade`), which was captured at save time — but the finder only started writing quality into `facts` on 2026-07-02, so every option saved before then (all 252 live market-listing options at the time, saved ~2026-07-01) showed a BLANK chip. `ClientOptionsTab` now falls back to the LIVE listing record in the slim `market_listings` store (which the DB trigger keeps `quality_grade`/`quality_score` on for every row, both riding the slim store) via a new `qualityBySourceId` map — the exact same "snapshot-first, live-store fallback" pattern already used for the ad-id chip. New saves keep using their snapshot; pre-feature options now render the current grade; a listing dropped from the catalogue simply shows no chip. Verified live: 252/252 pre-feature options had a null snapshot grade but a real live grade in the 47k-row store. `src/pages/Clients/components/tabs/ClientOptionsTab.tsx`. See [clients.md](clients.md).) | 2026-07-02 (**Listing quality in the finder.** Every market_listings result now carries the DB-computed `quality_score`/`quality_grade` in its `facts` (adapter → scoreProject facts → card → saved-option snapshot), rendered as the shared colored A–D chip (`src/components/market/QualityBadge.tsx`) on finder cards (both surfaces) and on saved market-listing option cards (`ClientOptionsTab`, incl. the manual `AddOptionModal` snapshot). Ranking: quality is an EQUAL-match-score TIEBREAK ONLY — server `byBandThenScore` and the client default score sort (`finderRefine.sortItems`) append `quality_score` as the final key; relevance always dominates, no minimum-grade filter, no change to scoreProject or the too_many guard. Items without a quality score (projects/units) tie-break as 0.) | 2026-07-02 (**Location matching + grouping fixes (3 bugs from field screenshots).** (1) *Wrong / missed "same district" matches:* the SPA now sends the client's selected districts as AUTHORITATIVE record ids (`requirements.district_ids`, built in `draftToMatchRequirements` from the location cascade + `preferred_districts` + `location_items` include-rules); the engine matches on those ids directly and runs the fuzzy per-name ILIKE resolution ONLY when no id resolved — so a same-named district in another city can no longer be admitted as "في نفس الحي", nor can a selected district silently mis-resolve and mark its own projects "outside". Multiple selected districts stay OR — inside ANY one = exact. (2) *Grouping is now purely location-centric:* `finderGroupFor` groups by the verified `location_tier` alone; the old "band `partial` → broader_fallback" override is GONE, so a wrong-unit-type or no-price project INSIDE a requested district stays under "في نفس الحي" (its weak fit still shows via the band badge + in-group ordering) instead of being falsely labelled "خارج المنطقة المطلوبة". (3) *Distance everywhere, measured to the nearest selected reference:* `scoreProject` computes `distance_km` for EVERY tier (same-city + fallback included), to the NEAREST of ALL selected references — every requested district centroid PLUS the client's selected location ELEMENTS (`location_items` element anchors — within_radius / within_distance / inside_area — resolved endpoint-side from `geo_elements` and passed as `FinderOptions.refPoints`) — and names WHICH one via the new `nearest_ref_name` field (on `ScoredProject` → `MatchResultItem` → `FinderMatch`). The card distance pill now reads "~4.2 كم من النرجس", and the deterministic nearby/same-city/fallback explanations name the closest selected area (fallback: "خارج الأحياء المحددة (يبعد حوالي X كم عن «Y»)"). Tests in `projectFinder.test.ts` + `requirements.test.ts`.) | 2026-07-02 (**Multi-select preferences now match as OR (any acceptable option counts).** When a client selects MULTIPLE acceptable options for one preference, they are alternatives — a result matching ANY of them now gets full credit for that dimension. (1) **Unit types:** `preferred_unit_type` is a multiselect but only the FIRST value was sent — a client wanting "شقة أو دور" saw every دور project scored `type: 0` and band-capped into the fallback tab. `draftToMatchRequirements` now sends ALL selected types as `requirements.property_types[]` (`property_type` stays = the first for single-type paths); the engine (`MatchRequirements.property_types`, `scoreProject`) unions the synonym expansion across all requested types and a project offering ANY of them earns full type credit — the wrong-type band cap fires only when NONE match. The market-listings DB pre-filter unions the ILIKE terms across all types too (previously only the first type's listings ever became candidates), and the `match_projects` AI-agent tool + the finder's LLM text parser can now express type alternatives (`property_types`). (2) **Cities:** the `location` cascade allows multiple cities but only the first was sent; `cities[]` now flows through and `matchProjectsCore` resolves EACH into the city-match set (`reqCityIds`), so a project in ANY acceptable city city-matches. (3) **Districts** already had OR semantics (`districts[]`, 2026-06-29) — unchanged. Deliberately NOT changed: `preferred_amenities` keeps proportional credit (amenities are cumulative wants, not alternatives — a client wanting "غرفة خادمة ومسبح" wants both). No scoring weights, bands, or tiers changed. Tests: `requirements.test.ts`, `projectMatchScoring.test.ts`.) | 2026-07-02 (**Finder embedded in the Client Options popup (new surface).** The shared `SuggestedProjectsView` gained a FOURTH host: the new `ClientOptionsModal` (`src/components/clients/ClientOptionsModal.tsx`) — the client-options popup opened from the WhatsApp chat header — embeds the finder inside the popup itself ("البحث عن خيارات أكثر / Find more options" swaps the popup body to the finder, `followupId=null`, seeded + geo-gated from the client; "تم / Done" returns to the options list). **Same-day fix (live report):** embedded in the popup on a laptop-height screen, the finder's open edit-preferences panel — part of the fixed chrome — consumed the whole height, leaving the results area 0px with nothing scrollable; the panel is now height-capped (max-h-[42vh], own scrollbar) and auto-collapses when Search is pressed so results are visible immediately (benefits the full-page finder on short viewports too), and the popup uses near-full-screen height. `ClientOptionsTab` also gained an `onFindMore` prop, so the Client 360 Options tab now links to the full-page client finder (`/model/clients/:id/projects`) too. See [chats.md](chats.md), [clients.md](clients.md).) | 2026-07-02 (**Market-listing finder cards: "التواصل مع المعلن / Contact advertiser" button.** Every `market_listings` result card (both finder surfaces — standalone `/project-finder` AND the Follow-up/client `SuggestedProjectsView`, including the standalone no-client mode) now carries a compact copper **Contact advertiser** action next to Details: if the listing already has a scraped `advertiser_phone` it opens the in-app WhatsApp `StartChatModal` immediately (pre-filled with the Aqar listing link); otherwise it enqueues the REGA advertiser lookup (`/api/rega-lookup` → Browserbase worker), shows a "جاري جلب الرقم…" spinner, and auto-opens the chat when the worker's write lands via Realtime — after which every later click opens the chat directly (the phone + `advertiser` link persist on the listing). Failures surface as an error toast. Shared component `src/components/market/ContactAdvertiserButton.tsx` + shared hook `src/lib/regaLookup/useContactAdvertiser.ts` (extracted from `ContactAdvertiserPanel`); reads the listing from the market_listings slim store by `project_id`, so no extra fetch per card. Same button also renders on the client Options tab's market-listing cards — see [clients.md](clients.md), [record-management.md](record-management.md).) | 2026-07-01 (**Project Finder is now reachable from the CLIENT profile.** Per the user, the finder can be run directly on a client. The Client 360 header (`DetailHeader`) gained a prominent copper **"الباحث عن المشاريع / Find projects"** button that navigates to a new full-page route `/model/clients/:recordId/projects` (`ClientProjectsPage`) — a thin wrapper (mirroring the follow-up `SuggestedProjectsPage`) that loads the client, seeds `prefDraft` from the saved client data, and renders the SAME shared `SuggestedProjectsView` with `followupId=null`. So the client-scoped finder has the full feature set — map/list view, refinement toolbar, edit-preferences + re-run, and saving into the client's unified `client_property_options` — geo-gated by the client (client_id + their saved `location_items`); **"تم / Done"** returns to the client profile. `SuggestedProjectsView` already tolerates a null `followupId` (the finder-handoff write is guarded; audit/save attach via `clientRec.id`). Route precedes the generic `:recordId` detail route (like the follow-up one). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Standalone Project Finder: pick a client → seeded, geo-gated search + full client-option actions; unified location editor.** The sidebar `/project-finder` tool reached parity with the Follow-up finder. (1) A **client picker** (optional, top of the filters panel) searches the `clients` records by name/phone/code; selecting one SEEDS the pickers from that client's saved preferences (`location` + `location_items`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`, `preferred_bedrooms`→bedrooms), passes `client_id` + the current `location_items` to `/api/project-finder` (so the PostGIS geo gate compiles + narrows for that client), and turns EVERY result card into a full client-option control — inline status selector (`onSetStatus`), bulk-select + "Save selected to client options" footer, and the eliminate-with-reason modal — writing into that client's `client_property_options` (`added_from:'project_finder'`). A "clear client" X returns to pure discovery (cards show Details only). (2) The `location` field now renders the **unified `ClientLocationField`** (district include/exclude chips + geo-element rules with "+ add district / + add element") instead of the bare cascade — threaded via `modelId`/`onPatch` into `DynamicField`, writing both `location` and `location_items` into the draft. Without a selected client, district include-rules still tier results (via the `requirements.districts` mirror) but geo-element rules don't gate (the server geo gate needs a `client_id`). All in `ProjectFinderPage.tsx`; handlers mirror `SuggestedProjectsView`. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-07-01 (**Finder results now have a MAP view (list ⇄ map toggle).** Per the user — "when presenting listings give me an option for a map view." Both finder surfaces (standalone `ProjectFinderPage` + Follow-up `SuggestedProjectsView`) gained a **List / Map** segmented toggle in the shared `FinderRefinementBar`. In map mode the active tab's results — the pinned OUR-projects **plus** that tab's other matches — render on the branded `WASSEL_MAP_STYLE` Google basemap as pins colored by SOURCE (green = our projects, chocolate = market, charcoal = listings DB), clustered by density (`MarkerClusterer` + `buildClusterIcon`), auto-fit to the pins. Clicking a pin opens an InfoWindow summary (source · score · name · district/city · distance · price) with a **"التفاصيل / Details"** button that runs the same `onOpenDetails` as a card. New component `src/pages/Followups/components/FinderMapView.tsx` (shared, read-only — client-option actions stay on the list). Coordinates now flow from the engine: `scoreProject` puts `facts.latitude`/`facts.longitude` (the resolved pin — verified polygon centroid when geo-verification ran, else the stored lat/lng) in `matchAgent.ts`; the finder endpoint passes `facts` through unchanged. A legend + coverage note ("N on the map · M without coordinates") sits under the map; the toggle only appears once results exist. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder cards: ad-ID chip + inline client-option status selector; district include-rules now land results in the exact-district tier.** (1) Market-listing cards now show a small ad-id chip (the external Aqar ad number, for looking the ad up) — reformatted to "@<external_id>" on 2026-07-04, when the id also reached the finder MAP pin hover titles, the Client Options tab cards, and the manual add-option picker (see clients.md) — `scoreProject` puts `facts.external_id`, `adaptListingToScorable` carries it through. (2) On the Follow-up finder EVERY card gained an inline STATUS `<select>` (main focus / reserved / interested / presented / suitable / not interested / closed / eliminated) that ENSURES the client option exists (creating it if new) THEN applies the chosen status in one step — `main_focus` routes through `setMainOption` (single active main), `eliminated` opens the reason-notes modal, and any non-eliminated pick reactivates a previously-eliminated option. This REPLACES the old per-card Save-as-option / Eliminate / Reactivate buttons (`StatusSelect` in `FinderCard.tsx`, colored to the status badge, RTL-safe native select; `onSetStatus` in `SuggestedProjectsView.tsx`). Bulk-select checkbox + footer bulk-save are unchanged; the standalone Project Finder (no client) still shows Details only. (3) A district picked in the unified location box is stored as a `location_items` district INCLUDE-rule (it drives the PostGIS geo gate), NOT the `location` cascade — so `draftToMatchRequirements` now mirrors those include-rule district ids (with the label stashed on the item as a name fallback) into `requirements.districts`; without it every gated result fell to the "same city / different district" tier even though it sat inside the requested district. Test in `requirements.test.ts`. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Finder result tabs: 3 tabs (Same district / Nearby / Alternatives) + our-projects pinned to the top of EVERY tab.** Per the user: the four engine tiers now collapse to THREE display tabs — "Same city" is dropped (the whole set is already the same city) and its items fold into **Alternatives** (= same_city + broader_fallback) — and OUR portfolio (source `our_projects`, ≥70) is lifted out of the tiers into a pinned "مشاريعنا / Our Projects" list shown at the TOP of every tab, best-first (score desc → closest), followed by that tab's other matches under a "خيارات أخرى / Other options" divider. Pure client-side display: shared `buildFinderTabs` + `DISPLAY_TAB_KEYS`/`DISPLAY_TAB_LABELS` in `finderRefine.ts`, wired into BOTH `ProjectFinderPage` and `SuggestedProjectsView`; tab badge = pinned-ours + that tab's tier count; the tier list stays windowed/infinite-scrolled while our-projects always render in full. The engine's four-group response is unchanged. See [followups-workspace.md](followups-workspace.md).) | 2026-07-01 (**Geo gate ~5× faster + un-truncated (was 7s/capped-at-1000).** Follow-up to the timeout band-aid below. (1) PERF: `wassell_geo_match` was a 7.05s seq-scan of all ~47.6k project+listing points with a per-row geometry fn. Rewrote it to use the GiST point indexes — polygon/buffer items (within_radius/inside_area/district, `geom NOT NULL`) via an `ST_Contains` spatial JOIN (**~38ms**, was ~7s), direction items (halfplane, `geom NULL`) via a GiST bbox pre-filter that drops the far side (27,019/47,580 for the north-of rule) then the exact `wassell_geo_dir_match` (**~1.5s**, was 7s). Verified identical record-id sets on prod before the swap (direction client 8046=8046; synthetic 3km polygon 349=349). Migration `2026-07-01_geo_match_index_optimized.sql`. (2) CORRECTNESS: the endpoint's row-returning `wassell_geo_match` call was capped at PostgREST's ~1000-row response (the area can be 8046), silently truncating the gate to 1000 AND non-deterministically (market count flickered 329↔489). Added `ORDER BY record_id` + a new `wassell_geo_match_ids(uuid)→uuid[]` (one row, no cap, scan runs once); `api/project-finder.ts` calls it instead. Live element-only finder: **8.7s → 3.4s** with the FULL 8046-record gate. Follow-up: clip direction halfplanes to the region polygon at compile time to put them on the index path too. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Geo gate was silently timing out for authenticated users — the element never narrowed anything.** Sibling fix to the market-AREA change below: even after it shipped, the element-only client STILL saw `geo_filter:null` + `needs_district`. Root cause: `wassell_geo_match` scans all ~46.6k project+listing points with a per-row geometry predicate (7.05s EXPLAIN ANALYZE for a Riyadh-wide rule), tripping the `authenticated` role's 8s `statement_timeout`; the endpoint's catch left `geoMatchIds` null. As SECURITY DEFINER fns, a function-level `statement_timeout` overrides the caller's value, so `alter function wassell_geo_match / wassell_compile_client_geo set statement_timeout='20s'` (migration `2026-06-30_geo_match_statement_timeout.sql`, applied to prod) lets the scan finish. Verified live: `geo_filter` now applied, the element-only client sees **489 in-area market listings** (was 0), no district picked (~8.7s; perf follow-up = make polygon items index-usable / clip direction halfplanes to the region). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Market listings now respect a `location_items` AREA — no district pick required.** The user set a geo-element rule ("داخل النخيل مول") but the finder still said "حدّد الحي لعرض إعلانات السوق" (needs_district) and hid market ads — defeating the point of the element. The market scan now scopes by the client's geo-matched record-id set when no district is set: `wassell_geo_match` already returns the in-area LISTING ids (`listing_points` populated, 46,608 rows; النخيل area = 7,904 listings). The market-candidates RPCs gained an optional `p_record_ids uuid[]` (migration `2026-06-30_market_candidates_geo_ids.sql`, applied to prod) that scopes by `r.id = any(p_record_ids)` instead of the district filter — same DB-side budget/bed/type filters, RLS re-check, `ORDER BY r.id`, count-then-paginate `too_many` guard. `matchProjectsCore` market block: scope = district when set, ELSE the `geoGate` area, ELSE `needs_district`. An element-only client now sees market ads (النخيل: 7,904 → 2,595 villas → 911 in 3–4M); a too-broad area still returns `too_many` asking for budget/bedrooms. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder edit-preferences panel now shows + edits the geo-element / district rules (`location_items`).** Bug: the panel rendered the `location` field via a bare `DynamicField`, so the unified `ClientLocationField` (district + "within radius of a landmark" chips with "+ إضافة حي / + إضافة عنصر" controls) never appeared — selected geo-elements weren't shown and there was no way to add one. Fix: thread `modelId` + `recordId` + `onPatch` into that `DynamicField` (the exact trigger that makes it render `ClientLocationField`, same as `PreferenceSummary`), so the box now shows the saved chips + add controls and writes both `location` and `location_items` into `editDraft` (location field spans full width). Because the server geo-gate compiles from the SAVED client (`wassell_compile_client_geo(p_client_id)` reads `clients.data.location_items` at request time), the "بحث / Search" button now **persists the edited prefs (incl. `location_items`) to the client first** (version-safe, only when changed) THEN re-runs the match — so a district/element rule actually narrows the results. The separate "Save to client" button was merged into Search (caption: "تُحفظ التفضيلات للعميل عند البحث"). See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md), [client_geo_matching](client_geo_matching).) | 2026-06-30 (**Edit client preferences on the finder page → re-run the search.** The full-page finder (`SuggestedProjectsView`) gained an "تعديل التفضيلات / Edit preferences" header toggle that opens an inline editor of the client's preference fields (those present on the live model: `location` cascade, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_bedrooms`, `preferred_amenities`) via `DynamicField` into a local `editDraft`. "بحث بالتفضيلات الجديدة / Search" rebuilds the SERVER-SIDE requirements (`buildReqs` — also maps `preferred_bedrooms.min` → the matcher's single `bedrooms`) and RE-RUNS `/api/project-finder` (a genuinely more-detailed search, not just the client-side slider/refine). The view's fetch was refactored from a once-on-mount snapshot into a reusable `runSearch(draft)` (abortable; resets slider/refine; re-seeds the 100%-auto-select); preference chips + `ctx` now reflect the last *searched* draft. Each search also updates the workspace hand-off so the edits carry back, and an explicit "حفظ التفضيلات للعميل / Save to client" persists `editDraft` to the client record version-safe (`saveRecord` + `expectedVersion`, same posture as `PreferenceSummary`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Follow-up Suggested Projects is now a FULL PAGE, not a modal.** Per the user — "most of the time we will use it here" and "it still doesn't look good" — the cramped 860px modal was replaced by a full-page route `/model/followups/:recordId/projects` (`SuggestedProjectsPage` → shared `SuggestedProjectsView`). It opens in the SAME tab via client-side nav from the workspace's "المشاريع المقترحة" launcher, stays CONNECTED to the follow-up (its id drives the recommendation audit log + saves into THAT client's `client_property_options`), lays cards out in a responsive 1/2/3-column grid inside a centered max-w-6xl, and carries the same refinement toolbar + images + bulk-save. A prominent **"تم / Done"** button (header + footer, plus Esc) returns the rep to the follow-up record (`navigate('/model/followups/:id')`). Preferences ride a module-singleton hand-off (`src/lib/matching/finderHandoff.ts`) the panel sets at click time (so unsaved edits are honored); on a cold URL load the page falls back to the saved client data, and on return the workspace restores the hand-off draft then clears it. `SuggestedProjectsModal.tsx` was deleted; `SalesAssistantSidePanel` now navigates instead of opening a modal. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Refinement toolbar now in the Follow-up Suggested Projects modal too (shared with the standalone page).** The score slider + sort + hard-filter "Refine" panel were extracted to a shared module (`src/lib/matching/finderRefine.ts` — `SortKey`/`Refine`/`passesRefine`/`sortItems`/`refineGroups`/`FETCH_FLOOR`) + a shared component (`FinderRefinementBar.tsx`), and wired into BOTH `ProjectFinderPage` and `SuggestedProjectsModal` so they behave identically. The modal already fetched everything ≥70; now it filters/sorts that set client-side, its group-tab counts + footer follow the refinement, bulk-save only saves cards still VISIBLE under the active refinement (never slider/refine-hidden ones), and it auto-hops to the first non-empty tab when refining empties the active one. This was the user's primary surface ("most of the time we will use it here"). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder cards now show the listing/project main image.** Every `FinderCard` (standalone Project Finder + Follow-up Suggested Projects modal) renders an `h-40` hero thumbnail at the top. The engine adds a single `facts.image` in `scoreProject`: market listings carry a raw photo URL (`adaptListingToScorable` reads `main_image_url`, else first `image_urls[]`); projects carry a files.id (`main_image`, else first `project_images[]`). The client resolves either through the existing `useSignedImage` hook — http(s) passthrough for the raw market URL, short-lived signed view URL for the project files.id (process-cached). The image block self-hides when there's no image or the load fails (`onError`). No new server signing per result — projects sign lazily client-side only for the ~24 windowed cards. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder — results refinement toolbar (score slider + sort + hard refine filters).** The standalone page now always fetches EVERYTHING scoring ≥ 70 (`FETCH_FLOOR`, no longer a left-panel select) and adds a client-side refinement toolbar above the results that operates on that set with NO re-fetch: (1) a **score slider** 70→100 (step 5) — drag right to raise the minimum and shrink the list, with a live "showing X of Y" count and per-group tab counts that follow; (2) a **sort** dropdown — best match (default), price ↑/↓, area large/small, nearest, most available units, name; (3) a **"Refine" panel** of HARD post-filters to focus a large result set — max price, area min/max, min bedrooms, available-only, coordinate-verified-only (these are real gates, unlike the soft scoring dimensions, so they directly answer "the finder returns far more than the table because area/price are only soft 10–25% nudges"). A fresh search resets the slider to 70 and clears refine. All logic is pure client-side in `ProjectFinderPage.tsx` (`refinedGroups` memo: filter by score≥threshold + `passesRefine`, then `sortItems`). See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Standalone Project Finder page — a sidebar tool usable WITHOUT a client.** The deterministic Finder is now reachable on its own at `/project-finder` (sidebar entry "الباحث عن المشاريع / Project Finder", `Compass` icon), registered as a `project_finder` custom page in `src/lib/customPages.ts` with `default_access:'all'` (gateable per-profile in Settings → Profiles, admins always see it). The new page `src/pages/ProjectFinder/ProjectFinderPage.tsx` renders STRUCTURED pickers only — no free text, no AI: it pulls the live `clients` model's own field definitions (`location` cascade, `preferred_districts`, `preferred_unit_type`, `budget`, `preferred_area`, `preferred_amenities`) by slug and renders each via `DynamicField` into a draft buffer, plus a native Bedrooms select, Sources checkboxes (our_projects + all_projects default, market_listings opt-in), and a Minimum-match selector (40/60/70/80). It feeds the SAME `draftToMatchRequirements` → `fetchProjectFinder` path the Follow-up modal uses (`parse:false`, `explain:false`, `perGroup:0`, no `client_id` → no geo-preference gate), and renders the four location-centric groups with `FinderCard`. Because there is no client to attach options to, `FinderCard` gained a `hideClientActions` prop that shows **Details only** (no select checkbox / save / eliminate / reactivate); the Follow-up modal still passes the full client-action set. Route guarded by `RequirePageAccess pageId="project_finder"` in `App.tsx`. See [followups-workspace.md](followups-workspace.md).) | 2026-06-30 (**Finder results now save into the unified Client Property Options store — replacing the old "Add to client" → preferences flow.** Finder cards (`FinderCard` in the Follow-up Suggested Projects modal) gained **selection checkboxes** — only matches scoring exactly **100** are auto-checked; everything below 100 is visible but unchecked unless the rep selects it — plus a footer **"Save selected to client options"** bulk-save, a per-card **"Save as option"**, and a per-card **"Eliminate"** (required reason notes). Saves write to the new `client_property_options` model (default status `suitable`, `added_from` `project_finder`) via `src/lib/matching/clientOptions.ts`, the deterministic dedup/main/eliminate engine — **not** to `clients.preferred_projects` / `preferred_market_listings` anymore. Cards cross-reference existing options (status chip; an eliminated source shows Reactivate and is never silently revived). The `preferred_*` fields remain preference INPUTS, no longer the finder's save target. Deterministic, no AI. See [clients.md](clients.md), [followups-workspace.md](followups-workspace.md).) | 2026-06-29 (**Multi-district search wired end-to-end.** A client with several preferred districts is now searched against ALL of them (previously only the first was sent). The requirements mapper (`src/lib/matching/requirements.ts`) now collects every district id from the `location` cascade array — union'd with `preferred_districts` if that field exists — resolves each to a name, and sends `districts[]`; `MatchRequirementsInput` gained `districts`. The endpoint + engine already supported it (resolve each, a project in ANY requested district is exact, across our_projects + all_projects + the market RPC). 12 live clients already had multiple districts. Tests: `src/lib/matching/requirements.test.ts`.) | 2026-06-29 (**Score honesty: a requested dimension with no project data no longer scores 100%.** Bug: a listing missing the price scored a perfect 100/strong when the client had a budget — because `scoreProject` RENORMALIZED the weighted average over only the dimensions it could evaluate, so a `null` budget (no price) was dropped from the denominator and the remaining dims (location/type/area/availability, all perfect) yielded 100%. Fix: a dimension the customer ASKED about but the project has NO data for is now kept at FULL weight earning ZERO (tracked via `requestedMissing` in `matchAgent.ts`), so it drags the score down instead of being ignored — e.g. the screenshot's no-price الازدهار villa now scores ~72/good with `budget: 0%` in the breakdown + the existing "no price data" gap chip, never 100/strong. A dimension the customer did NOT ask about is still dropped (no penalty), so unspecified prefs never inflate or deflate. This also means a data-complete listing always outranks a data-poor one. Tests added in `projectFinder.test.ts`.) | 2026-06-29 (**Market-listing cards now support Add-to-client — source-routed into a new `preferred_market_listings` field.** A new multi-value lookup `preferred_market_listings` (→ market_listings, display `title`) was added to the clients model's "Client Preferences" section, mirroring `preferred_projects` (→ all_projects). With it, market-listing cards in the Suggested Projects modal now show **Add to client** (previously omitted to avoid a broken cross-model lookup): `src/lib/matching/addToClient.ts` gained `addMarketListingToClient` (project → `preferred_projects`, listing → `preferred_market_listings`), and `SuggestedProjectsModal.tsx`'s `onAddToClient` / `FinderCard.tsx`'s actions route by `item.source`. Migration `supabase/migrations/2026-06-29_clients_preferred_market_listings.sql` (clients is unfrozen JSONB → updates `models.schema`; mirrored in `src/data/seedModels.ts`). Commit 810623d. See [followups-workspace.md](followups-workspace.md), [clients.md](clients.md).) | 2026-06-29 (**Suggested Projects modal: all three sources + Arabic explanations.** The Follow-up Suggested Projects modal now opts into ALL three sources (`our_projects` + `all_projects` + `market_listings`) instead of the catalog-only default, so reps see the full picture; each card stays source-labelled, and non-portfolio sources carry a source-accurate "verify before offering" banner (market listings now say "external market listings", not "All Projects database"). The deterministic per-card **explanation is now localized** — `/api/project-finder` takes a `locale` param (`buildExplanation` is bilingual; the modal sends the current UI language, default Arabic server-side), fixing the English description on an otherwise-Arabic card. Market-listing cards open Details on `/model/market_listings/<id>` (their id is a market_listings record, not all_projects) and **omit Add-to-client** — `clients.preferred_projects` is an all_projects lookup, so pushing a market id would create a broken lookup. Changed: `api/_lib/projectFinder.ts` (`FinderOptions.locale`, bilingual `buildExplanation`), `api/project-finder.ts` (reads `locale`), `src/lib/matching/projectFinder.ts` (sends `locale`+`sources`), `SuggestedProjectsModal.tsx` (requests all 3 + locale + source-aware Details), `FinderCard.tsx` (source-accurate banner, hide Add-to-client for market). **Market scan: narrow-don't-cap (never drop a good listing):** live-measured 2026-06-29, including `market_listings` via the old 20km-neighbour expansion (النرجس ≈ 16,871 listings) took ~25s and 504'd FUNCTION_INVOCATION_TIMEOUT (our+all alone = ~1.7s). A first attempt capped to the newest N — **rejected** because it silently drops potentially-good listings. Replaced with: (1) market is scoped to the **requested district(s) only** (the neighbour expansion was the blow-up; exact-district is the salesperson's actual ask — distribution: 167/170 districts ≤ 2,500 listings, max 5,866); (2) a new RLS-respecting RPC **`wassell_market_candidates`** applies **budget / bedrooms / unit-type filters DB-side** (price-/bed-/type-missing listings are NEVER dropped — only known-out-of-range ones), so adding criteria genuinely shrinks the set (النرجس 2,985 → 1,205 under 2M); (3) if the filtered exact-district set still exceeds `MARKET_SCAN_LIMIT = 4000`, the engine returns a **`too_many`** signal (with the missing criteria) and the modal asks the salesperson to add budget/type/bedrooms — it **never truncates**. `MatchCoreSuccess.marketInfo` (`ok | needs_district | too_many`) flows to `metadata.market`; the modal renders an amber notice. Migration `supabase/migrations/2026-06-29_market_candidates_rpc.sql`.) | 2026-06-29 (**The Follow-up Suggested Projects modal now consumes `/api/project-finder` — `/api/suggest-projects` retired from the UI.** The modal (`SuggestedProjectsModal`) was cut over from the legacy grouped `/api/suggest-projects` endpoint to the deterministic, boundary-verified `/api/project-finder` (via the new `src/lib/matching/projectFinder.ts`), sending `parse:false` + `explain:false` so NO LLM runs on the path — selection, scoring, ranking, AND each card's explanation are all deterministic. It now shows the **four** finder groups (exact district / nearby / same city / broader) via a new `FinderCard`, with actions limited to Details + Add to client. `/api/suggest-projects` still exists but is no longer reached by the Follow-up flow; its old card/chat components (`SuggestionCard`, `AssistantChatPane`, `ProjectWhatsAppFlow`) are now orphaned. **Production-verified live:** modal calls `/api/project-finder` (200); النرجس → 2 exact (coordinate-verified, ~1.75s); العارض → 1 exact (~1.75s); الدرعية → 0 false-exacts; no-preference → "ask the client" prompt; no 504; `used_ai` parse/explain = false. See [followups-workspace.md](followups-workspace.md).) | 2026-06-28 (**Direction narrowed to a single deterministic Project Finder; broad AI assistants unwired.** A new deterministic Project Finder engine became the one and only assistant surface: given a client's requirements it FINDS and RANKS the right projects — selection, scoring, ranking, and grouping are 100% code, geography **boundary-verified** by PostGIS point-in-polygon (`districts_for_points` against the 3,732 `district_boundaries` polygons), with results split into **four location-centric groups**. The LLM is bounded to TWO touchpoints only — *parse* messy text into requirements BEFORE ranking, and *explain* each already-ranked match AFTER — and a hard `assertRankingUnchanged()` guard guarantees it can never change score/band/source/match_type/order. New: `api/project-finder.ts`, `api/_lib/projectFinder.ts`, `api/_lib/projectFinderAI.ts`, `api/_lib/geoVerify.ts`, `supabase/migrations/2026-06-28_project_finder_geo.sql`; `matchProjectsCore` gained an opt-in `verifyGeo` option (+ multi-district `req.districts[]` and a bathrooms dimension); `api/suggest-projects.ts` now passes `verifyGeo:true`. **Concurrently the broad AI assistants were UNWIRED** behind the static build flag `PROJECT_FINDER_ONLY` in `src/lib/featureFlags.ts` (code + DB models/data kept for rollback): the AI sales agent (`ai_chats`), the reel copywriter (`copywriter_chats`), and the conversational match co-pilot (`matching_chats` — with its next-best-action / lead-temperature / WhatsApp-draft / task-creation tools) are hidden from the sidebar and their routes render a `RetiredAssistantNotice`. The Follow-up workspace assistant panel is now a focused launcher for the deterministic Suggested Projects finder (no free-text chat); the modal's right-pane chat and the chat-handoff card actions (Pitch / WhatsApp / Ask) are hidden under the flag. See [ai-agent.md](ai-agent.md), [copywriter-intelligence.md](copywriter-intelligence.md), [followups-workspace.md](followups-workspace.md), [data-storage.md](data-storage.md).) | 2026-06-28 (**Geography unified onto the `location` cascade field.** Every project / listing / client now stores district/city/region in ONE `location` cascade compound ({region,city,district} ids; multi-arrays for clients) instead of separate `district_lookup`/`city_lookup`/`region_lookup`/`preferred_districts`/`preferred_cities` fields. The engine is unchanged in behavior: the caller extracts each record's `location.district`/`location.city` ids and passes them (plus display names resolved from the geography records) to `scoreProject` via the GeoContext (`projDistrictId`/`projCityId`/`projDistrictName`/`projCityName`) — district id drives the exact tier, city id the same-city tier, exactly as before. The market-listings AREA pre-filter now filters `data->'location'->>'district'`/`'city'` (with swapped partial indexes `idx_records_market_loc_district`/`_city`). Client requirements read `location.district`/`location.city` arrays. New `matchAgent.ts` helpers: `recordLocationIds`, `geoNameMap`. Migration: `supabase/migrations/2026-06-28_location_cascade_cutover.sql`. See model-builder.md for the field type.) | 2026-06-27 (**Market Listings now AREA-pre-filtered server-side, with full ~46k coverage.** `market_listings` is now populated (~46,315 external Aqar for-sale ads), so the third source is live with real data — and `matchProjectsCore({ includeMarket })` no longer pulls an arbitrary order-less slice of it. Before scoring, the engine narrows the ~46k listings to the client's relevant AREA in Postgres: (1) when the requested district resolves with a centroid, every listing in that district OR any district whose centroid is within **20 km** of it (`nearbyDistrictIds` → `.in('data->>district_lookup', ids)`); (2) else, when only a city resolves, every listing in that city (`.in('data->>city_lookup', [cityId])`); (3) else (no area signal) the **newest 4,000** listings in a stable `created_at`-desc order (a capped, logged scan — never an arbitrary slice). The same deterministic `scoreProject` then tiers/scores them in memory alongside our_projects + all_projects (budget/type/area/etc. are applied there, not in the pre-filter). A market read failure is non-fatal and logged — the other two sources still return. Backed by two **partial expression indexes** on `records((data->>'district_lookup'))` / `((data->>'city_lookup'))` scoped to the market_listings model id (measured: ~624ms/page nested-loop scan → ~70ms/page index scan, flat across pages, on the النرجس case of 16,871 in-area listings). New helpers in `matchAgent.ts`: `nearbyDistrictIds`, `loadMarketByFilter`, `loadMarketNewestCapped`. Migration: `supabase/migrations/2026-06-27_market_listings_lookup_indexes.sql`. The grouped `/api/suggest-projects` endpoint surfaces all three sources with a source badge + verify banner exactly as before. See [data-storage.md](data-storage.md) for the lazy-load posture of the market_listings model.) | 2026-06-27 (**Third source — Market Listings (`market_listings`).** The Suggested Projects modal now matches across THREE sources, each labelled on the card: `our_projects` (مشاريعنا), `market_listings` (إعلانات السوق — external scraped public ads), and `all_projects` (كل المشاريع). A market listing is a single unit (scalar `price`/`area`/`bedrooms` + real per-listing lat/lng + `district_lookup`); `adaptListingToScorable` maps it into the project shape so the ONE deterministic `scoreProject` grades all three identically. `matchProjectsCore(req, {alwaysScoreAll, includeMarket})` — the grouped endpoint passes both so all three sources show side by side; the LLM `match_projects` tool calls it with NO opts so its two-tier fallback output is unchanged. **Source is now orthogonal to quality:** an all_projects/market match is bucketed by how well it matches (exact/nearby/budget/location) and carries a source badge + a verify banner (both all_projects and market are `requires_verification`, low confidence), instead of being auto-dumped to fallback. A **source filter** (الكل / مشاريعنا / إعلانات السوق / كل المشاريع, with counts from `metadata.source_counts`) sits above the cards. Market Listings has 0 rows today, so it shows nothing until listings are scraped in; the pipeline degrades gracefully (empty source). New: `adaptListingToScorable` + `MatchSource` type + `MatchCoreOptions` in `matchAgent.ts`; `SourcePill` in `SuggestionCard.tsx`; source filter in `SuggestedProjectsModal.tsx`. Tests: `marketListingAdapter.test.ts` + market/source cases in `suggestGroups.test.ts`.) | 2026-06-26 (**Assistant Product Completion — grouped "Suggested Projects" modal + Add-to-client + audit log + feature flag.** The Follow-up side panel's "Suggested Projects" button now opens a LARGE modal: top = current preferences + missing-preference warnings; left = grouped, ranked project cards in five honest buckets (`exact_matches` / `nearby_alternatives` / `budget_matches` / `location_matches` / `fallback_matches`); right = the same assistant chat for explain/compare/pitch/WhatsApp. Grouping is **deterministic, no LLM** — a new `POST /api/suggest-projects` runs the SAME `matchProjectsCore` scoring brain (extracted from the `match_projects` tool, which is unchanged) and a pure `groupMatchResults` that buckets candidates and stamps each with a `data_confidence` (high/medium/low) + normalized `data_gaps` (e.g. `missing_price_range`, `missing_project_coordinates`, `legacy_text_match_only`, `requires_verification`). Honest about the sparse data: a project with no price NEVER enters `budget_matches`; distance is district-CENTROID based (no project pins exist). Each card has actions: Why / Compare / Pitch / WhatsApp / Open details / **Add to client** (appends to `clients.preferred_projects` via the version-aware store write — user-triggered only). Every run writes one row to the new `assistant_recommendation_runs` audit table. The whole feature is gated by the `feature_flags.sales_assistant_enabled` kill switch (fail-open). The modal reads the UNSAVED follow-up preference draft, so recommendations reflect what the rep is editing before Save. See [followups-workspace.md](followups-workspace.md).) | 2026-06-25 (**Lookup-first matching with dual-read (Saudi geography layer):** the engine now resolves the requested district to the authoritative `districts` record (id + real centroid) via a new `resolveRequestedDistrict` helper, and a project whose `district_lookup` equals the resolved id is an EXACT relational match. The old text fuzzy-match on `preferred_neighborhoods` + per-project averaged centroids is RETAINED as the dual-read fallback for projects not yet backfilled. New geo helpers in `src/lib/locationUtils.ts`: `pointInPolygon`, `pointInDistrict`, `distanceBetweenCentroids`, `haversineKm`, `normalizeArabicDistrictName`, `normalizeEnglishDistrictName`. See [record-management.md](record-management.md) and [data-storage.md](data-storage.md).) | 2026-06-21
**Related PRDs:** [ai-agent.md](ai-agent.md), [copywriter-intelligence.md](copywriter-intelligence.md), [followups-workspace.md](followups-workspace.md), [data-storage.md](data-storage.md), [record-management.md](record-management.md)

## Direction narrowed — Project Finder ONLY (read first, 2026-06-28)
The product direction was deliberately narrowed to **one focused, deterministic capability: the Project Finder.** It does one thing well — turn a client's requirements into a ranked, geography-verified shortlist of the right projects — with no conversational AI, no next-best-action coaching, no message drafting, and no AI deciding which project is best.

The broad conversational assistants that this PRD previously described — the **AI sales agent** (`ai_chats`), the **reel copywriter** (`copywriter_chats`), and the **conversational match co-pilot** (`matching_chats`, with its lead-temperature / next-best-action / WhatsApp-draft / task-creation tools) — are **unwired**, not deleted. A static build flag `PROJECT_FINDER_ONLY` (`src/lib/featureFlags.ts`, currently `true`) hides them from the sidebar and routes their pages to a `RetiredAssistantNotice`; their code and their Supabase models + records are kept intact so a single flip to `false` restores every old surface. The rest of this PRD documents the **two states**: the live deterministic engine (current), and the legacy unified-assistant design retained under the flag for reference/rollback.

**What was REMOVED from the product surface** (code may still exist, but no UI reaches it): next-best-action, lead temperature, WhatsApp message drafting, task/follow-up creation, AI-picked projects, the conversational AI flow, and the copywriter. The deterministic ranked groups are the whole surface.

## Build status
| Capability | Status |
|---|---|
| **Deterministic Project Finder (select + score + rank, code-only)** | ✅ Live |
| **PostGIS boundary verification (point-in-polygon district)** | ✅ Live |
| **Four location-centric result groups** | ✅ Live |
| **Standalone Project Finder page (sidebar tool, structured fields, no client)** | ✅ Live |
| **Geo-integrity audit (view + summary function)** | ✅ Live |
| **Bounded AI: parse (before) + explain (after) only, with a hard ranking guard** | ✅ Live |
| Two-tier source search (our_projects → all_projects → market_listings) | ✅ Live |
| Deterministic score/band pass-through | ✅ Live |
| Grouped Suggested Projects modal + Add-to-client + audit log | ✅ Live |
| Conversational match co-pilot (`matching_chats`) | ⛔ Unwired (`PROJECT_FINDER_ONLY`) |
| Next-best-action / lead temperature | ⛔ Removed from surface |
| Message drafting | ⛔ Removed from surface |
| Task / follow-up creation from the assistant | ⛔ Removed from surface |
| AI-picked projects / free-text chat | ⛔ Removed from surface |

## What it is (in plain English) — current
The **Project Finder** turns a client's requirements — city/cities, district(s), property type(s), budget, area, bedrooms, bathrooms — into a ranked, fact-checked shortlist of the best-matching projects, grouped by how well their *location* matches. **Multiple selected options for one preference are alternatives (OR):** a client who accepts "شقة أو دور" or "الربيع أو العارض أو الملقا" gets full credit on that dimension for a result matching ANY of the acceptable options. (Amenities are the deliberate exception — they are cumulative wants, scored proportionally.) The groups:
1. **Exact district matches** — the project sits in the requested district, coordinate-verified.
2. **Nearby district matches** — a different district, ranked by real distance (closest first).
3. **Same-city matches** — the requested city, a different district.
4. **Broader fallback** — outside the requested area, surfaced only when the better groups are thin.

Every group is built by deterministic code. A project's district is decided by **where its map pin actually falls** (PostGIS point-in-polygon against 3,732 district boundaries) — the stored `location.district` text is NEVER blindly trusted; a stored district that conflicts with the pin is flagged as a `mismatch` and dropped to low confidence. Each match carries an honest `geo_confidence` (high = pin verified inside a polygon, medium = stored district but no coordinates, low = mismatch / outside known boundaries / no geography), `data_gaps`, and `mismatch_warnings`.

The optional AI does exactly two bounded things and nothing else: it **parses** a salesperson's messy free-text note into structured requirements *before* the engine runs, and it **writes a short 1–2 sentence explanation** for each match *after* the engine has ranked them. A hard server guard (`assertRankingUnchanged`) physically prevents the AI from changing which projects appear, their score, band, source, match type, or order — the AI can only swap explanation text for real matches.

The Finder powers the **Suggested Projects** button in the Follow-up Workspace (matching against the rep's live, unsaved preference draft) via the `POST /api/project-finder` endpoint. As of 2026-06-29 the Follow-up modal calls `/api/project-finder` directly (`parse:false` + `explain:false` — fully deterministic, no LLM, four location-centric groups). The legacy `POST /api/suggest-projects` endpoint (the old grouped five-bucket modal) is also boundary-verified (`verifyGeo:true`) but is **no longer reached by any UI** — it is retired from the Follow-up flow.

## Two surfaces for the same engine (current)
The deterministic Finder is reached from **two** places, both calling `POST /api/project-finder` deterministically (`parse:false` + `explain:false`):
1. **Follow-up "Suggested Projects" modal** (`SuggestedProjectsModal`) — client-scoped. Matches against the rep's live, unsaved follow-up preference draft; passes a `client_id` (so the client's `location_items` geo-preference gate applies) and the full client-action card set (select / bulk-save / save-as-option / eliminate / reactivate into `client_property_options`). On the full-page/embedded `SuggestedProjectsView` the bulk **Save options** action sits in the TOP header (primary, with a selected-count badge) next to a secondary **Done**; Done/Esc without a single option saved this visit triggers the `LeaveWithoutSavingModal` confirmation (2026-07-13).
2. **Standalone Project Finder page** (`src/pages/ProjectFinder/ProjectFinderPage.tsx`, route `/project-finder`, sidebar tool) — **no client required.** The salesperson fills structured pickers (sourced from the live `clients` model's own fields, rendered via `DynamicField` — location cascade, preferred districts, unit type, budget, area, amenities — plus Bedrooms / Sources / Minimum-match controls), then runs the same engine with **no** `client_id` (no geo-preference gate, nothing to save). Result cards render **Details only** via `FinderCard`'s `hideClientActions` prop. This is a pure discovery/lookup surface — useful for ad-hoc "what do we have in حي X under Y budget?" questions outside any client conversation. Visibility is per-profile (`project_finder` custom page, `default_access:'all'`). When a client IS selected, the bulk **Save options** button lives in the page header (selected-count badge), and clearing the client (or closing the tab) after results were shown without saving any option triggers the same `LeaveWithoutSavingModal` confirmation (2026-07-13).

## What it WAS (legacy unified assistant — retained under PROJECT_FINDER_ONLY)
The sections below describe the broad conversational **Sales Assistant** that preceded the narrowing. It is **unwired** today; its code + data are kept for rollback. Treat everything in "Product direction — ONE unified Sales Assistant", the conversational capabilities (next-best-action, message drafting, task creation, comparison-by-chat, customer understanding, feedback/insights), and the split-pane chat UI as **dormant** unless `PROJECT_FINDER_ONLY` is flipped to `false`.

## Product direction — ONE unified Sales Assistant (LEGACY — unwired)
This is intentionally designed as **one unified sales assistant face** — **"مساعد المبيعات" / "Sales Assistant"** — with multiple internal capabilities, NOT a collection of separate assistants/pages. The salesperson opens one assistant, one chat, one conversation history, and types naturally; the assistant routes to the right internal capability. **Project matching + sales-pitch generation is the FIRST active capability.** Future capabilities — next-best-action / sales consulting, follow-up & task support, project comparison, customer understanding — will be added **inside this same assistant** (same chat, new tool + new structured card type), never as a second face or a new page.

### One assistant, multiple SURFACES (2026-06-21)
The same assistant brain is exposed through more than one **surface** — never a second assistant product:
1. **Main Sales Assistant page** — the standalone split-pane chat at `/model/matching_chats` (persisted sessions).
2. **Follow-up record side panel** — a contextual `SalesAssistantSidePanel` embedded on the right of the **Follow-up Workspace** ([followups-workspace.md](followups-workspace.md)). It streams the **same `/api/match` brain** and renders the **same cards**; the only difference is it injects the current follow-up's client-preference **draft** (unsaved edits included) as context, so "Suggested Projects" matches against what the rep is editing live on the call — **before Save**. Panel state is ephemeral (not persisted). There is deliberately **no** "Follow-up Assistant" — it is the Sales Assistant, scoped to a follow-up.
3. **Suggested Projects modal (2026-06-26)** — the panel's "Suggested Projects" button opens a large grouped modal (`SuggestedProjectsModal`). Its left pane (grouped, ranked cards) is **deterministic** — fed by `POST /api/suggest-projects` (no LLM), grounded in the same draft-first preferences; its right pane is the same assistant chat (`AssistantChatPane`, reusing `streamMatchTurn`) for explain/compare/pitch/WhatsApp on the selected projects. This is the "Suggested Projects" surface of the SAME assistant — not a separate product.

**Surfaces gated by a kill switch (2026-06-26):** the Follow-up side panel + Suggested Projects modal are hidden when `feature_flags.sales_assistant_enabled` is `false` (read fail-open via `useFeatureFlag`). The standalone main page is unaffected. Rollback for the embedded assistant is a one-row flip, no schema change, no redeploy.

**Project WhatsApp from a Suggested Projects card (2026-06-27):** a card's **واتساب / WhatsApp** button opens the CLIENT's chat composer pre-filled with the project's stored WhatsApp message — REUSING the existing `chat_templates` + message-generation process, not a new one (`ProjectWhatsAppFlow`). It looks up a `chat_templates` record linked to the project by `project_id`; if found it opens `StartChatModal` (client preselected, new `initialBody` prop) with the body prefilled. If none exists, it offers to generate one with the SAME deterministic engine (`resolveProjectFacts` → `composeProjectMessage`, fed a synthetic record carrying the all_projects link so it works for any suggested project), lets the rep review/edit, and on approval STORES it as a `chat_templates` record (`language:'both'`, `project_id` link) before opening the chat. All as stacked popups over the modal — the rep never leaves the follow-up; the message lands in the client's chat on send. See [chats.md](chats.md).

**Naming (Phase 1.2, 2026-06-21):** the user-facing label is **مساعد المبيعات / Sales Assistant**. The technical name stays `matching_chats` and the endpoint stays `/api/match` (Option B — relabel only, no destabilizing rename of the deployed system). When future capabilities land, a backend rename to `sales_assistant` / `/api/sales-assistant` can be considered, but is not required.

## What it is (LEGACY unified assistant — unwired under PROJECT_FINDER_ONLY)
A live-call sales co-pilot. A salesperson on a call types what the customer wants — city, district, property type, budget, area, bedrooms, lifestyle — and the assistant instantly returns the best-matching projects, ranked, with a ready-to-say sales pitch for each. It searches **our curated projects first**; only if there's no good match there does it fall back to the broad **All Projects** database, clearly flagging those results as needing verification before being offered to the customer. It renders as a split-pane chat (past sessions on the left, the active conversation on the right) under the sidebar item **"مساعد المبيعات" (Sales Assistant)**.

## Why it exists
During a live call the salesperson needs a confident recommendation in seconds, grounded in real inventory — not a hand search through 1,372 projects, and not an AI that invents prices or availability. The assistant turns the customer's words into a ranked, fact-checked shortlist plus the exact sentence to say next.

## Full capabilities (one unified assistant — 2026-06-21)
The Sales Assistant is ONE chat that routes the salesperson's plain-language message to the right internal capability. All capabilities are tools + structured cards inside the same conversation — never a separate page/assistant.

1. **Project matching** — `match_projects` → `ProjectMatchCard`. (Detailed below.)
2. **Location intelligence (lookup-first + dual-read, 2026-06-25)** — `match_projects` is coordinate- AND district-aware. The requested district is resolved to the authoritative `districts` record (id + real centroid) via `resolveRequestedDistrict` (city-aware when a city is given). A project whose **`district_lookup`** equals the resolved district id is an **EXACT relational match** (no text needed). For projects not yet backfilled, the engine **dual-reads** the legacy path: text fuzzy-match on `all_projects.preferred_neighborhoods` + the project's stored `latitude`/`longitude`/`geo_source`/`geo_confidence` (from `2026-06-21_project_geo.sql`: inline map-link coords = high confidence, district centroids = medium, text-only = none). A candidate outside the requested district but within **12 km** of the district centroid is a **"nearby"** match carrying a real `distance_km` (haversine); tiers are exact → nearby → same_city → partial. The agent states the real distance, never invents proximity, and flags medium-confidence (centroid) coords as approximate.
3. **Project comparison** — `compare_projects(project_ids, requirements?)` → `emit_comparison` → `ComparisonCard`: side-by-side facts, deterministic per-project fit score/band, dimension winners (cheapest / largest / most bedrooms / most available), a verdict + recommended pick.
4. **Sales consultant / next best action** — `get_customer_context(client_id|phone|name)` returns the **deterministic** lead temperature (hot/warm/cold/won/lost — computed from stage + activity recency + status), stage, status, the already-scheduled next action (type/due/overdue), and preferences → `emit_next_action` → `NextActionCard` (risk, recommended action, talking points, follow-up timing).
5. **Message drafting** — `emit_message` → `MessageCard`: WhatsApp / SMS / call-script drafts built only from real facts, with a copy button.
6. **Task / follow-up creation (confirmation-gated)** — `propose_task` NEVER writes; it surfaces a `TaskProposalCard` with an editable due date and **Confirm / Dismiss**. Only on Confirm does the client create a `followups` record (via `saveRecord`). The agent is forbidden from claiming the task was created.
7. **Customer understanding** — the agent continuously extracts/refines the customer's structured requirements from the whole conversation (the chat history is its memory); cards re-serialize into history so context carries across turns.
8. **Feedback + telemetry + admin insights** — every assistant answer carries a 👍/👎 control. 👍 saves instantly; 👎 asks "what were you trying to do?" (find project / compare / understand customer / write message / create task / other) with an optional one-line note, so a miss is tagged by the rep's actual intent. Each answer also stores **inline per-turn telemetry** (which capabilities fired, response time, whether the turn errored) on the message — no schema change, no extra write path. An admin-only **Sales Assistant insights** dashboard (`/sales/assistant-insights`, gated by `useIsAdmin`, opened from a chart icon in the session list) aggregates the conversations the admin can see: capability/card usage, helpful rate (👍/👎), requests that didn't land (by intent), written rep comments, errored turns, and average response time. The aggregation (`aggregateInsights`) is pure and unit-tested; it tolerates any malformed message shape.
10. **Grouped Suggested Projects + Add-to-client (2026-06-26)** — a DETERMINISTIC endpoint (`POST /api/suggest-projects`, no LLM) reuses `matchProjectsCore` + a pure `groupMatchResults` to return five honest buckets (`exact_matches` / `nearby_alternatives` / `budget_matches` / `location_matches` / `fallback_matches`), each project carrying `data_confidence` (high = lookup + active + price/unit data; medium = lookup/active but missing price/unit; low = legacy text / requires-verification / partial / sold-out) and normalized `data_gaps`. Honest-data rules: a price-less project can NEVER be a `budget_match` (it surfaces `missing_price_range` instead); a strong-geo project with no price drops to `location_matches`; distance is district-centroid based and labelled as such (no project pins exist). The `SuggestedProjectsModal` renders the buckets as tabs + cards with a missing-preference banner; the **Add to client** card action appends the project to `clients.preferred_projects` (existing field) via the version-aware `saveRecord` — user-triggered only, never from AI text. Every run is logged to `assistant_recommendation_runs`. **No-criteria honesty (2026-06-26):** when the client has NO matchable preference at all (no location/budget/type/area/bedrooms), `scoreProject` has only `availability` left and renormalizes every available project to 100/strong — which is NOT a real match. The grouping detects this (`metadata.has_criteria=false`) and routes everything to `fallback_matches` as **"غير مطابق / Not matched"** (band forced partial, low confidence, reason `no_criteria`), and the modal shows a banner explaining these are general available inventory, not matched to the client — gather a district/budget/type for a real match.

9. **Find a project by name** — `search_projects(query)` resolves a named project to candidate `project_id`s (name, city, district, our_projects vs all_projects, requires_verification) so the agent can fetch its facts (`get_project`) or get the ids it needs for `compare_projects`. Powers the **"معلومات مشروع / Project Info"** quick action (info about a named project) and comparison-by-name; if more than one project matches, the agent asks which one. Added 2026-06-21 to close the long-standing gap where the prompt told the agent to "search first if you only have names" but no search tool existed.

**Deterministic-truth contract (all capabilities):** score / band / match_type / data_source / requires_verification / distance_km (matching, comparison) and lead_temperature / stage / next_action (consultant) are computed in code and quoted verbatim; the server reconciles `emit_recommendation` metadata so the model cannot re-score. The LLM only generates explanation, pitch, message, talking points, and questions — always from verified data.

## Key behaviors — Project Finder (current)
- **Selection, scoring, ranking, and grouping are 100% deterministic code.** `findMatchingProjects` (in `api/_lib/projectFinder.ts`) runs `matchProjectsCore` with PostGIS verification ON, then `groupForFinder` buckets the scored candidates into the four location-centric groups and ranks within each. No AI decides which project is best, scores a project, or sets the order.
- **District is decided by the map pin, not by stored text (boundary verification).** When `verifyGeo` is on, the engine resolves every candidate's containing district once per request via the `districts_for_points` PostGIS RPC (point-in-polygon against the 3,732 `district_boundaries` polygons). The polygon-derived district is the **source of truth** for an "exact" match — a project whose stored `location.district` conflicts with the polygon its pin sits in is classified `mismatch` (low confidence, warned), never a trusted exact. `classifyGeo` (in `api/_lib/geoVerify.ts`) is the pure classifier; its status/confidence recipe is kept **semantically identical** to the SQL audit view `v_all_projects_geo_integrity`.
- **Honest geo confidence + data gaps.** Each match carries `geo_status` (`verified_match` / `verified_derived` / `mismatch` / `outside_known_districts` / `unverified_no_coords` / `no_geography`), `geo_confidence` (high / medium / low), `mismatch_warnings`, and `data_gaps` (e.g. `missing_project_coordinates`, `coordinates_outside_known_districts`, `district_mismatch`). A project with no coordinates can still match — at **medium** confidence, with the gap recorded — never silently dropped or silently trusted.
- **Four location-centric groups.** `exact_district_matches`, `nearby_district_matches` (ranked by REAL haversine distance, closest first), `same_city_matches`, and `broader_fallback`. Grouping follows the verified `location_tier` ALONE (2026-07-02): a project inside ANY selected district is ALWAYS in the exact group — a weak overall fit (wrong type / missing price → `partial` band) shows via the band badge and sorts to the bottom of its group, but never relabels the geography (the old partial→fallback override falsely claimed in-district projects were "outside the requested area"). A `weak` finder band (low/no geo confidence or requires-verification) sorts below `strong`/`good`/`partial`.
- **Distance from the nearest selected reference, on every card.** `distance_km` is computed for EVERY tier (same-city and fallback included), measured to the NEAREST of all selected references — requested district centroids + the client's selected location elements (`location_items` anchors resolved from `geo_elements`) — and `nearest_ref_name` says which one ("~4.2 كم من النرجس" on the card pill and in the deterministic explanations).
- **Selected districts match by AUTHORITATIVE id.** The SPA sends `district_ids` (the actual records the client picked); fuzzy name resolution only runs for free-text/LLM-parsed requests where no id resolved.
- **The AI is bounded to two touchpoints, with a hard ranking guard.** The ONLY LLM calls (`api/_lib/projectFinderAI.ts`, a small/fast model `claude-haiku-4-5`): (1) `parseRequirements` — messy text → structured requirements, BEFORE ranking, with explicit fields always winning over parsed ones; (2) `explainMatches` — a short per-match explanation, AFTER ranking. `applyLlmExplanations` reads ONLY `{project_id → text}` and copies every ranking field straight from the deterministic result, and `assertRankingUnchanged()` throws if any score/band/source/match_type/order/membership differs before vs after. Both AI calls fail safe (return `{}` on any error) — the deterministic engine still runs without them.
- **Deterministic explanation fallback.** Even with no AI key, every match gets a fact-grounded templated explanation (`buildExplanation`) built only from verified facts (district, distance, price range, available units, source verification reminder). The LLM can only replace this string, never the ranking.
- **No matchable preference → ask, don't pad.** When the requirements carry no location/budget/type/area/bedrooms/bathrooms/amenities signal (`hasAnyCriteria` is false), the endpoint short-circuits with empty groups + `needs_preferences:true` + the missing required preferences, instead of returning "everything is a strong match."
- **Three sources, source is orthogonal to quality.** Our portfolio (`our_projects`), the broad database (`all_projects`), and external market listings (`market_listings`) are all scored by the SAME engine; every all_projects/market pick carries a verify-before-offering reminder in its explanation and (for market) `requires_verification`.
- **Send to client from the card (2026-07-04).** On client-scoped surfaces every result card carries **"إرسال للعميل / Send to client"**: reuses the prepared `chat_templates` message for that project (`project_id`) / listing (`listing_id` + `status:'ready'`), or starts the existing creation flow right from the card, then opens the client's chat composer (`StartChatModal`, client preselected, body prefilled, gallery/cleaned photos + direct video files riding along as separate messages). Hidden on the standalone no-client finder. The market-listing **Contact advertiser** button is the sibling action targeting the ADVERTISER instead.
- **Collapsible preferences area (2026-07-04).** The preferences-chips + missing-warnings block collapses to one thin summary row (count + truncated values + missing badge); popup hosts default it collapsed (`defaultPrefsCollapsed`) so the results own the height, and the edit-preferences panel stays height-capped + auto-collapsing on Search.
- **Districts can be picked on the map (2026-07-13).** The location editor's «اختيار من الخريطة» opens `DistrictMapPicker` — the city's real district boundary polygons (from `district_boundaries` via `wassell_city_district_shapes`) rendered tappable on the branded map; tap = toggle an include rule, red = saved excludes (chip-managed), Apply rewrites only the include-district items in `location_items`. Same polygons the geo verification uses, so the tap and the match can never disagree.
- **Free-drawn areas + exclusion zones (2026-07-13).** The map picker's «رسم منطقة» mode lets the rep draw one or MORE separate polygons; a polarity toggle in the header («أريدها» gold / «استثناء» red — the preview recolors mid-draft) decides whether each shape is a WANTED area (independent `drawn_area` include item, `{kind:'drawn_area', coordinates: closed [lng,lat] ring, label}`, OR-union) or an EXCLUSION zone (`polarity:'exclude'` — no results inside it, labelled «منطقة مستثناة N», rendered red). Compiled by `wassell_compile_geo_items` to a real polygon and matched/subtracted on the indexed ST_Contains path — as fast as a district rule (verified live: an include probe box grew a 4-district gate 476→1,549; an exclude box covering all 4 districts dropped it 476→0). Deletable via footer chips. TS twin `geoMatch.ts` mirrors the branch (tests included).
- **Timeout = recovery panel, not a dead end (2026-07-13).** When the Edge request exceeds its ~25s budget (504 `FUNCTION_INVOCATION_TIMEOUT` — typical trigger: a city-wide directional geo rule like "جنوب طريق الملك سلمان" ≈ 36.7k matched points), the finder shows a bilingual explanation + **Narrow preferences** (opens the edit panel) + **Try again** (re-sends the same draft) instead of a raw error. Typed via `FinderRequestError.timeout` in `src/lib/matching/projectFinder.ts`.
- **Select all filtered (2026-07-18).** On both client-scoped surfaces, a header «تحديد الكل / Select all» toggle (beside «حفظ الخيارات») selects every match passing the current refinement (score slider + refine filters, all tabs; badge = filtered total) — or clears exactly those when they're all selected. Bulk save persists only the filtered∩selected intersection, so "select all, then raise the slider to 90%, then save" saves only the ≥90 matches.
- **Soft budget floor (2026-07-19).** `budget_min` is a segment filter, not a hard wall: candidates priced up to 10% below it survive the market DB pre-filter (the RPC receives the softened floor) and score 0.9 on the budget dimension — so a 1.9M find still surfaces for a 2.0–2.1M client, ranked just under in-window matches. Further below the floor stays 0.2 ("wrong segment"). `budget_max` is unchanged (over-budget is only reachable via the 15% stretch, catalog scoring only).
- **Bedrooms mean "at least" (2026-07-19).** The request number is a MINIMUM everywhere: the market RPC filters `bedrooms >= p_bedrooms` (was exact `=`) and the scorer gives full credit when any unit offers ≥ the requested count (one-under = 0.6 near-miss). This matches the client-side `bedroomsMin` refine, which always treated it as a minimum.
- **Must-have amenities — a hard gate (2026-07-19).** Both finder UIs show an «إلزامية / Must have» checkbox under the amenities picker; when on, the selected amenities are sent as `required_amenities` and the engine DROPS any candidate (all three sources) that doesn't fuzzy-match ALL of them against its amenity evidence (Aqar `features` tags for market listings, `preferred_amenities` for catalog projects). Fails CLOSED — a candidate with no amenity data is excluded too, and an engine note reports how many candidates the gate removed so a thin list never reads as "that's all there is". The toggle is UI-only state (never written to the client record); required amenities still earn the amenities subscore.
- **Audit trail.** Every Finder run writes one row to `assistant_recommendation_runs` (input preferences, group/geo counts, result project ids, whether AI parse/explain were used) — best-effort, never fails the recommendation.
- **RLS-scoped.** `/api/project-finder` runs under the caller's Supabase JWT, so a salesperson only matches projects they can already see.

## Key behaviors — LEGACY unified assistant (unwired)
- **Deterministic matching in code, narration by the AI.** The agent extracts requirements and calls one `match_projects` tool that does ALL filtering, tiering, and scoring server-side. The AI only writes prose over the verified, scored results — so it can't invent a project or fudge a score.
- **Two tiers, never mixed.** Tier 1 = projects in the `our_projects` model (curated, real units). Tier 2 = the rest of `all_projects` (scraped/competitor, unverified). The tool returns them in **separate** arrays and only includes the Tier-2 group when Tier 1 has no good match (`used_fallback`).
- **A third source — Market Listings (`market_listings`), AREA-pre-filtered.** When `includeMarket` is set (the grouped `/api/suggest-projects` endpoint passes it; the LLM `match_projects` tool does not, so its two-tier output is unchanged), the engine also scores the ~46,315 external Aqar ads. To avoid scanning 46k rows per recommendation, it first narrows them server-side to the client's relevant area: district + every neighbouring district within **20 km** of the centroid, else the requested city, else the newest 4,000 (a logged capped scan, never an arbitrary slice). The same `scoreProject` then grades the in-area listings identically to projects (each listing is adapted to the project shape by `adaptListingToScorable`). Two partial JSONB-expression indexes on the model's `district_lookup` / `city_lookup` keep the filtered read fast (~70ms/page). Every market pick carries `data_source:"market_listings"` + `requires_verification:true`.
- **Verification warning on All Projects results.** Every Tier-2 pick carries `data_source:"all_projects"` + `requires_verification:true` and renders an amber "verify price, availability, details before offering" banner.
- **Location is lookup-first + coordinate-aware (dual-read, 2026-06-25).** The requested district is resolved to an authoritative `districts` record; a project whose `district_lookup` equals it is an exact relational match (full credit). For projects not yet backfilled, the engine falls back to the legacy text fuzzy-match on `preferred_neighborhoods` + stored coordinates. A different district within 12 km → a "nearby" match with a real `distance_km`; else same-city → half credit. The assistant states the real distance and never invents proximity. Reverting to text-only matching is a one-line flip (the dual-read fallback is always present).
- **Weighted scoring (0–100):** location 30, budget 25, property type 20, area 10, bedrooms 8, availability 5, amenities/lifestyle 2. Dimensions the customer didn't specify are excluded and the weights renormalize, so an unspecified field neither penalizes nor inflates. Bands: strong ≥75, good ≥55, partial ≥40; below 40 is never surfaced.
- **Budget stretch:** an option up to 15% over `budget_max` is surfaced as a "stretch" (scored down, labelled). Beyond that → no budget credit.
- **Sold-out excluded by default** (`available_units = 0`), unless the request opts in.
- **Anti-hallucination:** the AI states only facts present in the tool output; missing details become "not available in the data" + a question to ask the customer. The recommendation card's specs are built only from present values.
- **Structured recommendation card** (`emit_recommendation` → `ProjectMatchCard`): per pick — name, band + score, why it matches, key specs, selling points, a copy-able pitch, verification warning (if Tier 2), and questions to ask. Plus overall clarifying questions. Display-only (no record is created).

## User flows — Project Finder (current)
1. **Suggested Projects from a follow-up (happy path):** Rep opens a follow-up, captures the client's preferences inline → clicks **Suggested Projects** in the Workspace → the deterministic Finder runs against the rep's live (unsaved) preference draft → the modal shows the four ranked groups (exact district, nearby, same city, broader), each card with score/band, geo confidence, distance, price, data-gap chips, and a short explanation. The perfect (score-100) matches come pre-checked; the rep selects any others — or hits **تحديد الكل / Select all** to select every currently-filtered match at once — then **Save selected to client options** writes them into the client's unified **Client Property Options** (status `suitable`). Per-card, the rep can **Save as option** or **Eliminate** (with a reason); a source already saved shows its status, and an eliminated one offers **Reactivate**. See [clients.md](clients.md).
2. **Parse-from-text:** A caller passes free-text (`text`) to `/api/project-finder` → the bounded parser extracts structured requirements → the deterministic engine ranks → optional bounded explanations are attached → response returns the four groups + metadata (geo counts, used_ai_parse/explain, generated_at).
3. **Geo mismatch surfaced:** A project whose stored district disagrees with its pin's polygon is classified `mismatch`, dropped to low geo confidence, and its card shows "verify the record" — it is never offered as a trusted exact district match.
4. **No preferences yet:** The client has nothing matchable → the endpoint returns empty groups + `needs_preferences:true` + the missing required preferences, and the UI prompts the rep to capture a district/budget/type.
5. **Send to client from a card (2026-07-04):** Rep spots a good match in the finder (or on the Client Options list) → clicks **إرسال للعميل / Send to client** → a stored message for that project/listing opens the client's chat composer directly (client preselected, body prefilled, photos/videos attached as follow-up messages); no stored message → the existing creation flow runs in a stacked popup (project: deterministic compose + review; listing: AI text + photo cleaning + approve) and then continues into the chat. The rep reviews/edits the text and presses Send — the message lands in the client's WhatsApp conversation.

## User flows — LEGACY unified assistant (unwired; deep-link → RetiredAssistantNotice)
1. **Main happy path:** Salesperson opens Matching Assistant → "New match" → types the customer's request → the agent calls `match_projects` → a good Tier-1 (our_projects) match is found → the chat shows the ranked recommendation and a card with the pitch → salesperson reads/copies the pitch on the call.
2. **Fallback flow:** No good match in our_projects → the tool searches all_projects and returns those flagged `requires_verification` → the agent opens with the verification warning and the card shows an amber banner per pick.
3. **No exact district:** District not found → tool sets `district_exact_match:false` and returns same-city alternatives (`match_type:"same_city"`) → the agent says "no exact match in <district>, here are same-city options".
4. **Vague / thin request:** Too little to match well → the agent asks 1–2 sharp questions (budget? district? villa or apartment?) before searching.
5. **Empty / unrealistic:** Nothing scores ≥40 (or budget is unrealistic) → the agent says so plainly and names the single requirement to relax; it does not pad with weak options.

## Data touched — Project Finder (current)
- **Reads:** the same `unified_records` portfolio (`our_projects` / `all_projects` / optional `market_listings`) and the `districts` reference model as the legacy engine, PLUS — for boundary verification — the **`district_boundaries`** PostGIS polygons via the `districts_for_points(jsonb)` RPC (one bulk call per request with all candidate pins). The geo-integrity audit view `v_all_projects_geo_integrity` + `project_geo_integrity_summary()` read the same boundaries (security-invoker, RLS-respecting).
- **Writes:** one `assistant_recommendation_runs` audit row per `/api/project-finder` run (best-effort); the user-triggered **Save to client options** / **Eliminate** / **Reactivate** actions create/update `client_property_options` records (status, `data.facts` snapshot, match score) via the version-aware `saveRecord` — the only client-side writes (added 2026-06-30, replacing the old "Add to client" → `clients.preferred_projects` / `preferred_market_listings` flow; the `preferred_*` fields stay preference INPUTS). The Finder itself writes nothing to project data and persists no recommendation.
- **AI touchpoints:** Anthropic Messages API (`claude-haiku-4-5`) for the bounded parse + explain calls only; both fail safe (no key → engine still runs).

## Data touched — LEGACY unified assistant (unwired)
- **Reads:** `unified_records` (frozen-safe) for `all_projects` (the 1,372-row portfolio) and `our_projects` (the curated 11, via the `project` lookup → defines Tier 1); the `districts` reference model (via `resolveRequestedDistrict`) for the authoritative district id + centroid (and via `nearbyDistrictIds` for the within-20km neighbour set used to pre-filter market listings); `models.schema` of `all_projects` for `get_project` field labels + rollup flags. Matching reads `district_lookup` (lookup-first, 2026-06-25) plus the legacy fields used as the dual-read fallback: `project_name`, `preferred_city`, `preferred_neighborhoods`, `latitude`/`longitude`, `unit_types`, `project_status`, `preferred_amenities`, and the stored rollups `price_range` / `area_range` / `bedroom_range` / `bathroom_range` / `available_units` / `unit_count` (`{min,max}` objects + numbers). **When `includeMarket` is set, also reads `market_listings` (~46,315 rows) — AREA-pre-filtered server-side by `data->>district_lookup` (neighbour set) / `data->>city_lookup` (city) via the partial indexes, or the newest-4,000 capped scan; each row's scalar `price` / `area` / `bedrooms` + per-listing `latitude`/`longitude` + `district_lookup` are read by `adaptListingToScorable`. `market_listings` is a lazy-loaded model — see [data-storage.md](data-storage.md) — but this server-side read goes straight to `unified_records` and is unaffected by the SPA boot exclusion.**
- **Writes:** `records.data` (JSONB) on the `matching_chats` model only — each session's `messages` array, `title`, `status`, `message_count`, `last_message_at`. Nothing is written to project data. The structured recommendation is **not** persisted server-side (delivered over SSE, stored on the assistant message). **2026-06-26:** `/api/suggest-projects` writes one row per run to `assistant_recommendation_runs` (input prefs summary + result counts + project ids + draft/legacy flags, owner = auth.uid()); the **Add to client** action appends to the `clients` record's `preferred_projects` via `saveRecord` (the only project-side write, user-triggered).
- **Reads (2026-06-26):** `feature_flags` (the `sales_assistant_enabled` kill switch, fail-open); the loaded `districts` / `cities` records (to resolve a client's lookup preferences to district/city names for the requirements mapper).

## Key files
| File | What it does |
|---|---|
| **Project Finder (current)** | |
| `api/project-finder.ts` | **The Project Finder endpoint** (`POST /api/project-finder`, edge, JWT/RLS-scoped). Normalizes requirements, optionally LLM-parses free `text` (explicit fields win), runs the deterministic `findMatchingProjects`, optionally attaches bounded LLM explanations (then runs `assertRankingUnchanged`), short-circuits on no-criteria, writes the audit row. No next-best-action / lead-temperature / task / message — by design |
| `api/_lib/projectFinder.ts` | The deterministic engine layer: `findMatchingProjects` (runs `matchProjectsCore` with `verifyGeo:true`), `groupForFinder` (four location-centric groups, distance-ranked nearby), `buildExplanation` (templated fact-only fallback), `finderBand` / `finderGroupFor`, `hasAnyCriteria`, `missingPreferences` |
| `api/_lib/projectFinderAI.ts` | The ONLY two LLM touchpoints: `parseRequirements` (text → requirements, before) and `explainMatches` (per-match text, after), plus the guard trio `snapshotRanking` / `assertRankingUnchanged` / `applyLlmExplanations`. Cheap model `claude-haiku-4-5`; both calls fail safe |
| `api/_lib/geoVerify.ts` | `classifyGeo` — pure boundary-verification classifier (status + effective district + confidence + warnings + gaps). Recipe kept identical to the SQL audit view |
| `src/lib/matching/projectFinder.ts` | Browser client for `/api/project-finder` (`fetchProjectFinder`, sends `parse:false` + `explain:false`, plus `sources` + `locale`) + the mirrored `FinderResponse` / `FinderMatch` / `FinderGroupKey` types + `FINDER_GROUP_KEYS` / `totalFinderMatches`. The Follow-up modal passes all three sources (`our_projects` + `all_projects` + `market_listings`) and the current UI locale. Powers the Follow-up Suggested Projects modal |
| `src/pages/Followups/components/FinderCard.tsx` | One Project Finder result card — band/score, match type, geo_confidence + geo_status, distance_km, data_gaps, mismatch_warnings, facts, the deterministic explanation, a **selection checkbox** (auto-checked only at score 100), and **Save as option / Eliminate / Reactivate / Details** actions (replaced the old Add-to-client on 2026-06-30); client-scoped cards also render **"إرسال للعميل / Send to client"** (`onSendToClient` prop — the parent hosts the WhatsApp flow modal); market-listing cards also render the **Contact advertiser** button (`ContactAdvertiserButton`, shown even in the standalone no-client mode); cross-references the already-saved `client_property_options` status |
| `src/pages/Followups/components/ProjectWhatsAppFlow.tsx` | **Re-wired 2026-07-04 (was orphaned since 2026-06-29)** — the project "Send to client" flow: reuses the `chat_templates` record linked by `project_id`, else generates one deterministically (`resolveProjectFacts` → `composeProjectMessage`), rep reviews/edits, approval STORES the template, then `StartChatModal` opens with the client preselected + gallery images + the project's direct video files (`project_videos` via `directVideoUrls`) riding along |
| `src/components/matching/ListingWhatsAppFlow.tsx` | The market-listing "Send to client" flow (2026-07-04) — ready `chat_templates` message (`listing_id` + `status:'ready'`) → `StartChatModal` (client preselected, body prefilled, cleaned photos + direct listing video files as ride-alongs); no message → hosts the EXISTING `ListingMessageModal` creation flow and continues into the chat once approved |
| `src/lib/matching/sendToClient.ts` | Pure send-to-client helpers — `findProjectTemplate`, `findReadyListingTemplate` (drafts never send), `templateListingImageUrls`, `templateBody`, `directVideoUrls` (direct video FILES only; HLS/page links excluded). Unit-tested (`__tests__/sendToClient.test.ts`) |
| `src/components/market/ContactAdvertiserButton.tsx` | Compact "التواصل مع المعلن / Contact advertiser" action for a market listing outside its record form (finder cards + client option cards) — cached phone → open `StartChatModal` instantly; else enqueue the REGA lookup, spin, auto-open on the Realtime write; errors toast. Built on `src/lib/regaLookup/useContactAdvertiser.ts` |
| `src/lib/matching/clientOptions.ts` | The deterministic Client Property Options engine the finder writes to — `saveClientOption` / `bulkSaveOptions` / `setMainOption` / `eliminateOption` / `reactivateOption` / `finderSourceToOptionType`; dedup + eliminated-guard; version-aware `saveRecord`. Backs the `client_property_options` model + the Client 360 Options tab — see [clients.md](clients.md) |
| `src/components/clients/ClientOptionsModal.tsx` | The **Client Options popup** (opened from the WhatsApp chat header) — hosts the options list + the EMBEDDED `SuggestedProjectsView` (`followupId=null`) so the rep finds + saves more options for the client without leaving the conversation — see [chats.md](chats.md) |
| `src/components/matching/LeaveWithoutSavingModal.tsx` | Shared leave-without-saving confirmation (2026-07-13) — "هل أنت متأكد أنك لن تحفظ أي خيار لهذا العميل؟" with Stay (primary) / Leave-without-saving. Fired by `SuggestedProjectsView` (Done + Esc) and `ProjectFinderPage` (clear client) when results were shown but no option was saved for the client this visit; both surfaces also arm a `beforeunload` prompt in that state |
| `src/pages/Followups/components/SuggestedProjectsModal.tsx` | The Follow-up Suggested Projects modal — now fetches `/api/project-finder` and renders the four finder groups via `FinderCard` (replaced the legacy `/api/suggest-projects` + `SuggestionCard`/`AssistantChatPane` path on 2026-06-29) |
| `supabase/migrations/2026-06-28_project_finder_geo.sql` | `district_for_point` / `districts_for_points` PostGIS RPCs + the `v_all_projects_geo_integrity` audit view + `project_geo_integrity_summary()`. All read-only/additive over the existing `district_boundaries` GiST index |
| `src/lib/featureFlags.ts` | Adds `PROJECT_FINDER_ONLY` (static build flag) + `RETIRED_ASSISTANT_MODELS` + `isRetiredAssistantModel`, alongside the `feature_flags` DB reader |
| `src/components/RetiredAssistantNotice.tsx` | The "this assistant has been retired" panel rendered for deep-links to the unwired `ai_chats` / `copywriter_chats` / `matching_chats` models |
| **Legacy unified assistant (unwired under `PROJECT_FINDER_ONLY`)** | |
| `api/match.ts` | SSE endpoint — one turn of the agent's tool-use loop; emits `recommendation` / `comparison` / `next_action` / `message_draft` / `task_proposal` events; runs identity-safe `resolveClient` before any task proposal. Shared by EVERY surface |
| `api/suggest-projects.ts` | **Deterministic** grouped-recommendation endpoint (no Anthropic) — maps requirements → `matchProjectsCore` (`verifyGeo:true`) → `groupMatchResults` → missing-pref warnings → audit row → grouped contract. **Retired from the UI on 2026-06-29** (the Follow-up modal now calls `/api/project-finder`); the endpoint still exists but has no UI caller |
| `api/_lib/suggestGroups.ts` | Pure grouping engine — `groupMatchResults` (5 honest buckets), `data_confidence` + `data_gaps` derivation, budget-fit, reason codes. Imports only types from `matchAgent.ts` (no runtime deps) |
| `api/_lib/matchAgent.ts` | The agent brain — system prompt, all tools (`match_projects`, `get_project`, `search_projects`, `emit_recommendation`, `compare_projects`, `emit_comparison`, `get_customer_context`, `emit_next_action`, `emit_message`, `propose_task`), the deterministic geo-aware two-tier search + weighted scorer, lead-temperature, `resolveClientFromCandidates` (never guesses on duplicate names), and the lookup-first dual-read district resolver `resolveRequestedDistrict` (2026-06-25 — resolves the requested district to the authoritative `districts` record id + centroid; an exact `district_lookup` match beats text). **2026-06-28:** gained an opt-in `verifyGeo` option (PostGIS point-in-polygon district verification via `districts_for_points` + `classifyGeo`, attaching `geo_status` / `mismatch_warnings` / honest `geo_confidence` to each result), multi-district requirements (`req.districts[]`), and a bathrooms scoring dimension. The Project Finder always calls it with `verifyGeo:true`; `/api/suggest-projects` does too. **2026-06-26:** the `match_projects` body was extracted into an exported `matchProjectsCore` (returns the object; the tool wraps it with byte-identical JSON) so `/api/suggest-projects` reuses the exact same scoring; the scorer now also returns `district_match_basis` ('lookup' \| 'text' \| null) for confidence/gap derivation. **2026-06-27:** `matchProjectsCore({ includeMarket })` adds `market_listings` as a third scored source, AREA-pre-filtered server-side — new helpers `adaptListingToScorable` (listing → project shape), `nearbyDistrictIds` (districts within 20 km of the requested centroid), `loadMarketByFilter` (`.in('data->>district_lookup'\|'city_lookup', ids)`), `loadMarketNewestCapped` (newest 4,000 fallback); `MatchSource` / `MatchCoreOptions` types |
| `src/lib/locationUtils.ts` | Geo helpers used by matching: `haversineKm`, `distanceBetweenCentroids`, `pointInPolygon`, `pointInDistrict`, `normalizeArabicDistrictName`, `normalizeEnglishDistrictName` (the last four added 2026-06-25 for the geography layer) + map-link coordinate parsing |
| `src/pages/Matching/MatchingPage.tsx` | Main split-pane page surface (session list + active thread); admin-only Insights link |
| `src/pages/Matching/components/MatchingThread.tsx` | Active thread — sends turns, consumes SSE, renders all card types, captures per-turn telemetry, persists 👍/👎 feedback, and runs the confirmation-gated task write |
| `src/pages/Matching/components/ProjectMatchCard.tsx` | Structured recommendation card (specs, pitch, warnings, questions) — reused by both surfaces |
| `src/pages/Matching/components/TaskProposalCard.tsx` | Confirmation-gated task card — shows resolved client name+phone, blocks Confirm when identity is ambiguous/not-found, shows save failure + retry inline |
| `src/pages/Matching/components/MessageFeedback.tsx` | Per-answer 👍/👎; 👎 expands to intent chips + optional note |
| `src/pages/Matching/AssistantInsightsPage.tsx` | Admin-only usage/feedback dashboard (`/sales/assistant-insights`) |
| `src/pages/Followups/components/SalesAssistantSidePanel.tsx` | **Follow-up surface** — contextual side panel: draft-first preference summary, "Suggested Projects" (opens the modal) / "Project Info" quick actions, ephemeral chat, same `/api/match` brain + same cards. Self-hides on the `sales_assistant_enabled` flag |
| `src/pages/Followups/components/SuggestedProjectsModal.tsx` | **Suggested Projects modal (2026-06-26)** — preference summary + missing warnings, grouped tabs/cards (from `/api/suggest-projects`), and the embedded `AssistantChatPane`; wires card actions (pitch/WhatsApp/compare/details/add-to-client), draft-first |
| `src/pages/Followups/components/SuggestionCard.tsx` | **Orphaned (2026-06-29)** — the old grouped `/api/suggest-projects` card (score/band/category/distance, data-gap chips, confidence pill, action buttons); no longer imported after the modal moved to `FinderCard` |
| `src/pages/Followups/components/AssistantChatPane.tsx` | **Orphaned (2026-06-29)** — the reusable streaming chat that was the old modal's right pane; no longer imported (the `/api/project-finder` modal has no chat pane) |
| `src/lib/matching/suggestions.ts` | Browser client for `/api/suggest-projects` (`fetchSuggestedProjects`) + the mirrored `GroupedSuggestions` / `SuggestionItem` types |
| `src/lib/matching/requirements.ts` | `draftToMatchRequirements` — draft-first, lookup-first mapping of client preferences → matcher requirements |
| `src/lib/matching/addToClient.ts` | The source-routed "Add to client" writers — `addProjectToClient` → `clients.preferred_projects`, `addMarketListingToClient` → `clients.preferred_market_listings` — via the version-aware store write (user-triggered, append-dedup) |
| `supabase/migrations/2026-06-29_clients_preferred_market_listings.sql` | Adds `preferred_market_listings` (multi-value lookup → market_listings, display `title`) to the clients model's "Client Preferences" section so a market listing can be saved as a client preference without polluting `preferred_projects` (clients is unfrozen JSONB → updates `models.schema`; mirrored in `src/data/seedModels.ts`) |
| `src/lib/featureFlags.ts` | `feature_flags` reader (`loadFeatureFlags`, `useFeatureFlag`) — fail-open kill switch |
| `src/lib/followups/assistantContext.ts` | Pure helper that resolves the follow-up's preferences **draft-first** (draft > saved > missing) into the UI summary + the Arabic context preface fed to `/api/match` |
| `src/lib/matching/telemetry.ts` | Telemetry + feedback types, `FEEDBACK_INTENTS`, and the pure `aggregateInsights` aggregator |
| `src/lib/matching/cards.ts` | Comparison / next-action / message / task-proposal payload types + normalizers + `serializeCardForModel` |
| `src/lib/matching/client.ts` | Browser SSE stream consumer (`streamMatchTurn`); `MatchEvent` union over all card types — used by both surfaces |
| `src/lib/matching/recommendation.ts` | Recommendation payload type + lenient normalizer (`nearby` match type + `distance_km`) |
| `src/data/seedModels.ts` | `matching_chats` system model (`MATCHING_CHATS_MODEL_ID`) |
| `supabase/migrations/2026-06-18_matching_chats_model.sql` | Creates the `matching_chats` model row in prod (stable id, idempotent) |
| `supabase/migrations/2026-06-21_project_geo.sql` | Backfills `all_projects` lat/lng + geo confidence (inline coords + district centroids) |
| `supabase/migrations/2026-06-25_geography_models.sql` | Adds the `regions` / `cities` / `districts` reference models + the `district_lookup` (projects) / `preferred_districts` (clients) / district-mirror (units) fields the engine matches on |
| `supabase/migrations/2026-06-25_geography_physical_tables.sql` | PostGIS + `district_boundaries` (GiST) / `district_aliases` / `geography_import_runs` + the on-demand boundary RPCs used for point-in-polygon matching |
| `supabase/migrations/2026-06-26_sales_assistant_completion.sql` | Creates `assistant_recommendation_runs` (audit log, owner-RLS) + `feature_flags` (seeds `sales_assistant_enabled=true`, read-all RLS). Physical tables |
| `supabase/migrations/2026-06-27_market_listings_lookup_indexes.sql` | Two partial expression indexes on `records((data->>'district_lookup'))` / `((data->>'city_lookup'))` scoped to the `market_listings` model id — power the market-listings AREA pre-filter (applied live to prod 2026-06-27; idempotent) |
| `src/lib/__tests__/suggestGroups.test.ts` | Unit tests for `groupMatchResults` (bucketing, confidence tiers, data-gap codes, budget-fit, perGroup cap) |
| `src/lib/__tests__/suggestRequirements.test.ts` | Unit tests for `draftToMatchRequirements` (draft-first, lookup-vs-legacy) |
| `src/lib/__tests__/suggestGroups.integration.test.ts` | LIVE integration (skips without secrets) — runs `matchProjectsCore` + `groupMatchResults` against prod for the النرجس scenario |
| `src/lib/__tests__/districtGeo.test.ts` | Unit tests for the geo helpers (point-in-polygon, centroid distance, Arabic/English name normalization) |
| `src/lib/__tests__/projectMatchScoring.test.ts` | Unit tests for the deterministic scorer |
| `src/lib/__tests__/salesAssistant.test.ts` | Unit tests — geo distance, nearby tier, lead temperature, identity resolution |
| `src/lib/__tests__/assistantInsights.test.ts` | Unit tests for `aggregateInsights` (usage, helpful rate, failed intents, malformed-input tolerance) |
| `src/lib/followups/__tests__/assistantContext.test.ts` | Unit tests for the follow-up surface's draft-first preference resolution |

## Production Learnings (write these down so we don't relearn them)
These are the hard-won lessons from building a **write-capable** sales AI. They drove real fixes in `matchAgent.ts` / `api/match.ts` and they govern any future capability that can act on the user's behalf.

1. **Identity resolution is safety-critical — the wrong customer is the unacceptable failure.** The safety hierarchy is explicit: *attaching a task to the wrong client* is unacceptable; *missing a task* is acceptable; *asking one more question* is acceptable. So the assistant must always prefer "I need one more piece of information" over "this is probably the right customer." Concretely: `resolveClient` NEVER accepts a client id echoed by the model unless it resolves to a real record, and `resolveClientFromCandidates` is exact-match-first and requires a **unique** match — it never first-match-guesses on duplicate Arabic names.

2. **A write-capable AI is a different risk class than a read-only one.** Read-only matching that's slightly wrong wastes a few seconds; a wrong write creates a real orphaned record in `followups`. Every action capability therefore (a) resolves identity deterministically in code, (b) is confirmation-gated, and (c) is forbidden from claiming success it can't prove.

3. **Ambiguity must BLOCK execution, not get resolved by a guess.** When a name matches multiple clients, the assistant must surface the candidates and refuse to proceed — at **both** resolution points (`resolveClient` for the proposal *and* `get_customer_context`, which a live test of "عبدالله" proved could otherwise feed a valid-looking id straight into `propose_task`). The `TaskProposalCard` disables **Confirm** until the rep picks a specific client.

4. **User confirmation alone is insufficient if the identity underneath it is wrong.** A rep clicking "Confirm" trusts that the name/phone on the card is who the assistant resolved — so the card must show the resolved name + phone, and `confirmTask` must write only the validated id and report success **only** when `saveRecord` actually returns `'saved'`. Confirmation gates the *action*; deterministic resolution guarantees the *subject*.

## Open questions / known limitations
- **The broad assistants are unwired, not removed.** `PROJECT_FINDER_ONLY = true` hides `ai_chats` / `copywriter_chats` / `matching_chats` and the conversational capabilities; the code + DB models + records still exist. Rollback is one flag flip — but the longer they stay dormant, the more the legacy `/api/match` tools (still present, no UI caller) risk drifting from reality. Decide whether to fully retire or re-enable before they bit-rot.
- **Multi-district is now wired end-to-end (2026-06-29).** The requirements mapper (`src/lib/matching/requirements.ts`) collects ALL district ids from the client's `location` cascade district array (union'd with the `preferred_districts` field if present), resolves each to a name, and sends them as `districts[]`. The `/api/project-finder` endpoint already accepted `districts[]`, and the engine resolves each and treats a project in ANY of them as an exact match — across our_projects, all_projects, AND market_listings (the market RPC is scoped to the full requested-district set). So a client with several preferred districts is now searched against all of them. `district` still = `districts[0]` for single-district code paths.
- **Boundary verification only helps geocoded projects.** A project with no `latitude`/`longitude` cannot be point-in-polygon verified — it matches via its stored district at **medium** confidence with a `missing_project_coordinates` gap, and a project whose pin falls outside every known polygon drops to low confidence. The geo-integrity audit view exists to quantify and chase down those records.
- **Lifestyle matching is best-effort** (low weight): lifestyle keywords like "family/luxury" are fuzzy-matched against Arabic amenity values; many won't literally match. Treat as a tiebreaker, not a filter.
- **Not every project carries a district lookup yet.** 847 of 1,064 projects were backfilled with `district_lookup` (567 by PostGIS point-in-polygon, 278 by normalized-name, 2 by nearest-centroid); the rest carry a `district_match_status` (missing_coordinates / needs_manual_review / outside_known_boundary / ambiguous_match) and fall back to the legacy text + coordinate path via the dual-read. The 437 text-only projects (no coordinates) match on district/city text only; "nearby" distance applies just to geocoded projects, and medium-confidence (centroid) distances are flagged as approximate.
- **Whole-portfolio scan per call** — the tool pages all `all_projects` rows and scores in TypeScript (fine at ~1,372 rows). A `project_search_index` + SQL RPC is the performance path if the portfolio grows materially.
- **Insights are client-aggregated over visible conversations.** The admin dashboard computes from the `matching_chats` records the current admin can see (RLS-scoped), not a server-side rollup — accurate for a single admin's view; a cross-tenant rollup would need a server aggregate.

## Directional zones — "north of Riyadh" (added 2026-06-30)

The matcher only understands **cities + districts** (no cardinal direction). Free-text like "north of Riyadh / شمال الرياض" is now resolved DETERMINISTICALLY to a concrete set of districts:

- **Resolver:** `public.wassell_city_zone_districts(city, zone)` (SECURITY DEFINER) → district ids. A **curated `geo_zone_overrides`** row set (city × zone) WINS; otherwise the **geometric** rule classifies each district by its centroid's relative position within the city's bounding box (north = top 40% by latitude, etc.; diagonals supported). Mirrored for tests in `src/lib/market/zones.ts`. Riyadh "north" is seeded (geometric ∪ marquee districts النرجس/العارض/الياسمين/الملقا/حطين/الصحافة/النفل/القيروان/بنبان).
- **LLM parse** (`parseRequirements`) extracts `zone` from the words only — `{ city, zone }`. The expansion to districts is pure SQL; the AI never picks districts. An explicit named district always overrides a zone.
- **Engine:** `MatchRequirements` gained `zone` (audit) + `district_ids` (pre-resolved). When `district_ids` is set, `matchProjectsCore` batch-loads their centroids in ONE query (fast path) and treats a project in ANY of them as an exact-district match — no per-name lookup. Ranking/scoring unchanged.
- **Files:** `supabase/migrations/2026-06-30_geo_zone_resolver.sql`, `api/_lib/matchAgent.ts` (fast path + fields), `api/_lib/projectFinderAI.ts` (parse zone), `api/project-finder.ts` (RPC expansion), `src/lib/market/zones.ts`.
- **Limitation:** city is matched by Arabic name (`districts.city_name_ar`); an English city name returns nothing (the app passes Arabic). Geometric bands use the bounding box, so a far-flung outlier district can skew them — that's what the curated override is for.