# PRD: Data Migration

**Status:** Live
**Last updated:** 2026-06-23 (FUSION ROW-ALIGNMENT GUARD — source-fusion (`runFuseBatch`) silently corrupted units when the model returned a `values` array whose length didn't match the canonical `headers`. The old `headers.map((_, i) => vals[i] ?? '')` "normalization" only works if the extra/missing cell is at the END; the model actually inserts/duplicates a cell **mid-row** (emits the status twice, or a stray empty before the price), so positional truncation **shifted every field after the defect into the wrong column** and dropped the last cell — silently. It garbled **40 of 226 units** on الماجدية 174 (`55ab6c26`, 2026-06-23): blocks `A-21…B-9`/`F-18` shifted +1, `F-19…G-10` shifted +2 — the building letter landed under الدولة, the price under حالة الوحدة, etc. Fix (`worker/src/migrateAgent.ts` + the `api/_lib/migrateAgent.ts` copy + `worker/src/runMigrationJob.ts`): a wrong-length fused row is now treated exactly like a truncation — `runFuseBatch` throws `ExtractionTruncatedError`, and `fuseResolved` **auto-splits the batch and retries** (so a transient misalignment resolves on a smaller batch). A lone unit whose row stays misaligned at the split floor is left **blank + flagged in Source conflicts** (`header:'(row)'`) for manual entry, never banked scrambled. ALSO fixed a latent bug: `fuseResolved` keyed its auto-split on `err.code === 'max_tokens'`, but `ExtractionTruncatedError` never sets `.code` in the worker — so truncation/misalignment auto-split never actually fired (it failed the whole job); it now matches by `instanceof`. The already-garbled الماجدية 174 record was repaired in place from the source CSV (un-shift + price/parking backfill; C-3 area typo 178.04→178.78; operator PDF overrides B-7/F-23 preserved; backup `public._backup_migration_55ab6c26_20260623`). Previously, 2026-06-23: PREP-STEP AI-TURN FAILURES ARE NOW SURFACED — a failed **plan** (or discuss) turn used to be SILENT: the worker reflects a plan failure as `error_message` set + `prep_busy` cleared but **`status` stays `'draft'`** (planning is re-runnable), yet `StepPrep`'s error banner only rendered for `status==='failed'` (extraction). So when a clarify/submit-answers turn failed — e.g. an **Anthropic billing / API error** ("credit balance too low") — the operator saw the spinner stop with the question cards unchanged and NO explanation, and since the open questions were never cleared, the **"ابدأ الاستخراج / Start extraction" button stayed (correctly) locked** with nothing telling them why (a real incident, record b16e175e on 2026-06-23). The banner now shows whenever `error_message` is set and no turn is in flight and we're not mid-extraction, with wording that distinguishes an extraction failure ("فشل الاستخراج") from a plan-turn failure ("تعذّرت معالجة طلبك") — surfacing the underlying message instead of a dead button (CLAUDE.md silent-failure doctrine). The worker clears `error_message` on any successful turn, so a non-null value always pertains to the latest failure. SPA-only (`StepPrep.tsx`). Previously, 2026-06-23: TABLE FIELDS USE "|" SEPARATOR (not comma) — a project migration mangled the `table`-type fields (services / landmarks / features / guarantees): the extraction emitted parallel table entries with `|`/`/` separators while the importer split sub-columns on COMMAS, which collide with commas inside values AND inside numbers (thousands separators) — so `"70,000+ ساكن"` shredded into `"70"` + `"000+ ساكن"` and pipe-joined landmarks never split. Fix: `|` is now the canonical table-row separator end-to-end. New `splitTableValue` (`src/lib/textMatch.ts`) splits table sub-column cells on `|`/newline (NOT comma), KEEPING empty positional slots so sibling sub-columns stay index-aligned (`"A||C"` → `["A","","C"]`); `buildRecords` uses it for `table` fields (the tolerant max-length/skip-all-empty zip is unchanged); scalar multiselect/lookup keep the comma-based `splitMultiValue`. `TABLE_FIELDS_RULE` (`api/_lib/migrateAgent.ts` + the worker copy) now mandates `|` between parallel entries with empty slots between bars, never commas/slashes. Fixes future extractions; an already-extracted record needs a re-extraction to correct fields whose old extraction used `/`. Previously, 2026-06-23: UNDO A COMPLETED MIGRATION ("un-migrate" → edit → re-migrate) — a completed migration was read-only with no way back: to redo an import the operator had to start over and the old (possibly wrong) records lingered. Now a finished migration can be **undone**: the **Result step** shows an **"Undo this migration" / "التراجع عن الترحيل"** button (only when the run captured an undo ledger — see below) that, after a confirm spelling out the counts, deletes the records it created, reverts the projects it merged into, and re-opens the migration at the editable **Preview** step (status→`draft`) so the operator edits and re-runs (the **Migrate** button returns; re-run dedup sees the deleted records gone, so no duplicates). To make this possible, `runMigration` now records an **undo ledger** on the result: `MigrationResult.undo = { target_model_id, created_record_ids[], created_lookup_records[{model_id,id}], updated_projects[{model_id,id,data_before}] }` — the ids of new main records + new lookup-target records it created, and a **pre-merge snapshot** of each project it updated (project-profile mode). Undo runs as an in-tab job (`undoMigrationJob` in jobRunner, new `MigrationJobKind 'undo'` + `MigrationStatus 'undoing'` with a spinner/pill): it deletes the main records FIRST (so lookup-record deletes don't dangle), then the created lookup records (via the store's `deleteRecord` — local + `supabaseRecordDelete` + pending-sync retry), then restores each project's `data_before` (via `saveRecord` `expectedVersion:null`); it's idempotent (deleting an already-deleted id no-ops), so a reload mid-undo + retry is safe. **Created dropdown OPTIONS are intentionally KEPT on undo** (shared/deduped — removing risks breaking other records). **Going-forward only:** the button appears only when `result.undo` exists, so migrations run before this shipped (no ledger) show no Undo and are handled manually. SPA-only change (no worker/DB). Previously, 2026-06-23: EXTRACTION / PLAN / DISCUSS MOVED TO THE DURABLE WORKER QUEUE — the file-heavy AI vision steps no longer run synchronously inside a browser-held `/api/migrate` request. That request was capped at Vercel's 300 s `maxDuration` (and a 300 s client `AbortController`); a large image-heavy brochure (the `units` source-fusion path re-sends the brochure to Claude on every ~20-unit fuse batch) blew past it, Vercel killed the connection before a response, the browser threw `TypeError: Failed to fetch`, and the old **in-tab** job died on reload before its catch could persist `status='failed'` — freezing the record at `status='extracting'`, `error_message=null` (two real migrations, الماجدية 174 / 183). Now `extract` / `plan` / `discuss` **enqueue** a `data_migration_jobs` row and run on the always-on **Fly worker** (`worker/src/runMigrationJob.ts`; the agent is copied verbatim to `worker/src/migrateAgent.ts` since the worker can't import `api/_lib`) with **no timeout**; the worker patches the record (`status`/`phase`/`progress_done`/`progress_total`/`raw_table`/`prep_*`/`summary`) at every step and the SPA reads it **live via Supabase Realtime** — so closing/reloading the tab no longer interrupts a run, runs can exceed 300 s, and a crash is swept to `failed` by a 45-min watchdog (records can never stay stuck). `discover`/`fuse_batch` run INSIDE one `extract` job (keeps the unit index, the fuse prompt-cache, and the recursive `max_tokens` auto-split in one run). `/api/migrate` for these three is now enqueue-only — it validates, RLS-gates the source record, writes the busy/status field + (plan/discuss) appends the operator's message **server-side** (the browser never writes the record during a job — that would suppress the worker's updates via the realtime echo-dedup), inserts the job via `data_migration_job_enqueue`, pings the worker `/wake`, and returns 202; `suggest_mappings`/`standardize` stay synchronous, and the local `import` step (`startMigrationJob`) stays client-side (local record writes, not a held-open Anthropic call). New `MigrationData` fields `progress_done` / `progress_total` / `prep_busy` / `discuss_busy` drive the live spinners; `StepPrep` + `StepReviewRaw` read busy/progress from the **record** (Realtime), not the in-tab job store; `MigrationList` shows the worker fusion progress bar. Migration: `supabase/migrations/2026-06-23_data_migration_jobs_queue.sql` (queue + indexes incl. one-active-per-record unique, RLS, enqueue/claim/complete/fail/cancel/watchdog RPCs, + a one-time cleanup that flips legacy `extracting`/`migrating` records to `failed` so they offer Retry — including the two reported frozen migrations). New `cancelMigrationJob` client helper + a `cancel`/`status` action on `/api/migrate`. See CLAUDE.md "Data Migration extraction pipeline (records → worker queue)". Previously, 2026-06-23: PROMPT LIBRARY + ANSWERED-Q&A LOG IN THE PREP STEP — two prep-step additions. (1) **Prompt library:** a SHARED library of saved extraction-instruction templates (new `migration_prompts` table — RLS: everyone reads, author edits/deletes; client helpers `listMigrationPrompts`/`saveMigrationPrompt`/`deleteMigrationPrompt`; new `PromptLibrary.tsx` popover next to the instructions field). Before starting an analysis the operator can pick a saved prompt to fill the instructions, save the current instructions as a new named prompt, or delete one they authored. (2) **Answered-Q&A log:** answering a question card used to drop the card (the AI removes resolved questions from the open set) leaving the Q&A only as raw chat text. Now each answered card is persisted to `prep_answered` (`AnsweredQuestion[]`, upserted by question text) and rendered in a read-only **"Answered questions"** panel — so the question + your answer stay viewable, including after the migration is done. (3) The **prep page is now reviewable on a completed migration**: the done navigator gained an **"الأسئلة / Q&A"** chip, and `StepPrep` takes a `readOnly` flag (done migration) that disables the instructions, hides the clarify/chat/submit/start-extraction controls, and just shows the instructions + clarification transcript + structure + the answered-Q&A log; a `startExtractionJob` backstop also no-ops on a done record. Also 2026-06-23: COMPLETED MIGRATIONS ARE BROWSABLE — a finished migration (`status='done'`) used to dead-end on the result screen with no way back to see what was migrated. The wizard now shows a **view navigator** (Table · Mapping · Standardize · Preview · Result chips) whenever `status='done'`, so the operator can revisit every page + the result after the import. It's **read-only-for-the-import**: `StepPreview` in a done migration shows a green "already ran — read-only review" banner and replaces the **Migrate** button with **View result** (→ the result page), and `startMigrationJob` has a backstop that no-ops on a `done` record — so the import can never be re-run from the review (which would duplicate options/lookup records or re-apply a project update). Earlier setup pages (uploaded files, the clarification chat) remain reachable via each page's **Back**. Implemented in `MigrationWizard.tsx` (`DONE_NAV` chips + `isDone` + `canView` guard) + `StepPreview.tsx` (`alreadyMigrated`/`onViewResult`) + the `jobRunner.startMigrationJob` done-guard. Also 2026-06-23: REVIEW BEFORE EXTRACTION IS NOW MANDATORY — **"Start extraction"** in the prep step is GATED: it stays disabled until the operator has run the **"Review files & clarify"** pass at least once AND resolved every open question card. Previously prep was skippable ("Start extraction" was always available, one click away), so files could be extracted without the AI having read them or the operator confirming the proposed structure. Now `blocked = !reviewed || questions.length > 0`, where `reviewed` = the planning conversation (`prep_chat`) is non-empty; the footer lock pill + the button `title` explain whichever reason applies (**"Review & clarify the files first"** when not yet reviewed, vs **"Answer N questions first"** when questions remain), and the step subtitle no longer offers a direct-extract shortcut. (Excel **"use as source"** / blank-table paths still bypass prep entirely — they don't run AI extraction, so the gate doesn't apply.) Also 2026-06-23: CLARIFY STEP SURVIVES NAVIGATION — the prep step's **"Review files & clarify"** planning turn now runs as a **detached job in `jobRunner`** (new `startPlanJob`; a third `MigrationJobKind` `'plan'`), exactly like extraction and import — no longer inside `StepPrep`. Before, the turn was an inline `await planExtraction` gated by a **local `aiBusy` flag**, so leaving the migration mid-clarify (open another migration, or navigate off the page — the wizard is `key={recordId}`, so the step unmounts) killed the in-flight turn: the operator came back to an empty step and had to click **Review files & clarify** again. Now the turn keeps running detached and persists `prep_chat` / `prep_structure` / `prep_questions` / `prep_ready` even after the step unmounts; `StepPrep` derives "busy" from `job.kind==='plan'` (so the spinner + disabled inputs survive away-and-back) and adopts the persisted turn on its next render via a **forward-only** effect — only a turn that ADDS messages is taken, preserving the local-state immunity to a stale realtime echo that the prep step was built around. The now-dead `onPlanResult` prop + its `MigrationWizard` wiring were removed; the job persists directly via the freshest-read `patchMigrationRecord`. The freshest live conversation + the (possibly not-yet-debounced) instructions are captured from the still-mounted step at click time and handed to the job. Previously, 2026-06-22: PRE-EXTRACTION PREP STEP — a new **prep** step sits between upload and extraction. Every AI extraction now routes through it (the Excel "use as data source" and blank-table paths still skip it — no AI). The operator writes free-text **instructions** for how they want the data extracted, then chats with the AI, which **reads the files and replies with clarifying questions** (every contradiction between sources, anything unclear) plus a **proposed table structure** (each column + how its values are derived) — *before* the table is produced. The operator converses until satisfied, then clicks **Start extraction** (always available — prep is skippable for simple files). The instructions + resolved clarifications + confirmed structure are composed into an **operator-guidance** block threaded into every extraction phase (discover / fuse / project extract) so the run honors them — additive instruction only, it never coerces raw values. Implemented as: new backend action **`plan`** (`runPlan` / forced `emit_plan` tool → `reply` + `questions` + `proposed_columns` + `ready`), mirroring the post-extraction `discuss`; a `guidance?` param threaded through `runExtract`/`runDiscover`/`runFuseBatch` (via a shared `renderGuidance` helper) and their client wrappers; a new **`StepPrep.tsx`** (instructions textarea + clarification chat + proposed-structure panel + the extraction trigger, which **moved here from the upload step** along with its spinner/interrupted states); `jobRunner.startExtractionJob` builds the guidance from the record and passes it down; new record fields `extraction_instructions` / `prep_chat` / `prep_structure` / `prep_ready` + a new `'prep'` `MigrationStep`. Refined same day: the AI's questions/contradictions are now returned as STRUCTURED data (`emit_plan.questions[]` = `{question, kind, detail}`) and rendered as **answerable cards** (a quiz) below the chat rather than bullet text in the reply — the operator answers in the card or the chat, and **"Start extraction" is GATED (disabled) until every card is resolved** (the AI returns an empty question set once satisfied; new `prep_questions` field + `PrepQuestion` type). Also: StepPrep renders the conversation/cards/structure from **local component state** (a stale realtime echo was briefly blanking the in-progress view — the records channel only soft-TTL-dedups echoes, Audit H1) and persists each turn in ONE combined write. Previously, 2026-06-18: PROJECT-PROFILE UPSERT — update an existing project instead of duplicating it. A project-profile migration (All Projects target) used to ALWAYS create a NEW project record — the dedup field is `project_id` (an `auto_id` never filled from extraction), so it never matched; re-importing a project that already existed made a **duplicate**, and its document/analysis landed on the new copy while the real project (with its units) stayed empty. Now the **Preview step has a "Migration target" selector**: by default the migration **UPDATES the existing project matched by name** (token-aware match, so "جزيل / كمباوند جزيل / JAZEEL COMPOUND" still finds plain "جزيل"), **merging** the migrated profile + marketing document + analysis ONTO it — its **units and trigger-maintained rollups are preserved**. The user can switch to "✦ Create a new project" or pick a different existing project. Implemented via `BuiltRecord.matchedExistingId` + `MigrationData.project_update_target` (`undefined`=auto-match, `null`=create-new, an id=update-that) threaded through `buildMigrationPlan` → `runMigration` → `StepPreview` → `MigrationWizard`; `runMigration` merges into the existing record with **`expectedVersion: null`** (a deliberate overwrite — the project's version churns as rollup triggers fire) and counts `MigrationResult.updated` (shown on the done screen). New helpers `projectNameField` + `suggestProjectMatch` in `buildRecords`. Previously, 2026-06-18: PROJECT ANALYSIS NOW MIGRATED TO ITS FIELD — in PROJECT-PROFILE mode (All Projects target) the extraction's **intelligence sections** (the per-dimension analysis: overview / location / pricing / audience / design / USPs…) were shown read-only in review and then **dropped at import** — only the marketing document was written (→ `marketing_document`). Now the intelligence is **flattened to one Markdown document** (`renderProjectAnalysis` — a `## heading` per section) and **written into the `project_analysis` field** at migrate, via the same injection pattern as the doc. Threaded `projectIntelligence` through `BuildPlanArgs` → `buildMigrationPlan`/`runMigration` → `StepPreview` → `MigrationWizard`; new `ANALYSIS_DOC_FIELD='project_analysis'`. NB: both the doc and the analysis inject at the **final import step** (not at mapping), so they only appear on the created record after you finish the migration. Previously, 2026-06-18: TABLE-FIELD-AWARE EXTRACTION — when the destination model has a **`table` field** (a multi-row field with named sub-columns, e.g. الضمانات/guarantees = type · company · years), extraction now knows it and SEGMENTS a combined source entry into the sub-columns instead of dumping the whole phrase into one column. "ضمان تكييف من شركة ABC لمدة 5 سنوات" → type=تكييف · company=ABC · years=5; **multiple** entries become **parallel, same-order comma/'،'-separated lists** across the table's sub-columns (type="تكييف، سباكة" · company="ABC، XYZ" · years="5، 10") — exactly the shape the importer zips into table rows. Implemented as: `TargetFieldLite` carries a `table` grouping marker (set in `targetFieldLites` for each sub-column); the shared `renderHuntList` groups a table's sub-columns under a "TABLE «…»" header; and a `TABLE_FIELDS_RULE` is appended to the extract / discover / fuse system prompts whenever any table field is in scope. The mapping step already documents the `slug.colName` table rule, so the now-split columns map straight into the right sub-columns. Applies to BOTH project-mode (`runExtract`) and records-mode source fusion (`runDiscover` / `runFuseBatch`). Previously, 2026-06-18: ROUTED VALUES GET FULL DESTINATION RESOLUTION — when you route a value to a different field ("↪ نقل إلى…") in the standardize step, you now get the **same decide-what-to-do controls against the DESTINATION field** as a value that wasn't moved: match an existing option/record, **create a new option/record**, or leave blank. Previously routing only did deterministic match-or-fail, so a routed value that matched nothing in the destination was a blocking `route_invalid` error with no way to create it. Modeled as a nested **`routeDecision` (RouteResolution)** on the route decision; `buildMigrationPlan` replays it through the **same `resolveDecision`** path, so a routed "create new" registers a pending option/record on the **destination** field — flowing into the preview's pending-creates summary and the migrate step exactly like a normal create. The per-value resolution controls were extracted into a shared **`ResolutionControls`** component rendered twice (own field when not routed, destination field when routed); the destination resolution defaults to a deterministic Arabic-normalized option match, and scalar destinations are written as-is (no resolver). Legacy routes with no `routeDecision` keep the old match-or-fail. Previously, 2026-06-18: UPLOAD FROM FILES LIBRARY — the upload step gained an **"أو اختر من ملفات التطبيق / Or choose from app Files"** button: it opens a folder-browser modal over the app's Files system (the same RLS-gated `listFolders`/`listFiles` the Files page uses) to **multi-select** existing files. Picked files are fetched via a permission-checked signed URL (`/api/files/sign-download-url`), turned into `File` objects (`downloadLibraryFileAsFile`), and routed through the **same `addFiles` path as a drag-drop** — so a library pick behaves identically (docs → AI set, Excel/CSV → its per-file analyze/source choice, block-mixing rules apply). Selectable types are filtered to migration-readable ones (`isMigrationAttachableName`: PDF/images/Excel/CSV/text; Word/PPT/archives shown disabled). The Decks-only `PickFromFilesModal` was **generalized into a shared `src/components/files/PickFromFilesModal.tsx`** (configurable `isSelectable` filter + optional `multiple` select) and Decks repointed at it — one browser, no duplicate. Previously, 2026-06-18: SOURCE-FUSION UNITS EXTRACTION — records-mode extraction (units / clients / any non-projects target) was rebuilt from a single "re-type every cell in one model response" call — which overflowed the model's output-token cap on any sizable unit list and then **silently banked zero rows** (`runExtract` had no truncation check, unlike `runSuggestMappings`) — into a two-phase, batched **source-fusion** pipeline orchestrated client-side in `jobRunner`. **(1) discover** (`runDiscover` / `action:'discover'`): Claude inventories the uploaded files, identifies EVERY unit entity across ALL sources (sheet + price PDF + brochure + floor plans + images), and returns a compact unit **index** (identifiers only — stays small even for hundreds of units) + the **canonical header set**. **(2) fuse_batch** (`runFuseBatch` / `action:'fuse_batch'`): the index is processed in batches (≈20 units), each batch resolving **one row per unit** by MERGING facts about that unit from every source — price from the price list, area from the brochure, **bedrooms/bathrooms/components COUNTED from that unit's floor plan**, status from the sheet — and flagging every cross-source **conflict** (precedence: floor-plan wins for component counts, latest price list for price, otherwise marked for human review; the chosen value goes in the row, the disagreement is preserved). Batches are **prompt-cached** (the brochure/plans are sent once and reused across batches), and a batch that still hits the token cap is **auto-split in half and retried**, so output can never overflow. A hard **truncation guard** (`requireToolInput`) makes EVERY phase fail LOUDLY on a `max_tokens` cut-off (→ red Failed pill + retry) instead of saving `rows: []`. The review step gains a **Source conflicts** panel listing each disagreement (which source said what, what was chosen, why). **Excel/CSV is now just ONE source** feeding discovery/fusion — never assumed complete or authoritative, never force-retyped. PROJECT-PROFILE mode (All Projects) is unchanged — one aggregated row + the Arabic document, whose output is intrinsically small (it also got the same loud truncation guard). New raw-table fields: `conflicts`, `sources`, `row_keys`. Previously, 2026-06-18: TABLE-FIELD IMPORT + MAPPING FALLBACK — `table`-type target fields (e.g. All Projects' الخدمات/services, الضمانات/guarantees) are now importable via their **sub-columns**: each appears in the mapping dropdown as `field — sub-column` (a dotted `slug.colName` mapping, like a range's min/max), a single source cell holding a comma/Arabic-comma/semicolon/newline list becomes **one table row per item**, and sibling sub-columns mapped from other source columns **zip by row index**. The mapping step also got a robustness fix: the AI column-mapping suggestion is now **unioned with deterministic exact header→label matching**, so when the AI returns nothing (or skips an obvious column) exact matches still auto-map instead of leaving "0 of N mapped"; an AI slug that isn't a real target is ignored. (`table` is still NOT a routing destination, and its whole-field value is never a single mapping target — sub-columns only. attachment/image/auto_id/rollup fields remain non-mappable by design.) Previously, 2026-06-18: STANDARDIZATION LAYER HARDENED — the standardize→preview→migrate path was rebuilt to be a proper field-level records/options manager, not just AI matching. Lookup decisions now store the **exact selected record id** (no more record-id→display→re-match round-trip that could link the wrong record when two records share a display name); new dropdown/multi-select **options** are created with the app's shared deterministic slug (`slugifyOptionLabel`, hyphenated — the same one the record form uses) with Arabic-aware dedup, and new **lookup records** are created at migrate time from a stable pending decision (deduped against existing records by normalized display). One shared normalizer (`@/lib/textMatch`) drives ALL matching (pre-match, AI proposal, option/record lookup, dup-detection); multi-select cells split on comma / Arabic comma / semicolon / newline and standardize per token. **Frozen models:** option creation is allowed (options are JSONB-schema-only — no DDL, mirrors the record-form inline-create); lookup-record creation into a target that needs un-fillable required fields (or a custom-UI model) is **blocked with an actionable error** instead of minting an invalid record. **Routed values are re-standardized** against the destination field's type. **Preview is now strict + auditable:** shows pending new options/records, per-cell action markers (link / new / routed), and per-row validation; rows with a blocking error (unresolved controlled value, mapped-required blank, blocked create, invalid number) are shown but **cannot be selected and never migrate** — no raw labels are ever stored in a controlled field. New-option/new-record creates for rows the user excludes in preview are pruned (not created). Previously, 2026-06-18: REFRESH-SAFE DRAFTS — the wizard's saves bypass optimistic-version concurrency (`expectedVersion: null`, the single-record/single-writer "decks" posture): the wizard fire-and-forget-saves the whole record on every step/value change, and routing those through the version check drifted the tab's local `version` behind the server (realtime echo-dedup hides the tab's own bumps), so saves returned `version_mismatch` — which DROPS the write, trips the per-record circuit breaker, and makes the reload-on-conflict handler overwrite the in-progress wizard state with the stale server copy (a refresh "lost"/"deleted" the migration). With `null` the RPC skips the check, saves always land, and a reload resumes exactly where you were (`MigrationWizard.tsx` `patch` + `lib/jobRunner.ts` `patchMigrationRecord`). Previously, 2026-06-18: UPLOAD STEP = TWO FIELDS — the single drop zone is split into **"Documents (non-Excel)"** (PDF/images, always AI-analyzed) and **"Excel / CSV"**, where each added file gets a per-file choice — **Analyze with AI** (default; joins the extraction set as per-sheet CSV) or **Use as data source** (parsed in-browser, straight to review, no AI). The two are mutually exclusive (block-mixing): "use as data source" is only offered when the Excel is the SOLE file, and selecting it locks both drop zones. This replaces the old behavior where an Excel mixed with other files was silently forced into AI extraction and an Excel-only drop popped a one-off "direct table or AI?" chooser. Previously, 2026-06-11: EXCEL THROUGH AI EXTRACTION — an Excel/CSV-only drop asked "direct table or AI extraction?"; choosing AI converts the workbook to per-sheet CSV text and runs it through extraction exactly like a PDF (messy layouts, multi-sheet, mixed with brochures). Excel mixed with PDFs/images is no longer skipped — it joins the AI set. Same day: CONCURRENT MIGRATIONS — extraction + import run in a module-level job runner (`lib/jobRunner.ts`) detached from the wizard components, so any number of migrations can run at the same time; starting/opening another migration no longer stops a running one, the list shows live per-row progress, and interrupted runs (page reload) get explicit retry/resume. Also: PROJECT-PROFILE mode — migrating into All Projects produces THREE outputs: ONE project row (no unit table), a project-level intelligence briefing, and an Arabic Project Knowledge Document saved to the project's new `marketing_document` field; computed rollup fields removed from mapping targets)
**Related PRDs:** [import-export.md](import-export.md) (reuses the Excel import core), [record-management.md](record-management.md) (writes records via the same save path), [data-storage.md](data-storage.md) (RPCs + storage bucket), [model-builder.md](model-builder.md) (target field types + options)

## What it is (in plain English)

A guided wizard that turns the messy data developers send us — Excel files, PDFs, screenshots, photos, in any mix — into clean records inside any model, with a human approving every change before it lands. You open **Data Migration** (ترحيل البيانات) in the sidebar, start a new migration, pick which model to import into, and drop in your files. **Before** it extracts anything, you get a planning step: you can write **instructions** for how you want the data read, and the AI reads your files and comes back with **questions about anything that contradicts or is unclear** plus the **table structure it intends to produce** — so you confirm the plan *before* the table exists. This review is **required**: "Start extraction" stays locked until you've run *Review files & clarify* and resolved any questions the AI raised. Then the app reads them into one table, lets you review and fix that table, figures out which column maps to which field, and — for dropdown / multi-select / lookup fields — proposes how to standardize each raw value to match what the app expects (e.g. "شقه" → the option "شقة", "Apartment" → "شقة"), showing every proposed change as a before→after you approve, override, or redirect to a different field. When you're happy, you click **Migrate** and the records are imported.

Extraction is the smart part. **Before** reading your files the app looks at the target model's fields, so it knows *what to hunt for* (e.g. it knows you want a developer name) — but it captures values raw, never coercing them to the system (that's the later step's job). It writes numbers as plain numbers, and it **reads the floor plans deeply**: for each unit it counts the components (bathrooms, bedrooms, kitchens…) from the drawing and **combines that with the brochure text** — so an ambiguous "2 دورة مياة + الجناح الرئيسي يحتوي على دورة مياة" resolves to **3** because the plan confirms it. After extraction it writes you a **summary** of how it derived every value (especially each number, with its source), and you can **chat with it** to question a value or have it recount / pull a missed column by re-reading the brochure.

You don't have to start from messy files: if you already have a clean table, upload an Excel/CSV and choose **Use as data source** (or paste into the in-app grid) to jump straight to the review/mapping step. The upload step keeps **documents and Excel in two separate fields** so Excel is never silently forced into one behavior — you decide, per Excel file, whether it's analyzed like the rest or used as-is.

## Why it exists

Onboarding a developer's project + units used to be a manual, hours-long job: eyeball the files, retype into a spreadsheet, rename values to match the app's dropdowns, fix number/date formats, match developer/project names to existing records, then import. This wizard builds that whole process into the app, with AI doing the tedious extraction/matching and the operator staying in control via explicit approval gates.

## Key behaviors

- **One migration = one record** in the `data_migration` system model. The entire wizard state (target model, raw table, mappings, value decisions, result) lives on `record.data`, so you can close the tab and resume exactly where you left off.
- **Sidebar list management.** The left list isn't just navigation — each migration can be **renamed** (its `data.title`), **moved into a group** (a free-text `data.group` label; type an existing group or a new one), and **deleted**, all via a per-row ⋯ edit popover. Migrations render under **collapsible group headers** (named groups alphabetical, "Ungrouped" last, each with a count); a group can be **renamed** inline from its header (which re-labels every migration in it). Edits use the same single-writer `expectedVersion: null` save posture as the wizard. (`src/pages/DataMigration/components/MigrationList.tsx`.)
- **The file-heavy AI steps run on the durable worker queue; only the local import runs in-tab.** **Extraction (`extract`), the prep clarify turn (`plan`), and the post-extraction discuss turn (`discuss`)** are **enqueued** onto the always-on Fly worker (`data_migration_jobs` queue → `worker/src/runMigrationJob.ts`). The browser POSTs `/api/migrate` (which flips the record's busy/status server-side + inserts the job) and returns immediately — it never holds the connection open for the multi-minute vision call. The worker runs with no timeout and writes status / phase / progress / `raw_table` / `prep_*` onto the record; the SPA reads it **live via Supabase Realtime**, so **closing or reloading the tab no longer interrupts a run** (the job lives on the worker, not the tab), any number of migrations run concurrently, and the left list's pills + the records-mode fusion progress bar (`n/N`) stay live. A crashed worker is swept to `status='failed'` by the 45-min watchdog, so a record can never stay stuck at `extracting` with no error (the old in-tab failure mode). The **import** step (creating options / lookup records / records) still runs in the module-level **job runner** (`lib/jobRunner.ts`) **in-tab** — it's a loop of local record writes, not a held-open Anthropic call, so it isn't subject to the 300 s wall; a page reload mid-import still shows the explicit **Resume migration** state, and the loop yields to the browser every 20 rows. Uploaded source files are persisted on the record at upload time, so the worker (which mints its own signed URLs from them) can always be retried.
- **Upload step = two fields, with the Excel choice made explicit.** The drop area is split:
  - **Field 1 — Documents (non-Excel):** PDF, images, screenshots. Always uploaded into the AI extraction set; never offered a "use directly" path (Claude has to read them).
  - **Field 2 — Excel / CSV:** each file you add gets a per-file choice. **Analyze with AI** (the default) treats it like a PDF — the workbook is converted to **one CSV text file per non-empty sheet** (Claude can't ingest raw .xlsx) and joins the AI extraction set, right for messy layouts (title rows, merged cells, sub-tables), multi-sheet workbooks, or combining a price sheet with a brochure. **Use as data source** parses the sheet in the browser (never sent to AI, straight to review — the clean-sheet fast path; first sheet only, multi-sheet warns).
  - **The two are mutually exclusive (block-mixing):** "Use as data source" is only available when that Excel is the **sole** file in the step (no documents, no other Excel); choosing it **locks both drop zones** (a chosen data source IS your whole table). To analyze documents and an Excel together, leave every Excel on **Analyze with AI** — they all merge into one AI-extracted table. Default-Analyze means by default an Excel is analyzed alongside whatever documents you add.
  - Both drop zones are **forgiving** — a file dropped in the "wrong" field is routed by type. You can also **start with a blank table**.
- **The raw table is yours to edit:** edit cells in-app, add/remove rows & columns, **download it as Excel**, fix it externally, and **re-upload** — or keep editing in place. Re-uploading replaces the table and resets downstream mappings (headers may have changed).
- **AI suggests column→field mappings**, you adjust them — and the AI suggestion is **unioned with exact header→label matching** so obvious columns still auto-map even if the AI returns nothing. Range fields expose two targets (min/max); **`table` fields expose one target per sub-column** (`field — sub-column`), and a list cell becomes one table row per item (sibling sub-columns zip by row). Non-importable field types (formulas, mirrors, auto-IDs, files/attachments/images, notes, and unit-derived rollups) are never offered as targets.
- **Value standardization with approval (the core promise):** for each dropdown/multi-select/lookup column, per distinct value you pick one of: select an existing option/record (a **searchable picker** — LookupCombobox for lookups, an option search for dropdowns), **create** a new option/record (with an editable label, only on explicit approval), **leave blank**, or **route** this value's rows to a different field (splitting a column). Nothing is created or changed until you click Migrate. "Accept all high-confidence" bulk-applies the ≥90%-confident proposals.
- **Controlled fields store exact references, never display text.** A lookup decision stores the **exact selected record id** — there is no record-id → display-name → re-match step, so two records that share a display name can't be confused. A dropdown/multi-select decision stores the **exact option value (slug)**. A value that can't be resolved to an option/record (and wasn't explicitly blanked) makes the row invalid; raw labels are never written into a controlled field.
- **Create-new is deterministic and dedup-safe.** New options get the app's shared `slugifyOptionLabel` value (the same hyphenated slug the record-form inline-create uses — not a migration-only scheme), deduped against existing options by Arabic-aware normalized label so "Villa"/"villa"/"فيلا" map to one option. New lookup records are deduped against existing target records by normalized display (link the existing one instead of minting a duplicate). Both are created **only at Migrate**, only for rows that actually migrate (excluded-row creates are pruned).
- **Frozen / system models handled explicitly.** Adding a dropdown/multi-select option to a frozen model is safe and allowed (options live in the JSONB schema — no DDL, same path the record form uses) and is flagged in preview as "added to schema". Creating a lookup record into a target that needs required fields we can't fill (or a custom-UI/system model) is **blocked with an actionable error** in the standardize + preview steps — never a silent invalid record.
- **One shared normalizer everywhere** (`@/lib/textMatch.normalizeMatch`): deterministic pre-match, AI-proposal matching, option/record lookup, and duplicate detection all use the same Arabic-aware normalizer (strips diacritics/shadda, unifies alef/ya/taa-marbuta, lowercases) — no path silently fails where another succeeds.
- **Multi-select stays multi-value end-to-end** — a cell is split per token on comma / Arabic comma `،` / semicolon / newline (one shared splitter), each token standardized independently to its exact option value, never collapsed to one. Any unresolved token blocks the row unless explicitly blanked.
- **Routed values are re-standardized for their destination.** A value moved to another field is resolved against that field's type — dropdown/multi-select → a valid option value, lookup → an existing record id, number/range → validated/converted, text → passed through. An unresolvable routed value makes the row invalid (it is never dumped raw).
- **Pre-extraction prep — instructions + clarify before the table exists.** Every AI extraction first lands on a **prep** step (between upload and extraction). You can write free-text **instructions** (e.g. "use the price list not the brochure for prices; areas in m²; skip sold units; if the plans disagree with the sheet, ask me"). Pressing **Review files & clarify** sends your instructions + the files to the AI, which replies in a **chat** with a short summary/plan, and surfaces every **contradiction between sources** and anything **unclear** as **answer cards** (a quiz) shown below the chat — NOT buried in the chat prose. Each card shows the question (tagged contradiction vs. clarification) + the conflicting specifics, with its own answer box; you answer **in the card or in the chat** (both feed the next turn), and an `n/N answered` counter tracks progress. The AI also proposes a **table structure** (each column + how it'll derive that column's values) in its own panel. Each turn re-reads the files, drops the questions you've resolved, and refines the structure. **Extraction is BLOCKED until you've run the review AND resolved every question card** — "Start extraction" is disabled until the "Review files & clarify" pass has run at least once (lock pill: "Review & clarify the files first") and then while any question remains (lock pill: "answer N questions first"). Review is mandatory — you can't extract files the AI hasn't read and you haven't confirmed; a hand-off with no contradictions just surfaces no cards, so one review pass unlocks it immediately. Your instructions + the resolved Q&A + the confirmed structure are bundled into an **operator-guidance** block passed into the actual extraction (discover/fuse or project-extract), steering it without ever licensing the AI to clean/translate/coerce raw values. The Excel **Use as data source** and **blank table** paths bypass prep entirely (no AI). State persists on the record (`extraction_instructions`, `prep_chat`, `prep_structure`, `prep_questions`), so you can close the tab and resume. The conversation, cards, and structure render from **local component state** (seeded from the saved record, like the review step's `draft`) so a transient realtime echo can't blank the in-progress view; every turn still persists in one write.
- **Source-fusion extraction (records mode — units, clients, any non-projects target).** Extraction is NOT a spreadsheet import and the Excel is NOT the source of truth: the AI reads ALL uploaded files, identifies every unit, and **merges facts per unit from every source**. Two phases run automatically (the upload spinner shows which): **discover** (scan + classify the files, build the unit index + canonical headers) then **fuse** (resolve units in batches, one row per unit, ≈20 at a time). Each unit's columns are pulled from wherever the data lives — number/status from the sheet, price from the price list, area from the brochure, **bathrooms/bedrooms/components counted from that unit's floor plan**. When sources disagree the AI picks a value (floor-plan for counts, latest price list for price) and records the disagreement in a **Source conflicts** panel for you to verify. Because units are batched (and a too-dense batch auto-splits), arbitrarily large unit lists extract without ever overflowing the model's output — and a genuine truncation fails **loudly** (red Failed + retry), never as a silent empty table. **Per-row alignment is guarded:** a fused row whose cell count doesn't match the canonical headers (the model occasionally inserts/duplicates a cell mid-row) is treated exactly like a truncation — the batch is bounced and auto-split so a transient misalignment resolves on a smaller batch; if a lone unit's row stays misaligned it's left **blank and flagged in Source conflicts** for manual entry, never silently shifted into the wrong columns. (Before 2026-06-23 a wrong-length row was positionally coerced to the header count, which silently column-shifted every field after the defect — it garbled 40 units of الماجدية 174.)
- **Model-aware extraction (the hunt-list).** The chosen target model's fields are sent to extraction as a "what to look for" list — so the AI knows to find a developer name, a unit type, bathroom/bedroom counts, etc. — and it adds extra columns for any other useful data it finds. It is explicitly told **not** to clean, normalize, or coerce values to fit the system (raw is fine; cleaning is the standardize step). Numeric data comes out as **bare numbers** (`3`, not "3 دورات مياه"). Computed rollup fields (`is_computed` — the All Projects unit-derived ranges/counts) are excluded from both the hunt-list and the mapping targets, since they're recomputed live from units and an imported value would be silently ignored.
- **PROJECT-PROFILE mode (target = All Projects) — three outputs from one extraction.** When the migration target is the projects model, the system understands you want a detailed understanding of the **project as a whole**, not individual unit records:
  1. **The migration table** flips from "one row per unit" to **one row per project** (normally exactly one): the project's general information in detail — developer, location, type, status, unit types/models offered, amenities, guarantees, payment terms, licenses — with units still read deeply (floor plans included) but **aggregated, never enumerated as a unit table**.
  2. **A project-level intelligence briefing** (`data.project_intelligence`): titled sections that explain the project itself — overview, developer, location & neighborhood, concept & positioning, target audience, advantages, amenities, construction status, guarantees, design philosophy, architecture & facades, unit mix, pricing insights, area insights, distinguishing features, unique selling points. Factual and grounded; insights may include arithmetic derived from in-file numbers (clearly phrased as derived) but never external market knowledge; unsupported dimensions are omitted. Shown read-only in its own collapsible panel in the review step; persisted on the migration record; and **on import, flattened to one Markdown document (a `## heading` per section) and written into the project record's `project_analysis` field** (`ANALYSIS_DOC_FIELD`, via `renderProjectAnalysis` in buildRecords — same injection pattern as the marketing document; an explicit column mapped to `project_analysis` wins over the injection).
  3. **The Arabic Project Knowledge Document** (الوثيقة التسويقية): a comprehensive Markdown source of truth for the **sales, content, and marketing teams** — complete description, project story & positioning, location, developer, every unit model with its specs, design & architecture, construction info, finishes, amenities, guarantees, prices/payment plans as stated, key selling points, competitive advantages, target customer profile, and a closing quick-facts list. Informative, marketing-oriented prose — always in Arabic regardless of UI language, grounded only in the files. Appears as its own **editable panel** in the review step, is persisted on the migration record (`data.project_document`), and on import is written into the project record's **`marketing_document`** field (Project Content section) — where the record form shows it and the copywriter agent's `get_project` fact sheet picks it up automatically (sales people keep using the Units model for per-unit detail). An upload-step banner explains the mode; an explicit column mapped to `marketing_document` wins over the injection.
- **Counting happens IN extraction, from the floor plans.** There is no separate step-2 counter. For each unit the AI deeply analyzes its floor plan — matched to the unit by its type label (`نموذج A1` on the plan = the A1 row) — counts the components from the drawing (bathrooms = WC fixtures, bedrooms, kitchens…), and **combines that with the brochure text**, trusting the plan on disagreement. So "2 دورة مياة" + a master en-suite resolves to **3**. Counted values are ordinary numeric columns you map and review like any other; the AI's explanatory text follows the UI language (Arabic in the Arabic app).
- **Post-extraction summary + discussion chat (raw-table step).** Extraction returns a plain-English **summary** of what it captured — especially *how* each number was derived and its source (text / plan / both). Below it is a **multi-turn chat**: ask the AI to explain a value, or instruct it to recount / fill / add a column — it re-reads the brochure (incl. the plans) and returns column edits that are merged into the grid (existing columns are overwritten with its non-empty values, new columns appended; nothing fabricated). The summary + thread persist on the record. (Supersedes the one-shot "Ask AI" enrichment.)
- **Lookups avoid duplicates:** "link existing" stores the matched record's **exact id** (not its display) so the right record is linked even when display names collide; "create new" registers a pending record that is deduped against existing records by normalized display at migrate time. Only a genuinely-new name mints a new developer/project.
- **Standardization matches existing values reliably:** every distinct value is first matched **deterministically** (Arabic-normalized — strips diacritics/shadda, unifies alef/ya/taa-marbuta — so "شقه"→"شقة", "ريا"→"ريّا النخيل") against the field's options or **all** lookup records; only genuinely-unmatched values are sent to the AI. This is essential for large lookups (e.g. all_projects = 1,371 records, far more than fit in one AI prompt). For lookup columns the per-value control is a **searchable record picker** (LookupCombobox); for dropdowns it's a searchable option picker — so you search instead of scrolling.
- **Editable raw grid:** edit cells, add/remove rows & columns, and **fill-down** a column (type a value in the top cell, click ⤓ to apply it to every row). Excel/CSV is read as UTF-8 (Arabic is safe even without a BOM).
- **Final preview = strict + auditable:** before anything is written, the preview shows every record that *will* be created with resolved display values (option labels, linked-record names — including pending "create new" names) plus per-cell **action markers** (link / new / routed) and a summary of pending new options + new lookup records. Each valid record has a checkbox (approved by default); deselect any you don't want. **Rows with a blocking error** (an unresolved controlled value, a mapped-but-blank required field, a blocked create, an invalid number, an invalid routed value) are shown highlighted with their reasons but **cannot be selected and never migrate**; non-blocking issues (e.g. an unmapped required field) show as warnings. Exact duplicates are auto-skipped and counted. Built on a shared, pure `buildMigrationPlan`, so what you approve is exactly what gets saved.
- **Migrate is strict and decision-driven.** The migrator builds records directly from the approved decisions (exact option values / record ids) — it does **not** round-trip through `mapImportedRows` for the migration path, so a raw label is never silently stored in a controlled field. For the approved + valid rows it creates the referenced new options (into the model schema, frozen-safe) and new lookup records, then saves via the standard `saveRecord` path so auto-IDs, formulas, frozen-model dispatch, duplicate-skip, and the offline retry queue all work as elsewhere. Per-row failures are reported, never silently dropped. (`mapImportedRows` remains the core of the separate Excel **Import** modal.)
- **A completed migration stays browsable.** After a migration finishes (`status='done'`) the wizard shows a **view navigator** (Table · Mapping · Standardize · Preview · Result) so you can revisit exactly what was migrated and the result — it no longer dead-ends on the result screen. It's read-only for the import: the Preview page shows an "already ran" banner and a **View result** button (no Migrate), and the import can never be re-run in place (a `startMigrationJob` backstop no-ops on a done record).
- **Undo a completed migration ("un-migrate" → edit → re-migrate).** The Result step offers **"Undo this migration"** (when the run captured an undo ledger). After a confirm that states the counts, it **deletes the records the migration created** + the new lookup records it created, **reverts** any project it merged into (restoring the pre-merge snapshot — the project's units/rollups are separate records and untouched), and re-opens the migration at the editable **Preview** step (`status='draft'`) — so you adjust the table/mapping/standardization and **Migrate** again (re-run dedup sees the deleted records gone → no duplicates). **Created dropdown options are kept** (shared/deduped). Undo is an in-tab job (`status='undoing'` spinner + list pill); it's idempotent (safe to retry after an interrupted run). The undo ledger (`MigrationResult.undo`: created record ids, created lookup records, per-project pre-merge snapshots) is recorded at migrate time, so **only migrations run with undo support can be undone** — older completed migrations show no Undo button.
- **Prompt library in the prep step.** Next to the instructions field is a **Prompt library** — a shared set of saved instruction templates. Pick one to fill the instructions before an analysis, save the current instructions as a new named prompt, or delete one you authored. Stored server-side (`migration_prompts`, shared-read), so templates are reusable across migrations and devices.
- **Answered questions stay viewable.** When you answer a question card and submit, the AI drops the resolved card from the open set — but the question + your answer are now logged to `prep_answered` and shown in a read-only **"Answered questions"** panel, so the Q&A survives. On a completed migration the prep page is reachable from the done navigator's **Q&A** chip (read-only: no new turns, no extraction).
- **`data_migration` is never freezable and never a migration target** (it's excluded from the target picker and the Freeze modal).

## User flows

1. **Main happy path (messy files):** New migration → pick model → drop PDF/images → *Next: instructions & review* → **prep step** (optionally write instructions; *Review files & clarify* → answer the AI's questions / confirm the proposed structure — **mandatory: "Start extraction" stays locked until you've run the review and resolved every question**) → *Start extraction* → review/fix the raw table → *Suggest column mapping* → adjust → *Review values* → approve/override each value → *Preview records* → approve/deselect each record → *Migrate* → "Imported N records into <model>".
2. **Fast path (clean data — "start at step 5"):** New migration → pick model → drop a clean Excel/CSV into the **Excel / CSV** field → switch it to **Use as data source** (or start blank + paste) → review → mapping → standardize → migrate.
3. **Messy Excel path:** New migration → pick model → drop the Excel into the **Excel / CSV** field → leave it on **Analyze with AI** (add brochures/PDFs to the Documents field too if you have them) → the workbook becomes per-sheet CSVs in the extraction set → *Next: instructions & review* → prep (clarify/confirm — required before extraction) → *Start extraction* → continues like the messy-files flow.
4. **Project onboarding (All Projects target):** New migration → pick **All Projects** (banner explains project-profile mode) → drop the developer's full hand-off (brochure, price list, plans) → *Next: instructions & review* → prep (e.g. instruct "treat each نموذج as a model, not a unit"; let the AI flag any contradictions + confirm the structure — required before extraction) → *Start extraction* → ONE project row + the project intelligence briefing + the Arabic knowledge document → review (the intelligence and the document each have their own panel; the document is editable) → mapping → standardize (developer lookup, city, unit types…) → preview (note confirms the document is included; long-text cells are clamped) → *Migrate* → one project record created with `marketing_document` filled.
5. **Edit-and-re-upload:** at the review step, Download Excel → fix in Excel → Re-upload corrected (mappings reset) → continue.
6. **Parallel migrations:** start migration A's extract or import → click **New migration** and work on B (or C…) while A keeps running — the list shows each row's live status pill + import progress; click any row to re-attach to its progress.
7. **Error/empty states:** an AI step that fails or times out shows a loud toast + the wizard stays put so you can retry or fall back to manual mapping (a failed extraction/import also flips the record to a red **Failed** pill with the error persisted on the record + a Retry button); a run interrupted by a page reload shows an explicit interrupted state with Retry/Resume; a model with no dropdown/lookup columns skips straight to a "ready to migrate" state; very large tables (>500 rows) render the first 500 for in-app editing with a note that all rows still migrate (edit the rest via download).

## Data touched

- **Reads:** `models.schema` (target field types, options, lookup config), `records.data` of the lookup-target and target models (existing values for matching + duplicate-skip), uploaded files in the `wassel-migrations` Storage bucket, and `migration_prompts` (the shared prompt library).
- **Prompt library:** `migration_prompts` (id, label, body, created_by_user_id) — a shared table of saved instruction templates. RLS: any authenticated user reads; the author inserts/updates/deletes their own. Written from the prep step's Prompt library popover.
- **Writes:** `records.data` (JSONB) for the imported rows + any approved new lookup records (created with the plan's stable temp id, deduped against existing), via `record_save`; the target model's `schema` when the user approves new options (deterministic slug; safe for frozen models — JSONB only); the `data_migration` record's own `data` at every step (resume state — including the per-value standardization decisions, preview exclusions, and validation derives from them); uploaded files to `wassel-migrations` (`<auth.uid()>/<recordId>/uploads/...`).
- **Job queue:** `data_migration_jobs` (id, `migration_record_id` → records, `user_id`, `kind` ∈ plan/extract/discuss/import, `status`, `payload`, `result`, `error_message`, `attempts`, `worker_id`, timestamps). RLS: owner-SELECT only; all writes via service-role RPCs (`data_migration_job_enqueue` / `_claim_next` / `_complete` / `_fail` / `_cancel` / `data_migration_jobs_watchdog`). A partial unique index on `migration_record_id WHERE status IN ('queued','running')` enforces one active job per record (idempotent enqueue). Drained by the Fly worker's migration poll loop. (`supabase/migrations/2026-06-23_data_migration_jobs_queue.sql`.)
- **AI:** `POST /api/migrate` (`action: extract | plan | discuss | cancel | status | suggest_mappings | standardize`). **extract / plan / discuss are ENQUEUE-ONLY** — they don't call Anthropic; the endpoint RLS-gates the source record, writes its busy/status field (+ plan/discuss append the operator's message) server-side, inserts a `data_migration_jobs` row, pings the worker `/wake`, and returns `202 { job_id }`. The worker (`worker/src/runMigrationJob.ts`) then runs the copied agent below with no timeout. `discover`/`fuse_batch` are NOT separate actions — they run inside one `extract` job. `cancel` cancels the active job + resets the record's busy state; `status` reads the latest job (Realtime is primary). `suggest_mappings`/`standardize` are the only SYNCHRONOUS Anthropic actions (text-only, fast). The agent (now invoked from the worker): **plan** is the pre-extraction prep turn (`runPlan` / forced `emit_plan`): given the conversation so far + the operator's `instructions` + the signed-URL files + the field hunt-list, it returns `reply` + `questions` (contradictions / unclear items) + `proposed_columns` (header + how-derived) + `ready` — it does NOT extract any rows. The extraction actions accept an optional **`guidance`** string (instructions + resolved clarifications + confirmed structure) that's injected into their prompts. For **per-record targets** (units, clients…) extraction runs as **discover** (files → unit index + canonical headers) then a series of **fuse_batch** calls (a batch of units → one merged row each + cross-source `conflicts`); both are vision-deep, and the client prompt-caches the files across batches and **auto-splits** any batch the model truncates (a truncation returns `code:'max_tokens'`, never a partial save). **extract** (now the All Projects / `mode:'project'` path) is model-aware + vision and returns one aggregated row + a `summary`, plus the `intelligence` sections + the Arabic knowledge `document`. Every phase fails loudly on truncation (`requireToolInput` / `ExtractionTruncatedError`). **discuss** is the multi-turn chat (re-reads the brochure to recount/revise). Vision model for extract/discuss-with-files, text model for mapping/standardize. No data is persisted server-side; the endpoint fetches signed-URL files and returns structured JSON. Accepted file kinds: images, PDFs, and **CSV/plain text** (Excel sheets arrive as client-converted CSVs) — text is inlined as fenced blocks, char-capped (120k/file, 300k total) with line-boundary truncation reported via `truncated`/`files_skipped`.
- **Project-profile writes:** the live `all_projects` model gained a `marketing_document` textarea (Project Content section, added 2026-06-11 via SQL on the unfrozen model; `v_all_projects` regenerated automatically). `buildMigrationPlan` injects `data.project_document` into each built record's `marketing_document` (explicit column mapping wins), so preview = exactly what's written.
- **Bulk RPC available (not yet wired into the live path):** `records_bulk_save` + `record_reserve_auto_ids` exist as a future fast-path for very large imports.

## Key files

| File | What it does |
|---|---|
| `src/pages/DataMigration/DataMigrationPage.tsx` | Split-pane shell: past migrations list + active wizard |
| `src/pages/DataMigration/components/MigrationList.tsx` | Sidebar list with rename / group (collapsible, renamable) / delete per migration |
| `src/pages/DataMigration/components/MigrationWizard.tsx` | Step machine; reads/writes wizard state on the record. When `status='done'` it also renders the **view navigator** (`DONE_NAV` chips incl. **Q&A** → prep, `canView` reachability guard) so a finished migration's pages + result are browsable read-only |
| `src/pages/DataMigration/components/PromptLibrary.tsx` | The **prompt library** popover next to the instructions field — list/pick/save/delete saved instruction templates (via the `migration_prompts` client helpers) |
| `src/pages/DataMigration/components/steps/*` | One file per step (pick model, upload, review, mapping, standardize, migrating, done) |
| `src/pages/DataMigration/components/EditableRawGrid.tsx` | In-app editable raw table |
| `src/pages/DataMigration/components/ValueStandardizationColumn.tsx` | Per-value resolution UI (the core): searchable option/record picker, create-new with editable label, leave-blank, route, blocked-create + frozen warnings |
| `src/pages/DataMigration/components/steps/StepPrep.tsx` | **Pre-extraction prep step** — instructions textarea + the clarification chat + proposed-structure panel + the extraction trigger + the **live worker-extraction progress view** (phase/progress from the record, Realtime-driven; a Retry link re-enqueues). The clarify turn is **enqueued** via `startPlanJob`; "busy" is read from the record's `prep_busy` (set server-side, survives reload) plus a local `sending` bridge. **"Start extraction" is gated** (`blocked = !reviewed \|\| questions.length > 0`). Hosts the **prompt library** + a read-only **"Answered questions"** panel; a `readOnly` flag turns the page into a read-only review |
| `src/pages/DataMigration/components/steps/StepReviewRaw.tsx` | Editable grid + extraction summary + the discussion chat (**enqueued** via `enqueueDiscussTurn`; spinner from the record's `discuss_busy`; the worker's revised table is adopted when the turn finishes) + the **Source conflicts** panel |
| `src/pages/DataMigration/lib/jobRunner.ts` | `startExtractionJob` / `startPlanJob` are now thin **enqueue** wrappers (build the fields/mode/guidance, POST `/api/migrate`); the worker does the orchestration. `startMigrationJob` (the **import** step) + **`undoMigrationJob`** (delete created records/lookups → restore project snapshots → reopen at draft/preview) run here in-tab. `buildGuidance` is built here and frozen into the extract payload |
| `src/pages/DataMigration/lib/runMigration.ts` | the import engine; now also records the **undo ledger** (`result.undo`: created record ids + created lookup records + pre-merge project snapshots) during its create/merge loops |
| `src/pages/DataMigration/components/steps/StepDone.tsx` | result summary + the **"Undo this migration"** button (when `result.undo` exists) with a confirm |
| `worker/src/runMigrationJob.ts` | **The worker pipeline** (dispatch on `extract`/`plan`/`discuss`): records-mode `discover → batched fuse → assemble` with recursive `max_tokens` auto-split + `buildFusionSummary`, project-mode single `runExtract`, plan/discuss turns; per-call signed-URL minting; `record_save` + `p_expected_version` + retry-on-40001 patch helper; reflects a failure onto the record so the spinner exits |
| `worker/src/migrateAgent.ts` | **Verbatim COPY of `api/_lib/migrateAgent.ts`** (the worker can't import `api/_lib`). Keep in sync |
| `worker/src/index.ts` | The migration poll loop (`claimAndRunOneMigration` + `migrationPollLoop` + `runMigrationWatchdog`), `/healthz` `migration_busy`, `/wake` |
| `supabase/migrations/2026-06-23_data_migration_jobs_queue.sql` | The `data_migration_jobs` queue: table + indexes + RLS + enqueue/claim/complete/fail/cancel/watchdog RPCs + one-time stuck-record cleanup |
| `src/pages/DataMigration/lib/client.ts` | Upload + signed-URL helpers + the **enqueue** callers (`enqueueExtraction` / `enqueuePlanTurn` / `enqueueDiscussTurn` / `cancelMigrationJob`) + the synchronous `suggestMappings` / `standardizeColumn` + `downloadLibraryFileAsFile` (Files-library pick → `File`) + `isMigrationAttachableName` + the **prompt-library** helpers (`migration_prompts`) |
| `src/components/files/PickFromFilesModal.tsx` | Shared RLS-gated Files browser/picker (configurable `isSelectable` + `multiple`); used by the migration upload step AND Decks attachments |
| `src/pages/DataMigration/lib/buildRecords.ts` | **The strict, decision-driven record builder** — `buildMigrationPlan` (pure): resolves controlled fields from decisions to exact option values / record ids, registers pending options (deterministic slug) + pending lookup records (deduped), re-standardizes routed values, validates each row, dup-skips. No raw-label storage, no display round-trip. |
| `src/pages/DataMigration/lib/runMigration.ts` | `runMigration` (creates referenced new options into the schema + new lookup records, then saves approved + valid records) + `applyNewOptionsToModel` + `isRecordValid`; re-exports `buildMigrationPlan` |
| `src/pages/DataMigration/components/steps/StepPreview.tsx` | Strict + auditable per-record approval table (pending-creates summary, per-cell action markers, per-row validation, invalid rows blocked). In `alreadyMigrated` mode (done migration) it's read-only: green "already ran" banner + **View result** button instead of Migrate |
| `src/pages/DataMigration/lib/previewRecords.ts` | Resolve stored values → display labels for the preview table (incl. pending new-record temp ids → label) |
| `src/pages/DataMigration/lib/{types,targetFields,buildStandardization}.ts` | Shared types + field/option helpers (incl. `lookupCreateBlockReason`) + AI→decision bridge |
| `src/lib/textMatch.ts` | The ONE shared normalizer (`normalizeMatch`) + multi-value splitter (`splitMultiValue`) used across the whole standardization flow |
| `src/lib/optionSlug.ts` | Shared deterministic option slug (`slugifyOptionLabel`, `findExistingOption`, `uniqueOptionValue`) — reused by the record form AND the migrator |
| `api/migrate.ts` | Control plane: `extract`/`plan`/`discuss` → **enqueue** (RLS-gate + server-side busy/status write + message append + `data_migration_job_enqueue` + `/wake` + 202); `cancel`/`status` control; `suggest_mappings`/`standardize` synchronous. No vision runs here (maxDuration 60) |
| `api/_lib/migrateAgent.ts` | The Anthropic tool-use logic (copied to the worker for the file-heavy actions): `runPlan` (`emit_plan`), `runExtract` (project mode), `runDiscover` + `runFuseBatch` (records-mode source fusion), `runSuggestMappings`, `runStandardize`, `runDiscuss`, the shared `renderGuidance` + `requireToolInput` truncation guard + `ExtractionTruncatedError`, and prompt-caching on the fused-batch file blocks. The API imports it for the sync actions; the worker uses its copy for extract/plan/discuss |
| `src/lib/excelUtils.ts` | `readExcelFile`, `exportRawTable`, `mapImportedRows` (the latter now only powers the separate Import modal) |
| `src/data/seedModels.ts` | `data_migration` system model seed |
| `supabase/migrations/2026-06-01_*.sql` | `wassel-migrations` bucket + RLS; `records_bulk_save` / `record_reserve_auto_ids` RPCs |

## Open questions / known limitations

- **Model-aware extraction verified live (2026-06-03):** `runExtract` was run against the real ريّا النخيل brochure (54 pages, 39 unit models) — it produced model-aware columns, bare numbers, and **plan-derived bathroom counts that combine the plan with the text** (A1 → 3 = "2 دورة مياة" + master en-suite, exactly matching the source), plus an Arabic summary explaining each number's source. `runDiscuss` answered a sourced "how many bathrooms?" question correctly. Mapping/standardization quality still benefit from in-app spot-checks (MFA-gated); the non-AI paths (clean-Excel entry, grid editing, mapping adjustment, value approval, route-to-field, migrate) are fully verified.
- **Extraction size:** records-mode source fusion removes the old single-response output cap — units are discovered as a small index and resolved in auto-splitting batches, so unit count is no longer the limiter (the guard makes any genuine overflow fail loudly, never silently). The remaining bounds are Anthropic per-request **input** limits (images ≤5MB, ≤20 files, ~24MB total / 300k text chars) shared by every phase, and discovery enumerating a very large index in one **non-streaming** call (capped at the SDK's 16k-token non-streaming ceiling — `max_tokens` above that makes the SDK refuse with "Streaming is required…"; a bigger index hits the loud truncation guard → split the input). Cost/latency scale with batch count (prompt caching keeps the per-batch file cost low). All Projects (project mode) is still a single small-output call.
- **Worker-queue extraction — live-verification pending:** the refactor typechecks (SPA build + worker `tsc` + `api/migrate.ts` tsc all clean) but must be verified end-to-end on prod after deploy: the migration applied (RPCs + the two stuck records flipped to `failed`), the worker `/healthz` showing `migration_busy`, and a real extraction enqueued → worker claims it → progress streams to the SPA via Realtime → finishes at `review_raw`, including a **mid-run tab reload** (must keep progressing) and a forced failure (must persist `status='failed'` + `error_message`, never stuck at `extracting`). Plan + discuss turns should run async (reply arrives via Realtime). The جزيل / Jazeel hand-off (~116 units) is a good source-fusion smoke test. Deploy order: migration → Fly worker → Vercel.
- **Pre-extraction prep step — partially live-verified:** the `plan` action + `StepPrep` are live on prod — verified 2026-06-23 that *Review files & clarify* runs against a real uploaded file, returns a proposed structure + open-question cards, persists, survives navigating away/back, and gates "Start extraction". Still needs a deployed run against real *contradictory* hand-off files to confirm the AI surfaces contradictions well and that the produced table actually honors the instructions/guidance end-to-end (records mode + project mode).
- **Bulk insert path:** the live migrate uses `saveRecord` per row (correct + proven, slower at scale). `records_bulk_save` is built and DB-verified but not yet wired in — a clean follow-up to speed up multi-thousand-row imports.
- **Routed values ARE now re-standardized** (fixed 2026-06-18): a value moved to another field is resolved against that field's type, and an unresolvable routed value blocks the row instead of being stored raw.
- **Excluded-row creates ARE now pruned** (fixed 2026-06-18): a new option / lookup record is created only if a row that actually migrates references it; deselecting that row in preview drops its create too.
- **Required-field validation is two-tier by design:** a required field that was *mapped* but resolved to blank is a hard error (row blocked); a required field with *no source column at all* is a non-blocking warning (the wizard can't fill data it never received). This is a deliberate practicality call — blocking every row for an unmapped required field would make legitimate partial imports impossible.
- **Value-mapping memory not implemented yet** (explicitly deferred): standardization decisions aren't remembered across migrations — each migration re-resolves from scratch. A reusable mapping memory is a planned follow-up.
- **Scope rules on multi-value fields of frozen models** still fail closed (a pre-existing frozen-model RLS limitation, unrelated to this layer) — see data-storage.md.
- **One target model per migration:** units link to projects via the normal lookup auto-match (a "project name" column resolves/creates the project) — there is no combined parent+child wizard.
