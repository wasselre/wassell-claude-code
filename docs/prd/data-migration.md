# PRD: Data Migration

**Status:** Live
**Last updated:** 2026-06-18 (REFRESH-SAFE DRAFTS — the wizard's saves now bypass optimistic-version concurrency (`expectedVersion: null`, the single-record/single-writer "decks" posture). The wizard fire-and-forget-saves the whole record on every step/value change; routing those through the version check drifted the tab's local `version` behind the server (realtime echo-dedup hides the tab's own bumps), so saves began returning `version_mismatch` — which DROPS the write (not queued), trips the per-record circuit breaker, and makes the reload-on-conflict handler overwrite the in-progress wizard state with the stale server copy. Net effect: everything done after the first conflict never reached the DB, so a refresh "lost"/"deleted" the migration. With `null` the RPC skips the check, saves always land, and a reload resumes exactly where you were. Fixed in `MigrationWizard.tsx` (`patch`) + `lib/jobRunner.ts` (`patchMigrationRecord`).) 2026-06-18 (UPLOAD STEP = TWO FIELDS — the single drop zone is split into **"Documents (non-Excel)"** (PDF/images, always AI-analyzed) and **"Excel / CSV"**, where each added file gets a per-file choice — **Analyze with AI** (default; joins the extraction set as per-sheet CSV) or **Use as data source** (parsed in-browser, straight to review, no AI). The two are mutually exclusive (block-mixing): "use as data source" is only offered when the Excel is the SOLE file, and selecting it locks both drop zones. This replaces the old behavior where an Excel mixed with other files was silently forced into AI extraction and an Excel-only drop popped a one-off "direct table or AI?" chooser. Previously, 2026-06-11: EXCEL THROUGH AI EXTRACTION — an Excel/CSV-only drop asked "direct table or AI extraction?"; choosing AI converts the workbook to per-sheet CSV text and runs it through extraction exactly like a PDF (messy layouts, multi-sheet, mixed with brochures). Excel mixed with PDFs/images is no longer skipped — it joins the AI set. Same day: CONCURRENT MIGRATIONS — extraction + import run in a module-level job runner (`lib/jobRunner.ts`) detached from the wizard components, so any number of migrations can run at the same time; starting/opening another migration no longer stops a running one, the list shows live per-row progress, and interrupted runs (page reload) get explicit retry/resume. Also: PROJECT-PROFILE mode — migrating into All Projects produces THREE outputs: ONE project row (no unit table), a project-level intelligence briefing, and an Arabic Project Knowledge Document saved to the project's new `marketing_document` field; computed rollup fields removed from mapping targets)
**Related PRDs:** [import-export.md](import-export.md) (reuses the Excel import core), [record-management.md](record-management.md) (writes records via the same save path), [data-storage.md](data-storage.md) (RPCs + storage bucket), [model-builder.md](model-builder.md) (target field types + options)

## What it is (in plain English)

A guided wizard that turns the messy data developers send us — Excel files, PDFs, screenshots, photos, in any mix — into clean records inside any model, with a human approving every change before it lands. You open **Data Migration** (ترحيل البيانات) in the sidebar, start a new migration, pick which model to import into, and drop in your files. The app reads them into one table, lets you review and fix that table, figures out which column maps to which field, and — for dropdown / multi-select / lookup fields — proposes how to standardize each raw value to match what the app expects (e.g. "شقه" → the option "شقة", "Apartment" → "شقة"), showing every proposed change as a before→after you approve, override, or redirect to a different field. When you're happy, you click **Migrate** and the records are imported.

Extraction is the smart part. **Before** reading your files the app looks at the target model's fields, so it knows *what to hunt for* (e.g. it knows you want a developer name) — but it captures values raw, never coercing them to the system (that's the later step's job). It writes numbers as plain numbers, and it **reads the floor plans deeply**: for each unit it counts the components (bathrooms, bedrooms, kitchens…) from the drawing and **combines that with the brochure text** — so an ambiguous "2 دورة مياة + الجناح الرئيسي يحتوي على دورة مياة" resolves to **3** because the plan confirms it. After extraction it writes you a **summary** of how it derived every value (especially each number, with its source), and you can **chat with it** to question a value or have it recount / pull a missed column by re-reading the brochure.

You don't have to start from messy files: if you already have a clean table, upload an Excel/CSV and choose **Use as data source** (or paste into the in-app grid) to jump straight to the review/mapping step. The upload step keeps **documents and Excel in two separate fields** so Excel is never silently forced into one behavior — you decide, per Excel file, whether it's analyzed like the rest or used as-is.

## Why it exists

Onboarding a developer's project + units used to be a manual, hours-long job: eyeball the files, retype into a spreadsheet, rename values to match the app's dropdowns, fix number/date formats, match developer/project names to existing records, then import. This wizard builds that whole process into the app, with AI doing the tedious extraction/matching and the operator staying in control via explicit approval gates.

## Key behaviors

- **One migration = one record** in the `data_migration` system model. The entire wizard state (target model, raw table, mappings, value decisions, result) lives on `record.data`, so you can close the tab and resume exactly where you left off. Because it's a single record edited by a single tab, the wizard's saves intentionally **opt out of optimistic-version concurrency** (`expectedVersion: null`) — sending a version with every rapid full-record auto-save drifted the local version behind the server and made saves conflict, so the write was dropped and a refresh lost the in-progress work; skipping the check makes every save land and a reload resume reliably.
- **Any number of migrations run at the same time.** The two long jobs — AI extraction and the row-by-row import — run in a module-level **job runner** (`lib/jobRunner.ts`), keyed by migration record id and detached from the wizard's component lifecycle. Starting a new migration, switching between migrations, or navigating elsewhere in the app never stops a running job; coming back just re-attaches to its live progress (it never restarts — starting a job for a record that already has one is a no-op, which also kills the old remount-double-import bug). Outcomes (status flips, errors, results) are written onto the migration record, so the left list's pills stay live; rows with a running import also show an inline progress bar (`n/N`). Jobs are in-tab: a page reload interrupts them, and the wizard then shows an explicit **Retry extraction** / **Resume migration** state instead of a stuck spinner (resume re-skips already-imported rows only when the model has a duplicate-check field). Uploaded source files are persisted on the record at upload time (not at extraction end), so an interrupted extraction can always be retried. The import loop yields to the browser every 20 rows so very large offline runs don't freeze the tab.
- **Upload step = two fields, with the Excel choice made explicit.** The drop area is split:
  - **Field 1 — Documents (non-Excel):** PDF, images, screenshots. Always uploaded into the AI extraction set; never offered a "use directly" path (Claude has to read them).
  - **Field 2 — Excel / CSV:** each file you add gets a per-file choice. **Analyze with AI** (the default) treats it like a PDF — the workbook is converted to **one CSV text file per non-empty sheet** (Claude can't ingest raw .xlsx) and joins the AI extraction set, right for messy layouts (title rows, merged cells, sub-tables), multi-sheet workbooks, or combining a price sheet with a brochure. **Use as data source** parses the sheet in the browser (never sent to AI, straight to review — the clean-sheet fast path; first sheet only, multi-sheet warns).
  - **The two are mutually exclusive (block-mixing):** "Use as data source" is only available when that Excel is the **sole** file in the step (no documents, no other Excel); choosing it **locks both drop zones** (a chosen data source IS your whole table). To analyze documents and an Excel together, leave every Excel on **Analyze with AI** — they all merge into one AI-extracted table. Default-Analyze means by default an Excel is analyzed alongside whatever documents you add.
  - Both drop zones are **forgiving** — a file dropped in the "wrong" field is routed by type. You can also **start with a blank table**.
- **The raw table is yours to edit:** edit cells in-app, add/remove rows & columns, **download it as Excel**, fix it externally, and **re-upload** — or keep editing in place. Re-uploading replaces the table and resets downstream mappings (headers may have changed).
- **AI suggests column→field mappings**, you adjust them. Range fields expose two targets (min/max). Non-importable field types (formulas, mirrors, auto-IDs, files, notes) are never offered as targets.
- **Value standardization with approval (the core promise):** for each dropdown/multi-select/lookup column, the app proposes, per distinct value, one of: match an existing option/record, create a new option/record (only on your explicit approval), leave blank, or **move this value's rows to a different field** (splitting a column). Nothing is created or changed until you click Migrate. "Accept all high-confidence" bulk-applies the ≥90%-confident proposals.
- **Multi-select stays multi-value end-to-end** — values are kept comma/`،`-separated and standardized per token, never collapsed to one (a deliberate departure from the Excel template export, whose single-select in-cell dropdown can't represent multiple values).
- **Model-aware extraction (the hunt-list).** The chosen target model's fields are sent to extraction as a "what to look for" list — so the AI knows to find a developer name, a unit type, bathroom/bedroom counts, etc. — and it adds extra columns for any other useful data it finds. It is explicitly told **not** to clean, normalize, or coerce values to fit the system (raw is fine; cleaning is the standardize step). Numeric data comes out as **bare numbers** (`3`, not "3 دورات مياه"). Computed rollup fields (`is_computed` — the All Projects unit-derived ranges/counts) are excluded from both the hunt-list and the mapping targets, since they're recomputed live from units and an imported value would be silently ignored.
- **PROJECT-PROFILE mode (target = All Projects) — three outputs from one extraction.** When the migration target is the projects model, the system understands you want a detailed understanding of the **project as a whole**, not individual unit records:
  1. **The migration table** flips from "one row per unit" to **one row per project** (normally exactly one): the project's general information in detail — developer, location, type, status, unit types/models offered, amenities, guarantees, payment terms, licenses — with units still read deeply (floor plans included) but **aggregated, never enumerated as a unit table**.
  2. **A project-level intelligence briefing** (`data.project_intelligence`): titled sections that explain the project itself — overview, developer, location & neighborhood, concept & positioning, target audience, advantages, amenities, construction status, guarantees, design philosophy, architecture & facades, unit mix, pricing insights, area insights, distinguishing features, unique selling points. Factual and grounded; insights may include arithmetic derived from in-file numbers (clearly phrased as derived) but never external market knowledge; unsupported dimensions are omitted. Shown read-only in its own collapsible panel in the review step; persisted on the migration record; never written into the imported record.
  3. **The Arabic Project Knowledge Document** (الوثيقة التسويقية): a comprehensive Markdown source of truth for the **sales, content, and marketing teams** — complete description, project story & positioning, location, developer, every unit model with its specs, design & architecture, construction info, finishes, amenities, guarantees, prices/payment plans as stated, key selling points, competitive advantages, target customer profile, and a closing quick-facts list. Informative, marketing-oriented prose — always in Arabic regardless of UI language, grounded only in the files. Appears as its own **editable panel** in the review step, is persisted on the migration record (`data.project_document`), and on import is written into the project record's **`marketing_document`** field (Project Content section) — where the record form shows it and the copywriter agent's `get_project` fact sheet picks it up automatically (sales people keep using the Units model for per-unit detail). An upload-step banner explains the mode; an explicit column mapped to `marketing_document` wins over the injection.
- **Counting happens IN extraction, from the floor plans.** There is no separate step-2 counter. For each unit the AI deeply analyzes its floor plan — matched to the unit by its type label (`نموذج A1` on the plan = the A1 row) — counts the components from the drawing (bathrooms = WC fixtures, bedrooms, kitchens…), and **combines that with the brochure text**, trusting the plan on disagreement. So "2 دورة مياة" + a master en-suite resolves to **3**. Counted values are ordinary numeric columns you map and review like any other; the AI's explanatory text follows the UI language (Arabic in the Arabic app).
- **Post-extraction summary + discussion chat (raw-table step).** Extraction returns a plain-English **summary** of what it captured — especially *how* each number was derived and its source (text / plan / both). Below it is a **multi-turn chat**: ask the AI to explain a value, or instruct it to recount / fill / add a column — it re-reads the brochure (incl. the plans) and returns column edits that are merged into the grid (existing columns are overwritten with its non-empty values, new columns appended; nothing fabricated). The summary + thread persist on the record. (Supersedes the one-shot "Ask AI" enrichment.)
- **Lookups avoid duplicates:** "link existing" rewrites the value to the matched record's display so the importer resolves to that record; only an explicit "create new" mints a new developer/project.
- **Standardization matches existing values reliably:** every distinct value is first matched **deterministically** (Arabic-normalized — strips diacritics/shadda, unifies alef/ya/taa-marbuta — so "شقه"→"شقة", "ريا"→"ريّا النخيل") against the field's options or **all** lookup records; only genuinely-unmatched values are sent to the AI. This is essential for large lookups (e.g. all_projects = 1,371 records, far more than fit in one AI prompt). For lookup columns the per-value control is a **searchable record picker** (LookupCombobox), so you search instead of scrolling thousands of options.
- **Editable raw grid:** edit cells, add/remove rows & columns, and **fill-down** a column (type a value in the top cell, click ⤓ to apply it to every row). Excel/CSV is read as UTF-8 (Arabic is safe even without a BOM).
- **Final preview + per-record approval:** before anything is written, a preview step shows every record that *will* be created — in a table with resolved display values (option labels, linked-record names, not slugs/UUIDs). Each record has a checkbox (all approved by default); deselect any you don't want. Only approved records migrate (exact duplicates are auto-skipped and counted; new linked-record count is shown). Built on a shared, pure `buildMigrationPlan`, so what you approve is byte-for-byte what gets saved.
- **Migrate reuses the proven import core** (`mapImportedRows`) and the standard `saveRecord` path, so auto-IDs, formulas, frozen-model dispatch, duplicate-skip, and the offline retry queue all work exactly as they do elsewhere. Per-row failures are reported, never silently dropped.
- **`data_migration` is never freezable and never a migration target** (it's excluded from the target picker and the Freeze modal).

## User flows

1. **Main happy path (messy files):** New migration → pick model → drop PDF/images → *Extract data* → review/fix the raw table → *Suggest column mapping* → adjust → *Review values* → approve/override each value → *Preview records* → approve/deselect each record → *Migrate* → "Imported N records into <model>".
2. **Fast path (clean data — "start at step 5"):** New migration → pick model → drop a clean Excel/CSV into the **Excel / CSV** field → switch it to **Use as data source** (or start blank + paste) → review → mapping → standardize → migrate.
3. **Messy Excel path:** New migration → pick model → drop the Excel into the **Excel / CSV** field → leave it on **Analyze with AI** (add brochures/PDFs to the Documents field too if you have them) → the workbook becomes per-sheet CSVs in the extraction set → *Extract data* → continues like the messy-files flow.
4. **Project onboarding (All Projects target):** New migration → pick **All Projects** (banner explains project-profile mode) → drop the developer's full hand-off (brochure, price list, plans) → *Extract data* → ONE project row + the project intelligence briefing + the Arabic knowledge document → review (the intelligence and the document each have their own panel; the document is editable) → mapping → standardize (developer lookup, city, unit types…) → preview (note confirms the document is included; long-text cells are clamped) → *Migrate* → one project record created with `marketing_document` filled.
5. **Edit-and-re-upload:** at the review step, Download Excel → fix in Excel → Re-upload corrected (mappings reset) → continue.
6. **Parallel migrations:** start migration A's extract or import → click **New migration** and work on B (or C…) while A keeps running — the list shows each row's live status pill + import progress; click any row to re-attach to its progress.
7. **Error/empty states:** an AI step that fails or times out shows a loud toast + the wizard stays put so you can retry or fall back to manual mapping (a failed extraction/import also flips the record to a red **Failed** pill with the error persisted on the record + a Retry button); a run interrupted by a page reload shows an explicit interrupted state with Retry/Resume; a model with no dropdown/lookup columns skips straight to a "ready to migrate" state; very large tables (>500 rows) render the first 500 for in-app editing with a note that all rows still migrate (edit the rest via download).

## Data touched

- **Reads:** `models.schema` (target field types, options, lookup config), `records.data` of the lookup-target and target models (existing values for matching + duplicate-skip), uploaded files in the `wassel-migrations` Storage bucket.
- **Writes:** `records.data` (JSONB) for the imported rows + any auto-created lookup records, via `record_save`; the target model's `schema` when the user approves new options; the `data_migration` record's own `data` at every step (resume state); uploaded files to `wassel-migrations` (`<auth.uid()>/<recordId>/uploads/...`).
- **AI:** `POST /api/migrate` (`action: extract | suggest_mappings | standardize | discuss`). **extract** is model-aware (gets the target fields as a hunt-list) + vision (deep floor-plan analysis, combined with text) + returns a `summary`; with `mode: 'project'` (All Projects target) it additionally returns the `intelligence` sections + the Arabic knowledge `document`. **discuss** is the multi-turn chat (re-reads the brochure to recount/revise). Vision model for extract/discuss-with-files, text model for mapping/standardize. No data is persisted server-side; the endpoint fetches signed-URL files and returns structured JSON. Accepted file kinds: images, PDFs, and **CSV/plain text** (Excel sheets arrive as client-converted CSVs) — text is inlined as fenced blocks, char-capped (120k/file, 300k total) with line-boundary truncation reported via `truncated`/`files_skipped`.
- **Project-profile writes:** the live `all_projects` model gained a `marketing_document` textarea (Project Content section, added 2026-06-11 via SQL on the unfrozen model; `v_all_projects` regenerated automatically). `buildMigrationPlan` injects `data.project_document` into each built record's `marketing_document` (explicit column mapping wins), so preview = exactly what's written.
- **Bulk RPC available (not yet wired into the live path):** `records_bulk_save` + `record_reserve_auto_ids` exist as a future fast-path for very large imports.

## Key files

| File | What it does |
|---|---|
| `src/pages/DataMigration/DataMigrationPage.tsx` | Split-pane shell: past migrations list + active wizard |
| `src/pages/DataMigration/components/MigrationWizard.tsx` | Step machine; reads/writes wizard state on the record |
| `src/pages/DataMigration/components/steps/*` | One file per step (pick model, upload, review, mapping, standardize, migrating, done) |
| `src/pages/DataMigration/components/EditableRawGrid.tsx` | In-app editable raw table |
| `src/pages/DataMigration/components/ValueStandardizationColumn.tsx` | Per-value before→after approval UI (the core) |
| `src/pages/DataMigration/components/steps/StepReviewRaw.tsx` | Editable grid + extraction summary + the discussion chat |
| `src/pages/DataMigration/lib/jobRunner.ts` | Module-level job runner: extraction + import run here, detached from the wizard, concurrent per record; writes status/result/error onto the record |
| `src/pages/DataMigration/lib/client.ts` | Upload + signed-URL + `/api/migrate` callers (`extractRawTable`, `discussExtraction`, with timeouts) |
| `src/pages/DataMigration/lib/applyStandardization.ts` | Rewrites raw cells to canonical values + routes values to other fields |
| `src/pages/DataMigration/lib/runMigration.ts` | `buildMigrationPlan` (pure: apply → `mapImportedRows` → dup-skip, records tagged with source row) + `runMigration` (saves approved records) |
| `src/pages/DataMigration/components/steps/StepPreview.tsx` | Final per-record approval table (built from `buildMigrationPlan`) |
| `src/pages/DataMigration/lib/previewRecords.ts` | Resolve stored values → display labels for the preview table |
| `src/pages/DataMigration/lib/{types,targetFields,buildStandardization}.ts` | Shared types + field/option helpers + AI→decision bridge |
| `api/migrate.ts` + `api/_lib/migrateAgent.ts` | The AI endpoint + Anthropic tool-use logic |
| `src/lib/excelUtils.ts` | `readExcelFile`, `exportRawTable`, `mapImportedRows` (reused) |
| `src/data/seedModels.ts` | `data_migration` system model seed |
| `supabase/migrations/2026-06-01_*.sql` | `wassel-migrations` bucket + RLS; `records_bulk_save` / `record_reserve_auto_ids` RPCs |

## Open questions / known limitations

- **Model-aware extraction verified live (2026-06-03):** `runExtract` was run against the real ريّا النخيل brochure (54 pages, 39 unit models) — it produced model-aware columns, bare numbers, and **plan-derived bathroom counts that combine the plan with the text** (A1 → 3 = "2 دورة مياة" + master en-suite, exactly matching the source), plus an Arabic summary explaining each number's source. `runDiscuss` answered a sourced "how many bathrooms?" question correctly. Mapping/standardization quality still benefit from in-app spot-checks (MFA-gated); the non-AI paths (clean-Excel entry, grid editing, mapping adjustment, value approval, route-to-field, migrate) are fully verified.
- **Extraction size:** bounded by Anthropic per-request limits (images ≤5MB, capped file count) and the model's output budget (≈a few hundred rows). For very large datasets use the clean-Excel path; oversized inputs surface a `truncated` notice rather than failing silently.
- **Bulk insert path:** the live migrate uses `saveRecord` per row (correct + proven, slower at scale). `records_bulk_save` is built and DB-verified but not yet wired in — a clean follow-up to speed up multi-thousand-row imports.
- **Routed values aren't re-standardized:** a value moved to another field is stored as-is; if that target field is itself a dropdown, the value may need manual fixing afterward.
- **Auto-created lookup records aren't pruned for excluded rows:** if you deselect a record in the preview, any *new* linked record (developer/project) its row would have created is still created. The preview surfaces the new-linked-record count so it's visible.
- **One target model per migration:** units link to projects via the normal lookup auto-match (a "project name" column resolves/creates the project) — there is no combined parent+child wizard.
