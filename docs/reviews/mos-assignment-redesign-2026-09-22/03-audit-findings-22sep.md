

# AREA state-22sep

## [CONFIRMED] 23 actionable open tasks; composition shifted since 21 Sep as مريم's writing drained into حسام's review queue
CLAIM: There are 83 rows with closed_at IS NULL, but only 23 are status='open' (the actionable set, matching the 21 Sep baseline of 23). Composition changed: writing waiting-on-capacity fell 14→4, 7 new writing_review tasks appeared on حسام, and سارة's open design grew 4→8.
MECH: مريم completed 7 of her 10 writing tasks during 21 Sep; each submit ran workflow_advance_role_path → inserted a writing_review task for حسام and consumed the pre-booked writing_review reservation. That moved load from 'writing/capacity' into 'writing_review'.
IMPL: The correction must key 'open work' off status='open', NOT closed_at IS NULL — the latter over-counts by 60 stale skipped rows. حسام's writing_review queue (7, uncapped) is the current live front.
EVID: workflow_role_tasks: status='open' & closed_at IS NULL = 23 (breakdown: design سارة 8 [7 on mos_content + 1 row 5abd0ded], design ريان 1 [phantom], writing NULL/capacity 4, writing مريم 3, writing_review حسام 7). Full per-row detail in table 'open_tasks_23'. The other 60 closed_at-null rows are status='skipped' (table 'skipped_unclosed').

## [CONFIRMED] The 21 Sep 14:00 sweep handed مريم exactly her full daily writing cap (10)
CLAIM: At 2026-09-21 14:00 Riyadh the sweep dispatched exactly 10 writing tasks to مريم (her post cap = 10); 7 are now closed→submitted and 3 remain open. A later 19:40 sweep dispatched 3 more design tasks to سارة; the 7 writing_review handoffs to حسام arrived 14:54–22:34 via advance.
MECH: mos_dispatch_sweep dispatched the whole ready batch up to capacity in one 14:00 tick; due_at = mos_work_due_at(clock, allowance) = assigned+24h (writing) / +12h (review), Friday-skipping.
IMPL: Due_at is computed from dispatch clock, so an early-dispatched task gets a due earlier than its plan date (see 'due_at ignores plan date' finding). No false lateness yet — late_flag is False on every open row.
EVID: 7 writing tasks closed by مريم 09-21 14:54–22:34, all assigned_at 09-21 14:00, result submitted (table 'closed_since_21'); 3 writing tasks still open on مريم assigned 09-21 14:00 (P-479/480/487). 10 total = cap. Design to سارة: P-472/475/476 assigned 09-21 19:40 due 09-22 19:40; row design 5abd0ded assigned 09-21 18:50 due 09-22 18:50. writing_review handouts + due_at in table 'handouts_since_1350'.

## [CONFIRMED] mos_plan_repair fired at the 22 Sep 00:00 boundary and re-dated 13 review reservations from 09-21 to 09-22
CLAIM: Exactly 13 reserved rows were re-dated at 2026-09-22 00:00:00 Riyadh: 7 design_review (weight 9, حسام) + 6 design_writer_review (weight 6, مريم), all moved from original planned_start/end 2026-09-21 to 2026-09-22. The weight-3 row-based design_review (80865042) was expanded to 09-22→09-24.
MECH: Cron /api/cron/planning-sweep → mos_plan_start_due then mos_plan_repair: repair stales unconsumed reserved rows with planned_end<today then re-dates them to start today (capacity-blind, no history row).
IMPL: These 13 review steps are now bunched onto 09-22 but are gated on their upstream design (open on سارة) completing, so re-dating created no capacity pressure (reviews are uncapped) — but it destroyed the original date in-place. The correction should re-date from the recovered original, not the current DB value.
EVID: 20 reservations have updated_at > 21 Sep 12:00: 7 consumed writing_review (updated 09-21 as they bound to حسام's tasks) + 13 reserved all updated_at exactly 09-22 00:00:00 Riyadh (table 'redated_13'). Post-repair there are 0 stale and 0 reserved rows with planned_start < 09-22.

## [CONFIRMED] Every original booked date is recoverable EXACTLY from the stored plan JSON; stage_deadlines are bounds, not booked dates
CLAIM: mos_campaign_plans.plan->'reservations' is a complete, durable copy of all 520 original bookings with plannedStart/plannedEnd per reservation, keyed by itemKey(=content_key)/stepKey/cycleKey/rowKey. All 13 re-dated rows and all 520 reservations are recoverable exactly. mos_content_plan.stage_deadlines are NOT booked dates.
MECH: The compiler wrote the reservations array into the approved plan at confirm (09-20 13:54); it is never mutated by dispatch or repair, unlike the live mos_task_reservations rows.
IMPL: Recovery source ranking: (b) plan JSON = exact & complete for ALL rows [use this]; (a) task.scheduled_start = exact but only for the 46 consumed rows; (c) stage_deadlines = bounds only, must NOT be used; (d) no dedicated audit/history table exists (mos_task_reservations has only updated_at).
EVID: Plan reservation counts equal DB counts per plan (115=115, 135=135, 135=135, 135=135 → 520=520). Matching the 13 re-dated rows against the plan JSON yielded 1 exact match each, all original 2026-09-21/2026-09-21 (table 'recovery_matrix'). Sample stage_deadlines collapsed all 5 stages to one date = row's required_ready_at = production_start (09-23), which cannot be the backward-scheduled per-stage booked dates. Consumed reservations also carry their date onto workflow_role_tasks.scheduled_start/end.

## [CONFIRMED] All 6 launch creatives are still in design on سارة — none reached design_review or approval on launch day
CLAIM: The six 22 Sep launch creatives (P-471…P-476) all have writing done+approved but design is open (round 1) on سارة; none progressed to design_writer_review, design_review, or final approval. Only P-474 has an approval_asset_id set.
MECH: Single designer (سارة, cap 7) received the design batch late (19:31–19:40 on 21 Sep) with due 09-22; the design step had not completed by the 08:12 read on 22 Sep.
IMPL: Launch-day creatives are unfinished. Any publication-risk flag must distinguish this real production risk from routine dispatch timing; the correction should not mark these late against a plan target if the handoff to سارة was itself late.
EVID: For P-471..476: writing r1 done/submitted (09-20 18:42–09-21 06:07), writing_review r1 done/approved (09-20 19:31–09-21 08:31), design r1 open (table 'launch_creatives'). The 6 'initial' creative slots (activate_on 09-22) are all status='reserved' (unactivated). No on_hold/rejected/archived.

## [CONTESTED] Meta launch shells are live (3 running executions, 6 active ad sets) but carry zero ads; no publications are scheduled at all
CLAIM: The 22 Sep launch exists at the Meta layer as 3 running executions (each with a platform_campaign_id) and 6 active ad sets (each with a platform_adset_id), but 0 execution_ads and 0 platform_ad_ids. No publication row anywhere has a scheduled_at, so nothing is booked for 22 Sep 18:00.
MECH: Ad shells/ad sets were created and set running on Meta ahead of creative completion; ads are only created once creatives are produced (still in design). Organic publications are 'planned' placeholders without a booked time.
IMPL: 'running execution' overstates readiness — the campaigns burn no impressions with 0 ads, but a naive status read would call the launch live. Publication timing (18:00 organic) is entirely un-booked.
EVID: mos_campaign_executions: 3 running meta (starts 09-22, pcid present), 6 paused meta, 1 draft meta, 1 draft instagram. Running execs: adsets 2 each / ads 0 each / ads_with_platform_id 0. mos_ad_sets on running execs: 6 active, all with platform_adset_id. mos_publications: 138 planned + 10 draft, ALL scheduled_at NULL; the 22 Sep window returns 0 rows (table 'launch_meta').
  * refuted=True (high): The claim's factual evidence is all verified true, but its conclusion ("no publications are scheduled at all" / "nothing is booked for 22 Sep 18:00") does not follow — it rests on reading the wrong column, so the claim as titled/concluded is refuted.

VERIFIED TRUE (re-ran read-only SQL):
- mos_campaign_executions by status/platform: 3 running meta (starts_on 2026-09-22, all 3 with platform_campaign_id), 6 paused meta (all with pcid), 1 draft meta, 1 draft instagram. Matches exactly.
- Each running meta exec has 2 ad sets and 0 rows in mos_execution_ads: exec 0b2fdda7 pcid 120253540521700020, 
    CORRECTION: The factual layer is correct: 3 running meta executions (each with platform_campaign_id) and 6 active ad sets (each with platform_adset_id) but 0 execution_ads / 0 platform_ad_ids, and every mos_publications row (138 planned + 10 draft) has scheduled_at NULL. The empty ad shells are real and plausibly explained by creatives still being in design.

But 'no publications are scheduled at all / nothing is booked for 22 Sep 18:00' is wrong. In this pipeline scheduled_at is written only when a row is handed to the publisher (publishRelease.ts:284-285); the plan's booked publish time lives in planned_at, which is the field the system reads as effective (monthActions.ts:1057). All 138 planned rows have planned_at set, the earliest being 2026-09-22T15:00Z = 18:00 Riyadh. Six instagram (organic) publications are booked for 22 Sep 18:00-18:10 under the draft instagram execution 1bc0f56b, and their 
  * refuted=False (high): Reproduced every quantitative sub-claim against the live DB. mos_campaign_executions GROUP BY status,platform: 3 running meta (all with platform_campaign_id, all starts_on=2026-09-22), 6 paused meta, 1 draft meta, 1 draft instagram — exact match. Per running exec (0b2fdda7, 100f97f9, f9b4482c): 2 ad sets each / 0 ads each; all 6 ad sets status='active' with platform_adset_id; 0 platform_ad_id — exact match. mos_publications: 148 total = 138 planned + 10 draft, count(scheduled_at)=0, count(published_at)=0; the 22 Sep window by scheduled_at returns 0 rows — exact match. The mechanism (ad shells/
    CORRECTION: The Meta layer of the 22 Sep launch is live but ad-less, and no publication is BOOKED — confirmed. Corrected/qualified: (a) the launch is not plan-less — 6 instagram publications carry planned_at=2026-09-22 18:00 Riyadh and a publish batch exists for that day (IG status 'late', 3 meta 'planned'); what is missing is a booked scheduled_at (the field the publisher acts on), so the accurate statement is 'planned target present, not yet booked into scheduled_at', not 'nothing scheduled at all'. (b) '0 execution_ads/0 platform_ad_ids' applies to the 3 RUNNING launch execs only; overall there are 41 execution_ads (31 with platform_ad_id), all on PAUSED executions. (c) There is no 'launch_meta' table; the launch is represented in mos_publish_batches/mos_publications.

## [CONFIRMED] Phantom a588fc62 persists: an open design task on archived content, 16 days overdue, no reservation
CLAIM: Task a588fc62 (P-135, design, round 2, assignee ريان) is still status='open', blocked=False, due_at 2026-09-06 17:00, with no reservation and no scheduled_start/end. Its content P-135 was archived 2026-09-16 10:00.
MECH: Pre-plan orphan (assigned 09-05, before the 09-20 confirm) on content later archived; nothing closes an open task when its content is archived, and it has no reservation so the plan machinery ignores it.
IMPL: It is unchanged since the 21 Sep read. It counts as an open task and as design load on ريان indefinitely; the correction should close/void tasks whose subject is archived.
EVID: workflow_role_tasks a588fc62: step design, round 2, status open, blocked False, assigned 09-05 23:30, due 09-06, reservation_id NULL, scheduled dates NULL, units 1. mos_content P-135 archived_at 09-16 10:00, on_hold False (table 'open_tasks_23').

## [CONFIRMED] The 09-21 design day was overbooked to weight 9 against سارة's cap 7 and is now fully consumed
CLAIM: Seven design reservations for planned_start 2026-09-21 total weight 9 (6 creatives ×1 + 1 row ×3), all assigned to سارة whose design cap is 7 — a 2-over overbooking written at confirm. All 7 are now status='consumed'.
MECH: The compiler booked 9 weight onto one 7-cap day at confirm; why the commit's capacity re-check admitted it remains untraced (out of scope for this read).
IMPL: Confirms the reported overbooking materialized (all consumed → dispatched). The correction's in-transaction capacity guard must count tasks + consumed reservations exactly once per occupied day to prevent a repeat.
EVID: mos_task_reservations design/planned_start=09-21/سارة: 7 rows, sum(weight)=9, all consumed. Design cap = mos_step_rules design daily_limit 7 = mos_user_capacity سارة post 7.

## [CONFIRMED] R1 date inversion still present: no paired item has design booked after its writing
CLAIM: Of 104 content items that have both a writing and a design reservation, 0 have design planned strictly after writing; 35 have design strictly BEFORE writing (inversion) and 69 have design on the SAME day as writing.
MECH: Consistent with the known schedule.ts:263-272 effectiveDeadline max()-instead-of-min() bug, which collapses design's deadline to ≤ writing's, so design is never scheduled after the writing it depends on.
IMPL: Since design depends on writing→writing_review, even the 69 same-day pairs are effectively un-executable as booked; all 104 paired items are mis-ordered. The correction must re-derive design dates as writing_end + review + allowance, not reuse the booked date.
EVID: Aggregating min(planned_start) per content_key: inversions (design<writing)=35, same_day=69, correct(design>writing)=0, total_paired=104.

## [CONTESTED] Single writer + single designer; no leaves or holidays; load has room most days, design at cap 3 days/week
CLAIM: All writing is booked to مريم (cap 10/day) and all design to سارة (cap 7/day). There are 0 active/upcoming leaves, no upcoming holidays (only Friday off), and opt-outs exist only for the admin. Booked weight sits well under cap except مريم on 09-28/10-05/10-12 (=10) and سارة on the 3rd–5th working day of each week (=7).
MECH: Reservations pre-pick a single assignee per role; there is no second writer or designer to spread to.
IMPL: The bottleneck is structural (one person per role). The correction has slack on most days but cannot add design on the 3 at-cap days/week without spilling to the next day; Fridays (09-25, 10-02, 10-09) are correctly skipped in all bookings.
EVID: mos_user_capacity post: مريم 10, سارة 7, ريان 4. mos_step_rules: writing 24h/cap10, design 24h/cap7, three reviews 12h uncapped. mos_leaves: 0 rows ending ≥ today. mos_holidays: none ≥ today. mos_auto_assign_opt_out: 2 rows, both admin (writer, montage). Per-day matrix in table 'load_matrix'.
  * refuted=True (high): The mechanism and most facts are correct, but a result in the load sub-claim was misread, which per the task's rule triggers refutation. Verified live: writing has one assignee (مريم 3eea9a65, 104 rows), design one assignee (سارة faaad82b, 104 rows); reviews are also single-assignee — so "single writer + single designer, nothing to spread to" is established. mos_user_capacity post = مريم 10 / سارة 7 / ريان c5f60676 4; mos_step_rules = writing 24h cap10, design 24h cap7, three reviews 12h uncapped; mos_leaves 0 rows; mos_holidays 0 rows; mos_auto_assign_opt_out = 2 admin rows (montage+writer). 
    CORRECTION: Corrected: All writing is booked to a single writer (مريم 3eea9a65, cap 10) and all design to a single designer (سارة faaad82b, cap 7); reviews are single-assignee too. There are 0 leaves and 0 holidays (Friday-only off), and opt-outs are the two admin rows. Booked writing weight reaches cap (=10) on FIVE days — 09-28, 10-05, 10-12, 10-19, 10-26 (not three) — and design reaches cap (=7) on three consecutive days per week (Sat/Sun/Mon of a Sat-start Fri-off week). Crucially, load is NOT uniformly at-or-under cap: 2026-09-21 design is over cap (wsum 9 > 7, the confirm-day overbooking) and writing that day is 9/10. The mechanism (pre-picked single assignee per role, no second writer/designer) is correct.
  * refuted=False (high): Core claim reproduced from the live DB. (1) All 520 reservations = 104 subjects x 5 steps; each step has exactly ONE assignee (mos_task_reservations grouped by step_key/assignee: writing -> مريم 3eea9a65 x104; design -> سارة faaad82b x104). So all writing is booked to مريم and all design to سارة, with no second writer/designer to spread to. (2) mos_leaves is EMPTY (0 rows total), mos_holidays is EMPTY (0 rows total) -> 0 active/upcoming leaves and no upcoming holidays; only Friday off is correct (mos_weekend_days=[5], and no planned_start falls on a Friday). (3) mos_auto_assign_opt_out has exa
    CORRECTION: Corrected claim: All writing is pre-booked to مريم (post cap 10) and all design to سارة (post cap 7); there are 0 leaves (table empty), 0 holidays (table empty; only Friday off), and opt-outs exist only for the admin (writer + montage). Booked weight is well under cap on single-subject days (weight 3). At-cap days: مريم reaches cap (weight 10) on FIVE Mondays — 09-28, 10-05, 10-12, 10-19, 10-26 — with Sundays at 8; سارة reaches cap (weight 7) on the FIRST THREE working days of each week (Saturday, Sunday, Monday), i.e. 3 days/week — not the '3rd–5th working day'. One design day, 2026-09-21 (Mon), is booked at weight 9, exceeding the cap of 7 (the known confirm-day overbooking). The '3 days/week at cap' count in the title is correct; the ordinal-position and the '=7 peak' phrasing are the parts to fix.

## [INFERRED] due_at is computed from the dispatch clock, not the plan date, so early-offered work gets an earlier deadline than its plan target
CLAIM: Tasks dispatched early (e.g. مريم's writing scheduled_start 09-27/09-28 but assigned 09-21) receive due_at = dispatch_time + step allowance (09-22), which is earlier than the plan's target date for that content.
MECH: mos_task_dispatch sets due_at = mos_work_due_at(clock_timestamp(), allowance); the reservation's planned date is copied to scheduled_start/end but not used for due_at.
IMPL: Matches the operator requirement 'starting work early must not change its planned deadline' being at risk: the dispatch-clock due_at diverges below the plan target. No false lateness has fired yet, but a slow early-offered task could be flagged late against a deadline earlier than its own plan date. The correction should base the deadline on planned handoff + allowance, and track publication risk separately from the dispatch due.
EVID: Open writing on مريم: scheduled_start 09-27/09-28 yet assigned 09-21 14:00 with due 09-22 14:00. writing_review on حسام: scheduled_start 09-27/09-28, due 09-22 (assigned+12h). All late_flag=False (completed within the dispatch allowance).

OPEN: Why did mos_campaign_plan_commit's in-transaction capacity re-check admit the 09-21 design overbooking (weight 9 on سارة against cap 7)? Out of scope for this read (task marks it UNTRACED); the overbooking is confirmed present and now fully consumed. | Is the row-based design_review re-span (80865042: single day 09-21 → 3-day 09-22→09-24) intended by mos_plan_repair, or a side effect of re-dating a weight-3 row? The original was a single day; the repair produced a multi-day span. | Should tasks whose subject content is archived (phantom a588fc62 on P-135, archived 09-16) be auto-voided? It remains open with a 09-06 due and counts as ريان's design load indefinitely. | The 4 writing tasks waiting on 'capacity' (P-488–491, planned_start 09-27) are future work offered early but blocked because مريم (sole writer) is at her 24h cap — confirm this is the intended 'optional early work, one future day at a time' behavior and not a stuck state. | Are the 60 legacy August skipped-but-unclosed rows safe to close/backfill closed_at, or do any downstream readers rely on them? They currently pollute the closed_at-IS-NULL 'open' set.

PLAIN: Live re-read on 2026-09-22 08:12 Riyadh. The scheduling machine is running and internally consistent (520 reservations, 0 stale, 0 duplicates, all consumed rows correctly linked to tasks), but the 22 Sep launch is not actually ready.\n\nWhat happened since 21 Sep: yesterday at 2 pm the system handed مريم (the only writer) her full daily load of 10 writing tasks at once. She finished 7; each finished piece went straight to حسام for review, so his review queue now holds 7 items. سارة (the only designer) was handed the 6 launch designs late in the evening (7:40 pm) and has not finished any of them. At midnight the nightly repair job moved 13 review reservations that were dated 21 Sep forward to 22 Sep because their day had passed.\n\nThe launch itself: on Meta, 3 campaign shells are 'running' with 6 active ad sets, but there are ZERO actual ads because the 6 creatives (P-471 to P-476) are all still sitting in design on سارة — none has been reviewed or approved. And no organic post is scheduled for 22 Sep 6 pm (in fact none of the 148 publications has a real scheduled time yet). So the launch looks live in the status columns but would put nothing in front of a customer today.\n\nGood news for fixing it: every original planned date is fully and exactly recoverable. The approved plan stores a complete, never-touched copy of all 520 bookings with their dates (the counts match the live table exactly). The midnight repair overwrote the live dates in place, but the plan copy still shows the 13 moved rows were originally 21 Sep. Do NOT use stage_deadlines to recover dates — those are just each item's ready-by day repeated across all five steps, not the real staggered dates.\n\nThree standing problems the correction must handle: (1) design is never scheduled after the writing it depends on — of 104 items with both, 35 have design dated before writing and the other 69 on the same day, zero correctly after (the known max/min bug); (2) deadlines are set from the moment work is dispatched plus the allowance, not from the plan date, so work offered early gets a deadline earlier than its own plan target (no false 'late' has fired yet, but the risk is built in); (3) one old task (a588fc62) is stuck open on content that was archived on 16 Sep, and 60 old August tasks were marked skipped but never closed, so any count of 'open work' that uses 'not closed' instead of 'status = open' over-counts badly. Capacity has room on most days (writer peaks at cap only 3 days, designer sits at cap 3 days a week), nobody is on leave, and there are no upcoming holidays — the constraint is simply that there is only one writer and one designer."


# AREA commit-guard

## [CONFIRMED] The overbook is real, singular, and split across four plans in one transaction
CLAIM: سارة (faaad82b…, montage) is booked to 9 post-units on 2026-09-21 against a cap of 7 — the ONLY over-cap (assignee,day,bucket) cell in the entire 520-reservation confirm. The 9 units are 1 row-design reservation (weight 3, row_id set) + 6 creative-design reservations (weight 1), all bucket='post', spread across FOUR distinct plan_ids (1 organic-row plan 1612301b + 3 paid plans a66cd25b/bada9d59/c9fc468a), all inserted in a single transaction.
MECH: mos_campaign_plan_commit_month (live body) loops jsonb_array_elements(p_plans) and calls mos_campaign_plan_commit once per plan; the month planner emits the launch month as 4 plans (one organic-row lane + one per paid execution), so a single subject-day's design load is distributed across separate plans, each committed by its own inner call.
IMPL: A per-plan capacity check can never see one plan in isolation exceed the cap here — no single plan books more than 3 of the 9. Any correct guard must evaluate the union of all plans' bookings (plus existing ledger load) per (assignee,day,bucket), which is why the fix belongs at the month level under the shared lock, not only inside the per-plan commit.
EVID: Live query: reservations for سارة on 2026-09-21 = {is_row=true,weight=3,n=1}+{is_row=false,weight=1,n=6}, all bucket=post, plan_ids {1612301b(row), c9fc468a(2), a66cd25b(2), bada9d59(2)}. Blast-radius scan (same-day-by-row rule, all assignees/days): exactly ONE row returned — sarah.a@wassel.re, 2026-09-21, post, load 9, cap 7. All 520 reservations share created_at=2026-09-20T10:54:00.081685+00 (distinct_created=1); created_at default is now() (constant per txn) ⇒ ONE transaction. mos_user_daily_slots(سارة,'post')=7 via mos_user_capacity(post,7).

## [CONTESTED] Exact capacity re-check and how it composes cross-plan
CLAIM: The re-check (live mos_campaign_plan_commit body, the WITH prop/spread/agg block, dump lines 104-139) computes 'proposed' from THIS plan's p_reservations, spread by mos_spread_effort_mode keyed on the payload's free-text spread field (default 'per_day'), and 'existing' from mos_work_ledger_v (which has NO plan filter, so it does see sibling plans' reservations already inserted earlier in the same transaction). It raises WS409 'capacity_conflict' only when existing+proposed > mos_user_daily_slots for a cell the current plan touches.
MECH: Because inner commits run sequentially in commit_month and each inserts its reservations at the END of its body (after its own check), plan N's `existing` picks up plans 1..N-1. So cross-plan accumulation IS covered by construction — provided each reservation's proposed contribution is counted the SAME way the ledger later charges it.
IMPL: The guard's correctness hinges on the proposed-side spread matching the ledger-side spread. The two use DIFFERENT criteria (payload 'spread' label vs structural row_id), so they can silently disagree — the exact fragility that produced this incident.
EVID: Quoted from live body: line 109 `COALESCE(NULLIF(x ->> 'spread', ''), 'per_day') AS spread`; lines 115-117 `mos_spread_effort_mode(GREATEST(prop.ps, mos_perf_today()), prop.total, prop.spread = 'same_day')`; lines 128-132 `(SELECT COALESCE(sum(l.weight),0) … FROM mos_work_ledger_v l WHERE l.user_id=a.uid AND l.day=a.day AND l.bucket=a.bucket) … WHERE mos_on_leave(...) OR (x.existing + a.proposed) > mos_user_daily_slots(a.uid,a.bucket)`. mos_work_ledger_v reservation arm: `FROM mos_task_reservations r CROSS JOIN LATERAL mos_spread_effort_mode(GREATEST(r.planned_start,today), r.weight, r.row_id IS NOT NULL) … WHERE r.status='reserved' AND r.planned_end>=today` — same-day is decided by r.row_id IS NOT NULL, and there is no plan_id filter.
  * refuted=True (high): Every FACTUAL sub-assertion in the claim is verified against the live bodies, but the claim's headline conclusion — "cross-plan accumulation IS covered by construction" — is falsified by a live counterexample, so the mechanism the claim asserts is NOT established.

WHAT IS CORRECT (verified):
- Section (c) of live mos_campaign_plan_commit computes 'proposed' from THIS call's p_reservations, spread by mos_spread_effort_mode keyed on COALESCE(NULLIF(x->>'spread',''),'per_day')='same_day'; 'existing' from mos_work_ledger_v with NO plan_id filter; raises WS409 'capacity_conflict' for cells in this
    CORRECTION: Corrected claim: The commit-guard's plumbing is exactly as described and the spread proviso is genuinely met (plan.ts:640 ties spread='same_day' to rowKey, which becomes row_id), so proposal and ledger use the same discriminator, bucket, weight and day. mos_campaign_plan_commit_month does run each plan's section-(c) check sequentially in one locked transaction with NULL inner hash, and 'existing' (no plan filter) does pick up earlier siblings' just-inserted reservations. HOWEVER, 'cross-plan accumulation IS covered by construction' is NOT established and is refuted by live data: a single commit_month wrote weight 9 into (سارة, 2026-09-21, post) across four plans against cap 7 without raising capacity_conflict. Because the plumbing and the proviso both hold, the last plan touching that cell was mathematically forced to see existing+proposed=9>7, so the check must have under-counted for a 
  * refuted=False (high): CONFIRMED by reproduction against the live bodies (pg_get_functiondef / pg_get_viewdef, 2026-09-22).

1) The re-check computes 'proposed' from THIS plan's p_reservations, spread by mos_spread_effort_mode keyed on the payload's free-text spread (default 'per_day'). Live mos_campaign_plan_commit(uuid,jsonb,text,jsonb,uuid) body: line 104 `WITH prop AS (SELECT (x->>'assignee_user_id')::uuid AS uid ... FROM jsonb_array_elements(COALESCE(p_reservations,'[]'::jsonb)) x`; line 109 `COALESCE(NULLIF(x->>'spread',''),'per_day') AS spread`; lines 115-117 `mos_spread_effort_mode(GREATEST(prop.ps, mos_perf
    CORRECTION: The claim is correct as stated; only add for completeness: mos_work_ledger_v has TWO plan-blind reservation arms (fresh reserved+future, lines 23-31; and stale/expired-reserved re-projected onto today, lines 33-41), not one — both feed 'existing'. And the proviso is not merely assumed: it is actively maintained in code (actions.ts:399-400 sets spread='same_day' iff rowKey is set, matching the ledger's row_id-based same-day keying), so cross-plan accumulation is covered by construction AND counted consistently in the present state.

## [CONTESTED] Root cause: the row was counted per_day in the check but same_day in reality — a 2-unit under-count that landed exactly on the cap
CLAIM: The check spread the weight-3 row reservation as per_day (1 slot on 09-21, the rest on 09-22/09-23) instead of same_day (all 3 on 09-21). That made the check's total for (سارة,09-21,post) = 6 creatives + 1 = 7, and `7 > 7` is FALSE, so it passed. The ledger and dispatcher charge the row same_day (row_id IS NOT NULL) → 6 + 3 = 9. The under-count is exactly weight−1 = 2, which is precisely the excess over the cap.
MECH: commit body line 109 defaults a missing/empty payload spread to 'per_day'; when the row's payload spread is not the literal 'same_day', the row is spread one-slot-per-working-day. Whichever of the 4 plans commits last then sees existing = 7 − own and proposed = own, summing to 7 — never > 7 — in every commit order. Under the correct same_day count the same last-commit arithmetic sums to 9 > 7 and would have raised WS409 regardless of order.
IMPL: Fix the operand, not the operator: derive same-day from the SAME fact the ledger uses. Replace `prop.spread = 'same_day'` with `NULLIF(x->>'row_key','') IS NOT NULL` (row_key is what becomes row_id), so the proposed side and the ledger can never diverge. Optionally reject a payload whose stated spread contradicts its row_key, to fail caller bugs loudly.
EVID: Reproduced against live rows: mos_spread_effort_mode('2026-09-21',3,false)=[{09-21:1},{09-22:1},{09-23:1}]; (…,3,true)=[{09-21:3}]. Per-plan 09-21 contribution — reality(same_day-by-row_id): {row 3, paid 2,2,2}=9; check-as-ran(per_day): {row 1, paid 2,2,2}=7. Totals: reality_total_sameday=9, check_asran_total_perday=7, cap=7. 09-21 is Monday (dow=1), not Friday (mos_weekend_days()=[5]), so the two displaced units fall on real working days 09-22/09-23.
  * refuted=True (high): TECHNICAL: The overbook itself is real and I confirmed it — سارة/09-21/post/design carries 6 paid creatives (content_key exec:meta:*, weight 1) plus 1 organic row (content_key 2026-09:w3:2026-09-22:b, plan 1612301b, weight 3, row_id set), all written at confirm (identical created_at 2026-09-20T10:54:00.081685Z), against mos_user_daily_slots(سارة,'post')=7. Same_day that is 6+3=9. I also confirmed line 109 (COALESCE(NULLIF(x->>'spread',''),'per_day')) and mos_spread_effort_mode outputs ([1,1,1] per_day vs [3] same_day) exactly as the colleague states. But the claim's MECHANISM as a root cause d
    CORRECTION: Corrected claim: The overbook is genuine — at confirm, mos_campaign_plan_commit_month materialised 6 paid weight-1 design creatives and 1 organic weight-3 design ROW on سارة/2026-09-21/post = 9 same_day against cap 7, with no WS409. The row IS charged same_day (weight 3) — both by the ledger (row_id-based) and by a spread-aware proposal — so "reality = 6+3 = 9" is right. What is wrong is the root-cause mechanism: (a) the commit's `existing` term same_day-counts the row via mos_work_ledger_v's `row_id IS NOT NULL` arm, INDEPENDENT of the payload `spread`, so the line-109 per_day default can only under-count the single plan that PROPOSES the row, not the plans that read it from the ledger; (b) the real commit order is organic-FIRST (monthCompiler `plans=[organicPlan,...paid]`), so by the time the three paid plans check, the row is already inserted (status reserved) and each paid plan's `ex
  * refuted=True (high): The claim is PARTLY right: its symptom and magnitude reproduce exactly, but its load-bearing causal MECHANISM (commit line 109 defaulting a missing/empty payload spread to per_day) is contradicted by the code and does not reproduce.

REPRODUCED. Live mos_spread_effort_mode('2026-09-21',3,false) returns [09-21:1, 09-22:1, 09-23:1] and (...,3,true) returns [09-21:3] — a weight-3 row differs by exactly 2 on 09-21 between the two modes. The real overbook is confirmed in live mos_task_reservations for سارة (faaad82b) bucket=post planned_start=2026-09-21: 6 paid creatives (weight 1 each, is_row=fals
    CORRECTION: CONFIRMED part: the divergence is real and its magnitude is exactly weight−1=2. If the commit's capacity re-check spreads the weight-3 row per_day (1 slot on 09-21) while the ledger/dispatcher charge it same_day (3 on 09-21, keyed on row_id IS NOT NULL), then the last-committing of the four plans sums to existing+proposed=7≤cap and passes, while reality is row(3)+paid(2+2+2)=9>7. That arithmetic and the overbook composition reproduce exactly against live data.

REFUTED part (the cited mechanism/root cause): line 109's per_day default was NOT the trigger. The planner STATES the spread — plan.ts:640 sets spread='same_day' for every row, actions.ts:401/reservationsPayload passes it through, and this shipped atomically with the month engine (b1715d1a), so the month confirm's payload carried spread='same_day'. With that payload, line 109 reads 'same_day', prop.spread='same_day' is TRUE, and t

## [CONTESTED] Candidate mechanisms adjudicated
CLAIM: Of the listed suspects, only the same-day under-count of a weight-3 row is operative. Ruled out: bucket mismatch (all 9 reservations are bucket='post'; cap for that bucket is 7); weight-vs-count (weights are summed, not counted); the OR-escape (the on_leave OR clause only ADDS reasons to flag, it cannot suppress a cap breach); planned_end>=today filtering (all planned_end=09-21 >= today 09-20, so all are visible); same-txn view visibility (it works — and even WITH full visibility the per_day total is 7, so it passes anyway).
MECH: The per-plan split is a contributing condition, not the root cause: it guarantees no single plan's own proposed reaches the cap, so detection depends entirely on the cross-plan `existing` sum — which the per_day under-count deflates from 9 to 7.
IMPL: Do not chase bucket/visibility/OR-escape fixes. The single corrective is same-day parity between the guard and the ledger; the month-level union check is the robustness layer that removes the dependence on sequential cross-plan visibility.
EVID: Reservation grouping shows uniform bucket='post'. mos_work_ledger_v reservation arm filter is `status='reserved' AND planned_end>=today` — 09-21>=09-20 true. The WHERE is `on_leave OR over_cap`; سارة is not on leave (no flag added), so the pass is purely because over_cap evaluated false at 7. Single-txn established by identical created_at.
  * refuted=True (high): Read the live bodies of mos_campaign_plan_commit (capacity re-check block, lines 106-140), mos_work_ledger_v, mos_spread_effort_mode/_same_day/mos_spread_effort, mos_user_daily_slots, and mos_campaign_plan_commit_month. The claim pins the overbook on a per_day/same-day under-count of the weight-3 row in the ledger's reservation arm, deflating the cross-plan `existing` from 9 to 7. That is false on two counts. (1) The ledger reservation arm calls mos_spread_effort_mode(start, r.weight, r.row_id IS NOT NULL); the offending row (1b00ae3f, row_id NOT NULL, weight 3, 3 items) therefore takes same_d
    CORRECTION: The weight-3 row is NOT under-counted in `existing`: the ledger reservation arm keys same_day on `r.row_id IS NOT NULL`, so a row is charged its full weight (3) on its planned day, and the guard's proposed side likewise gets same_day=3 because the payload carries spread='same_day' for rows. There is no per_day under-count anywhere in the existing (or proposed) path for a row, so `existing` is not deflated 9→7. The claim is right that the per-plan split means no single plan's own proposed hits the cap, but wrong that detection then fails on a deflated cross-plan `existing`: because mos_campaign_plan_commit_month commits all four plans in one transaction, each later inner guard's `existing` correctly sees the earlier siblings' reserved rows (row weighted 3), so the final sibling's re-check would compute existing>=7 + proposed>=2 = >=9 > cap 7 on سارة/post/2026-09-21 and raise capacity_conf
  * refuted=True (high): Reproduction lens against live function bodies (pg_get_functiondef) and two empirical checks. The colleague's ruled-out suspects hold: all reservations on سارة/09-21 are bucket='post' (verified), cap 7 (mos_user_daily_slots=7), on_leave=false, re-check sums weights, and the WHERE `mos_on_leave(a.uid,a.day) OR (x.existing + a.proposed) > mos_user_daily_slots(...)` (commit body lines 131-132) means the OR only adds flag reasons. planned_end>=today visibility holds (09-21>=09-20). BUT the positively-identified operative mechanism — a 'per_day under-count of a weight-3 row' deflating existing 9→7 
    CORRECTION: Refuted on the operative mechanism, partly right on eliminations. Corrected: (a) The cell sums to the full 9 in the ledger, not 7 — mos_spread_effort_same_day returns GREATEST(effort,0), so the weight-3 row is charged 3 on 09-21, and row_id-NOT-NULL routes it to the same_day path. There is NO per_day under-count. (b) The colleague appears to have assumed the row was spread [1,1,1] per_day (only 1 landing on 09-21); it is not — row reservations use same_day=true. (c) The per-plan split (4 plan_ids, one txn) is real and is the only genuine structural condition, but it does NOT by itself cause the bypass: commit_month runs the per-plan re-checks sequentially with same-txn visibility, so the crossing plan sees existing=6 or 7 and existing+proposed=9>7 in every ordering → the guard should have raised WS409. (d) The eliminations (bucket, weight-vs-count, OR-escape, planned_end visibility, same

## [INFERRED] The live commit function IS the function that ran the 09-20 confirm; the payload — not the DB code — carried the non-same_day row
CLAIM: The latest migration defining mos_campaign_plan_commit is supabase/migrations/2026-09-15_23_reservation_cycle_bind.sql (git-committed 2026-09-16); no migration after that redefines the commit fn or its capacity check. So the pg_get_functiondef body I read is the same code that ran the 2026-09-20 13:54 confirm — and it already has the B4 spread plumbing. The current planner (plan.ts:640 `spread: rowKey ? 'same_day':'per_day'`, actions.ts:401 `spread: r.spread`, row_key at actions.ts:371) emits same_day for rows. Empirically the 09-20 payload's row must have read as non-same_day, since that is the only way 9>7 survives one transaction with cross-plan visibility.
MECH: Most likely the deployed API build that produced the confirm payload lagged the DB migration (DB ahead of Vercel), or a payload-serialization path omitted the spread field for the row lane, so the field arrived absent/empty and line 109 defaulted it to per_day. This cannot be distinguished read-only because the historical p_reservations JSON is not persisted (mos_task_reservations has no spread column).
IMPL: Because the guard cannot be relied on to receive a correct label from every caller and every deploy generation, it must not trust the label at all — deriving same-day from row_key makes the guard self-defending against exactly this deploy-skew / serialization-drop class.
EVID: grep of supabase/migrations for the commit fn returns only 2026-09-14_02, 2026-09-15_22, 2026-09-15_23 (none later). B4 git commits dbb76474/b1715d1a dated 2026-09-16, before the 09-20 confirm. Live body honors prop.spread='same_day' and defaults absent to per_day (line 109). Reproduction proves per_day at check time.

## [CONFIRMED] No overlap with assert_engine_conformance.sql or mos_task_rules_audit R2 — a new commit-guard fixture is needed
CLAIM: assert_engine_conformance.sql checks only STATIC config agreement (design limit == mos_role_load == mos_user_capacity, and mos_task_units row-cost); it never calls the commit RPC and cannot catch a dynamic per-day overbook. mos_task_rules_audit R2_capacity is a POST-HOC audit over dispatched workflow_role_tasks using mos_capacity_used (rolling 24h ending at assigned_at, keyed on capacity_key), and it currently returns 0 for this incident because the dispatcher throttled actual hand-out to <=7/24h even though the plan promised 9 on the calendar day. Neither overlaps a fixture that exercises the commit-time guard.
MECH: R2 measures what the dispatcher DID (24h rolling, units received, single-task carve-out via `> COALESCE(t.units,1)`); the commit guard measures what the plan PROMISES (calendar-day reservation forecast vs mos_user_daily_slots). Different layer, different table, different cap surface — complementary, not redundant. R2=0 is itself proof the overbook is a planning-forecast defect, not a dispatch defect.
IMPL: Add a NEW transactional CI fixture (db-migrations job) that BEGINs, seeds a montage holder at cap 7, calls mos_campaign_plan_commit (and _month) with a crafted payload, asserts SQLSTATE WS409 'capacity_conflict', then ROLLBACKs. Cases: (a) single plan booking 6×w1 + 1 row w3 (row_key set) on one working day → WS409; (b) the real split shape — 4 plans each ≤ cap summing to 9 via commit_month → WS409 (the case both existing checks miss); (c) drift guard — a row reservation with spread='per_day' but row_key set must STILL raise WS409 (proves the guard ignores the label); (d) negative — 7 units on a 7-slot day passes. See the corrected-guard SQL skeleton table.
EVID: assert_engine_conformance.sql lines 26-99 are pure catalog/config assertions. Live mos_task_rules_audit R2 body: `FROM workflow_role_tasks t JOIN mos_step_rules sr ON sr.capacity_key IS NOT NULL WHERE mos_capacity_used(assignee, capacity_key, assigned_at) > sr.daily_limit AND > COALESCE(t.units,1)`. mos_capacity_used sums units in `assigned_at > p_at - 24h AND <= p_at`. Live audit now: R1_order=100, R2_capacity=0.

OPEN: Exact upstream trigger for the non-same_day row label at the 09-20 confirm cannot be determined read-only: mos_task_reservations stores no spread column and the historical p_reservations JSON is not persisted, so I cannot distinguish (a) a deployed API/planner build lagging the DB migration from (b) a payload-serialization path that dropped the spread field. The mechanism (per_day under-count) is reproduced regardless; only the specific cause of the missing label is uncertain. | Whether the 6-creatives-plus-1-row stacking on the single launch day (09-21) is itself caused by the known schedule.ts:263-272 effectiveDeadline max/min inversion (which compresses design onto the launch day). This is out of scope for the capacity-guard trace but is the reason 09-21 is the one day where the −2 under-count crossed the cap; worth confirming when that separate bug is fixed. | Whether the corrected month-union guard should also exclude reservations of superseded plans from `existing` on a re-confirm (re-preview→re-confirm of a still-'proposed' plan for the same campaign) — the current single-plan guard is masked by the status='approved' short-circuit, but the union guard needs an explicit exclusion or it will false-positive WS409 on legitimate re-plans. | The dispatcher's own gate uses `used+units<=daily_limit OR used=0` (the OR used=0 escape admits the first task of the day even if it is a large row); confirm the corrected commit guard plus that gate together never let a row's whole weight land on top of an already-full day — a separate check on the dispatch path, not the commit path.

PLAIN: What happened, in plain terms: when the marketing month was approved on 20 Sep, the plan promised the designer (سارة) NINE pieces of design work on Monday 21 Sep, but her limit is SEVEN a day. A safety check is supposed to block a plan that overloads anyone, and it should have blocked this — but it let 9 through. \n\nWhy it let it through: one of those nine jobs was a 'batch of 3 posts designed in one sitting' — it counts as 3 units all on the same day. The safety check, however, read that batch off a text label on the request that said (or defaulted to) 'spread it over several days' instead of 'all on one day'. So the check counted that batch as 1 unit on Monday instead of 3. 6 single jobs + 1 (mis-counted) batch = 7, and 7 is exactly the limit, so the check said 'fine'. But the real system charges the batch as 3, so the real load is 6 + 3 = 9. The two-unit gap is exactly what tipped it over. I reproduced this against the live data: the check's arithmetic comes to 7, reality comes to 9, the cap is 7.\n\nA second reason it slipped by: the nine jobs were split across FOUR separate plans (one organic batch + three ad executions), and each plan on its own was well under the limit. The check does add up work across plans, so that alone would not have hidden it — but combined with the batch being under-counted by 2, the running total capped out at exactly 7 and never crossed the line.\n\nThe fix: stop trusting the text label. Decide 'all-on-one-day' the same way the real system does — a batch is a batch because it IS a batch (it has a row/batch id), not because a label says so. That one change makes the check count 9 and block the plan. On top of that, do ONE combined capacity check over the whole month's four plans at approval time (not four separate checks that each look fine), so this kind of split can never hide an overload again.\n\nWhy the existing tests didn't catch it: one test only checks that the configured limits agree with each other (7 = 7) — it never simulates approving a real plan. The other (R2) is an after-the-fact audit of work already handed out, and it reads zero here because the dispatcher throttled the actual hand-out to 7/day — the overload only ever existed as a broken PROMISE in the plan, which is exactly what shows up later as false 'this is late / at risk of missing publication' flags. So neither test overlaps with what's needed: a new test that actually approves a 9-on-a-7-day plan and confirms it is rejected."


# AREA callchain-recursion-idempotency

## [CONFIRMED] mos_task_dispatch does BOTH admission and application in one function
CLAIM: mos_task_dispatch is not an 'apply' function: it embeds the entire admission policy (capacity gate + holder pick) and then applies the result (assignee, assigned_at, due_at, notify) in the same body. To make a refill process the sole decider, the SELECT ... INTO v_pick block must be removed from dispatch and moved into refill; only the final UPDATE + notify should remain — best as a new mos_task_assign_apply(task, user, notify).
MECH: Because admission lives inside dispatch, every caller that opens a task and calls dispatch is silently making an admission decision. The requirement 'One process makes assignment decisions (refill); the assignment function applies them without a conflicting admission policy' is violated at the function boundary itself.
IMPL: Split dispatch: (1) mos_task_assign_apply(p_task_id, p_user, p_notify) = only the SELECT ... FOR UPDATE guard + the UPDATE + notify + due-date/leave math, NO holder pick, NO capacity WHERE; (2) the holder pick + capacity accounting move into refill, which computes the whole batch's assignments counting capacity once, then calls apply per task. Keep: the subject-active / working-day / already-assigned guards and mos_work_due_at usage. Remove from the assignment path: the OR used=0 clause (see separate finding).
EVID: Live body of public.mos_task_dispatch(p_task_id uuid, p_notify boolean). The ADMISSION (capacity gate + holder pick) is one statement:
  SELECT h.uid INTO v_pick
    FROM public.mos_role_routine_holders(v_task.role_key) h(uid)
   WHERE v_rule.capacity_key IS NULL
      OR public.mos_capacity_used(h.uid, v_rule.capacity_key, v_now) + v_units <= v_rule.daily_limit
      OR public.mos_capacity_used(h.uid, v_rule.capacity_key, v_now) = 0
   ORDER BY (SELECT max(t.assigned_at) FROM public.workflow_role_tasks t
              WHERE t.assignee_user_id = h.uid AND t.role_key = v_task.role_key) ASC NULLS FIRST,
            h.uid
   LIMIT 1;
The WHERE clause is the capacity gate; the ORDER BY (least-recently-assigned first, NULLS FIRST) + LIMIT 1 is the holder pick over the pool from public.mos_role_routine_holders (active users holding role mos_<role_key>, minus mos_auto_assign_opt_out). The APPLICATION is:
  v_due := public.mos_work_due_at(v_now, v_allow); [+ approved-leave extension]
  UPDATE public.workflow_role_tasks SET bucket=v_bucket, units=v_units, assignee_user_id=v_pick, assigned_at=v_now, due_at=v_due, waiting_since=NULL, waiting_reason=NULL WHERE id=p_task_id;
  IF p_notify THEN 

## [CONTESTED] Two independent capacity gates exist — the 'conflicting admission policy' the requirement forbids
CLAIM: There are two admission policies today: dispatch's per-holder WHERE and mos_role_has_room's existence check. mos_plan_start_due gates opening a subject on mos_role_has_room, then path_start→consume→dispatch re-decides with dispatch's own WHERE. These are separate code paths that can disagree, which is exactly the 'no conflicting admission policy' constraint.
MECH: mos_role_has_room asks 'does SOME holder have room?'; dispatch then picks a SPECIFIC holder by recency. The holder mos_role_has_room was thinking of may not be the one dispatch picks, and dispatch's used=0 escape can place a task the room-check would also have allowed but on a different person, so the two policies can diverge on WHO and on whether it lands at all. mos_dispatch_sweep is a THIRD entry into the same policy (it re-runs dispatch over every open unassigned task).
IMPL: Collapse all three (dispatch WHERE, mos_role_has_room, sweep) into one place: refill. mos_plan_start_due should call mos_task_open (create+bind, no admission) and let refill decide admission once; delete mos_role_has_room as a separate gate or reduce it to a pure helper refill calls internally.
EVID: mos_role_has_room body: SELECT EXISTS(SELECT 1 FROM mos_role_routine_holders(p_role_key) h LEFT JOIN mos_step_rules sr ON sr.step_key=p_step_key WHERE sr.capacity_key IS NULL OR mos_capacity_used(h.uid,sr.capacity_key,clock_timestamp())+p_units <= sr.daily_limit OR mos_capacity_used(...)=0). This is the SAME three-term predicate as dispatch, but as an ANY-holder existence test. mos_plan_start_due calls it as a pre-gate: IF NOT public.mos_role_has_room(v_first->>'role_key', v_first->>'key', public.mos_task_units(r.st,r.id)) THEN CONTINUE; and only then PERFORM workflow_role_path_start(...) which reaches dispatch.
  * refuted=True (high): I read all six live function bodies with pg_get_functiondef. The colleague's mechanism (the two gates are "independent" and "can disagree on WHO and on whether it lands") is not established; it is contradicted by the code.

1) SAME predicate. mos_role_has_room's WHERE and mos_task_dispatch's WHERE are the identical three-term test: `capacity_key IS NULL OR mos_capacity_used(h,key,t)+units <= daily_limit OR mos_capacity_used(h,key,t)=0`. Same rule source (mos_step_rules), same units (mos_task_units on the same subject), same holder set (both call mos_role_routine_holders(role) — the SAME functi
    CORRECTION: Corrected claim (the accurate residue): The admission predicate is DUPLICATED across two functions — an existence pre-check in mos_role_has_room and the selection WHERE in mos_task_dispatch — which is a single-source-of-truth / latent-drift risk and, if the requirement demands exactly one admission policy applied once, is worth collapsing (e.g., have refill/mos_role_has_room stop pre-filtering on capacity and let dispatch be the sole authority). But the two are NOT independent and do NOT conflict as written: they use the identical three-term predicate, the identical holder set (mos_role_routine_holders), the identical rule source (mos_step_rules) and capacity function (mos_capacity_used), evaluated microseconds apart under one advisory xact lock with no intervening change to any holder's assigned-task count. mos_role_has_room selects no holder, so there is no "WHO" divergence; and becaus
  * refuted=True (high): I reproduced the code structure exactly as the colleague described, but the specific defect claimed — a CONFLICTING admission policy where the two gates can DISAGREE on WHO or on whether a task lands — does not reproduce.

WHAT REPRODUCES (structure is real):
- mos_role_has_room (verified body) is an EXISTS over mos_role_routine_holders with predicate: capacity_key IS NULL OR mos_capacity_used(uid,key,clock)+units<=daily_limit OR mos_capacity_used(uid,key,clock)=0.
- mos_task_dispatch's holder pick (verified body) uses the byte-identical three-term predicate over the same mos_role_routine_hold
    CORRECTION: Reframe from 'two conflicting admission policies that can disagree on WHO/whether a task lands' to: 'one admission predicate is DUPLICATED — mos_role_has_room (an EXISTS pre-gate on the mos_plan_start_due start path) restates dispatch's own holder-selection WHERE verbatim.' It is redundant, not conflicting: identical predicate + identical holder set + within-transaction-consistent capacity counts mean the two always agree on admission, has_room names no holder so it cannot diverge on WHO, and each capped role currently has a single holder anyway. The real (weaker) issues are a DRY/drift hazard and that the requirement's 'assignment function applies them without a conflicting admission policy' is best met by deleting the has_room pre-gate and letting dispatch be the sole admission authority — but there is no present-day disagreement to reproduce.

## [INFERRED] The `OR used = 0` clause is the overbooking escape hatch (weight-9-on-cap-7)
CLAIM: A holder with zero capacity used in the rolling 24h is eligible for a task of ANY size, because the gate is `used+units<=limit OR used=0`. This is the most plausible mechanism for the untraced 09-21 design overbooking (weight 9 placed on سارة whose design cap is 7).
MECH: The used=0 clause exists so a single oversized item can never be permanently unschedulable (nobody would ever have room for a weight>limit task). But it also lets the FIRST task of the day exceed the cap without bound. At commit, mos_campaign_plan_commit consumes reservations → dispatch → the first design task of a fresh 24h window slips through used=0.
IMPL: In refill, replace `OR used=0` with an explicit oversize rule: admit a task with units>limit ONLY when it is the sole task and the holder is at 0, and cap the day at that one task; otherwise enforce used+units<=limit strictly. Never let used=0 admit an arbitrary-size batch when smaller executable tasks are competing for the same holder. I did not reproduce the exact commit-time sequence, so the 09-21 attribution is inferred, not traced.
EVID: In both mos_task_dispatch and mos_role_has_room the third disjunct is `public.mos_capacity_used(h.uid, v_rule.capacity_key, v_now) = 0`. mos_capacity_used sums COALESCE(t.units,1) over tasks assigned in the last 24h (assigned_at > now-24h). mos_step_rules: design cap 7 per the task context. If سارة's rolling-24h design units were 0 at the instant the weight-9 batch's reservation was consumed at commit, `0+9<=7` is false but `used=0` is true, so she is picked and jumps to 9.

## [CONTESTED] mos_ledger_lock is a reentrant advisory xact lock — it does NOT stop recursion within a transaction
CLAIM: mos_ledger_lock is pg_advisory_xact_lock, which is reentrant within one transaction: a nested call that re-acquires it succeeds immediately. It provides cross-session mutual exclusion but is useless as a recursion guard, so a refill→path_start→refill chain would re-enter freely.
MECH: Every scheduling RPC opens with PERFORM mos_ledger_lock(). Two different sessions serialize correctly. But within one call stack, the nested PERFORM is a no-op re-acquire, so nothing prevents a function that holds the lock from calling another that takes it again and does more assignment work.
IMPL: The re-entrancy guard must be separate from the lock: a transaction-local GUC (set_config('mos.in_refill','1',true) checked at refill entry) or a queued 'refill_again' flag drained once at the end of the outermost refill. Keep mos_ledger_lock for cross-session exclusion.
EVID: Full body: CREATE FUNCTION public.mos_ledger_lock() RETURNS void LANGUAGE sql SECURITY DEFINER AS $$ SELECT pg_advisory_xact_lock(hashtext('mos_work_ledger')); $$. Postgres semantics: a session already holding an advisory lock always re-acquires it; xact-level advisory locks release only at transaction end. A search of pg_proc for set_config/current_setting across mos_*/workflow_* functions returned only mos_caller_is_trusted_service — i.e. NO re-entrancy GUC exists anywhere in the scheduling code.
  * refuted=True (high): The claim has two separable parts. Part 1 (pure Postgres semantics) is CORRECT and verified: mos_ledger_lock's live body is `SELECT pg_advisory_xact_lock(hashtext('mos_work_ledger'))` — an xact-level advisory lock, which is reentrant within a transaction, so a holder that re-acquires it succeeds immediately. It is cross-session mutual exclusion, not a within-call recursion guard. I confirmed nested re-acquisition really occurs (mos_plan_start_due holds the lock and calls mos_dispatch_sweep and workflow_role_path_start, both of which re-take it).\n\nBut the claim is filed as a DEFECT with a spe
    CORRECTION: Corrected claim: mos_ledger_lock IS a reentrant pg_advisory_xact_lock and therefore is genuinely NOT a within-transaction recursion guard — that Postgres-semantics observation is accurate, and there is no re-entrancy GUC anywhere in the scheduling code. However, the defect the claim infers from it is not present: the live assignment call graph is a finite ACYCLIC tree, so no "refill→path_start→refill" (or any other) recursive chain exists for the lock to fail to guard. mos_dispatch_sweep / mos_plan_start_due (the "refill" layer) call downward into workflow_role_path_start → mos_plan_consume_reservation | mos_perf_place_open_task → mos_task_dispatch, and mos_task_dispatch is a pure leaf that never calls back up. Nested re-acquisition does happen (start_due→sweep, start_due→path_start) but is bounded and acyclic, and every step guards the specific task/reservation rows with FOR UPDATE + st
  * refuted=False (high): Reproduced every element of the claim. (1) mos_ledger_lock's body is verbatim `SELECT pg_advisory_xact_lock(hashtext('mos_work_ledger'))` (pg_get_functiondef). (2) Empirically proved reentrancy: `WITH a AS (SELECT mos_ledger_lock()), b AS (SELECT mos_ledger_lock())` yields a_ok=1, b_ok=1, and advisory_rows=1 in pg_locks — the second acquire in the same transaction is a no-op re-acquire that neither blocks nor creates a distinct lock. This is the documented session-owned, reference-counted, xact-level advisory-lock behavior; it gives cross-session mutual exclusion but is NOT a recursion guard. 
    CORRECTION: Corrected claim: mos_ledger_lock() is pg_advisory_xact_lock(hashtext('mos_work_ledger')), which is reentrant within a single transaction — a nested call by a function already holding it re-acquires immediately (empirically: two acquires in one txn, one pg_locks row, both succeed). It provides only cross-session mutual exclusion; it is not a recursion guard, and no re-entrancy GUC exists (only mos_caller_is_trusted_service uses current_setting/set_config). Free intra-transaction re-entry is real and occurs through these actual chains (not the stated 'refill→path_start→refill', since the refill fn mos_perf_place_open_task does not take the lock): mos_campaign_plan_commit[_month] → {mos_plan_consume_reservation, mos_plan_repair, mos_plan_start_due, workflow_advance_role_path}; mos_plan_start_due → {mos_dispatch_sweep, workflow_role_path_start}; workflow_role_path_start / workflow_advance_ro

## [CONTESTED] Recursion hazard: refill that calls path_start (which triggers assignment) restarts assignment mid-pass
CLAIM: Today no function is literally named refill, but mos_dispatch_sweep is the de-facto assignment decider and mos_plan_start_due is the de-facto opener+assigner. The requirement's target — refill both opens due subjects (path_start) and assigns — is unsafe with the current call graph: path_start→consume→dispatch already assigns inside the open step, so folding assignment into refill and having path_start call refill would re-enter refill on every opened subject and restart the whole assignment pass before the batch is computed.
MECH: If refill = 'open due subjects + assign the ready batch', and path_start (called by refill) internally assigns via dispatch, then assignment fires per subject BEFORE refill finishes opening the batch — refill's global capacity tally is computed against a half-updated world. Worse, wiring path_start→refill (so the new task is assigned by the central decider) makes refill re-enter itself on each opened subject; because the ledger lock is reentrant, nothing stops it, and each nested refill re-scans and re-assigns already-processed tasks (mid-pass restart, and capacity double-count of tasks assigned earlier in the same pass).
IMPL: Introduce mos_task_open(subject) = create-and-bind ONLY (insert first-step task + bind reservation via a dispatch-free consume; NO mos_task_dispatch). refill and path_start both call mos_task_open. refill is the ONLY caller of assignment (mos_task_assign_apply). path_start/advance/revise never assign directly; they open+bind and then request one refill drain. Guard refill with the in_refill GUC so a nested request sets refill_again and is drained by the outermost pass — no recursion, one assignment pass.
EVID: Call graph read from live bodies + prosrc scan:
- mos_task_dispatch callers: mos_dispatch_sweep, mos_perf_place_open_task, mos_plan_consume_reservation.
- workflow_role_path_start callers: mos_plan_start_due (only).
- mos_plan_consume_reservation callers: content_revise, mos_campaign_plan_commit, workflow_advance_role_path, workflow_role_path_start.
mos_plan_start_due today: PERFORM mos_ledger_lock(); v_sweep := mos_dispatch_sweep(); [assigns ALL open unassigned]; then FOR each due subject: workflow_role_path_start(...) which does 'IF mos_plan_consume_reservation(v_new_id) IS NULL THEN mos_perf_place_open_task(v_new_id)' — and BOTH consume and place call mos_task_dispatch. So assignment happens (a) once globally in the sweep and (b) again per-subject inside the open loop. mos_perf_place_open_task is a one-line wrapper: PERFORM mos_task_dispatch(p_task_id,false).
  * refuted=True (high): I read the live bodies of mos_plan_start_due, workflow_role_path_start, mos_plan_consume_reservation, mos_perf_place_open_task, mos_ledger_lock, mos_dispatch_sweep, and mos_task_dispatch (pg_get_functiondef), and scanned pg_proc.prosrc for callers.

FACTS THE CLAIM GETS RIGHT (verified):
1. mos_plan_start_due IS the de-facto opener+assigner: PERFORM mos_ledger_lock(); v_sweep := mos_dispatch_sweep(); then a FOR loop over due subjects calling workflow_role_path_start. Confirmed verbatim.
2. path_start assigns inside the open step: after INSERT of the task it runs "IF mos_plan_consume_reservatio
    CORRECTION: Corrected (design note, not a confirmed defect): Two true observations survive. (1) Within mos_plan_start_due, assignment currently happens in TWO places — globally in mos_dispatch_sweep AND per-subject in the open loop via consume/place→mos_task_dispatch — so a redesign should consolidate assignment into one decider. (2) IF a future combined "refill" (open+assign) were wired so that the opener (workflow_role_path_start) calls refill, mutual recursion refill→path_start→refill WOULD occur, and it would NOT be stopped by mos_ledger_lock because pg_advisory_xact_lock is reentrant within a transaction; the depth would scale with the number of due subjects (path_start's pre-insert "already open" guard prevents re-opening the same subject, so it is bounded re-scan/mid-pass churn, not infinite). The correct fix is exactly the requirement's stated constraint — "No recursive refill": refill decid
  * refuted=True (high): REPRODUCTION lens on the "callchain-recursion-idempotency" claim. I read every live body with pg_get_functiondef and scanned pg_proc.prosrc for callers.

The claim's own text concedes it is hypothetical: "Today no function is literally named refill", "having path_start call refill WOULD re-enter refill", "wiring path_start->refill ... MAKES refill re-enter itself". So the titled defect (recursion that "restarts assignment mid-pass") describes a naive FUTURE refactor, not current code.

Attempting to reproduce recursion in the CURRENT call graph:
- Caller graph confirmed exactly (prosrc scan): 
    CORRECTION: Corrected claim: There is NO recursion, mid-pass assignment restart, or capacity double-count in the current code, and none can be reproduced - mos_task_dispatch has no back-edge into path_start/sweep/start_due, so the call graph is acyclic; no function named refill exists; the sweep and the path_start open-loop in mos_plan_start_due operate on disjoint task sets; and mos_task_dispatch's "already_assigned" guard plus fresh per-call mos_capacity_used reads make any repeat dispatch a harmless no-op. Two factual sub-points survive and should be kept as guidance for the planned refactor, not as a live defect: (a) mos_ledger_lock is pg_advisory_xact_lock (reentrant), so it would NOT prevent recursion if a future refill were wired so that path_start calls refill and refill calls path_start - build refill so it does the opening itself (or has path_start NOT call refill) to honor the already-sta

## [CONFIRMED] mos_plan_consume_reservation must be split so binding does not force a dispatch
CLAIM: mos_plan_consume_reservation ends with PERFORM mos_task_dispatch(v_task.id,false) — binding a reservation is inseparable from assigning today. The create-and-bind-only primitive requires a bind function that stops before dispatch.
MECH: Every consumer of consume_reservation (path_start, advance, revise, plan_commit) therefore assigns as a side effect of binding, which is why assignment is scattered across four RPCs instead of centralized.
IMPL: Extract mos_plan_bind_reservation(task) = everything in consume_reservation EXCEPT the final dispatch. mos_task_open calls bind. Keep the current consume_reservation only if a legacy immediate-assign path is still needed; otherwise route all four callers through open+bind and let refill assign.
EVID: Tail of mos_plan_consume_reservation: after UPDATE mos_task_reservations SET status='consumed'... and UPDATE workflow_role_tasks SET scheduled_start=v_res.planned_start, scheduled_end=v_res.planned_end, effort_days=GREATEST(v_res.weight,0.5), reservation_id=v_res.id ... it runs: PERFORM public.mos_task_dispatch(v_task.id, false); RETURN v_res.id;. It opens with PERFORM mos_ledger_lock() (reentrant).

## [CONFIRMED] workflow_advance_role_path selects the task by SUBJECT, not by task id — the double-advance root
CLAIM: The completion RPC ignores which task the caller meant. It selects 'the open task for this subject FOR UPDATE'. A retried or concurrent-duplicate completion therefore closes and advances the SUCCESSOR (a different, newer open task for the same subject), advancing the workflow twice and opening a further downstream task.
MECH: Subject + status='open' is a moving target: after one advance the 'open task' is the successor. Nothing in the RPC asserts the closed task is the one the caller observed.
IMPL: Require p_task_id and select WHERE id=p_task_id AND subject...=... AND status='open' FOR UPDATE; if not found/open, RAISE MOS:TASK_ALREADY_CLOSED and never touch the successor. Add p_event_id + mos_completion_events for replay (next finding).
EVID: workflow_advance_role_path signature: (p_subject_table, p_subject_id, p_result, p_note, p_targets, p_finish, p_return_to). No p_task_id, no p_step_key, no p_round. Its selection:
  SELECT * INTO v_task FROM public.workflow_role_tasks
   WHERE subject_table=p_subject_table AND subject_id=p_subject_id AND status='open' FOR UPDATE;
  IF NOT FOUND THEN RAISE EXCEPTION 'MOS:NO_OPEN_TASK'; END IF;
After closing v_task it inserts the next step and calls consume/place. The p_targets and p_return_to params only affect revision routing (return_to is validated as a prior revision-creating step); neither pins the task being closed. There is no mos_completion_events / idempotency table (information_schema search for %completion%/%idempot% returned []).

## [CONFIRMED] The API pre-check narrows but does not close the double-advance window; content_id-only and concurrent duplicates still double-advance
CLAIM: The SPA does send task_id, and both API cases pre-resolve the open task by that id, returning 409/404 if it is already closed — so a SEQUENTIAL retry-with-task_id is caught. But the check is a non-transactional read before an RPC that re-selects by subject, so (a) a truly concurrent duplicate still double-advances the successor, and (b) any content_id-only / row_id-only completion advances the successor on retry.
MECH: Concurrent duplicate: two requests both read 'X is open' at the API before either commits; both enter the RPC; the ledger lock serializes them; req1 closes X and opens successor Y; req2 acquires the lock, the RPC re-selects the open task for the subject → finds Y → advances Y. The API id-check for req2 already passed and the RPC never re-validates X. content_id/row_id-only path skips even the id pre-check.
IMPL: Move the identity check inside the RPC under the lock (p_task_id required) and add idempotency (p_event_id). The API should stop accepting content_id-only completions, always forward task_id as p_task_id, and forward a client-generated event id.
EVID: SPA: src/lib/marketingOS/client.ts:563-582 completeTask sends { task_id, result, ... } to 'task_complete'; rowClient.ts:202-218 completeRowTask sends task_id when present else row_id. API task_complete (api/marketing-os.ts:2382): accepts task_id OR content_id (guard 'task_id or content_id is required'); resolves tq = taskId ? eq('id',taskId) : eq('subject_id',contentId) with eq('status','open').maybeSingle(); if !cur.data → 404 'no open task found'; then calls sb.rpc('workflow_advance_role_path',{ p_subject_table:'mos_content', p_subject_id: contentId, ... }) at :2499 — the task id is NOT forwarded. row_task_complete (:5684) mirrors this and returns 409 MOS:STEP_ALREADY_CLOSED at :5714 when the id no longer matches an open row; adv call at :5763 again passes only p_subject_id.

## [CONFIRMED] Idempotency design: p_task_id + p_event_id + mos_completion_events, validated under the ledger lock
CLAIM: Bind completion to the expected task and a unique event id. Under mos_ledger_lock: if the event id was seen, return the stored outcome (replay); if the task is not open, raise MOS:TASK_ALREADY_CLOSED without touching the successor; concurrent duplicates serialize on the lock and the event-id PK.
MECH: An event-id PK plus an in-txn replay lookup makes retries safe regardless of whether the client resends the same body; the p_task_id guard makes 'which task' explicit; the ledger lock makes concurrent duplicates serial.
IMPL: DDL + RPC skeletons in the tables. content_revise: add p_event_id and take mos_ledger_lock so it serializes with advance; transfer: add p_event_id and short-circuit on replay so the note is not appended twice. API: forward task_id as p_task_id and a client uuid as p_event_id (or an Idempotency-Key header the endpoint maps to p_event_id); SPA: generate the uuid once per user action (crypto.randomUUID) and reuse it across retries of that action.
EVID: No idempotency infra exists (mos_completion_events absent). content_revise is only partially idempotent: it re-uses an already-open (step,round) task rather than inserting a second (SELECT t.id ... WHERE step_key=v_next AND round=v_round; IF v_new IS NULL THEN close+insert) — but it computes v_round=max(round)+1 and takes only the content-row FOR UPDATE (NOT mos_ledger_lock), so it does not serialize against advance on the same subject. workflow_role_task_transfer takes p_task_id and status='open' (good) but on replay re-applies and appends a duplicate 'transferred by ... at ...' note line.

OPEN: Exact commit-time sequence that let the weight-9 design batch land on سارة (design cap 7) on 09-21 is inferred to be the `OR used=0` clause but NOT reproduced against mos_campaign_plan_commit's consume loop; a targeted trace of the commit transaction (order of reservation consumption vs. mos_capacity_used's 24h window at that instant) would confirm it. | Whether any non-SPA caller (worker, other endpoint, manual SQL) ever calls task_complete/row_task_complete with content_id/row_id only — that path has no API id pre-check and double-advances on a plain sequential retry. I confirmed the SPA always sends task_id, but not that no other caller omits it. | The precise definition of 'dependencies satisfied' for building refill's ready batch (which downstream steps become executable only after the predecessor is approved) is implied by the workflow step list but not centralized in one predicate today; refill needs one. mos_content_plan.stage_deadlines is read by mos_plan_repair for at_risk but the requirement warns it is NOT the original booked date — the source of truth for a step's planned deadline vs. planned handoff needs to be pinned before refill computes lateness. | Whether mos_campaign_plan_commit should call mos_task_open+refill instead of consume_reservation (which force-dispatches) — i.e. whether commit-time assignment should also route through the single decider, or deliberately assign at commit. Its body was not read in full here.

PLAIN: Three problems, in plain terms.\n\n1) The function that assigns work (mos_task_dispatch) is doing two jobs at once: deciding WHO should get a task (checking each person's daily limit and picking the least-recently-used person) and then actually stamping the assignment. Because that decision lives inside the assign step, and three other functions call it, there is no single place in charge of assignment — there are effectively three capacity checks (dispatch, mos_role_has_room, and the sweep) that can disagree. One of those checks has a loophole: 'if this person has done nothing in the last 24 hours, give them the task no matter how big it is.' That loophole is the most likely reason a 9-unit design batch landed on سارة whose cap is 7. Fix: make ONE 'refill' process decide who gets what (counting everyone's load once), and turn dispatch into a dumb 'apply' that just records the decision. Remove the 'nothing done yet, so anything goes' loophole.\n\n2) The lock that is supposed to keep things orderly (mos_ledger_lock) is a Postgres advisory lock that a single transaction can grab as many times as it likes — so it stops two browsers colliding, but it does NOT stop a function from calling itself in a loop. If we naively make 'refill' both open new work and assign it, and have the open step call back into refill, refill restarts itself in the middle of a pass and re-counts work it already handled. Fix: add a create-and-bind-only step (open the task and attach its reserved dates, but DO NOT assign), used by both refill and the open path, plus a simple in-memory flag that says 'a refill is already running — just note that another is needed and drain it once at the end.'\n\n3) Completion is not tied to a specific task. When someone approves or submits, the app sends the task's id, but the database function throws that id away and just grabs 'whatever task is currently open for this item.' So if the request is retried or double-clicked at the wrong moment, it grabs the NEXT task (which the first click already created) and pushes the item two steps forward. The app catches the simple sequential retry, but a true double-fire or a call made with only the item id slips through. Fix: require the task id AND a one-time event id on completion; under the lock, if we have seen that event id before, just return the same answer; if the task is already closed, refuse politely and never touch the next task. The app should generate that event id once per click and resend the same one on retry."


# AREA deadline-model

## [CONTESTED] Intra-day timing inventory: exactly one authoritative clock (publish 18:00) and one due-arithmetic (mos_work_due_at); production planning is entirely day-granular
CLAIM: The whole planner carries civil DATES for every production step. The only intra-day timestamps that exist anywhere are RELEASE/publish instants; no step handoff or step deadline is stored as a timestamp today.
MECH: mos_work_due_at(p_from,p_hours) (live body) is the sole intra-day due arithmetic: it advances p_hours of wall-clock forward from p_from skipping whole non-working days via mos_is_working_day (which reads mos_weekend_days()=[5] + mos_holidays). It is NOT 'working hours' (none are modelled) — it is 'clock hours minus day-off days'. mos_perf_due_after(p_start,p_hours) is a near-duplicate that hardcodes EXTRACT(DOW)=5 instead of the config and is STABLE-not-SECURITY-DEFINER — legacy, and a drift risk.
IMPL: There is a clean seam to add the handoff+deadline model: production steps have no time today, so adding planned_handoff_at/planned_due_at is purely additive. Reuse mos_work_due_at as the single calendar-aware clock for BOTH the plan chain and the enforced due_at; do not introduce a second working-day helper (retire or converge mos_perf_due_after onto mos_work_due_at to avoid the hardcoded-Friday drift).
EVID: mos_month_template.publish_time = '18:00:00', intra_row_gap_minutes = 5 (live DB). The engine's ONE day+HH:MM→instant is toInstant() (src/lib/marketingOS/scheduling/calendar.ts:187), used ONLY for publish instants via rowReleaseInstant/rowSlotTime (releases.ts:188,214; DEFAULT_ROW_PUBLISHING={publishTime:'18:00',intraRowGapMinutes:5} releases.ts:85) → PlannedPlacement.plannedAt (plan.ts:266) → mos_publications.planned_at. Live publications: planned_at 15:00/15:05/15:10 UTC = 18:00/18:05/18:10 Riyadh for a row's 3 feed posts. Production stages carry only civil days: PlannedStage.start/end/deadline are YYYY-MM-DD (types.ts:313-346); PlannedReservation.plannedStart/plannedEnd are days; mos_task_reservations.planned_start/planned_end are DATE columns (live DB). mos_content_plan.stage_deadlines is jsonb of DATES and collapses: for row 2026-09:w3:2026-09-24:c all five stages = '2026-09-23', production_start='2026-09-23', required_ready_at='2026-09-23' (live DB). Planning defaults: publishBufferDays=1, manualTaskWeight=0.5, releaseEffortDays=0.25 (types.ts:751-772; snapshot.ts:48-64). Step allowances in mos_step_rules (live): writing 24h/cap10, writing_review 12h/uncapped, design 24h/cap7
  * refuted=True (high): The claim's TITLE ("production PLANNING is entirely day-granular") and most of its EVIDENCE are correct, and its MECHANISM (mos_work_due_at is the sole intra-day due arithmetic; mos_perf_due_after is a STABLE, non-SECURITY-DEFINER, DOW=5-hardcoded near-duplicate) checks out against live function bodies. But the CLAIM as written makes a universal statement that is false: "The only intra-day timestamps that exist ANYWHERE are RELEASE/publish instants; no step handoff or step DEADLINE is stored as a timestamp today."

Live schema + data refute it. workflow_role_tasks.due_at is timestamp with time
    CORRECTION: Corrected claim: Production PLANNING is day-granular — mos_task_reservations.planned_start/planned_end, mos_content_plan.production_start/required_ready_at (+ stage_deadlines jsonb-of-dates), and workflow_role_tasks.scheduled_start/scheduled_end are all DATE — and the only intra-day timestamp the PLANNER emits is the publish/release instant (mos_publications.planned_at timestamptz, 18:00 + 5-min row gaps via toInstant; publish_time='18:00:00', intra_row_gap_minutes=5). mos_work_due_at is the sole intra-day due arithmetic and mos_perf_due_after is an unused, DOW=5-hardcoded, STABLE (non-SECURITY-DEFINER) legacy near-duplicate. HOWEVER, step deadlines ARE stored as timestamps at DISPATCH time: workflow_role_tasks.due_at is timestamptz, set by mos_task_dispatch to mos_work_due_at(now, step allowance_hours), and 324 live tasks currently hold intra-day due_at values that are not publish insta
  * refuted=False (high): Every cited fact reproduces against the live DB and the exact source lines; the core inventory claim (planner is day-granular, one due-arithmetic mos_work_due_at, one authoritative publish clock at 18:00) is confirmed. Only one absolute sub-clause is over-stated.

REPRODUCED — planner is day-granular:
- mos_task_reservations.planned_start / planned_end are DATE (information_schema).
- mos_content_plan.production_start = date, required_ready_at = date, stage_deadlines = jsonb of DATES. Live row 2026-09:w3:2026-09-24:c:post:{1,2,3}: all five stages (writing, writing_review, design, design_writer
    CORRECTION: Corrected claim: The PLANNER/reservation layer is entirely day-granular — every production step carries civil DATES (mos_task_reservations.planned_start/end, mos_content_plan.production_start/required_ready_at/stage_deadlines). Intra-day timestamps appear in exactly two places: (1) PLAN publish/release instants (mos_publications.planned_at, built by toInstant from publish_time 18:00 + 5-min gap), and (2) EXECUTION-layer step deadlines (workflow_role_tasks.due_at, and the unused mos_content.due_at column), each computed at dispatch by the single due-arithmetic mos_work_due_at (clock-hours-minus-day-off, STABLE SECURITY DEFINER). mos_perf_due_after is a STABLE non-SECURITY-DEFINER near-duplicate that hardcodes DOW=5 and is called by no live function (legacy/drift risk). Drop the word 'anywhere' and the phrase 'no step deadline is stored as a timestamp': step deadlines ARE stored as timesta

## [CONTESTED] due_at today encodes only the actual-handoff leg (dispatch moment + allowance) and never the plan; no planned deadline exists to compare against
CLAIM: mos_task_dispatch sets due_at = mos_work_due_at(clock_timestamp(), allowance) (+leave). There is no plan_due_at anywhere, so due_at cannot be GREATEST(plan, actual+allowance) — the plan target is simply absent.
MECH: Because due_at is anchored to clock_timestamp() at ASSIGNMENT (not to the plan and not strictly to the handoff), it drifts up to one 10-min cron tick from the true handoff, and it has no lower bound at the planned deadline. An early dispatch would give a tighter-than-planned due; a late dispatch always re-grants full allowance. Today no misleading lateness fires only because there is no planned deadline and the late sweep needs due_at NOT NULL (a waiting task is exempt).
IMPL: The minimal correct fix is to (1) carry plan_due_at onto the task at consume time and (2) change the ONE line in dispatch to v_due := GREATEST(COALESCE(v_task.plan_due_at,'-infinity'), mos_work_due_at(v_handoff, v_allow)), anchoring on the actual handoff (predecessor closed_at / production start) rather than clock_timestamp(). Everything downstream (late sweep, block-pause) keeps reading due_at unchanged.
EVID: mos_task_dispatch (live body): v_now:=clock_timestamp(); v_allow:=COALESCE(v_rule.allowance_hours,24); v_due:=mos_work_due_at(v_now,v_allow); then +leave hours; UPDATE workflow_role_tasks SET due_at=v_due. mos_plan_consume_reservation copies planned_start/planned_end→scheduled_start/scheduled_end (DATE) and effort_days, then calls mos_task_dispatch (which overwrites due_at from clock). workflow_role_path_start inserts the first task with a placeholder due_at=now()+due_days*'1 day' (default 2, not calendar-aware) then consumes/dispatches. When no holder has room, dispatch sets due_at=NULL, waiting_reason='capacity'. mos_perf_task_block on unblock does due_at:=due_at+(now()-blocked_at) (clock pause).
  * refuted=True (high): The claim's central technical assertion is verified against the LIVE function bodies: mos_task_dispatch sets due_at from clock_timestamp()+allowance (+leave), with no plan floor and no GREATEST(plan, actual+allowance); mos_perf_late_sweep keys lateness off due_at (NOT NULL AND < now()), exempting waiting/blocked tasks. That much is correct. However the claim overreaches on TWO load-bearing conclusions, both refuted by the live schema and live data, so the claim as stated is refuted.

(1) "the plan target is simply absent" is FALSE. mos_plan_consume_reservation (live body) copies v_res.planned_
    CORRECTION: CORRECT part of the claim: due_at is dispatch-anchored — mos_task_dispatch sets due_at = mos_work_due_at(clock_timestamp(), allowance) + leave, with NO plan floor (it is not GREATEST(plan, actual+allowance)); mos_perf_late_sweep keys lateness off this due_at (NOT NULL AND < now()), exempting waiting (due_at NULL) and blocked tasks; a late handoff re-grants full allowance from the dispatch moment; mos_perf_task_block shifts due_at by the paused duration on unblock; workflow_role_path_start / workflow_advance_role_path insert a placeholder due_at = now()+due_days*'1 day' (default 2, not calendar-aware) that dispatch then overwrites.

WRONG in the claim (corrected): (1) A planned target is NOT absent. mos_plan_consume_reservation copies planned_start/planned_end into the task's scheduled_start/scheduled_end (DATE) columns — populated on 46 live tasks. The accurate statement is: the plan tar
  * refuted=False (high): REPRODUCED in full against live function bodies (pg_get_functiondef) and the live schema. Every sub-assertion of the claim holds.

(1) due_at is clock-anchored, plan-blind. mos_task_dispatch live body: v_now:=clock_timestamp(); v_allow:=COALESCE(v_rule.allowance_hours,24); v_due:=mos_work_due_at(v_now,v_allow); leave add; UPDATE workflow_role_tasks SET due_at=v_due. No GREATEST, no reference to plan/scheduled/reservation dates.

(2) No planned-deadline exists to compare against. workflow_role_tasks has one deadline column, due_at timestamptz; plan data lands only as scheduled_start/scheduled_e
    CORRECTION: The claim is correct as stated; only the MECHANISM's magnitude estimate needs a note. "Drifts up to one 10-min cron tick from the true handoff" is the best case (an immediately-dispatchable task picked up by the /api/cron/planning-sweep tick, or ~0 drift on the inline workflow_advance_role_path/content_revise path where dispatch runs in the predecessor's close transaction). When no holder has room, dispatch sets due_at=NULL/waiting_reason='capacity' and the task can wait arbitrarily long; when a holder later frees up it is dispatched at THAT clock moment with a fresh full allowance — so the gap between readiness and the clock-anchored due can be hours or days, unbounded, not one tick. This makes the "no lower bound at the planned deadline / late dispatch re-grants full allowance" point stronger, not weaker. Everything else in TITLE, CLAIM, MECHANISM and EVIDENCE reproduces exactly.

## [INFERRED] RECOMMEND Option A (mos_step_rules.target_hours as DATA) over Option B (compiler equal-split of a working day)
CLAIM: A per-step planned turnaround target column, chained calendar-aware, is the smallest coherent way to produce handoff timestamps and is strictly better than deriving turnaround from the compiler's same-day chain.
MECH: Option B (equal split): a 5-step same-day row would split one undefined-length 'day' into 5 equal slices — (i) needs an assumed day length that does not exist; (ii) cannot express that a 12h review turns around faster than 24h production; (iii) degenerates on the paid multi-day path where each step already occupies its own booked day; (iv) bakes pace into code, not data. Option A (target_hours): handoff_1 = dayStart(bookedDay_1)=00:00 Riyadh (matches automatic release at day start); cand_k = mos_work_due_at(handoff_{k-1}, target_{k-1}) (calendar-aware); handoff_k = GREATEST(dayStart(bookedDay_k), cand_k); deadline_k = mos_work_due_at(handoff_k, allowance_k). Cross-day spacing comes from the booked-day anchor (the GREATEST), so target_hours only orders steps within a shared day and sets forecast pace — which keeps target_hours ≤ allowance_hours coherent even though a review's allowance (12h) and a full day differ.
IMPL: Add mos_step_rules.target_hours numeric (≤ allowance_hours, CHECK). Proposed defaults (fit a one-day row well before next-day 18:00 with slack): writing 4, writing_review 2, design 4, design_writer_review 2, design_review 2. These are DATA the operator can tune without a migration.
EVID: mos_step_rules already has allowance_hours (numeric) as DATA per step (live). A target_hours column mirrors it exactly. The compiler's chainWorkingDays (monthCompiler.ts:359-370) proves 'one day' for a same-day row but the model has NO working-hours definition (calendar.ts header: 'no working hours'), so an equal split of a day has no hour basis. types.ts:749 states defaults are 'all overridable as DATA'.

## [CONTESTED] The chain must be computed from step sort_order + booked-day anchors, NOT from planned_start — this makes deadlines robust to the live R1_order inversion
CLAIM: Because 103 paid creatives have design.planned_start earlier than writing.planned_start, any chain that trusts planned_start order would emit inverted deadlines; folding by mos_step_rules.sort_order with handoff_k = GREATEST(bookedDay_k 00:00, predecessor+target) yields monotonic handoffs regardless.
MECH: If design's bookedDay is 10-04 but writing's is 10-05, the recurrence computes handoff_writing≈10-05 00:00, then handoff_design = GREATEST(bookedDay_design 10-04 00:00, handoff_writing_review + target) which resolves to ~10-05, ignoring the inverted 10-04 booking. So the DEADLINE chain is coherent even while the underlying capacity booking is still on the wrong day.
IMPL: The chain function (skeleton below) orders by sr.sort_order and folds the GREATEST recurrence; it does NOT sort by planned_start. State explicitly that this fixes deadline COHERENCE only — the capacity misbooking still needs the separate schedule.ts min/max fix, so the deadline model and the effectiveDeadline fix are complementary, not substitutes.
EVID: Live inversion confirmed: exec:meta:...:c2:s5 has design planned_start 2026-10-04 but writing planned_start 2026-10-05 (design a day before its own writing); 10 such rows sampled, all paid (cycle_id NOT NULL). Root cause is schedule.ts:263-272 effectiveDeadline returning GREATEST(cap,deadline) where the comment intends the cap (min) — 'capped by the successor's start'.
  * refuted=True (high): The colleague's substantive claims check out, but a headline result is misread, which per the task rule ("Default refuted=true if ... a result was misread") makes the claim-as-stated refuted with a precise correction.

CORRECT parts (verified live):
1. Root cause. schedule.ts:264-272 effectiveDeadline returns `daysBetween(cap, r.deadline) < 0 ? cap : r.deadline`. calendar.ts:78-81 defines daysBetween(a,b)=b−a, so `< 0` means r.deadline precedes cap → returns cap; else returns r.deadline. That is MAX(cap, r.deadline). The comment "Latest END this stage may take: its own deadline, capped by the 
    CORRECTION: Corrected claim: The colleague's mechanism is sound and their root-cause is right, but the scale is misstated. (a) Confirmed: schedule.ts:264-272 effectiveDeadline returns MAX(cap, deadline) where the comment intends MIN, and this genuinely produces the design-before-writing bookings (under the sameDayChain, design is displaced one working day earlier by سارة's cap-7, and the MAX lets writing/writing_review stay on the later ready day instead of being pulled back to design's day). (b) Confirmed: folding by mos_step_rules.sort_order with handoff_k = GREATEST(bookedDay_k, handoff_{k-1}+allowance_{k-1}) yields strictly monotone handoffs regardless of inverted bookedDays, so a deadline model built that way is robust to the live inversion. (c) WRONG: it is not "103 paid creatives" that have design.planned_start earlier than writing.planned_start — the live count is 35 subjects (all paid), and
  * refuted=False (high): Reproduced all reproducible parts of the claim in the live DB and source. (1) INVERSION EXISTS: joining writing+design reservations by content_key (verified exactly one row per content_key,step_key — max_rows_per_key=1, keys_with_dupes=0, so no double count), 35 of 104 paired subjects have design.planned_start strictly < writing.planned_start; all 35 are paid (cycle_id NOT NULL) and 0 of 23 organic subjects are inverted — matching 'all paid (cycle_id NOT NULL)'. (2) SPECIFIC EVIDENCE ROW reproduced exactly: exec:meta:93d521e4-f4cf-4400-9d95-739351f32507:c2:s5 has design planned_start 2026-10-0
    CORRECTION: Claim confirmed with two adjustments. (a) Update the count to the live figure: 35 paid inversions as of 2026-09-22 (was 103 on 21 Sep), the drop caused by mos_plan_repair re-dating unconsumed reserved rows at the day boundary — cite the live number when quoting scale. (b) State the scope explicitly: computing the chain from sort_order + booked-day anchors via handoff_k = GREATEST(bookedDay_k, handoff_{k-1}+target_{k-1}) makes the DEADLINE CHAIN monotonic and robust to the inversion, but does NOT repair the wrong capacity booking (design still sits on 10-04); the migration must separately recover/correct the booked date. (c) The GREATEST recurrence must add each step's allowance (writing/design 24h, reviews 12h) respecting the working calendar (Friday off), not by raw hour arithmetic, to satisfy the requirement that deadlines respect the working calendar. The underlying defect to fix rega

## [INFERRED] Employee lateness = closed_at > due_at, with due_at = GREATEST(plan_due_at, mos_work_due_at(actual_handoff, allowance))
CLAIM: This single GREATEST simultaneously satisfies 'late handoffs give the recipient full allowance', 'starting early does not change the planned deadline', and 'finishing early creates no extra mandatory work'.
MECH: Late handoff → actual_handoff+allowance > plan_due_at → due=actual+allowance (full allowance from the late handoff). Early handoff → actual+allowance < plan_due_at → due=plan_due_at (employee keeps until the plan; not forced to finish early). On-time → they coincide. actual_handoff = predecessor's closed_at (or, for step 1, the subject's production start / task opened_at), calendar-aware.
IMPL: Store plan_due_at on the task at consume; change dispatch's one due_at line to the GREATEST; keep the +leave extension and the block-pause (due_at += now-blocked_at) unchanged since they operate on the final due_at.
EVID: Requirements list (task): (b) leg is exactly mos_work_due_at(actual_handoff, allowance); the plan target must remain visible; publication risk tracked separately from employee lateness. Current dispatch already computes the second leg but omits the GREATEST with the (nonexistent) plan_due_at.

## [INFERRED] Handoff slip is informational only and must never feed discipline
CLAIM: handoff_slip = actual_handoff − plan_handoff_at is a diagnostic that separates 'the chain arrived late to this employee' from 'this employee is slow'; it drives no warning or deduction.
MECH: Because a late upstream handoff re-grants full allowance downstream (the GREATEST leg), the downstream employee is NOT late even though the chain slipped. handoff_slip captures that slip without penalizing the recipient. It accumulates down the chain and is what publication risk reads.
IMPL: workflow_role_tasks.handoff_slip (interval/numeric hours), filled when the task's handoff is known; excluded from mos_perf_late_sweep entirely.
EVID: Requirement: 'Late handoffs give the recipient their full response allowance. The original plan target remains visible; publication risk is tracked separately from employee lateness.'

## [INFERRED] Publication risk = projected_ready vs required_ready_at / publish time, recomputed on each handoff
CLAIM: projected_ready = (actual handoff of the current earliest-open step, else its planned handoff) + Σ remaining targets, calendar-aware; at_risk when it passes required_ready_at, late when it passes publish time.
MECH: projected_ready ≤ required_ready_at → on_track; required_ready_at < projected_ready ≤ publish → at_risk; projected_ready > publish → late. Remaining steps use TARGETS (plan pace); the current step is anchored on its ACTUAL handoff once its predecessor closed, so it is a live forecast that improves each hop. risk_reason names the step whose slip pushed projected_ready over the line.
IMPL: workflow_role_tasks.at_risk boolean + risk_reason text, recomputed by the planning sweep from the chain function; also usable to replace the coarse mos_plan_repair at_risk that reads the collapsed stage_deadlines date.
EVID: Requirement (3c). Publish instant exists as mos_publications.planned_at (18:00 Riyadh, live). required_ready_at is the last production step's planned_due_at (= publish − buffer); mos_content_plan.required_ready_at (date) is the coarse equivalent today.

## [INFERRED] Routine 00:00-00:10 dispatch and a within-allowance finish do NOT flag; the legitimate trigger is chain slip that pushes projected_ready past publish
CLAIM: With the GREATEST due and target-based forecast, the two routine cases are clean and only real publication jeopardy fires.
MECH: Routine dispatch: employee due = GREATEST(plan_due_1=D+1 00:00, work_due_at(D 00:05,24h)=D+1 00:05)=D+1 00:05; task just opened, not late; projected_ready=D 00:05+14h=D 14:05 ≤ required_ready D+1 00:00 → on_track. Within-allowance finish: writer closes ≤ her due; design's handoff = her close, re-anchored, still ≤ publish. Legitimate trigger: cumulative handoff slip so that at design_review's handoff, handoff+Σremaining targets > D+1 18:00 → late; and separately any single employee with closed_at > their own GREATEST due_at → employee-late. A late upstream handoff can make the publication at_risk WITHOUT making the downstream employee late — the intended three-way separation.
IMPL: The 10-min cron cadence and the day-start anchor must both feed the SAME clock (00:00 plan handoff vs ~00:05 actual) so slip is ~minutes, never a spurious day. Anchor plan_handoff at 00:00 Riyadh, not at dispatch time.
EVID: Cron */10 dispatches step 1 at ~00:05 on booked day D; plan_handoff_1=D 00:00; slip=+5min (informational). Worked-example chain (defaults) gives ~28h of slack before publish D+1 18:00 (see tables).

## [INFERRED] Storage: immutable originals + corrected columns on mos_task_reservations; live plan mirror + slip/risk on workflow_role_tasks
CLAIM: Keep the booked plan of record on the reservation (originals immutable, corrected columns for legitimate re-dates) and copy the effective plan onto the task at consume.
MECH: mos_task_reservations: add planned_handoff_at timestamptz + planned_due_at timestamptz (ORIGINAL, immutable) and planned_handoff_at_corrected + planned_due_at_corrected (NULL until a repair/re-commit moves them). Existing planned_start/planned_end (date) remain the capacity-booking granularity. workflow_role_tasks: add plan_handoff_at + plan_due_at (copied at consume as corrected ?? original), handoff_slip, at_risk, risk_reason. due_at (existing) becomes the enforced GREATEST deadline; plan_due_at is the visible plan target.
IMPL: mos_plan_repair writes the *_corrected columns (never the originals) so 'corrected target vs recovered original' stays distinguishable — exactly the migration requirement. The migration must recover originals from verified records (the pre-repair reservations / plan commit), NOT assume mos_content_plan.stage_deadlines equals the booked dates (they collapse to one day).
EVID: mos_task_reservations already survives revise/commit and is 'the record of what the user changed' (CLAUDE.md); it has only a PK, consumed_task_id FK ON DELETE SET NULL. mos_plan_repair re-dates stale reservations to start today capacity-blind (live body: sets planned_start/planned_end via mos_working_days_from(v_today,span)), which OVERWRITES the booked dates — hence originals must be separate.

## [CONTESTED] mos_perf_late_sweep needs NO logic change — the fix lives at dispatch/consume where due_at is computed
CLAIM: Discipline already keys on due_at (due_at IS NOT NULL AND due_at < now()); making due_at mean GREATEST(plan_due_at, work_due_at(actual_handoff, allowance)) is the whole change, so the sweep automatically stops flagging routine early dispatch and within-allowance finishes.
MECH: Change point 1: mos_plan_consume_reservation copies reservation plan_handoff_at/plan_due_at (corrected ?? original) onto the task before it calls dispatch. Change point 2: mos_task_dispatch computes v_handoff = actual handoff (predecessor closed_at for k>1, else production start / opened_at) and v_due := GREATEST(COALESCE(v_task.plan_due_at,'-infinity'), mos_work_due_at(v_handoff, v_allow)) + leave. Non-reservation tasks (mos_perf_place_open_task) have plan_due_at NULL → GREATEST degenerates to the current behavior, which is safe. The sweep stays byte-for-byte the same — 'discipline uses due_at only' is achieved by making due_at correct, not by editing the sweep.
IMPL: Smallest-diff path. The only sweep-adjacent care: ensure a task waiting for capacity keeps due_at NULL (dispatch already does) so it is never swept; and that block-pause continues to shift due_at (not plan_due_at).
EVID: mos_perf_late_sweep (live body) selects open, not-blocked, not-late, due_at<now(), assignee not on leave, then sets late_flag, inserts mos_late_events, and escalates to mos_discipline_actions at the 4th event. It reads due_at only. due_at is written by exactly one place that matters (mos_task_dispatch); mos_dispatch_sweep nulls due_at on role loss; mos_perf_task_block pauses it.
  * refuted=True (high): The claim's narrow assertion — that mos_perf_late_sweep's body needs no edit — is defensible in isolation: the live body filters only on `t.due_at IS NOT NULL AND t.due_at < now()` (plus status/blocked/late_flag/assignee/subject-existence), so if due_at carried the correct discipline deadline in every committed state, the sweep would flag correctly. But the surrounding CLAIM and MECHANISM overreach and are not established by the evidence, so the whole is refuted.

(A) "due_at is written by exactly one place that matters (mos_task_dispatch)" is FALSE. Live pg_get_functiondef shows three other c
    CORRECTION: Partly right, precisely: mos_perf_late_sweep's own body does not need editing — it correctly keys on due_at, and any due_at-only reader (also mos_perf_on_task_close's on-time XP) would follow automatically IF due_at were made to hold the intended discipline deadline in every committed state. That single true kernel is what the claim gets right.

Everything the claim bundles around it is wrong or unproven:
1. "The whole change is at dispatch/consume" is false. There is no plan_due_at/plan_handoff_at on mos_task_reservations or workflow_role_tasks, and the plan is day-granular (planned_start/end dates) with no stored handoff time. Delivering GREATEST(plan_due_at, work_due_at(handoff, allowance)) requires: (a) a schema migration adding plan timestamps to reservations and tasks; (b) the planner (TypeScript) writing them, including the corrected value AND the recovered-original per the migrat
  * refuted=False (high): REPRODUCED the claim's core. (1) mos_perf_late_sweep (live body) is a pure due_at consumer: both the workflow_role_tasks and mos_manual_tasks branches select on `status='open' AND NOT blocked AND NOT late_flag AND due_at IS NOT NULL AND due_at < now() AND assignee_user_id IS NOT NULL`, skip on-leave, then set late_flag / insert mos_late_events / escalate to mos_discipline_actions at the 4th event. No independent deadline logic exists in the sweep, so making due_at carry the right meaning genuinely requires no sweep edit. (2) mos_plan_consume_reservation (live body) does NOT write due_at — it c
    CORRECTION: The headline is correct and reproduces: mos_perf_late_sweep needs no logic change (it is a pure due_at consumer), and the fix belongs at dispatch/consume where due_at is computed. But 'making due_at = GREATEST(plan_due_at, work_due_at(actual_handoff, allowance)) is the whole change' understates the work in two verifiable ways. First, the referenced fields do not exist: workflow_role_tasks has no plan_due_at/plan_handoff_at and mos_task_reservations has no plan_handoff_at/plan_due_at/corrected (only day-granular planned_start=planned_end). So the change also requires a schema migration to carry a plan deadline on the task AND computing a time-granular plan_due_at (= mos_work_due_at(planned_handoff, allowance) on the working calendar), because that value is stored nowhere today — consume must populate it, not merely copy it. Second, the stated actual-handoff for a first step ('production s

OPEN: actual_handoff anchor: use the predecessor task's closed_at (true handoff) or this task's opened_at? closed_at is more honest but requires the successor to read the predecessor row; opened_at is convenient and, given the */10 cron, differs by ≤10 min. For step 1, is the anchor the subject's production_start (00:00 booked day) or opened_at? Recommend closed_at for k>1 and dayStart(production_start) for k=1. | required_ready_at definition: the chain's last-step plan_due_at, or publish_at minus publishBufferDays? They should coincide by construction but mos_content_plan.required_ready_at (date) is coarser — confirm which is authoritative for the risk verdict. | Multi-day steps: if a step's effort legitimately spans >1 working day (large weight), target_hours ≤ allowance (≤24h) cannot represent it. Confirmed current live data is all weight-1 / same-day rows, so this is latent; decide whether to carry multi-day spacing purely via booked days (recommended) or add a target_working_days fallback. | mos_plan_repair re-dates stale reservations capacity-blind and overwrites planned_start/planned_end; the migration must recover ORIGINAL handoff/due from verified pre-repair records (plan commit history), not from mos_content_plan.stage_deadlines (which collapse to one day). Where is the authoritative original — the plan JSON on mos_content_plan/commit, or a backup table? | The deadline model fixes deadline COHERENCE but not the schedule.ts:263-272 effectiveDeadline min/max bug, which still misbooks CAPACITY (design before writing). Confirm both are shipped together, since a coherent deadline over a wrongly-booked day is still a wrong plan. | mos_perf_due_after (legacy, hardcoded Friday, STABLE-not-DEFINER) duplicates mos_work_due_at. Should it be retired/converged as part of this change to prevent the two working-day helpers from drifting on holidays? | Rows: the risk function's publish_at lookup (mos_publications by content_id) needs to resolve a row to its member publications; confirm the row→members join and whether the row uses min(member planned_at) as its publish deadline.

PLAIN: Right now the marketing planner thinks only in whole days for production work. The one and only clock time it uses is the 18:00 publishing time for posts; every step of making a post (write, review, design, two more reviews) is stored as just a date, with no time of day. And the 'deadline' each employee is judged against is not the plan at all — it is simply 'the moment the task was handed to you, plus your allowance' (24h for writing/design, 12h for reviews), calculated fresh when the task is assigned. There is no stored 'this is when the plan said this step should be done.'\n\nThe fix is small and additive. Add one number per step, a 'target' turnaround (e.g. writing 4h, reviews 2h, design 4h) — data the operator can tune, never bigger than the allowance. From the day a post is booked, start its first step at midnight (which is exactly when the system already releases the work), then chain the steps forward by their targets, skipping Fridays and holidays automatically using the calendar function the system already has. That gives every step a planned handoff time and a planned deadline. A worked example shows a one-day post finishing its plan around 2pm on the work day, with roughly 28 hours to spare before the next-day 6pm publish — so it comfortably fits.\n\nThree separate measures then keep everyone honest without false alarms: (1) an employee is 'late' only if they close after the LATER of their planned deadline and 'their handoff plus full allowance' — so getting the work early never shortens their clock, and getting it late always gives them their full time; (2) 'handoff slip' just records whether the chain reached them behind schedule, for information only, never a penalty; (3) 'publication risk' forecasts whether the post will still make its 6pm slot, recomputed each time a step is handed off. This is deliberately designed so the routine midnight auto-dispatch and a writer using her normal time never trip a flag; only a real chain delay that threatens the publish time does. Nothing about the discipline/late sweep needs rewriting — it already judges by 'due_at'; we just make due_at mean the right thing by fixing two lines (copy the plan onto the task, then take the LATER of plan-deadline and handoff-plus-allowance). New columns hold the immutable original plan plus any later corrected dates, so we can always tell the original target from a re-dated one. Two important caveats: the chain must be computed by step order, not by the stored dates, because 103 paid creatives currently have 'design' dated before its own 'writing' (a known separate bug); and this deadline model makes the deadlines coherent but does NOT fix that capacity-misbooking bug, which still needs its own one-line change in schedule.ts."


# AREA correction-dryrun

## [CONFIRMED] Inventory: 104 subjects / 520 steps — 25 done, 21 in-progress, 474 not-started
CLAIM: The month plan is 104 production subjects (23 organic 3-post rows, 21 materialized paid creatives, 60 unmaterialized paid creatives that exist only as reservations for the c2–c5 ad refresh cycles). Of the 520 step-reservations, 25 are done, 21 in-progress (open tasks), 474 not-started (reserved). 23 open tasks exist right now: 8 سارة design, 7 مريم writing (of which 4 are unassigned, waiting_reason='capacity'), 7 حسام writing_review, and 1 stale ريان design (round 2, on an archived subject, no reservation).
MECH: Reservations were written one-per-subject-per-production-step at confirm (2026-09-20 13:54). A reservation is 'consumed' when workflow_role_path_start/advance opens the round-1 task and mos_plan_consume_reservation binds it (copying planned_start/end→scheduled_start/end); consumed rows map to a done or open task. Reserved rows are the not-yet-started plan. Cycles r2–r5 have not reached production_start_on<=today (09-24/10-01/10-08/10-15), so their content shells are not created and their reservations carry content_id=null (virtual subjects).
IMPL: The correction must operate on reservations even where no task/content exists yet (the 60 virtual subjects = 100 of the 116 moves), because mos_plan_consume_reservation will later copy the corrected planned dates into the task's scheduled_start/end. The stale ريان design on archived content is an orphan that mos_dispatch_sweep already skips (subject inactive) and should be force-closed, not scheduled.
EVID: SQL q_inventory CTE returned not_started=474/104subj, done=25/15subj, in_progress=21/21subj. Reservation aggregate: base plan 1612301b=115 res/23 rows; ad plans a66cd25b+bada9d59+c9fc468a across cycles r0..r5 (refresh Tue 09-22/09-29/10-06/10-13/10-20/10-27). mos_content_rows=23 batch_days 09-22..10-31. Open-task query returned the 23 rows incl. ريان round-2 design due 2026-09-06 on archived content and 4 مريم writing waiting_reason='capacity' (a9f8 c1 s2–s5, scheduled_start 09-27).

## [CONFIRMED] All remaining-step violations are R1_order (kind 1); zero against completed or in-progress predecessors
CLAIM: Among remaining (not-started) steps there are 72 immediate-predecessor ordering violations: 40 design_review planned before its predecessor design_writer_review, and 32 design planned before its predecessor writing_review (equivalently before writing). Counting a step against ALL prior steps gives 100 violating pairs across 52 subjects. There are ZERO violations of kind 2 (successor before a DONE predecessor's actual finish) and ZERO of kind 3 (successor before an IN-PROGRESS predecessor's due_at). Exactly 1 multi-day-span remaining reservation exists.
MECH: schedule.ts:263-272 effectiveDeadline returned max(cap,deadline) instead of min. In ad creatives the backward scheduler therefore placed design one working day BEFORE the writing/writing_review block, and design_review one day before design_writer_review. Kinds 2 and 3 are absent because every completed/in-flight predecessor (the c0/c1 launch creatives whose writing/writing_review/design are done at 09-20/09-21) has its downstream reserved steps planned for today (>= the predecessor's finish), so no reserved successor lands before a done/in-progress predecessor's ref day.
IMPL: The fix is purely an ordering repair of reserved rows against their (still-reserved) predecessors; the completed/in-flight work needs no adjustment. Because step position is only the 4th sort key, the dispatcher cannot self-heal these inverted dates — the planned dates themselves must be corrected. The 1 multi-day span should be collapsed to a single day (day-granular reservation).
EVID: SQL q_viol (immediate pred): design_review<design_writer_review=40, design<writing_review=32, both pred_state='not_started'. SQL q_viol2 (vs any prior step, by pred_state): succ<pred_planned=100 pairs/52 subjects; succ<pred_actual_finish (done pred)=0; succ<pred_due_at (in_progress pred)=0. Multi-day span: only 2026-09:w3:2026-09-22:b design_review, planned_start 2026-09-22, planned_end 2026-09-24 (a mos_plan_repair span artifact; ceil(weight 3)=3 working days from today).

## [CONTESTED] سارة's design bucket is saturated at 7/day — this, not the inversion count, is the binding constraint
CLAIM: The plan books سارة's design (post bucket, cap 7) at exactly 7/day on every ad-heavy day. Correcting the design inversions (shifting each inverted design forward onto its writing_review day) pushes designs onto days that are already at cap: ~10 working days exceed cap 7 by 1–3 after the ordering fix, forcing forward spill. This is why fixing 72 ordering violations produces 116 step-moves (the extra 44 are capacity cascades: a design bumped past its design_writer_review day drags dwr and design_review with it).
MECH: The R1_order bug gave designers a false one-day buffer by scheduling design a day early. Removing that buffer exposes that سارة is at 100% design utilization on the ad-cadence days, with the only slack sitting OUTSIDE each creative's [writing…publish] window. Since the rule forbids moving a step earlier than its own planned_start (and never before its predecessor), the only legal direction for spill is forward — into and up to the publish day.
IMPL: Move count is dominated by capacity, so it is sensitive to the repack policy (seed-all-then-displace-only-violated, processing subjects by planned-date then publish then rank, was used — it never displaces a non-violated step and never moves an organic row: 0 organic moves). The durable fix is not just correcting the max/min bug but either raising سارة's design throughput on the ad-launch days or spreading each cycle's 15 designs across more days. A whole-creative-to-a-free-day policy would move fewer distinct rows than the minimal per-step nudge.
EVID: SQL q_demand: after the minimal inversion-fix, سارة design load exceeds 7 on 2026-09-27(8),09-28(9),10-04(8),10-05(10),10-11(8),10-12(10),10-18(8),10-19(10),10-25(8),10-26(10). Raw reserved design load is at exactly 7 on 09-26/27, 10-03/04/05, 10-10/11/12, etc. Engine (interpretation B): rows moved=116, days moved {1:64, 2:32, 3:20}=188 step-days; reason inversion=84, inversion+capacity=32; post-correction Sara design days>cap=[] (fully resolved).
  * refuted=True (high): The claim is partly right on facts but its headline ("saturation, NOT the inversion count, is the binding constraint") is refuted by the colleague's own numbers, and it contains a misread result and an unreconciled base count.

CONFIRMED (colleague right): (1) سارة's design (post) bucket cap = 7 (mos_user_capacity faaad82b… post = 7) and the reserved design load sits at exactly 7 on the mid-cluster ad-cadence days — local saturation is real. (2) The R1_order bug is real and puts each inverted design exactly ONE working day early: schedule.ts:264-271 effectiveDeadline returns `daysBetween(cap, 
    CORRECTION: سارة's design (post) bucket IS locally saturated at 7/7 on the ad-cadence cluster days, and correcting the 35 R1_order design inversions by the earliest-legal placement (onto the writing_review day) does overflow ~10 days by +1/+3 (09-28 = 10, not 9). BUT this is a SECONDARY, local capacity cascade, not "the binding constraint over the inversion count": the colleague's own engine attributes ~72–84 of 116 moves to ordering and only 32 to capacity, so ordering dominates. The correction base is 35 design inversions (R1_order, سارة/post), plus a distinct 40 design_review<design_writer_review inversions in the APPROVALS bucket (مريم + manager) that the saturation mechanism does not explain at all — 75 pair-violations / 55 subjects total, not 72. Of the 35 inverted designs, only 15 have a single-day window that genuinely forces the overflow; 20 have a later legal slot (dwr>wr), so a capacity-a
  * refuted=False (high): REPRODUCTION lens on area "correction-dryrun". The claim's load-bearing core reproduces EXACTLY from the live DB.

1) Saturation premise (SQL, mos_task_reservations, assignee faaad82b = سارة, step_key='design'): load is SUM(weight) vs cap 7 (mos_user_capacity post bucket daily_slots=7; weight 1/creative, 3/row). Raw reserved design load = exactly weight-7 on every ad-cadence day: 09-26, 09-27, 10-03/04/05, 10-10/11/12, 10-17/18/19, 10-24/25/26. Slack (weight-3 single-design days) falls only OUTSIDE the ad windows (09-23/24/30, 10-01/07/08...). Matches "at 100% design utilization on ad-cadence 
    CORRECTION: CONFIRMED (not refuted): سارة's design bucket is saturated at weight-7/day on every ad-cadence day, and this saturation — not the raw inversion count — is the binding constraint on any inversion correction. Reproduced exactly: (1) raw reserved design load = weight-7 on 09-26/27, 10-03/04/05, 10-10/11/12, 10-17/18/19, 10-24/25/26; (2) all 35 design inversions sit 1 working day before their writing_review (false buffer); (3) moving each inverted design onto its writing_review day puts سارة over cap 7 on exactly 10 days {09-27:8, 09-28:9, 10-04:8, 10-05:10, 10-11:8, 10-12:10, 10-18:8, 10-19:10, 10-25:8, 10-26:10}, forcing forward spill. Corrections to the colleague's wording: the correctable design-inversion count is 35 (all weight-1 creatives), not 72; load must be read as SUM(weight) vs cap 7, not task count (e.g. 09-26 is 5 tasks = weight 7); and the downstream engine tallies (116 step-m

## [CONFIRMED] Correction is feasible within capacity: 0 subjects unrepairable before publish, but 38 lose their ready_by buffer
CLAIM: No subject's corrected final approval lands AFTER its publish date (0 unrepairable). But 38 paid creatives plus the in-flight 09-22 organic row become 'ready on the publish day' (they miss the Monday ready_by target and land on the Tuesday publish day) — they lose all slack. 65 subjects remain on_track. Every named launch batch is projected publishable: 22 Sep (organic row + meta c0 ×6), 24 Sep organic row, 29 Sep (organic row + meta c1 ×15) all OK.
MECH: projected-ready = corrected day of the last step (design_review). Because forward spill stops at the publish day (a working Tuesday with residual capacity), nothing overflows past it. But the buffer between ready_by (Mon) and publish (Tue) is consumed by the spill, so the honest projected-ready equals the publish day for the saturated creatives.
IMPL: Publication risk must be surfaced SEPARATELY from employee lateness (operator requirement): these 38 are not 'late employees' — they are creatives the plan cannot comfortably fit given سارة's design cap. Caveat: under the strict allowance-chained deadline model (writing/design 24h + 3 reviews 12h ≈ 84h ≈ 3.5 working days), the tight creatives' chained design_review DUE lands after publish (e.g. P-488 design due 09-30, design_review due 10-01 vs publish 09-29). The day-granular schedule says 'produce on publish day'; the worst-case deadline chain says 'at risk of missing publish' — both should be shown, as day-plan for capacity and deadline-chain for risk.
EVID: final.py: on_track=65, at_risk(ready on publish day / misses ready_by)=38, tight(organic 09-22)=1, LATE=0. today.py batch risk: 22 Sep organic ready 09-22=publish (ok); meta c0 all 6 ok; 24 Sep organic ready 09-23 (ok); 29 Sep organic ready 09-28 (ok); meta c1 ×15 all ok (11 ready 09-28, 4 [P-488..491] ready ON publish 09-29). The 38 at_risk are the c2–c5 inverted creatives (a9f8 s2–s5, 93d s5, b66 s1–s2 per cycle) whose corrected design_review lands on refresh_on.

## [CONTESTED] Today (2026-09-22): mandatory tail work is gated on in-flight predecessors; optional early work exists but is capacity-blocked
CLAIM: Today's mandatory not-started work is حسام 7 design_reviews (only 1 dependency-ready, 6 waiting on their design/dwr predecessor) and مريم 6 design_writer_reviews (0 ready, all waiting on design). سارة's 'today' is her 8 already-assigned in-flight designs. The optional early batch (next future planned day = 09-23) is small (مريم 1 writing + 1 dwr; سارة 1 design; حسام 1 review), with later days queued behind it. Separately, 4 of مريم's c1 writing tasks were pulled early and have sat unassigned with waiting_reason='capacity' since ~09-21.
MECH: The tail reviews for the 22-Sep launch (c0 creatives + the 09-22:b row) are reserved for today but their predecessors (design/dwr) are still in-flight, so they become dispatchable only as those complete — correct chaining, not a bug. The 4 early-pulled writing tasks are future (09-27) work consumed early; mos_task_dispatch left them waiting because used+units>daily_limit and used>0 at dispatch time.
IMPL: The daily refill must offer the optional early batch one future planned day at a time and select the WHOLE ready batch of that day (not just the first task), and must not let a blocked earliest-future day hide later executable reservations. The 4 capacity-waiting early-pulled writings show early pull already happening; finishing them early must NOT change their planned deadline (they should keep their c1 targets).
EVID: today.py: MANDATORY today — Hossam 7 design_review (dep-ready 1, waiting 6); Maryam 6 design_writer_review (dep-ready 0, waiting 6). OPTIONAL EARLY (09-23): Maryam {writing:1, dwr:1} (1 executable), Sara {design:1} (blocked), Hossam {writing_review:1, design_review:1} (blocked); later days queued=True. Open-task query: a9f8 c1 s2–s5 writing, assignee NULL, waiting_reason='capacity', scheduled_start 09-27.
  * refuted=True (high): I re-derived the colleague's framing (they project "today's mandatory work" from mos_task_reservations whose planned_start = 2026-09-22, not from open workflow_role_tasks) and re-checked every number against the live DB and live function bodies.

WHAT THE CLAIM GETS RIGHT (verified):
- 7 design_review reservations planned 09-22, all assigned حسام (1947fc, marketing_manager); 6 design_writer_review reservations planned 09-22, all assigned مريم. SQL: mos_task_reservations grouped by planned_start/step/assignee.
- The 6 design_review + 6 dwr sit on the 6 c0 launch creatives (subjects 5a13,8518,a7
    CORRECTION: The claim's structure and mechanism are correct, but two results are misread. Corrected claim: "Today (2026-09-22), the mandatory tail work is حسام 7 design_reviews and مريم 6 design_writer_reviews (as reservations planned for today, none yet consumed). ALL 7 design_reviews and ALL 6 dwr are dependency-waiting, NOT '1 dependency-ready + 6 waiting' — every one of the 7 design_review subjects has an in-flight design: the 6 c0 creatives have their round-1 design OPEN on سارة, and the 7th (row d5c3) is in a round-2 design revision (its round-1 dwr requested changes on 09-21), so its design_review is 2+ steps away. سارة's 8 assigned open designs (6 launch r1, 1 future r1 @09-28, 1 row r2) are the current in-flight designs. The optional early batch (next future planned day 09-23) is small — مريم writing 1 + dwr 1, سارة design 1, حسام design_review 1 + writing_review 1 (2 reviews, not 1) — only
  * refuted=False (high): Reproduced every material element of the claim against the live DB at 2026-09-22 09:04-09:07 Riyadh.

MANDATORY TODAY (from mos_task_reservations WHERE planned_start='2026-09-22' AND status='reserved'):
- 7 design_review, all assignee 1947fc00 (marketing_manager = حسام): six c0 creatives (93d521e4 c0 s1/s2, a9f8d65e c0 s1/s2, b66e165b c0 s1/s2) + one row 2026-09:w3:2026-09-22:b. EXACT match to "حسام 7 design_reviews".
- 6 design_writer_review, all assignee 3eea9a65 (writer = مريم): the same six c0 creatives (rows get no dwr). EXACT match to "مريم 6 design_writer_reviews".

READINESS: The six c
    CORRECTION: Corrected claim (state confirmed as of 2026-09-22 09:04-09:07 Riyadh): Mandatory not-started work TODAY, taken from the plan reservations dated 09-22, is 7 design_reviews for حسام (marketing_manager 1947fc00) — 1 dependency-ready (the 2026-09-22:b row, whose design is done) and 6 waiting on their in-flight c0 design predecessor — and 6 design_writer_reviews for مريم (writer 3eea9a65), 0 ready, all waiting on the same six open c0 designs. سارة (montage faaad82b) has 8 already-assigned in-flight designs (six c0 planned 09-21, one 09-28, one undated). The optional early batch (next future planned day 09-23) is small: مريم {writing 1, dwr 1}; سارة {design 1}; حسام {design_review 1, writing_review 1} = 2 reviews (correct "1 review" to 2); later days queued behind. Separately, 4 of مريم's c1 writing tasks (exec:meta:a9f8…:c1 s2–s5) were pulled early from their 09-27 plan date and have sat unas

## [CONFIRMED] Original booked dates: 461 reserved rows recoverable from the row; 13 re-dated by mos_plan_repair are (mostly) unrecoverable
CLAIM: 461 of the 474 reserved rows still hold their confirm-time (2026-09-20) booked dates unchanged, so their original = current planned_start. 13 reserved rows were staled and re-dated to start today by mos_plan_repair at the 09-22 00:00 boundary (their planned_end had fallen before today); the repair keeps no history, so their original booked date is not recoverable from the reservation row. 12 of those 13 are materialized c0 tail steps whose mos_content_plan.stage_deadlines still reads 2026-09-21 (a usable cross-check); the 1 organic-row design_review (2026-09:w3:2026-09-22:b) has no content_plan/stage_deadlines and is unrecoverable.
MECH: mos_plan_repair stales reservations with planned_end<today and unconsumed, then RE-DATES them via mos_working_days_from(today, ceil(weight)) — capacity-blind, no history column. CLAUDE.md and the task both note stage_deadlines may not equal original booked dates; here it happens to match for the flat sameDay c0 creatives but does not exist for the unmaterialized cycles or rows.
IMPL: The migration should populate *_orig columns from the reservation's own planned_start for the 461 untouched rows, cross-check the 12 materialized re-dated rows against stage_deadlines, and explicitly FLAG the 1 organic-row re-dated reservation as original-unrecoverable. Distinguish 'corrected target' (new date) from 'recovered original' in the schema so the repair's damage is auditable.
EVID: Reserved rows with updated_at>=2026-09-22 00:00+03: 13 (else 461). The 13 = design_writer_review + design_review of the six c0 launch creatives (b66/93d/a9f8 s1,s2) plus the 09-22:b row's design_review (planned_end 09-24, the span artifact). stage_deadlines for a9f8 c0:s1 = all steps '2026-09-21' (matches the pre-repair original); c2:s5 (unmaterialized) has no content_plan row at all.

## [CONFIRMED] Structural/concurrency risks around the correction and the untraced 09-21 overbooking
CLAIM: Three structural facts make a naive commit unsafe. (1) workflow_advance_role_path selects the subject's open task with 'status=open FOR UPDATE' and no LIMIT and no completion-event id — a retry or concurrent duplicate keyed on the subject can advance whatever open task exists, risking double-advance/duplicate insert. (2) mos_task_reservations has only a PK (no uniqueness on subject+step), so nothing structurally prevents duplicate reservations. (3) The historical 09-21 overbooking (سارة design weight 9 vs cap 7) is real and confirmed, but it is CONSUMED/in-flight history and is correctly left untouched by the correction.
MECH: The advance is subject-scoped, not task-id/event-scoped, so idempotency depends on there being exactly one open task per subject; a concurrent second caller under the same advisory lock serializes but a stale retry after commit could act on a newly-opened task. The 09-21 overbook was written at confirm and the commit's capacity re-check passing it is untraced (hypothesis: the confirm-time check does not enforce the per-day daily_slots cap on the first/launch production day, or evaluates a different bucket/rolling window than mos_capacity_used).
IMPL: Per the operator requirement, completion/advance must be bound to the specific expected task id (or a validated unique completion-event id) under mos_ledger_lock so retries/concurrent duplicates cannot advance twice or duplicate assignments; and the commit's capacity guard must count tasks AND consumed reservations exactly once per occupied day (which would have caught the 09-21 weight-9 write). A uniqueness constraint on (subject,step,round) for reservations would harden the ledger.
EVID: pg_get_functiondef(workflow_advance_role_path): 'SELECT * INTO v_task FROM workflow_role_tasks WHERE subject_table=.. AND subject_id=.. AND status=''open'' FOR UPDATE' (no LIMIT, subject-based). SQL: sum(weight)=9 across 7 consumed design tasks on 2026-09-21 for سارة (cap 7). mos_plan_consume_reservation orders candidate reservations FOR UPDATE and can bind either a subject-matched or content_key-matched row. blocked column confirmed never read by dispatch.

OPEN: Why did the confirm-time capacity re-check pass the 09-21 سارة design overbooking (weight 9 vs cap 7)? UNTRACED. Hypothesis: the confirm check does not enforce the per-day daily_slots cap on the launch/first production day, or it evaluates a different bucket or a rolling window than mos_capacity_used counts. Needs tracing in monthCompiler/monthActions before the corrected commit is trusted. | Which repack policy does the operator want for the capacity spill: minimal per-step forward nudges (this dry run: 116 step-moves, keeps each step near its own day) OR move a whole inverted creative onto the nearest day with a free 5-slot chain (fewer distinct row-moves, but larger single jumps)? The move count and which creatives lose their buffer depend on this choice. | Should publication risk be judged by the day-granular schedule (produce-on-publish-day = feasible, 0 LATE) or by the strict allowance-chained deadline (~84h chain, which puts the tight creatives' final approval after publish)? The two disagree for the 38 buffer-losing creatives; the operator said deadlines are targets and publication risk is tracked separately from lateness, but the reservation calendar is day-granular. | For the 1 organic-row re-dated reservation (2026-09:w3:2026-09-22:b design_review) whose original booked date is unrecoverable (no content_plan/stage_deadlines for rows), what value should *_orig carry — null-with-flag, or the inferred 09-21 from its sibling steps? | Is the sameDayChain intent that all 5 steps of a creative truly share ONE calendar day (incompatible with the 84h allowance chain), or that they are booked around a production day with the allowance chain spanning ~3.5 days? This determines whether the corrected reservation should stay single-day or be spread.

PLAIN: Plain version: The month's content plan has 104 pieces of work broken into 520 small steps (write, review, design, two more reviews). Right now 25 steps are finished, 21 are being worked on, and 474 haven't started. A known bug had scheduled the DESIGN step one day before the WRITING it depends on (and the final approval before the review before it) — 72 such out-of-order steps remain, all in the paid ad creatives; the organic daily posts are fine. Nothing is scheduled before a step that's already finished or in progress, so the only problem is order, not missed handoffs.\n\nThe real trap: the designer سارة is already booked to her daily limit (7 designs a day) on the busy ad days. The bug had quietly given her a fake one-day head start. When we fix the order and move each design back to the correct day, those days are already full, so the work spills forward. Fixing 72 out-of-order steps therefore ripples into 116 step-moves across 52 pieces of work (moving 1–3 days each). No organic post is touched, and nothing ends up scheduled after its publish date — so everything is still deliverable. BUT 38 ad creatives lose their one-day safety cushion and end up 'ready the same day they publish' — tight, but not late. This is a capacity problem (سارة is maxed out), not a people-being-slow problem, and it should be shown to managers as publication risk, not as anyone being behind.\n\nFor today: سارة should finish her 8 designs already in hand; حسام has 7 final approvals and مريم has 6 reviews queued for today, but most are waiting for the design in front of them to finish first — that's normal chaining. A little optional 'get-ahead' work is available for tomorrow if anyone finishes early, and finishing early must not change any deadline. Four of مريم's writing tasks were already pulled forward and are stuck waiting for capacity.\n\nThis was a read-only dry run: I changed nothing in the database or code. When this is turned into a real migration, save each step's original date (recoverable for 461 of the 474 not-started steps; 13 were overwritten by an automatic repair and 1 of those can't be recovered), fix the out-of-order dates under the shared lock counting each day's load exactly once, and tie 'mark complete' to the exact expected task so a double-click or retry can't advance the work twice."]