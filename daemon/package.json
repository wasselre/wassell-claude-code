{
  "name": "wassell-presentations-daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Local background worker that polls Supabase for queued presentation jobs and runs the matching Claude Code slash command. See docs/prd/presentations.md.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "smoke": "tsx src/smoke.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "chokidar": "^3.6.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
