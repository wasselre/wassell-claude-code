// Version string stamped on the daemon_status heartbeat. Bump when the wire
// contract with the app or Claude CLI changes, so the frontend could surface
// a "daemon out of date" hint later.
export const DAEMON_VERSION = '0.1.0';
