{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "_comment": "Hashed build artifacts (e.g. index-abcd1234.js). Safe to cache forever because the filename changes whenever content changes.",
      "source": "/assets/:name(.+-[A-Za-z0-9_-]{8,}\\.(?:js|css|woff2?|ttf|otf))",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "_comment": "Static public/ assets (logo, images, etc). Must NOT be immutable — they're updated in place. 1h at edge, 1d in browser with revalidation.",
      "source": "/assets/:name(.+\\.(?:png|jpg|jpeg|gif|webp|svg|ico))",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=3600, s-maxage=3600, must-revalidate" }
      ]
    }
  ]
}
