# ────────────────────────────────────────────────────────────────────
# Wassell CRM — .gitignore
# Keeps secrets, build output, and heavy assets out of the repo.
# ────────────────────────────────────────────────────────────────────

# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
logs/
*.log

# Editor / IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.DS_Store
Thumbs.db
*.swp
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude / local scratch
.claude/settings.local.json
.claude/plans/
.claude/hooks/*.log

# Heavy brand assets (kept outside the production bundle)
Wassel Branding/
Wassel\ Branding/

# Unrelated side project that happens to live in this folder — NOT part of Wassell.
# It's a separate Supabase Edge Function (haberchat-webhook / WhatsApp bot). Keep
# it out of the Wassell repo so the two projects don't get tangled.
supabase/functions/

# TypeScript build info
*.tsbuildinfo

# Test / coverage
coverage/
.nyc_output/

# OS junk
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Desktop.ini
